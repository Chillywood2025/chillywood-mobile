# ROADMAP

## Planning Rule
`ROADMAP.md` now records Chi'llywood's phased product planning truth rather than older chapter-by-chapter execution history.

- current checkpoint truth belongs in `CURRENT_STATE.md`
- detailed older checkpoint history belongs in `docs/archive/`
- the immediate next working lane belongs in `NEXT_TASK.md`
- Locked Chi'lly Chat communication doctrine and locked Rachi official-account doctrine remain carried forward unchanged and are not reopened by this planning rewrite.

## Public v1
- Profile Media Modern Review and Full-Page Background Fix on May 31, 2026 clarifies the Brand Studio/public Platform boundary and hardens personal Profile media saves. Public Platform still renders only reviewed/published/safe Brand Studio assets. Platform Studio keeps owner-only `Preview Brand Draft` routing for saved Brand Studio visuals before review, while normal `Preview Platform` remains the reviewed visitor view. Profile Photo/Profile Background saves stage Android `content://` images to cache when needed, upload to the profile-only `profile-media` bucket through Supabase Storage REST with SDK fallback, verify a signed read-back before updating `user_profiles`, and keep owner-controlled `active` / `user_removed` policy. The picker uses the modern phone photo-library path with native editing disabled because the Android cropper was broken; Chi'llwood now shows an in-app review sheet with real preview plus Fill/Fit/Center before saving. Profile Background now renders as a readable full-page Profile skin, not just a top cover/header. Android one-device proof lives at `/tmp/chillywood-profile-brand-media-one-device-proof-20260531/`; remove/fallback, viewer/signed-out masking, and backend `user_removed` read-back remain the next proof gap.
- Brand Studio Modern Asset Manager Upload Fix on May 30, 2026 keeps Brand Studio scoped to public Platform branding and Profile media scoped to Profile Appearance. Brand Studio uploads now stage Android content URIs, upload to private `platform-brand-assets` with bearer-auth REST plus SDK fallback, verify read-back bytes, and only then create draft asset rows. The Brand tab is now a compact asset manager with collapsed cards for Hero Media, Background, Avatar and Logo, Theme, Scene Presets, and Review and Publishing. Fit/Fill/Center and overlay/blur controls remain but appear only after media exists; Hero Reel and Brand Mark remain unavailable where public rendering is not backed. Draft, pending-review, rejected, removed, scan-failed, quarantined, and malware-detected brand media remain blocked from public Platform rendering by the existing public-safe gates.
- Fresh Current-HEAD AAB Play Upload Proof on May 30, 2026 closes the current local build/install/open evidence gap while preserving the external Play-upload boundary. From `main` HEAD `12c97e56de6bb0a5f435f1c9aa81742f700af4dc`, the successful high-memory release build produced `android/app/build/outputs/apk/release/app-release.apk` (`205639147` bytes / `196M`, SHA-256 `abc67ba63c4679ca005d9b3fcb9dc2a5286dd74c48525f1580c7d1ea94f5ed33`) and `android/app/build/outputs/bundle/release/app-release.aab` (`132125002` bytes / `126M`, SHA-256 `57f8f8da17f21959ef7d3f4abb661fad5135757caa277d2b9a03ddec192ad199`) for package `com.chillywood.mobile`, versionName `1.0.0`, versionCode `8`, targetSdk `36`. The fresh APK installed on `R5CR120QCBF` and opened past splash; route smoke captures Home, Explore loaded, Live, Library, Profile, Settings/legal, Player, Platform Studio, Money Center, and Admin with no app-specific fatal/ANR crash-scan matches. Proof lives at `/tmp/chillywood-current-head-play-upload-proof-20260530/`. The local Gradle release config still signs with `signingConfigs.debug` (`CN=Android Debug`), so this is current-HEAD artifact proof and release APK install/open proof, not a claim that the local AAB is Play-upload-signed. Actual Play upload still requires the owner-approved EAS/Play signing path or a corrected release signing config, plus external Play/Data Safety/account-deletion/legal acceptance.
- Firebase Test Lab Android Smoke Setup on May 30, 2026 adds the cloud-device smoke-test path without changing app behavior. Runbook: `docs/android/FIREBASE_TEST_LAB_RUNBOOK.md`; proof path: `/tmp/chillywood-firebase-test-lab-proof-20260530/`. Google Cloud SDK `567.0.0` and Firebase CLI `15.17.0` were available locally, active project was `chillywood-app`, and Firebase/Test Lab APIs were enabled. `cd android && ./gradlew assembleRelease bundleRelease` passed for `com.chillywood.mobile`, versionName `1.0.0`, versionCode `8`; APK `android/app/build/outputs/apk/release/app-release.apk` is `196M` with SHA-256 `94a5154c5ab894d57ce03009115a6e86ff2888d750d7d7b9423c2df217b82e5e`, and AAB `android/app/build/outputs/bundle/release/app-release.aab` is `126M` with SHA-256 `e90211578a50521cdec71b58e9ef379aa1ae636e061282986f94e537b1d1b41b`. One bounded Firebase Test Lab Robo test passed on virtual `MediumPhone.arm`, Android API `35`, English portrait, matrix `matrix-xfre4x5gqc47a`, in `308` seconds; result link is `https://console.firebase.google.com/project/chillywood-app/testlab/histories/bh.e9371a121da8f5fe/matrices/6982988100476756190`. This proves cloud install/open and signed-out auth/login-surface smoke only. It does not prove signed-in route coverage, physical Test Lab devices, Play acceptance, LiveKit multi-user, TURN/cellular, real mic/camera, Watch-Party capacity, or route coverage beyond Robo actions. Billing/quota status was not verified because the local `gcloud beta billing` command required installing the beta component; no billing setup, paid run expansion, app behavior, LiveKit, Watch-Party, Premium, Money, Profile, Platform Studio, Rachi, Spectator, Admin, Supabase schema, RLS, or secrets changed.
- LiveKit Multi-Participant Device Emulator Proof on May 30, 2026 attempted the strongest local setup without changing production behavior. Proof lives at `/tmp/chillywood-livekit-multi-participant-proof-20260530/`. The attached physical device was `R5CR120QCBF`; available AVDs were `Chillywood_API_34`, `Maestro_Pixel_6_API_33_1`, and `Pixel_8`. Two emulators booted but became Android system/launcher-ANR unstable; the single-emulator fallback opened the current app only to splash before system ANR blocked room navigation. The physical device installed/opened the current release APK and focused `MainActivity`, but it was locked on the Android PIN bouncer, so visual route and joined-room proof could not continue through adb. No multi-participant room proof, media proof, reconnect proof, TURN/cellular proof, or load proof is claimed. Seat limits remain unchanged, final adb state returned to the physical device only, and the next proof attempt requires unlocked devices, stable emulator/device capacity, safe signed-in accounts, and a valid room fixture.
- LiveKit Simulcast Dynacast Safe Optimization Pass on May 30, 2026 enables the supported LiveKit room-efficiency options only on camera-room surfaces. Watch-Party Live shared-player camera seats and Live Watch-Party / Live Stage now use `adaptiveStream: true` and `dynacast: true`; the existing shared LiveKit v1 helper keeps `simulcast: true`, 720p max capture, 30fps max, and a 1.7 Mbps camera encoding cap. SDK audit confirms Audio RED exists as an inherited default and was not changed. Chi'lly Chat video calls were audited and intentionally excluded because the current route uses `useCommunicationRoomSession` with direct `RTCPeerConnection`, not LiveKit Room ownership; chat calls keep the separate four-participant, 640x480 ideal / 720p max / 24fps max communication posture. New guard `guard:livekit-simulcast-dynacast-policy` locks the two intended call sites and proves Party Room, standalone Player/HLS/VOD, Spectator, Chi'lly Chat, token issuer, video-audio publisher behavior, and the four-seat launch caps stay untouched. Android proof is at `/tmp/chillywood-livekit-simulcast-dynacast-proof-20260530/`; two-device media/performance, TURN/cellular, reconnect, 10-participant staging/load, and server metrics proof remain required before increasing live seat limits.
- Google Play Console Field-By-Field Completion Assistant on May 30, 2026 turns the Play execution package into an operator packet without claiming external submission. New docs `docs/google-play/PLAY_CONSOLE_FIELD_BY_FIELD_ANSWERS.md`, `PLAY_REVIEWER_TEST_ACCOUNT_PACKET.md`, `CONTENT_RATING_QUESTIONNAIRE_PREP.md`, and `RELEASE_UPLOAD_CHECKLIST.md` give exact owner-facing answers, confidence levels, evidence, owner actions, screenshot/legal-review requirements, Data Safety answer guidance, account deletion field copy, reviewer test-account instructions, content-rating prep, ads guidance, and release upload checklist. `STORE_LISTING_ASSET_CHECKLIST.md` is field-ready with draft copy, asset gaps, version fields, and AAB proof boundaries. The packet keeps P0 at 1 because Google Play/Data Safety/content-rating/account-deletion acceptance, final legal approval, support/SLA commitments, and final Play upload signing remain external; the older AAB proof boundary is superseded by the fresh current-HEAD local build proof above.
- Google Play Data Safety Account Deletion Acceptance Closeout on May 30, 2026 creates the repo-side Play Console execution package while keeping the final P0 external. New docs under `docs/google-play/` map App content, Privacy Policy, Data Safety, Ads, App access, Target audience, Content rating, Account Deletion, Store Listing, Permissions, UGC/moderation, Billing, account-deletion URL content, reviewer instructions, and store assets. Public URL proof at `/tmp/chillywood-google-play-acceptance-closeout-20260530/public-url-check.tsv` returned HTTP 200 after redirects for Privacy, Terms, Account Deletion, Copyright, Copyright Report, Support, Moderation Policy, Community Guidelines, and Creator Rules. Android proof at `/tmp/chillywood-google-play-acceptance-closeout-20260530/android/` captures Settings Legal and Support, Privacy, Terms, Account Deletion, Copyright Report, and Moderation Policy; direct Support deep link proof remains covered by the prior May 29 release proof until recaptured. The P0 count remains 1 because Play Console/Data Safety/content-rating/account-deletion acceptance, legal approval, and operational owner commitment are external and not faked.
- Media Malware Scanning Pipeline Implementation and Production Deployment on May 30, 2026 replace the earlier scanner-policy deferral with a real repo-side pipeline, linked-Supabase runtime proof, production runtime, and Admin scan-result review. Remote-applied migrations `20260530191115_media_malware_scanning_pipeline.sql`, `20260530193203_fix_dmca_scan_enqueue_loop.sql`, and `20260530203440_admin_media_scan_read_model.sql` add scan metadata, `media_scan_jobs`, service-role claim/complete RPCs, upload/update triggers, public-safe rendering/storage gates, sanitized Admin scan read model, and System history scan rows. `ops/malware-scanner-worker/` adds a ClamAV worker that downloads Supabase Storage objects with service-role auth, runs `clamscan`, and writes clean/malware/failed/manual results back. `infra/hetzner/docker-compose.malware-scanner.yml` plus `ops/malware-scanner-worker/deploy-hetzner.sh` deploy the long-running healthy `chillywood-malware-scanner` service on `chillywood-prod-01`. Runtime proof at `/tmp/chillywood-malware-scanner-runtime-proof-20260530/` scanned temporary private `dmca-evidence` objects against linked Supabase. Production proof at `/tmp/chillywood-malware-scanner-production-proof-20260530/` shows benign `clean`, EICAR `malware_detected`, Admin read model visibility, and cleanup. The blocker counts are now P0 1, P1 10, P2 10.
- Admin Users/Usage/System Read Model Production Closeout on May 30, 2026 modernizes the broad Admin drilldown surfaces and closes the backed read-model gaps where current schema supports them. Remote-applied migrations `20260530173834`, `20260530174810`, and `20260530180452` add permission-gated RPCs for broader Users account/Premium/report/block/profile-media/deletion-request signals, recent Usage rows over usage/provider/room/media metadata, and System history over immutable audit/event tables, including provider readiness audit and Stripe provider webhook event evidence where backed. `_lib/adminReadModels.ts` and `app/admin.tsx` render those rows with masked identifiers, no destructive controls, no raw storage paths, no auth secrets, no provider secrets/payload values, no LiveKit tokens/raw room tokens, no service-role keys, no fake money, and no live-money action. Current release Android proof on `R5CR120QCBF` lives at `/tmp/chillywood-admin-read-model-gap-closeout-20260530/` and captures Users, Usage, and System read-model surfaces, including Provider Rows. The consolidated Admin IA remains Overview / Money Center / Users / Reports / Live Ops / Rachi / Legal / System / Owner Security. Release build/deploy history remains unclaimed until backed by a real event source.
- Standalone Player Playback Regression and Playback Control Simplification on May 30, 2026 restores standalone playback while keeping the standardized overlay off room/live surfaces. The Android regression came from native video/tap-layer ownership after the overlay pass: video loaded, but center tap-to-play did not reliably reach the standalone playback handler. The Player now passes native video touches through an overlay gesture target, keeps the target below top/bottom controls, and updates play/pause state after native playback calls. Normal title Player release proof on `R5CR120QCBF` advanced to `0:03` at `/tmp/chillywood-standalone-player-playback-menu-fix-20260529/`. The follow-up Player polish removes the black standalone Playback sheet entirely: no visible `Playback`, `Speed and quality`, `Quality`, Auto-quality row, or tune/settings icon remains. Quality stays automatic/internal, and the remaining compact `1x` chip cycles speed directly without opening a panel. Current Android proof lives at `/tmp/chillywood-player-playback-control-20260530/`. Normal title/content Player, creator-video/public Platform/Rachi Player paths, and safe Spectator playback still share top-left social/report actions, top-right Watch-Party Live only where eligible, and bottom timeline/playback/fullscreen controls. Watch-Party Live shared Player, Live Watch-Party / Live Stage, Party Room, LiveKit participant tiles, Audio Mix, LiveKit token issuer, Watch-Party route ownership, old-room handling, Premium gates, Money Center, Profile, Platform Studio, Clip Studio, Brand Studio, Spectator routing, and Admin behavior were not changed.
- Store Legal Account Deletion Ops Closeout on May 29, 2026 maps the remaining Public V1 store/legal/account-deletion blockers without claiming external acceptance. Public legal URLs for Privacy, Terms, Account Deletion, Copyright, Copyright Report, Support, Community Guidelines, Creator Rules, Moderation Policy, and Premium Terms returned HTTP 200 after redirects, and proof is recorded at `/tmp/chillywood-store-legal-account-deletion-ops-closeout-20260529/`. Cloudflare MX, SPF, and DMARC baseline are present for `chillywoodstream.com`; DKIM remains pending until a real outbound provider is configured and verified. New runbooks document Play/Data Safety/account-deletion acceptance, account deletion operations, support/moderation/deletion SLA roles, outbound email/DKIM setup, and malware scanning readiness: `docs/legal/STORE_LEGAL_ACCOUNT_DELETION_ACCEPTANCE_CLOSEOUT.md`, `docs/legal/OUTBOUND_EMAIL_DKIM_RUNBOOK.md`, and `docs/security/MALWARE_SCANNING_READINESS_PLAN.md`. No Play Console acceptance, attorney approval, support staffing, automated email, DKIM verification, or disposable report lifecycle proof is claimed. After the May 30 malware-scanning production closeout, the blocker counts are P0 1, P1 10, P2 10; the P0 is external store/legal/account-deletion acceptance.
- Blocker 8 moderation/legal ops closeout on May 29, 2026 updates the Public V1 blocker map without adding features. The repo now treats report lifecycle/admin moderation as backed rather than unbuilt: safety report intake, admin status updates, target hide/remove/restore, immutable report audit rows, DMCA intake/counter-notice tooling, public legal/support surfaces, Profile media report actions, and Profile media flagged/admin_removed/restore public masking are all code/schema-backed. Proof is recorded at `/tmp/chillywood-blocker8-moderation-legal-closeout-20260529/` plus the existing release screenshots for Support, Account Deletion, Copyright Report, Moderation Policy, Admin Reports, Privacy, and Terms. Scanner runtime proof is recorded at `/tmp/chillywood-malware-scanner-runtime-proof-20260530/`, and production scanner/Admin review proof is recorded at `/tmp/chillywood-malware-scanner-production-proof-20260530/`. This does not close the external launch blocker: attorney approval, Play Data Safety/content-rating/account-deletion acceptance, support/account-deletion SLA ownership, outbound email/DKIM, and optional disposable-fixture report lifecycle visual proof remain before broad public launch. No RLS, LiveKit, Watch-Party, Premium, money, or picker behavior changed in that lane.
- Public V1 Eight Blocker Burn-Down on May 29, 2026 updates the launch boundary in `docs/PUBLIC_V1_READINESS_BLOCKER_MAP.md`. Current release Android build/install/open proof is now captured from `main`: `assembleRelease` and `bundleRelease` passed, the release APK installed on `R5CR120QCBF`, and the app opened past splash into Home with bottom navigation. Fresh proof also covers Profile from the top avatar, public Platform, Platform Studio, Player, Money Center, Admin, Watch-Party, Live Watch-Party, Spectator unavailable state, Settings/Profile Appearance, Support, Account Deletion, Copyright Report, Moderation Policy, Privacy, and Terms. The remaining P0 launch blocker is external store/legal/account-deletion acceptance; after the May 30 malware-scanning production closeout, the map records 10 P1 blockers and 10 P2 deferrals. Profile media save/read-back/remove/fallback is intentionally left partial for manual safe-asset runtime proof, while second-account/blocked/private, Watch-Party two-device, Spectator live-compatible fixture, RevenueCat/Google signed sandbox, and external moderation/legal ops remain exact non-faked blockers. Proof lives at `/tmp/chillywood-public-v1-eight-blocker-burndown-20260529/`, and full validation is logged at `/tmp/chillywood-public-v1-eight-blocker-burndown-20260529/full-validation.log`. The next recommended lane is Store Legal Account Deletion Acceptance Closeout, followed by release diagnostics/log-redaction and fixture-backed runtime closure.
- Admin Denial IA Consolidation and Drilldown Production Pass on May 29, 2026 makes the Owner/Admin Command Center feel like a safer production control center without faking data. The visible Admin IA is consolidated to Overview / Money Center / Users / Reports / Live Ops / Rachi / Legal / System / Owner Security, while specialized state keys remain routable and permission-owned behind those groups. Users staff-roster rows now open masked admin-safe detail sheets; Usage summaries open inspect-only read-model drilldowns; System cards open inspect-only no-secret detail sheets. Admin Search audit writing remains active and an owner/admin Android receipt is captured at `/tmp/chillywood-admin-denial-ia-drilldown-proof-20260529/`. Normal-user API/RLS denial proof passed with a configured non-staff account, but Android normal-user panel denial remains unclaimed until a safe normal-user session and owner-session restore path are available. No fake admin rows, fake users, fake money, secret exposure, RLS weakening, LiveKit/Watch-Party/Premium/old-room change, or live-money activation was added.
- Owner/Admin Main Tabs UI/UX Audit and Search Modernization on May 29, 2026 audits every current Admin main tab and records the route-safe path toward a simpler Overview / Money Center / Users / Reports / Live Ops / Rachi / Legal / System / Owner Security model in `docs/ADMIN_MAIN_TABS_UI_UX_AUDIT.md`. No risky tab rewrite happened; current specialized tabs remain permission-gated and hidden from normal users. Admin Search now has ranked results, result-type count chips, and session-local recent-search chips that skip email-shaped or secret-like queries and are not persisted. Query-level audit writing stays active through `write_admin_search_audit`, Admin email lookup remains masked/Admin-only, and no fake admin rows, fake money, provider secrets, LiveKit route/token changes, Premium changes, or live-money activation were added. Android proof path is `/tmp/chillywood-admin-main-tabs-ui-ux-audit-proof-20260529/`.
- Admin Search Audit Denial Spectator Profile Proof Closeout on May 29, 2026 closes the post-search audit writer without changing public discovery or money activation. Migration `202605290004_admin_search_query_audit.sql` adds masked Admin Search audit helpers and `write_admin_search_audit`; Admin Search now writes query-level, email-lookup, denied, and result-open audit events into immutable Admin audit logs with scope, query type, masked preview, status, result count, and no raw email/plain query storage in metadata. Normal-user API/RLS denial proof passed with a configured non-staff proof account, including denied Admin Search audit RPC response, zero visible Admin audit rows, and public People email query returning no rows/email fields; Android normal-user panel denial remains unclaimed because the attached device session was owner/admin. Spectator Live Watch-Party / Reaction remains blocked on a true public-safe live-stage-compatible fixture, while previous Watch-Party Live and replay child-room proof remains current. Profile media save/read-back remains blocked because Android DocumentsUI showed a safe Chi'llywood image but did not return the selection during automation; no private gallery image was used. Android proof lives at `/tmp/chillywood-admin-search-audit-denial-spectator-profile-proof-20260529/`. No LiveKit, Watch-Party route ownership, Premium gate, old-room handling, live money, or public search behavior changed.
- Explore People Search and Admin Email Lookup Safety Pass on May 29, 2026 makes Explore the public people discovery surface without turning Profile into global search. Explore search now has All / Content / People / Platforms / Live / Events scopes, backed public People results through `search_public_people`, and compact rows with avatar/fallback, display name, username, official/Platform badges, public bio, View Profile, and View Platform where backed. Remote-applied migration `202605290003_public_people_search_operator_proof_hardening.sql` keeps the RPC searching username/display name/public Platform name only, blocks email-shaped public queries, respects `can_view_profile_content` privacy/block policy, masks non-active profile media, excludes active owner/operator/moderator/security/support/system accounts, internal/proof/service ids, and proof/operator display markers, and returns no email, phone, private ids, role data, fake stats, fake uploads, or activity. Rachi can appear only as explicit public official `Official Chi'llwood`. Owner/Admin email lookup remains Admin/staff-only and was not added to public Explore. Android `R5CR120QCBF` proof lives at `/tmp/chillywood-explore-people-search-proof-20260529/`. No fake users, fake creators, Profile global search, LiveKit, Watch-Party, Premium, Money Center, or Rachi backend behavior changed.
- Explore Typeahead and Owner/Admin Search Production Pass on May 29, 2026 builds on the public People search pass. Explore now uses debounced typeahead/autocomplete with All / Content / People / Platforms / Originals / Live / Events scopes, grouped backed suggestions after two characters, a clear search action, public-safe People/Profile rows, public Platform rows, Rachi public-safe Originals, backed Live/Event suggestions, and compact empty states. Public Explore still has no email lookup, no Admin/Money/Provider/Audit scope, no fake suggestions, and no owner/admin/security/system/proof-operator account exposure. Admin Command Center now has a permission-gated `Search Admin` typeahead over already-loaded Admin sources: users/staff roles, reports, DMCA, money audit events, kill switches, provider readiness, Rachi posts/Originals, Live Cost Guard/Live Ops, legal requests, and immutable audit rows. Admin email lookup is Admin-only and masks email identity in result rows; no new public RPC, RLS weakening, provider-secret rendering, money activation, LiveKit, Watch-Party, or Premium changes were made. Android proof path is `/tmp/chillywood-explore-typeahead-admin-search-proof-20260529/`, and new guard coverage includes `guard:admin-search-policy`.
- Restore Home Hero as Continue Watching Only on May 29, 2026 removes the random title fallback while preserving the cinematic Home hero. `Chicago Streets` appeared because the old Home spotlight chain fell back from missing Continue Watching to a programmed/latest-title hero; Home now shows a large branded Chi'llwood hero when no eligible progress exists, and a cinematic `Continue Watching` hero only for the most recently watched eligible item. Eligibility requires at least 10 seconds of real progress, less than 94% completion when duration is known, and an available title row that is not unpublished/draft/scheduled/archived/deleted/private/restricted/ticketed. Not-started, finished, saved/favorite, unavailable, and broad discovery titles no longer dominate Home: Explore owns browse/discovery and Library owns saved/favorites/history. Android `R5CR120QCBF` proof lives at `/tmp/chillywood-home-continue-watching-proof-20260529/` and captures Home with branded cinematic hero/no random title hero, Explore, Library saved/zero-progress state, Player opening the saved title, and Rachi/Originals still visible. No fake progress, fake rows, LiveKit, Watch-Party ownership, Premium gate, Explore, Library, Player, Rachi, or schema behavior changed.
- Home Top Picks Browse Favorites Removal and Profile Header Cleanup on May 29, 2026 keeps bottom navigation at Home / Explore / Live / Library and tightens the post-IA surfaces without redesigning the app. Home no longer renders Top Picks, Browse, Favorites, followed-Platform, or latest-public-upload catalog sections; Explore covers browse/discovery work and Library covers saved/favorites work, while Home stays focused on cinematic branded/Continue Watching hero, Live Now, Rachi Official Updates, Chi'llwood Originals, From Your Chi'lly Circle, Upcoming Events, and the existing ad slot. Home, Explore, Live, and Library now expose top Profile/avatar and Settings access, while Profile remains hidden from bottom navigation and detail/room/Profile/Platform/Studio/Admin/Player routes keep route-local controls. Rachi Official Updates show Rachi identity with avatar/fallback, `Rachi`, `Official Chi'llwood`, and backed timestamps, and public Rachi Originals cards no longer leak internal proof/fixture wording in normal Home copy. Profile feed empty states now say `No posts yet` / `No public posts yet`; the old `Your feed is ready when you are` card and random feed-level Platform CTA are removed. Android `R5CR120QCBF` proof lives at `/tmp/chillywood-home-profile-cleanup-proof-20260529/`. No fake content, fake saved rows, LiveKit, Watch-Party ownership, Premium gate, Money Center, Rachi backend, or schema behavior changed.
- Profile Photo Modern Picker UI Correction on May 29, 2026 simplified the owner avatar edit entry without changing Profile routes or media storage. That first-sheet correction is now superseded by the May 31 Profile Media Modern Review fix: Profile Photo/Profile Background still start from compact sheets, but native Android crop/edit is disabled and the app-owned in-app review sheet handles Fill/Fit/Center before save. Android `R5CR120QCBF` proof for the original compact sheet lives at `/tmp/chillywood-profile-photo-picker-proof-20260529/`; current proof for the updated review/full-page background path lives at `/tmp/chillywood-profile-brand-media-one-device-proof-20260531/`.
- Remaining UX blocker burn-down on May 29, 2026 moved Explore and Library from planned/honest placeholders to backed-only surfaces where safe. Explore now reads title search, public discovery feed rows, public creator videos, Rachi public-safe Originals, and public event summaries, with honest empty states for live, Platforms, events, and replays when no rows exist. Library now reads saved titles, watch progress, and followed Platform profile read-back, while replays/events/clips stay hidden until saved rows exist. Player gained scoped surface-mode labels/resolution instead of a risky rewrite, and the Watch-Party waiting room gained a UI-only host preflight for real title-linked Watch-Party Live starts. Android proof lives at `/tmp/chillywood-public-v1-blocker-burndown-proof-20260529/`. Remaining Public v1 proof gaps are fixture/device/account gaps only: Profile media picker/save/remove/read-back needs a safe non-private asset and working edit sheet proof, Spectator Live Watch-Party/Reaction needs a real compatible fixture, and Watch-Party Live speech ducking needs a second joined device/account.
- navigation clarity now targets the app's actual product modes instead of the older thin tab shape. The May 28, 2026 Navigation Terminology and App Flow Clarity Pass first moved normal bottom navigation to Home / Explore / Live / Library / Profile, added a Live entry surface that preserves existing Live Watch-Party and Watch-Party Live route ownership through existing Premium/runtime preflight, added a Profile tab handoff to canonical Profile, renamed My List to Library without faking new saved sections, tightened Explore copy around current title-only search scope, and recorded the app mode/terminology/deeplink map in `docs/NAVIGATION_TERMINOLOGY_MAP.md`. The follow-up Live Hub Modern UI Density Pass compressed the Live tab into a modern launcher with Hero header, Choice chips, Compact action cards, Action rows, Status pills, Primary/secondary CTAs, Progressive disclosure / Collapsible details, and an honest Empty state while preserving the same routes and Premium preflight; Android proof lives at `/tmp/chillywood-live-hub-density-proof-20260528/`. The IA completion pass then added backed-now/planned-later scope to Explore, clarified Library as saved-title-only with future My Stuff sections, source-audited Player naming without rewriting it, and updated the guard/doc map; Android proof lives at `/tmp/chillywood-nav-ia-completion-proof-20260528/`. The final modern navigation pass removes Profile from the visible bottom tab bar, leaving Home / Explore / Live / Library while keeping Profile reachable through the Home top avatar/profile entry, direct profile routes, Settings, and Profile actions; Android proof lives at `/tmp/chillywood-modern-nav-ia-proof-20260528/`. Platform Studio remains a Profile/Platform owner action, and Admin remains outside normal viewer navigation.
- login, settings, and logout; login includes a subtle Supabase-backed `Forgot password?` reset-email request targeting the allowlisted mobile recovery link `chillywoodmobile://reset-password`, plus an in-app `/reset-password` recovery route and recovery-link rescue, with no reset tokens or passwords logged
- Settings now keeps account controls, notification preferences, Admin Dashboard discoverability for authorized staff only, and signed-in Change Password; private recent/backed activity lives on the signed-in user's own Profile feed, while public profile feeds stay public-only. The May 24, 2026 Settings and Premium UI/UX Compression Pass turns Settings into a branded Chi'llywood control center with Account, Profile and Platform, Notifications, Privacy and Safety, Premium, Legal and Support, and App Info accordions; compresses Account and Notifications details behind grouped accordions; removes Legal Policy Center word counts; keeps legal/support/account deletion links reachable; and makes Manage Premium user-facing with honest Not active / Temporarily unavailable copy, Restore purchases, Premium unlocks, and no normal-user backend/debug proof wording. This pass changed layout/copy only and did not change Premium gates, RevenueCat/store entitlement logic, auth/session behavior, legal policy meaning, Watch-Party Live, Live Watch-Party, LiveKit, or admin tabs.
- home and discovery, including Explore client-side title filtering over already loaded platform `titles` rows; future unified Explore must wait for backed creator/platform/upload/live/event/read models and must not fake results
- customizable basic profiles
- public Platform route at `/channel/[userId]`
- public Platform safe-area/stat polish at `4873f08`, with role-safe public layout, no fake follower counts, no non-owner Platform Studio controls, and no draft/private/unpublished content exposure
- Profile / Public Platform / Platform Studio runtime proof plus Creator Media runtime proof closed on May 13, 2026 using one physical Android device with real owner and backed viewer accounts by account switching. Profile posts, comments, replies, likes, links, supported attachments, report UI, public/draft/deleted visibility, Channel owner/viewer/signed-out/blocked states, Follow/Following, Platform Studio owner settings, creator media upload/playback, metadata edit, publish/unpublish/delete, storage removal, non-owner denial, and safe missing-media behavior are proved without fake data, storage-secret exposure, owner-only leakage, D7F spectator regression, LiveKit routing change, billing/finance change, or unrelated UI redesign. The signed-out public channel read blocker was fixed with a sanitized public-profile RPC instead of broad RLS access.
- Platform Studio Production UI/UX and Routing Pass is implemented at `2061ca85795fd1b4ebef1e2a234cbf9f34f909b8`: older creator-platform wording is replaced with Platform, Home is compressed into identity/actions/Today/Needs Attention plus Create and Manage, Live and Events, Audience, Monetization, Moderation and Safety, Insights, and Brand Settings accordions, every Quick Action routes to a backed destination or a friendly locked/unavailable state, public Preview Platform uses `preview=public` with no owner controls and no draft exposure, and no Premium, LiveKit, upload, payout/live-money, creator-gate, auth/session, or admin behavior changed. The May 26, 2026 Money Center consolidation keeps one creator-facing `Monetization` tab but makes `Money Center` the page title/source of truth with collapsible Overview, Digital Sales, Tips, Watch-Party Seats, Paid Content, Merch, Creator Balance, Payouts, Tax & Legal, Provider Status, Future Tools, and owner/dev-only Technical checks; old `Monetize`, `Revenue`, `Payouts`, `Premium`, `Stripe`, `Store`, and commerce params/routes map into Money Center sections. Google Play/RevenueCat remains the Android digital purchase path, Stripe Connect remains the creator-payout path, merch stays separate as physical goods, creator balance is ledger-first, and no fake earnings, fake balances, fake tips, fake paid content, fake Watch-Party seat sales, fake merch sales, fake payout readiness, checkout, payouts, transfers, withdrawals, fake Premium access, or live money movement is enabled. The Provider-Link Readiness Scaffold adds a backend-owned readiness source for RevenueCat, Google Play, Stripe, Stripe Connect, payouts, revenue imports, tips, paid content, commerce, ads, and internal monetization policy; Platform Studio reads sanitized readiness summaries, all seeded rows stay non-live, webhook shells fail closed, and Provider-Link Sandbox Proof marks only Stripe webhook signature readiness as `sandbox_ready`. RevenueCat and Google Play remain setup-required until their server/webhook secrets are linked, and no production active/live-money claim exists.
- The May 27, 2026 Money Center production scaffold adds backend-enforced Owner/Admin kill switches without activating live money. `platform_money_kill_switches` and append-only `platform_money_kill_switch_audit` now control `money_center_visible`, digital sales, tips, Watch-Party seats, paid content, merch, creator balance visibility, payouts, Stripe Connect, RevenueCat/Google Play, provider webhooks, creator monetization, revenue imports, tax/KYC, ads revenue, sponsorships, and `live_money_enabled`. Creator Money Center reads sanitized switch summaries plus provider readiness. Defaults keep live money, sales, tips, seats, paid content, merch, payouts, revenue imports, tax/KYC, ads, and sponsorships off; store/Connect/webhook readiness is sandbox-only. Switches alone cannot create checkout, tips, paid access, balances, transfers, withdrawals, payouts, provider grants, or live money. The follow-up Android refresh proof applied and aligned the switch migration in the linked Supabase environment, refreshed `R5CR120QCBF`, rebuilt a release APK with current JS when Metro/dev-client did not attach to Admin, installed over the owner session, and captured current JS Money Center plus Owner/Admin Money Controls at `/tmp/chillywood-money-center-android-refresh-proof-20260527/`. Backend proof kept `live_money_enabled=off`, denied unauthorized switch writes, and the Admin high-risk Live money confirmation sheet was opened then cancelled with no live-money state change. The Owner/Admin Money Center consolidation pass then replaced separate visible Admin Premium, Kill Switches, Ads, Revenue, Payouts, Sponsors, and Fraud money tabs with one visible Money Center tab. It organizes Overview, Kill Switches, Premium / RevenueCat / Google Play, Sponsors / Ads, Fraud & Risk, Digital Sales, Tips / Watch-Party Seats / Paid Content, Merch, Creator Balance / Ledger, Payouts / Stripe Connect, Provider Webhooks, Tax & Legal, Audit Trail, and Technical Checks into collapsible sections, maps old Admin money params into those section anchors, keeps Usage/Networks/Live Cost Guard separate as non-creator-money operations, and still activates no live money or fake money. The Money Center Ledger Audit Explorer follow-up makes creator setup/readiness/ledger rows clickable with sanitized `Money Event Detail`, adds an owner/admin `Money Audit Explorer` with filters for sandbox/setup/provider/ledger/payout/sponsor/fraud/webhook/blocked rows, labels sandbox and setup rows `Not payable`, filters secret-like/raw provider fields, and keeps the explorer inspect-only with no payout approval, revenue import, checkout activation, sandbox-to-production promotion, balance creation, or withdraw/cash-out action. Android consolidation proof at `/tmp/chillywood-admin-money-center-proof-20260527/` captures the new Admin tab row, expanded sections, high-risk reason sheet opened/cancelled, audit/technical checks, and creator Money Center consistency after a current release APK install over the owner session. Android drilldown proof at `/tmp/chillywood-money-audit-explorer-proof-20260527/` captures creator event rows/detail, creator balance detail with no verified earnings/not payable, Provider Status readiness, Owner/Admin Money Audit Explorer metrics and Sandbox/Setup filters, sandbox row detail, kill-switch event detail, sponsor/fraud/money-control drilldowns, no secret exposure, no fake money, and no withdraw/cash-out action. The provider CLI follow-up at `/tmp/chillywood-provider-cli-proof-20260527/` fired and resent a Stripe CLI test-mode `payment_intent.succeeded` webhook to the enabled Connect test endpoint, verified `livemode=false`/`pending_webhooks=0`, and proved the Admin Money Audit Explorer row remains sandbox-only, not payable, ignored, and duplicate-safe. RevenueCat/Google signed webhook proof remains blocked on missing server webhook secrets/provider permissions, not live-money activation.
- Profile Production UI/UX and Navigation Pass is implemented: Profile now reads as the social identity hub with owner actions for Platform Studio, Platform, Chi'lly Chat, Chi'lly Circle, and Settings; viewer actions for Follow/Following or sign-in follow handoff, Chi'lly Chat, View Platform, Share/Profile safety where backed; no top Profile Upload CTA; creator-video creation remains in Platform Studio; user-facing Channel labels on Profile are Platform; the duplicate bottom Platform tab/pill is removed and the Profile tab row is Posts, Live, Community, and About; the composer/feed are one connected Posts surface; post attachments remain; public Platform preview uses `preview=public` with no owner controls or draft exposure; and no Premium, LiveKit, Watch-Party, Brand Studio, monetization, auth/session, or native config behavior changed. The May 26 Rights Disclosure UI Disablement Pass removes all visible Rights Disclosure UI from Clip Studio, creator-video upload/publish forms, Watch-Party Live waiting room, Watch-Party Live Party Room, Live Watch-Party waiting room, Live Watch-Party Live Room / Live Stage, setup/status panels, room-code panels, and Spectator pages. Backend disclosure helpers/tables, if kept, are dormant future audit support only. Copyright safety currently relies on Terms, Community Guidelines, Report/Copyright flow, DMCA/takedown, repeat-infringer policy, and moderation/admin removal. No note field, long legal paragraph, LiveKit change, source eligibility bypass, Premium bypass, or DMCA/report bypass was added. The May 26 Watch-Party Live Audio Mix pass is scoped only to the shared player in `app/player/[id].tsx`: video audio remains local player audio, voices remain LiveKit audio, Auto-duck defaults on with video `85%` / ducked `30%`, and `Audio Mix` is absent from Party Room, Live Watch-Party / Live Stage, and waiting rooms.
- Rachi Official Circle and Originals Production Pass makes Rachi the official Chi'llywood account, first pinned Chi'lly Circle official connection, official update voice, and Chi'llwood Originals source. Rachi is not a private-chat watcher, not a fake normal user, and not owner/admin authority. Rachi Help is opt-in and says Rachi only sees messages sent there. Home reads real public Rachi posts for `Rachi Official Updates` and real public-safe Rachi-owned videos for `Chi'llwood Originals`; empty states remain honest when no backed content exists. Admin's Rachi tab can create official Rachi posts through an owner/operator-only audited RPC, shows real recent posts and real public Originals, and does not fake followers, engagement, posts, or uploads. Android owner/operator proof created one real Rachi update through Admin and showed it on Rachi Profile plus Home. The Rachi Originals fixture proof adds remote-applied `official_rachi_original_videos` linkage plus proof video `6e1c3405-7db8-4cb2-98f3-5a7642e82126`, `Chi'llwood Originals Proof Fixture`, attributed to `Big Buck Bunny by Blender Foundation, CC BY 3.0.`, and Home plus Rachi public Platform now show it through sanitized public cards with no raw playback/storage paths, drafts/private rows, fake stats, or owner/admin controls; public link-table reads also require the linked video to remain public and clean/reported. Admin Rachi Profile Picture uses the device photo gallery through `Choose from Gallery` and never asks operators to paste an image URL; actual gallery save proof still waits for a safe non-private gallery image. Normal users cannot post as Rachi or manage Rachi Platform/Studio, and public Rachi surfaces keep draft/admin controls hidden.
- Profile Viewer State Runtime Proof Closeout is recorded on Android `R5CR120QCBF` with screenshots/UI dumps at `/tmp/chillywood-profile-viewer-state-proof-20260525/`: signed-out public Profile opens with no owner controls/composer/Attach/delete/draft badges, Follow and Chi'lly Chat show sign-in-required handoffs, and View Platform opens public Platform without Studio/drafts. Signed-in non-owner proof used the available authenticated account viewing the official Rachi Profile: owner controls, delete, draft/reported badges, composer, and Attach were absent; Chi'lly Chat opened the backed official thread; View Platform opened public Platform. Owner regression after viewer tests confirmed Platform Studio, Preview Platform public view, Chi'lly Chat inbox, Chi'lly Circle, Settings, composer, Attach, owner delete, and owner draft markers still work. No blocked/private runtime fixture or true second-account credential was available, so static source/guard proof covers `resolveProfilePrivacyAccess`, Profile `canViewFullProfile` gates, blocked Chi'lly Circle handling, public Platform `isViewerBlocked`, and `includeDrafts: false`; a later fixture lane should runtime-smoke real blocked/private accounts. Validation passed with the requested type/runtime/Profile/payment/creator/Clip/Brand/Watch-Party/provider guard stack, targeted Profile grep/static proof, and diff whitespace checks.
- Profile Social Interaction Regression plus attachment UX pivot is recorded on Android `R5CR120QCBF` with screenshots/UI dumps at `/tmp/chillywood-profile-social-interaction-proof-20260525/`: a real owner Profile post with a real image attachment was created, rendered with attachment preview plus Like/Comment/Share/Delete controls, like/unlike was proved, a real owner comment was posted, and the proof post/comment were cleaned through backed owner RLS. Profile and Chi'lly Chat now show the shared modern attachment sheet with Photos and Files choices; the same sheet is wired into creator-video comments, Watch-Party room comments, and Live Stage comments while existing storage, validation, runtime controls, privacy/RLS, and raw-storage-path protections stay unchanged. A follow-up correction removed Platform Studio from the social attachment sheet entirely, made Photos open the phone gallery through `expo-image-picker`, and kept Files on `expo-document-picker`; creator media/legal evidence/Brand Studio pickers remain separate. The operator checked Player/Watch-Party/Live Stage sheet behavior, so route-specific screenshots are no longer a remaining attachment-proof blocker. Reply submission, Share sheet runtime, true second-account, and blocked/private fixture proof remain future runtime lanes rather than faked proof.
- Profile Avatar Background and User Actions Sheet is implemented repo-side and `202605260001` is migration-applied: owner avatar tap/long-press opens an edit sheet; Profile Settings has Profile Appearance controls for Profile Photo, Profile Background, and Preview Profile; uploads use the phone gallery with safe in-app fit controls, MIME/size validation, owner-only `user_profiles` writes, and profile-only `profile-media` storage. Viewer avatar tap/long-press opens Profile Actions with View Profile Photo, Chi'lly Chat, View Platform, Block User, Report User, and Share Profile where backed. Block User requires sign-in and confirmation, rejects self-block, writes through the existing viewer-owned `channel_audience_blocks` helper path, refreshes relationship state, and blocked Chi'lly Chat refuses direct thread creation. Profile media is separate from Platform/Brand Studio media; locked/private/blocked shells do not render private avatar/background media; sheets never show raw storage paths. The owner-controlled media-status follow-up is remote-applied as `202605260002_profile_media_status_policy.sql`: valid Profile photo/background uploads publish immediately as `active`, owner removals become `user_removed`, reported media can be targeted as `profile_media`, admin hide/remove/restore maps to `flagged`/`admin_removed`/`active`, and public Profile RPC rendering masks non-active avatar/background URLs without a default manual approval queue. Android `R5CR120QCBF` startup proof after lazy image-picker loading is under `/tmp/chillywood-profile-avatar-actions-proof-20260526/`; the latest current-build proof is `/tmp/chillywood-profile-brand-media-one-device-proof-20260531/`. Safe second-account/blocked/private fixtures and richer Profile media moderation/review UI remain future work only if product needs it.
- Platform Studio Brand Studio and Hero Media Production Pass is implemented at `32b46780132b6755e356afbd259e836a83bff5ad` as repo-side creator-facing Platform customization. Brand Studio now has grouped Hero Media, Background, Avatar and Logo, Theme, Scene Presets, Public Preview, and Platform Defaults sections; hero image/background/avatar/logo uploads use private draft assets and safe metadata previews; Hero Reel and watermark remain honest unavailable states until backed; public Platform rendering uses only published moderation-safe brand assets and falls back to the Chi'llwood city look when none exist.
- Platform Brand Studio Review and Failure-State Closeout is repo-side implemented, Supabase-applied, and happy-path proved: remote migrations `202605240001` through `202605240007` now add private brand assets/profiles, public-safe asset-id sanitization, reviewer events, `review_platform_brand_asset`, immutable admin audit writes, UUID/text reference fixes, owner/operator/moderation-only pending asset/storage queue access, service-role-only cleanup candidates, explicit cleanup runtime guard, and a transaction-local review context so the authorized review RPC can update moderation fields while normal clients cannot. Linked `supabase/database.types.ts` was regenerated after `202605240007`. Real proof used a temporary scoped reviewer to upload, approve, publish, reject, archive, and leave pending real Brand Studio image assets; public reads returned only the four approved/published hero/background/avatar/logo assets, rejected/archived/pending controls stayed private, review/admin audit rows were written, cleanup candidates included terminal controls and excluded approved published assets, unsupported upload was rejected, and the temporary reviewer was revoked then denied with `brand_review_forbidden`. Android `R5CR120QCBF` proof showed Brand Studio and public Platform preview with approved media and no Studio controls. Hero Reel, watermark, Premium, RevenueCat, LiveKit, Watch-Party, payout, creator-video, and public preview gates remain unchanged. Next Brand Studio work should focus on Level 2 cropper support and service-role cleanup automation.
- Chi'llwood Clip Studio MVP is implemented at `40c6fea9554fbb6ce084241daaa7c35b5792eecb`, Save Draft hardening is implemented at `a716ee1ed5a880922b8d6dc49896655392fc9b25`, cover/poster proof hardening is implemented at `84a9109b2ccd939da85ed3595301d5a578bc6165`, and the May 25, 2026 closeout commits add the Title Card/Templates preview layer, safe public cover/poster cards, and sanitized public published-card title/template metadata. Platform Studio has `Create Clip` entry points, a Clip tab, content-card `Edit Clip`, selected-video preview through the existing picker/upload path, format/fit metadata, private cover image upload support, Title Card controls for optional overlay title/subtitle, Top/Center/Bottom placement, Clean/Bold/Spotlight/Trailer styles, template presets for Trailer, Highlight, Promo, Event, Reaction, and Platform Intro, approved Brand Studio mark preview-only integration, Save Draft/Publish Clip controls, a private owner-only `creator_clip_edits` table, and a `guard:clip-studio-policy` script. Save Draft blocks duplicate taps, writes the real video row and Clip Studio metadata, reads both back, refreshes Content Library, and only shows success after the saved id appears in the owner library. Migration `202605240009` is remote-applied to fix the UUID/text trigger mismatch found during proof, and migration `202605250001` is remote-applied to allow the Trailer style plus title/subtitle length checks. Cover/poster uses the existing hardened media-storage signed upload path, verifies readable bytes, cleans failed objects, stores matching `videos.thumb_storage_path` and `creator_clip_edits.cover_storage_path`, and resolves thumbnails through the video's actual storage provider. Public creator-video card surfaces now use a safe `public-creator-video-cards` resolver for published, moderation-safe cover/poster images and sanitized metadata fields (`clip_metadata_public`, `clip_title_text`, `clip_subtitle_text`, `clip_template_preset`, `clip_title_style`, `clip_title_position`) without returning raw storage/path/object/playback fields or full Clip Studio edit rows; fallback remains the existing thumbnail/default card state. Content Library owner cards can still show draft title/template preview metadata to the creator only. Android `R5CR120QCBF` proof now shows a saved Title Card/Template draft reopening with restored preview, controls, Save Draft read-back, owner-only Content Library card, published public cards rendering safe cover/poster images, and public Platform/Profile cards rendering sanitized title/subtitle/template metadata for published videos. Draft/private videos stay private, public Player video surfaces do not show burned-in overlays, and trim/export, poster frame extraction, video-rendered overlays, timeline tools, transitions, captions, AI cut, effects, audio sync, and full renderer remain honest unavailable states. The May 25, 2026 trim/export research audit is recorded in `docs/CLIP_STUDIO_TRIM_EXPORT_RESEARCH.md`; it recommends metadata-only trim preview first, server-side render/export as the long-term true-export path, and native Android Media3 Transformer only after isolated prototype plus release-build, device, APK-size, codec, backgrounding, cleanup, and parity proof.
- Discovery Feed Foundation D2 is pushed at `c2c6b04`, Discovery/Spectator D3-D10 foundation is pushed at `e68251a`, D7B Live Spectator Broadcast Schema Foundation is pushed at `1e54530`, D7C backend start/stop/status skeletons are pushed at `e36eb67`, D7C output-config alias source is pushed at `8ba557b`, D7-D10 proof closeout / D7G metadata-only ad-policy label is pushed at `1a1ddf0`, the D7D private test Egress path is pushed at `b87dde3`, D7D private start/stop proof completed on May 11, 2026, and the Spectator child-room relay is now repo-side wired. Home Feed V1, Public Platform live/upcoming shelves, `/spectate/[itemId]`, spectator eligibility helper, D9 production notification/activity Android proof, deterministic ranking helpers, admin/operator-owned `room_broadcast_sessions` schema, backend-only admin/operator broadcast functions, and server-verified Spectator-to-child-room creation exist without original-room spectator tokens, fake feed items, fake trending, fake counts, fake notification completion, or protected-content public spectator video. D9 is closed for Android: Expo/EAS production FCM V1 is assigned for `com.chillywood.mobile`, the backend sent real pushes for all five trigger classes, physical device `R5CR120QCBF` displayed a Chi'llywood alert notification, tapping that notification opened the safe `/spectate/[itemId]` target, blocked/private/ineligible states stayed silent/blocked, and proof rows were removed from public discovery. D7D permits real Egress ids/private HLS locations only for private `D7D_TEST_` proof rows; the completed proof called LiveKit Egress start/stop and verified cleanup without returning HLS URLs or enabling public playback. D7E Hetzner Object Storage public-safe HLS delivery proof completed on May 12, 2026 with physical Android device `R5CR120QCBF` publishing real tracks into a private proof room, Egress returning a real Egress id, public `.m3u8` fetch returning `200` with a real playlist marker, and a public segment fetch returning `200` with `video/mp2t`. D7F is runtime-proved as a controlled, eligible-public-safe, watch-only HLS gate through `spectator_hls_playback_records`, `spectator-playback`, and `/spectate/[itemId]`; eligible sources can create child rooms linked through `spectator_child_room_sources` with `root_source_id`. The May 26 child-room fixture closeout applied proof-scoped safe fixtures, added narrow signed-out public-safe Spectator reads, hardened deployed `spectator-playback` controlled URLs to HTTPS, and proved on Android `R5CR120QCBF` that an eligible Spectator source can start a Watch-Party Live child room with source attribution and no original host controls/member list. Signed-out handoff, private/source-ended/reuse-disabled UI states, and private/blocked/ended/reuse-disabled backend denials are proved without child ids or token fields. The later May 26 replay fixture closeout applied `202605260006_spectator_replay_archive_fixture.sql` and proved replay Watch-Party Live child room `NSHU7J` from a `replay_available_later` source with `source_is_live=false`, safe source attribution, controlled HTTPS playback, and a visible child shared Player frame after playback starts. Successful Live Watch-Party / Reaction Room launch still needs a true live-stage-compatible safe fixture; public-safe candidate discovery returned zero true live-compatible rows, so no VOD/replay source is claimed as live. This keeps raw HLS URLs, original LiveKit tokens, original publish permission, original host controls, protected/title/private/ticketed playback, fake live status, fake counts, and Premium full-room bypass blocked. `PUBLIC_HLS_BASE_URL` remains unset.
- owner Platform Studio at `/channel-studio` with `/channel-settings` compatibility
- standalone player
- Watch-Party Live core flow
- Watch-Party Party Room polish is pushed at `e0116d3`: `/watch-party/[partyId]` remains title/player-based and separate from Live Stage, with clearer Party Screen/Party Feeds language, Player/Invite/Report/Leave action dock, confirmed Leave behavior, and no Player-control or Live Stage changes.
- Watch-Party Live local participant pinning is pushed at `c4b16ca`: `/watch-party/[partyId]` lets each viewer long-press a Party Feeds participant tile, choose inline Pin/Unpin, persist one local pinned participant, and keep that participant first locally without changing Live Stage, Chi'lly Chat calls, Player controls, LiveKit token/join logic, playback sync, room authority, Premium gates, or other users' views.
- Live Watch-Party / Live Stage core flow
- Party Room / Watch-Party Live launch cleanup is pushed: Party Room hides the Chi'llyfects foundation box for launch, Watch-Party Live Player dock removes `People` and keeps people management in Party Room, duplicate rapid Player opens are guarded while the LiveKit boundary prepares, and Android back from Watch-Party Live returns to the canonical Party Room path. No room behavior, LiveKit/token permission, Premium gate, route ownership, or backend system changed.
- Refresh/video/data cost policy is implemented for Public v1: live publish defaults are 720p / 30fps, v1 live max is 30fps, 60fps live is not a default, room heartbeat is 15 seconds with a 45-second active membership window, Watch-Party Live snapshot fallback is 30 seconds, Live Stage comment fallback is 15 seconds while realtime remains primary, and Home/Profile/Public Platform/Platform Studio stay load-on-open/focus/manual. VOD quality ladder/resolver foundation is repo-backed with rendition metadata and backend resolver truth. Live schema/function progress is now applied through remote migration `202605140010_vod_quality_ladder_resolver.sql` and deployed `media-storage` ACTIVE version 37, but live Free/Premium quality enforcement remains pending instead of faked until real rendition files, trusted `video_renditions` rows, real viewer/owner auth contexts, and backed Premium entitlement proof pass. Existing Player test video `84c486e9-a62e-4121-8e70-ee79e17b1bf0` is the preferred proof candidate, but the Free rendition proof is currently blocked before source download because this shell has no safe owner/operator/storage credential path. Automatic transcoding remains pending.
- Live Stage role/access hardening is pushed: mode switching is host-only, non-host route-param mode mutation is blocked, Live First viewers cannot self-promote into camera/speaker seats, and shared participant-state writes require the authenticated room host
- Live Stage top chrome cleanup is pushed: the top `Menu` button and sheet are removed, the existing `Live Room` return button sits beside `Lock controls`, and the bottom `Live First / Live Watch-Party` mode toggle stays in place. No room behavior, LiveKit/token logic, Premium gates, route ownership, or backend systems changed.
- Old Live Stage, Live Watch-Party, and Watch-Party Live room handling is hardened and closure-proved: old `watch_party_rooms` rows may remain for history/debug, but active joins, room-code/deep-link snapshots, discovery/live badges, active-room counts, and LiveKit token issuance require `is_active=true` plus fresh room activity; stale LiveKit server assignment reuse is rejected by heartbeat; fallback/avatar UI is not counted as successful media proof. May 18 proof deployed `livekit-token` ACTIVE version 54, proved fresh-token then `410 room_expired` inactive/stale rejection for retained proof rooms, and captured current-APK stale-link/room-code not-found behavior. Chi'lly Chat voice/video call handling now applies the same repo-side liveness doctrine to `communication_rooms`, active-call thread reuse, and `chat-call` token source.
- Live Stage / Live Watch-Party / Live-First media-contract hardening preserves the approved people-first room layout and adds guard coverage for canonical route ownership, layout lock markers, Approved Seats token authority, downgrade/local media disable, fallback-not-success semantics, old/stale room handling, participant visibility, comments/reactions/composer, and reconnect grace. No new AAB is needed for this lane because no native package/signing/build-profile change was made.
- Watch-Party Live shared Player controls are host-only: the host controls play/pause/replay, seek/scrub, speed/rate, up-next, and playback sync writes; viewers stay synced and keep comments, reactions, Party Room navigation, camera/mic request, LiveKit seats, participant bubbles, and the approved layout. Player native video promise rejections are caught/logged safely, and `guard:watch-party-livekit` protects the host-only playback lock plus non-playback room interactions.
- all full Live First, Live Watch-Party, and Watch-Party Live access gated by Premium
- no free full LiveKit room/token/connect access and no free preview mode
- comments, reactions, and basic social interaction
- basic Chi'lly Chat or simple direct messaging, with helper-level current-user membership checks before normal thread/message/attachment/send/call-cleanup access; `/chat` owns the canonical inbox, `/chat/[threadId]` owns canonical direct threads, `/communication/index.tsx` and `/communication/[roomId].tsx` are compatibility-only, mapped `/communication/[roomId]` rooms redirect to `/chat/[threadId]?openCall=1`, unmapped rooms redirect to `/chat`, `openCall=1` may open an existing active call panel only, and bottom-tab Chat is deferred until it can avoid duplicate route ownership
- preserved Rachi official seeded-account foundation on canonical Profile, public Platform, Chi'lly Circle, Home, Admin, and opt-in Rachi Help surfaces
- Premium subscription gate, with Google Play plus RevenueCat entitlement `premium`, RevenueCat `premium` offering preferred, a safe `premium_subscription` fallback to a purchasable RevenueCat current/default/first offering for offer display/purchase selection only, and planned price `$9.99/month`. Premium subscription revenue belongs to Chi'llywood/platform, is not split with creators, and must not create a creator subscription revenue pool.
- Premium billing internal-test proof is closed for Google Play product load, purchase sheet, test purchase, restore, active entitlement, expired entitlement blocking, and Premium gate blocking after expiry. Google Cloud project `chillywood-app` has the required Play/Reporting/Pub/Sub/Monitoring APIs enabled, and service account `chillywood-revenuecat-play@chillywood-app.iam.gserviceaccount.com` exists with scoped Pub/Sub editor plus Monitoring viewer roles and JSON stored outside the repo. RevenueCat API proof confirms project `projc5629a24`, Play Store app `appd24db94dd8` for `com.chillywood.mobile`, an Android production public SDK key, entitlement `premium`, offering `premium`, and product `premium_subscription:monthly`; the public SDK key is configured in EAS production env. EAS production AAB build `aedded50-3818-4d7b-8bcf-9bb9c2d36513` completed for versionCode `2` and was manually uploaded to Google Play Internal Testing as `Internal test v2 - RevenueCat Premium setup`; after that, Android Publisher package visibility returns `200`. New EAS build `96e60082-00b1-48dc-843b-132ce82a7710` completed after the admin-login safety fix, advanced versionCode to 3, produced a production AAB artifact, and was submitted by EAS to the internal track only as submission `920c452f-1b9f-401c-94c9-616801684939`. Google Play subscription `premium_subscription` exists, monthly base plan `monthly` is `ACTIVE`, billing period is `P1M`, and the US price is `$9.99/month`. RevenueCat product `premium_subscription:monthly` is attached to entitlement `premium`, attached to package `$rc_monthly` in offering `premium`, and offering `premium` is current. On May 15, 2026, physical device `R3CXA0DS5JV` installed versionCode `3` from Google Play; `/subscribe` loaded the configured offer; the Play test sheet showed Chi'llywood Premium with test-card/no-charge copy and `$9.99/5 min`; purchase completed; RevenueCat showed active sandbox Play Store entitlement; Restore worked; cancellation plus test-cycle expiry dropped active entitlements to zero; app Recheck returned `Entitlement Missing`; and Watch-Party showed the Premium-required $9.99 gate. Fresh public-login/Owner/Admin device proof, final release smoke, Play listing/Data Safety/account deletion acceptance, and any creator paid-content purchase proof remain separate.
- Temporary Premium proof hold is active for LiveKit recheck: the app keeps RevenueCat/backend entitlement reads, but the Premium purchase shell is hidden/disabled and live/watch-party entry is temporarily opened for internal media proof without granting Premium. This hold must be removed and rechecked before launch-ready Premium gates are claimed.
- Creator monetization systems foundation is repo-backed, remote-applied, and default-off: migration `202605140011_creator_monetization_systems_foundation.sql` is installed on the linked Chi'llywood Supabase project, and `monetization_system_settings`, creator monetization profiles, creator content prices, paid-content purchases, content access grants, creator products/orders, tip transactions, append-only creator earnings ledger, payout requests, webhook idempotency, audit logs, and fail-closed RPCs exist for future creator Platform monetization managed from Platform Studio. Post-apply proof shows dry-run up to date, linked lint clean, default live-money flags false, and anon direct money-table writes denied. `_lib/creatorMonetization.ts` exposes safe client helpers, explicit scheduled payout `$0` fee constants, the 1.5% no-cap instant cash-out helper, Platform Studio now has one disabled/read-only `Monetization` tab with Premium, Revenue, Payouts, Stripe, store, future-tool, and owner/dev technical sections, the public Platform route is now product-copy framed as the creator Platform with a no-checkout commerce shelf state, and the Player checks the paid-content resolver before signing creator-video playback. The payment rail foundation now has app and Supabase Edge shared policy helpers plus `guard:payment-rail-policy`, keeping Premium on Google Play/RevenueCat, Android in-app digital creator content off Stripe unless an approved Play billing path is proved, pure support tips separate from digital perks/access, merch on future Stripe/commerce proof, and payout/cash-out on future Stripe Connect proof. Stripe CLI is installed/logged into the sandbox and `guard:stripe-connect-policy` pins the test-mode/server-only Connect boundary; the deployed webhook foundation has a valid Supabase webhook secret, active test endpoint, unsigned-request rejection, and a delivered Stripe CLI `payment_intent.succeeded` test fixture with no money action. Paid checkout, tips, merch checkout, cash-out, payouts, production Stripe Connect, and live Premium purchase proof remain blocked until provider/legal/security proof passes. No fake purchase, tip, order, earning, balance, payment intent, checkout session, transfer, payout release, or live money is active.
- Admin Command Center V1A on the canonical `/admin` route, protected by signed-in plus beta/platform-role/backend permission checks. Owner is now modeled as an invisible platform-owner/superuser rather than a public creator profile; Admin/operator authority is moving to owner-granted scoped permissions, with moderator management preserved through the existing Staff & Roles flow when `manage_moderators` is granted and admin management separated behind `admin_grants`.
- Admin Command Center Home Production Pass is closed repo-side. The `/admin` Home tab now functions as a launch-control overview with a Command Overview hero, 2x2 backed snapshot grid, prioritized Needs Attention rail, truth-labeled Connected Systems grid, Operational Truth boundaries, immutable-audit Recent Signals feed, and non-destructive Quick Drill-Downs. Reports, DMCA, Live Ops, immutable audit, Premium entitlement, active room/upload DB estimates, App Config, Live Cost Guard, and Ads states remain explicitly labeled as `Backed`, `Connected`, `Foundation Only`, `Observe Only`, `DB Estimate`, `Not Connected`, `Read Only`, `Manual Review Required`, or `Queue Ready`; no fake zero counts, fake health, LiveKit truth, ad provider state, money state, or Home remediation controls were added.
- Admin Content Programming Center Production Pass is repo-side implemented, Supabase-applied, backend-denial proved, and current-APK Android read/scan proved, with safe active-admin mutation drills still pending before full launch closure. The `/admin` Content tab is now a programming dashboard with backed saved-config state, title-count snapshot, theme preset gallery, homepage hero/top-picks resolver preview, rails manager, title programming board, feature/config runtime, foundation-only monetization labels, doctrine-locked branding copy, defaults/grants, reason-required confirmation, and content audit timeline. Remote migrations through `202605220008` are applied and linked lint/dry-run are clean; owner/operator RPCs cover config read/save, audited title programming actions, audited creator grants, and fail-closed content audit reads. No fake counters, fake presets, RevenueCat changes, Ads provider changes, room behavior changes, Player playback changes, LiveKit actions, or destructive media deletes were added.
- Admin Roles & Permissions Center Production Pass is closed for backend/UI production scope, with optional physical normal-user UI denial proof still available as a smoke follow-up. The `/admin` Roles tab is now an access-control dashboard with staff snapshot counts, masked active/revoked roster rows, protected Owner states, reason-required grant/revoke controls, grouped multi-select scoped permissions, unsaved permission deltas, reason-required Save Permissions confirmation, Permission Templates shortcut, Protected Owner Rules, and filtered Role Audit Timeline. Remote migrations `202605220009_admin_roles_permissions_center.sql` and `202605220010_harden_staff_permission_read_null_actor.sql` are applied; `list_staff_scoped_permissions_by_email`, `admin_update_platform_staff_permissions_by_email`, and `list_admin_role_audit_events` are owner/admin-gated and fail closed, while role/permission audit helpers now require reasons and write immutable audit rows. Supabase live proof used disposable proof users to grant a Moderator target, save two scoped permissions together, verify readback plus role/permission/immutable audit rows and timeline visibility, then revert all active proof access with active Owner count unchanged. No fake staff records, fake permission states, hardcoded staff emails, room behavior changes, LiveKit actions, Premium/RevenueCat changes, Ads changes, or service-role secrets were added.
- Admin Immutable Audit Log Production Pass is closed for repo-side/admin-device/backend-denial and physical normal-user UI denial scope. The `/admin` Audit tab is now the high-level immutable overview, distinct from Audit Explorer, with a compact Audit hero, real health metrics, Visibility / Scope panel, recent immutable timeline with category filters, high-risk known-type rail, category summary, row detail bottom sheet, and Audit Explorer shortcut. The source remains append-only `platform_admin_audit_logs`; if a sub-summary such as role audit visibility fails, Audit shows `Partial Visibility` while keeping immutable rows visible. Current Android owner/admin proof showed `193` visible immutable rows, a real row detail sheet, and a working Audit Explorer shortcut; the configured non-staff proof account was physically denied at `/admin` with no Audit overview rows/surfaces visible. No fake audit rows/counts, audit edit/delete/clear UI, backend duplicate audit system, room behavior, LiveKit action, Player/Premium/RevenueCat/Ads change, or secret exposure was added.
- Security Context and IP Address Audit is closed, Security Request Context Backend Implementation plus masked readout closeout is Supabase-applied/Edge-deployed for the first production slice, Security Context Event Link Expansion is remote-applied/Edge-deployed for the safest restricted audit/event paths, and Trusted Proxy Network Proof backend contract is remote-applied/Edge-deployed with a deployed Cloudflare Worker proxy plus runtime route wiring. `docs/admin/SECURITY_CONTEXT_IP_AUDIT.md` records the architecture and status. Migration `202605230002_security_request_context_backend.sql` adds restricted `security_request_context`, safe summary RPC, first `security_context_id` links, and append-only `livekit_token_request_audit`; migration `202605230003_security_context_event_link_expansion.sql` extends nullable context links to restricted DMCA, Reports, media-storage, Live Ops, Live Cost Guard, payout/network/fraud, and staff role audit tables without adding raw IP columns or public content/message-row network proof; validator fix migration `202605230004_fix_security_context_metadata_validator.sql` corrects the metadata validator to compare uuid user ids to `auth.uid()`; migration `202605230005_trusted_network_proof_contract.sql` adds `network_proof_verified`, proof source/version/error/timestamp, and `trusted_header_source` fields plus safe summary output. The shared helper now verifies signed `x-chillywood-network-proof*` headers with server-only `CHILLYWOOD_NETWORK_PROOF_SECRET`; direct `x-forwarded-for`, `x-real-ip`, `forwarded`, `x-client-ip`, and `cf-connecting-ip` are ignored as spoofable client/proxy headers unless wrapped inside verified signed proof. The Worker source lives at `ops/trusted-network-proof-proxy/`, is deployed on `https://network-proof.chillywoodstream.com`, strips spoofable headers, signs masked/hash network proof server-side, and forwards no raw IP. App runtime config now defaults `runtime.supabaseFunctionsUrl` to the trusted proxy, rewrites Supabase `.functions.invoke` calls through it, and points default LiveKit token/media-storage function calls at it while leaving Auth/REST/storage direct. `admin-owner-controls` ACTIVE version `30`, `livekit-token` ACTIVE version `67`, Live Ops/Live Cost Guard/media-storage functions, and payout/provider/sponsor/fraud preflights are redeployed with the signed-proof helper without changing room/live/player/premium/upload/chat behavior or money movement. Owner Security current/trusted device rows and Audit Explorer rows with linked `security_context_id` receive safe masked network proof through the backend summary path, with verified/unverified proof copy. Physical Android owner-session proof on `R5CR120QCBF` showed masked `/24` prefix, approximate location/ISP, `Verified network proof`, `Captured`, `signed_chillywood_proxy`, and shortened context id, with no raw full IP and no dangerous owner action. Public UI still never exposes IP, raw IP is not scattered across tables, spoof guards pass, and the remaining work is Audit Explorer row-level live proof plus optional gateway/firewall blocking of direct Supabase Edge origin bypass.
- Admin Reports Backend Completion is closed for production triage scope. The Reports Triage Center now uses backed `safety_reports` severity/status/resolution fields, overview/list/detail/status/action/audit RPCs, Critical and Actioned filters, reason-required Mark Reviewed/Dismiss/Escalate actions, immutable admin audit inserts, and non-destructive target visibility actions for creator-video plus supported profile/social targets whose public read paths honor `moderation_status`. Client report intake is guarded server-side so normal users cannot set admin workflow fields. Remaining future roadmap items are reopen, account/channel enforcement, and additional target types only when corresponding report sources and public filtering guarantees exist.
- Platform Owner/Admin/Moderator role management is live-proof closed through `platform_role_memberships`; internal `operator` is the public-facing Admin role. Remote migrations `202605140008_platform_staff_role_management.sql` and `202605140009_platform_staff_role_management_null_actor_guard.sql` are applied. Owner `rob2008gn@gmail.com` and Admin `chillywood92@gmail.com` are live auth users with active normalized bootstrap memberships. Server-side proof passed for Owner managing Admins/Moderators, Admin managing Moderators only, Moderator/regular denial, last-active-Owner protection, revoked-role loss of access, direct-write denial, and grant/revoke audit rows. The `202605140009` follow-up fixed a NULL actor-role permission fallthrough found during proof.
- Public Login is normal Chi'llywood auth only; Admin access uses `/admin` plus backed platform-role checks, with no separate Admin sign-in link, local grant, stored credentials, hardcoded Admin account, or bypass
- Ads Launch Foundation V1A/V1B/V1C and Ads Config V1D1/V1D2 as provider-neutral, no-SDK, no-real-rendering infrastructure; the Home native/feed placeholder foundation and placeholder interstitial controller foundation exist, runtime owners now read normalized `app_configurations.config.adsLaunch` with default-disabled fallback, normal runtime keeps ads hidden while `ads_enabled=false`, and real ad SDK integration is not live yet
- 18+ launch posture with H1A signup confirmation and H1B2 legal acceptance storage pushed
- Public V1 Hardening H2 upload/content lifecycle polish in Platform Studio Content, with one clear `Video Upload` form, honest no-file/selected/ready/uploading/succeeded/failed local states, and backed draft/published/media-ready labels
- Public V1 Hardening H3 security/compliance/moderation hardening: Settings Support entry, Support sponsorship/ad/scam concern copy, and backed Report Sheet category guidance without new schema/actions
- Public launch legal / creator rules / copyright / trust-safety policy drafting now has a canonical production-draft source in `legal/policies.mjs`, shared by mobile legal pages, Settings launcher, static public site generation, and Admin Canary legal-readiness checks. The bundle has 12 full pages over 1,500 words each: Terms, Privacy, Community Guidelines, Creator Rules, Copyright/DMCA, Support & Account Help, Account Deletion, Premium/Subscription Terms, Live/Watch-Party/Chat Rules, Law Enforcement/Legal Requests, Moderation/Appeals, and Creator Monetization/Revenue Disclaimer. It includes creator ownership retention, Chi'llwood's broad service content license, no automatic creator payment, deletion/retention rules, DMCA/counter-notice/repeat-infringer language, Google Play deletion path language, law-enforcement/legal-hold/evidence handling, Premium/RevenueCat/store truth, and owner/admin operational notes. Studio upload/publish and live/replay event save now show required acknowledgement checkboxes. Attorney approval remains required before public launch.
- Public legal URL static site generation exists under `public-site/legal-site/` for 18 generated pages, including `/terms`, `/privacy`, `/creator-rules`, `/community-guidelines`, `/copyright`, `/copyright-report`, `/account-deletion`, `/support`, `/premium-terms`, `/live-rules`, `/law-enforcement`, `/moderation-policy`, and `/creator-monetization`. Cloudflare Pages project `chillywood-legal` was redeployed on May 22, 2026 from the expanded route set, and `chillywoodstream.com` remains the preferred public policy host. Current proof returned HTTP 200 after redirect for `/copyright-report/`; May 21 proof remains valid for expanded paths including `/privacy/`, `/creator-monetization/`, and `/live-rules/`; `https://chillywoodstream.com/account-deletion` remains the Google Play account deletion web URL candidate, `https://chillywoodstream.com/support` remains the public support URL, and `https://chillywoodstream.com/copyright-report` is the canonical public DMCA notice URL. This is still not public launch legal completion: attorney approval, Google Play account deletion/Data Safety acceptance, uploader-facing counter-notice submission if required, DMCA attachment storage/scanning/retention if required, outbound email automation if required, and DKIM after real outbound-provider setup remain blockers.
- Google Play Android developer/package verification is complete for `com.chillywood.mobile`; Play Console accepted the ADI proof and verified the selected signing certificate fingerprint `2B:A7:84:1B:70:6B:E8:69:E8:0F:EA:DA:43:8D:14:33:34:D6:E8:70:99:63:FA:B9:63:94:07:4E:9D:D0:95:43`. The ADI verification APK is archive proof only. Premium internal-test product/purchase/restore/expiry proof is closed. Remaining Play/release blockers include Data Safety/account deletion acceptance, final release-route smoke, and final Play listing approval.
- Public V1 current-build release proof completed on Android current build with core launch surfaces opening and default ads hidden
- Chi'llywood Native Room / Screen UI doctrine is current Public v1 guardrail: every new or materially changed route must define owner, primary experience, top/middle/bottom/More placement, native mobile safe-area/keyboard/no-overlap proof, empty/loading/error/locked states, role/privacy/Premium gates, and background preservation before it is production-complete. Generic Expo/demo/admin-looking consumer screens, fake data/counts/participants/money, route ownership drift, Premium-gate weakening, and global background/theme changes remain forbidden.
- moderation basics
- analytics, error monitoring, and admin visibility
- backend/server ops alert automation safety gate under `ops/alert-automation`: Alertmanager webhook intake, dry-run action planner, persistent job store, JSONL audit log, approval token gate, sanitized job-list/detail APIs, optional disabled-by-default SMTP email notification, LiveKit admin action planner, and network-shaping script planner exist with no default live execution. Destructive LiveKit actions require approval plus `ALLOW_LIVE_ACTIONS=true` plus `DRY_RUN=false`; network shaping requires approval plus `ALLOW_NET_SHAPING=true` plus `DRY_RUN=false`. Email notifies only and cannot approve or execute actions. The Admin Command Center has an Ops Alerts visibility tab, but Approve/Deny is disabled there until a secure server-side admin proxy owns `OPS_APPROVAL_TOKEN`. This is not a consumer mobile app feature and does not change Watch-Party Live, Live Stage, Player, Supabase, Hetzner HLS, creator-video flows, or public user-facing routes.
- Admin Live Cost Guard foundation exists as an owner/operator-only LiveKit/TURN cost control plane with settings, events, action audit logs, Admin visibility, Alertmanager webhook/action Edge Function scaffolding, and a narrow token-issuance guard for Live Watch-Party and Watch-Party Live. TURN Spike Protection repo-side hardening includes `docs/TURN_SPIKE_PROTECTION_RUNBOOK.md` plus read-only/operator scripts in `scripts/infra/` for TURN snapshots, burn-rate estimation, and emergency cap guidance. The webhook secret is configured in Supabase Edge Function secrets, and direct observe-only webhook delivery proof passed with one safe fake warning event, zero action rows, and settings still `enabled=false` / `mode=observe_only`. Production metrics remain not connected yet, TURN caps are request/runbook only, and no automatic kicking, throttling, room blocking, SSH, host/firewall/coturn mutation, billing, payouts, Player UI, Live Stage UI, Watch-Party Live UI, creator media, Supabase content, Hetzner Object Storage, spectator HLS behavior, or normal-user live behavior changes are claimed live.
- Owner/Admin Live Ops Fix Center exists repo-side as an additive reliability remediation lane for Live Stage, Watch-Party Live, and Chi'lly Chat calls. It adds backed incident/audit tables, a server-side owner/operator proxy, Admin incident cards, ops classifiers for the required diagnosis examples, GitHub issue/PR/job-rerun actions, LiveKit registry drain/block/standby/stale-assignment actions, server-proven stale chat-call cleanup, and protected restart/rollback hooks. `chat-call` remains the canonical LiveKit token surface; `chat-video-call` / `chat-audio-call` are incident display categories only. All real actions remain disabled by default through dry-run and explicit allow flags; no auto-merge, auto-deploy, fake health, fake stats, fake calls, fake participants, route layout change, call UI change, call permission change, chat history deletion, or Premium gate change is part of this lane. Production activation still requires deployment, secrets, real metrics/Alertmanager proof, owner/operator approval, and bounded manual remediation proof.
- LiveKit Server Registry + Room Router + Drain Mode is remote-activated and proved for the current single Hetzner box, `chillywood-prod-01`, with public connect URL `wss://live.chillywoodstream.com`. New RTC rooms route through the backed registry only when that server is `active`, fresh-heartbeating, and under capacity; existing room assignments stay pinned, and draining blocks new rooms without disconnecting existing rooms. No full autoscaling, standby provisioning, active-room migration, room deletion, participant disconnect, Watch-Party UI, Live Stage UI, Player UI, D7F spectator-token path, HLS behavior, notification behavior, or user-facing redesign is added.
- layered room participation truth with limited active live seats, scalable participant browsing, and a clear distinction between joined presence and true live-seat media
- Public v1 should stay focused on the core social streaming experience instead of the full long-term platform vision

## Post-v1
- 10-participant LiveKit proof remains a separate follow-up for Live Stage Live First, Live Stage Live Watch-Party toggle, and Watch-Party Live; do not claim it until real devices or a controlled synthetic/load equivalent proves audio/video publish/subscribe, roster/tile visibility, reconnect/leave, and role/Premium gates.
- Route-tree utility exposure cleanup is pushed: unreferenced exposed route-tree utilities `app/data/titles.ts` and `app/lib/_supabase.ts` were removed, and active Watch-Party helper modules were moved to `_lib/watch-party/room-shared.ts` and `_lib/watch-party/waiting-room-shared.ts` with import updates. Future helper modules must stay outside `app/` unless they intentionally own routes.
- Discovery follow-up after D3-D10/D7B/D7C/D7G/D7D readiness now has D7E delivery proof, D7F runtime proof, and repo-side Spectator child-room creation. `room_broadcast_sessions` remains admin/operator-owned; D7D real Egress ids/private HLS locations remain limited to private `D7D_TEST_` proof rows, while D7F-approved public-safe rows may be surfaced only through `spectator_hls_playback_records` and the controlled `spectator-playback` resolver/proxy. Repo-side LiveKit Egress software scaffold exists only in `infra/hetzner` behind the explicit Docker Compose `egress` profile; it is backend/media infrastructure and is not mobile app software. Hetzner Object Storage is the current D7E target and D7E delivery proof is complete. D7F remote migrations/functions are applied/deployed, a fresh eligible public-safe HLS row returned `available`, the controlled playlist/segment proxy returned `200`, blocked states leaked no raw URL/token, and temporary proof rows/access were cleaned up. Spectator child rooms require server-side source eligibility in `spectator-start-room`, creator/source reuse flags, public-safe playback and broadcast-session approval, and safe `spectator_child_room_sources` linkage; they do not change original LiveKit token issuance or publish grants. `PUBLIC_HLS_BASE_URL` remains unset because D7F does not require app clients to know the public HLS base. Keep full spectator LiveKit tokens, raw HLS URL exposure, fake trending, viewer counts, spectator counts, fake HLS URLs, fake Egress ids, protected/title/private/ticketed public playback, Premium full-room bypass, and unapproved Admin broadcast controls blocked.
- Modern 2026 live controls remain a later dedicated UI pass: compact bottom dock, More bottom sheet, role-correct host/viewer actions, safe-area/keyboard/player-overlap proof, and no Player/Watch-Party/Live Stage layout redesign outside that exact lane. The pushed Party Room Player/Invite/Report/Leave action dock is scoped Party Room polish and does not close that broader lane.
- Real AppLovin MAX integration later only after external account/app/ad-unit setup is ready, keeping the provider wrapper architecture and avoiding an AdMob-only path
- Admin V1B1 runtime controls config foundation is pushed; Admin V1B2A enforces `new_accounts_enabled` on signup only; Admin V1B2B enforces `uploads_enabled` on new creator-video upload submit only; Admin V1B2C enforces `comments_enabled` on backed Profile post and creator-video comment submit only; Admin V1B2D enforces `profile_posting_enabled` on backed Profile post creation submit only; Admin V1B2E enforces `creator_posting_enabled` on new creator event creation only; Admin V1B2F enforces `attachments_enabled` on selected non-chat social attachments only; Admin V1B2G/V1B2H enforce `chat_attachments_enabled` on selected standalone Chi'lly Chat and room attachments only; Admin V1B2I-A enforces `chat_enabled` on standalone Chi'lly Chat send/call/profile-entry/starter-thread paths only; Admin V1B2I-B enforces `chat_enabled` on room invite direct-message sends only; Admin V1B2I-C enforces `chat_enabled` on Watch-Party and Live Stage room-native text/comment submits only; Admin V1B2J enforces `live_first_enabled`, `live_watch_party_enabled`, `watch_party_live_enabled`, and `max_upload_size_mb` on scoped live/watch-party entry and creator-video upload-size paths only; additional kill switch work should focus on backed Admin write/toggle permissions and proof, with no fake toggles
- Admin V1B runtimeControls closeout truth is updated: live-room switches and watch-party switches are enforced through the Premium helper layer without weakening Premium, `max_upload_size_mb` is enforced before creator-video storage upload, Admin `runtimeControls.ads_enabled` remains separate from Ads Launch config, and Premium-required controls remain Premium helper truth rather than runtime switches
- Ads V1D2 is pushed: `hooks/useAdsLaunchConfig.ts` lets Native/feed and Interstitial runtime owners read normalized `app_configurations.config.adsLaunch` with default-disabled fallback; Admin `runtimeControls.ads_enabled` is still separate and not the Ads Launch runtime source
- Admin Usage Metering Foundations 37-39 is pushed as schema/helper/read-only Admin foundation, and the usage remote migration/type refresh is complete. Admin Usage Writer V1A0/V1A1/V1A2 is pushed and runtime-proved for successful creator-video upload usage only: the secure RPC and upload helper call record `video_uploaded` and `storage_bytes` metadata after a backed video exists, metering failures are non-fatal to upload success, replay skips without double-counting, and V1A2 adds/proves an owner/operator daily rollup RPC foundation for those backed rows. Admin Usage Provider Imports V1D3/V1D4 are pushed as server-side Edge Function foundation for Cloudflare R2, Hetzner Object Storage, and Hetzner Servers; mobile never calls provider APIs directly. Provider Billing Reconciliation Foundation is pushed/deployed as backend-only owner/operator reconciliation over existing provider usage/internal summary rows, with immutable admin audit rows and Admin Usage/Networks readouts. Provider Billing API Import Preflight Foundation is pushed/deployed as backend-only owner/operator refusal for provider billing imports; it returns `preconditions_required`, rejects provider credential and money/invoice/payment inputs, and writes requested/blocked audit rows only. Hetzner Object Storage imports S3 inventory metadata estimates only, not traffic, billing, overage, invoice, revenue, payout, or storage-billing truth. Provider billing reconciliation/import preflight functions do not call provider billing APIs, import provider bills, send invoices, charge customers, create payment links, execute overage billing, or create fake provider bills. OVH remains not connected until a later exact API lane. Ledger Systems 4A-4D finance foundation is remote-applied schema/helper/read-only Admin foundation with current-build Admin read-only proof complete. Finance F2B/F2D/F2E payout-provider foundation is remote-applied/type-refreshed, visible in Admin Payouts as read-only counts, and seeded only with non-live `not_active` proof rows marked as foundation/not-live money. Stripe Connect Schema Foundation is remote-applied/type-refreshed as additive provider/onboarding/webhook/eligibility schema only. Stripe Connect Test-Mode Backend Functions (Payout S3C) is pushed/deployed as backend-only test-mode Edge Functions for account, onboarding-link, account-sync, and webhook flows; `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` are configured as backend Edge Function secret names by digest only. Provider Transfer Records Sync Foundation is remote-applied/type-refreshed and deployed as backend-only/admin-operator/test-mode transfer status import for existing local records only. Payout Transfer Preflight Foundation and Payout Release Preflight Foundation are deployed as backend-only/admin-operator refusals for transfer creation and payout release. Network Billing Foundation plus Network Invoice Draft Workflow + Overage Warning/Readout Foundation are remote-applied, visible in Admin Networks as read-only counts, and seeded only with non-live proof rows for planned accounts, plans, quotas, internal draft invoices, draft/foundation line items, warning-only overage readouts, overage events, and audit logs. Fraud Enforcement Foundation, Fraud Review Queue Foundation, Creator Revenue Share Ledger / Fraud Hold Enforcement Foundation, and Real Source Revenue Import Foundation are remote-applied/type-refreshed, visible in Admin Revenue/Fraud as read-only counts, and seeded only with non-live proof rows for fraud holds, fraud reasons, evidence, planned actions, review queue rows, review notes, appeal placeholders, fraud audit logs, revenue-share rules, revenue-share ledger rows, source-import records, fraud enforcement policies, and non-executable fraud action readiness. The deployed `revenue-source-import` skeleton is backend-only and returns `not_configured` with no provider secret reads or provider calls. Real bandwidth writers, participant-minute writers, LiveKit billing truth, storage billing truth, actual provider billing API imports, finance provider imports, real provider source revenue imports, live payout provider writes, actual transfer creation, payout release, live network billing actions, sponsor checkout, fraud enforcement actions, runtime enforcement hooks, and broader event writers remain later separately scoped lanes.
- Sponsor Checkout Foundation is remote-applied/type-refreshed, visible in Admin Sponsors as read-only counts, and seeded only with proof rows for sponsor brands, sponsor deals, creatives, placements, disclosures, review logs, payment records, and payout split records. No sponsor checkout, brand payment, payment link, Stripe Checkout, sponsor approval action, fake sponsor revenue, payable balance, or payout split execution is active.
- Sponsor Checkout Preflight Foundation is pushed/deployed as backend-only owner/operator refusal for sponsor checkout. It returns `preconditions_required`, rejects checkout/payment/provider credential/customer/charge/currency/amount/Stripe inputs, writes requested/blocked audit rows only, and creates no Stripe Checkout session, payment link, brand charge, sponsor approval, sponsor activation, creator payout split execution, creator payout release, fake sponsor revenue, or live money movement.
- Sponsor Brand Payment Preflight Foundation is pushed/deployed as backend-only owner/operator refusal for brand payment. It returns `preconditions_required`, rejects payment/checkout/provider credential/customer/charge/currency/amount/Stripe inputs, writes requested/blocked audit rows only, and creates no payment intent, Stripe Checkout session, payment link, brand charge, sponsor paid status, payable creator split, creator payout release, fake sponsor revenue, or live money movement.
- Sponsor Reporting / Fraud Integration Preflight Foundation is pushed/deployed as backend-only owner/operator refusal for sponsor reporting and fraud integration. It returns `preconditions_required`, rejects provider credential, fake performance, revenue, payment, checkout, charge, risk-score, and enforcement inputs, writes requested/blocked audit rows only, and imports no sponsor reports, creates no fake sponsor performance or revenue, enforces no fraud action, restricts no creators or sponsor deals, activates no sponsors, charges no brands, releases no creator payouts, and moves no money.
- Sponsor Deal Review Queue Foundation, Sponsor Disclosure / Moderation Foundation, and Sponsor Payment Test-Mode Foundation are remote-applied/type-refreshed through migration `supabase/migrations/202605080012_sponsor_monetization_foundation.sql`, visible in Admin Sponsors as read-only counts, and seeded only with foundation/proof rows for sponsor review queue, disclosure/moderation safety review, payment foundation, payout split foundation, review logs, and immutable audit readiness. Sponsor review queue is foundation-only, disclosure/moderation is foundation-only, sponsor payment foundation is not checkout, no sponsor can be approved or activated, no brand can be charged, no checkout or payment link exists, no creator payout split can execute, and no fake sponsor revenue or fake payable balance exists.
- Fraud Enforcement Foundation is remote-applied/type-refreshed, visible in Admin Fraud as read-only counts, and seeded only with proof rows for fraud holds, fraud reasons, evidence records, planned actions, review notes, appeal placeholders, and fraud audit logs. No payout pause, account restriction, upload restriction, live restriction, monetization disable, fraud risk score, or active enforcement action exists.
- Immutable Admin Audit Log Foundation is remote-applied/type-refreshed, visible in Admin Audit as read-only/latest immutable rows plus the separate derived role/safety summary, and seeded only with proof rows for audit, finance, network billing, sponsor checkout, and fraud foundation readiness. The audit table is append-only through RLS plus a narrow update/delete prevention trigger. No audit row edit/delete/clear UI, dangerous money/fraud action, payout approval/release, invoice send/charge, sponsor checkout/approval, fraud enforcement, account restriction, upload/live restriction, monetization disable, or admin role write is active.
- Creator Payout Readiness Foundation is remote-applied as policy-only migration `supabase/migrations/202605080007_creator_payout_dashboard_read_policy.sql` plus the later Stripe Connect schema/functions. Platform Studio now shows payout readiness inside `Monetization > Payouts`; legacy `tab=payouts` links normalize into the consolidated Monetization tab. Regular creators can read only their own `creator_payout_ledger_entries` rows and cannot write payout rows; Stripe setup links are backend-created and test-mode only. Creator payouts are not active yet: no Withdraw, Cash Out, live KYC/tax approval, payout approval/release, transfer creation, fake payable balance, fake earnings, or live money movement exists. The app now derives explicit blocked reasons for provider readiness, KYC, tax/1099, hold, minimum payout, live-money, payout, cash-out, and production-Connect gates.
- Stripe Connect Schema Foundation is remote-applied/type-refreshed through migration `supabase/migrations/202605080008_stripe_connect_schema_foundation.sql`. It adds payout account provider/config/capability/readiness fields, onboarding session records without long-term onboarding URL storage, provider webhook event records with idempotent provider/environment/event id uniqueness, creator payout eligibility/readiness records, and immutable admin audit linkage fields only. It adds no Stripe SDK, Stripe keys, provider calls, Stripe accounts, onboarding links, transfers, payouts, Connect Stripe UI, withdrawal/cash-out, KYC/tax live flow, fake payable balance, fake earnings, or live money movement.
- Stripe Connect Test-Mode Backend Functions (Payout S3C) is pushed in `supabase/functions/stripe-connect-account`, `stripe-connect-onboarding-link`, `stripe-connect-account-sync`, `stripe-connect-webhook`, and shared helper `supabase/functions/_shared/stripe-connect.ts`. Account/link/sync are deployed ACTIVE version 32 and webhook is ACTIVE version 34 on Supabase. They remain backend-only/test-mode only, validate internal auth where applicable, reject client-supplied provider account ids, require backend-only Stripe test secrets for provider calls, can write immutable audit rows and payout provider/readiness rows only from Edge Functions, and verify/store signed webhook events only when `STRIPE_WEBHOOK_SECRET` is configured. Remote Edge Function secrets now include `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` by name/digest only; no values are committed or documented. Test-mode account creation/reuse, onboarding-link creation, account sync, signed webhook handling, and duplicate-event idempotency proof passed; provider readiness remains non-payable and payout execution inactive. Invalid webhook signatures are rejected, no-auth account setup returns `401`, and Stripe test accounts remain non-payable while requirements/KYC/tax are incomplete. Transfer, payout, checkout session, fake payable balance, fake earnings, or live money movement still require separate approval and proof.
- Payout Review Queue + Batch Draft Workflow Foundation is remote-applied/type-refreshed through migration `supabase/migrations/202605080009_payout_review_batch_foundation.sql`. It adds payout review records, review notes, batch items, and draft/foundation fields on payout batches, and Admin Payouts shows read-only/foundation counts only. Proof rows are zero-cent foundation rows marked `live_money_action: false`. No payout approval, release, batch processing, transfer creation, Stripe payout, Connect Stripe UI, fake payable balance, fake earnings, or live money movement exists.
- Provider Transfer Records Sync Foundation is remote-applied/type-refreshed through migration `supabase/migrations/202605080010_provider_transfer_sync_foundation.sql` and deployed as backend-only `stripe-connect-transfer-sync` ACTIVE version 29. It adds `provider_status` and `last_provider_sync_at` to local provider transfer records, keeps sync states test/foundation labeled, inserts only a zero-cent foundation proof row with no fake provider transfer/payout id, and lets Admin Payouts show read-only Provider Transfer Records, Sync Required, Synced/Test, and Failed Sync counts. The sync function requires internal Supabase bearer auth plus admin/operator role, accepts only local record ids, rejects client-supplied provider transfer/payout ids, reads Stripe secrets only from Edge Function secrets, and imports status only for existing local test-mode provider records. It creates no transfers, payouts, checkout sessions, paid status, payable balances, Connect Stripe UI, Mark Paid/Retry/Send Money/Release/Process actions, or live money movement.
- Payout Transfer Preflight Foundation is deployed as backend-only `stripe-connect-transfer-create` ACTIVE version 28. It is an admin/operator-only closed gate that returns `preconditions_required`, rejects client provider references and money instructions, reads no Stripe secrets, makes no Stripe calls, writes no provider transfer rows, and may write immutable requested/blocked audit rows only. It creates no transfers, payouts, checkout sessions, paid status, payable balances, Connect Stripe UI, payout release, or live money movement.
- Payout Release Preflight Foundation is deployed as backend-only `payout-release-preflight` ACTIVE version 28. It is an admin/operator-only closed gate that returns `preconditions_required`, rejects client money/provider/release instructions, reads no provider secrets, makes no provider calls, writes no payout/provider rows, and may write immutable requested/blocked audit rows only. It creates no payout release, payout approval, transfers, payouts, checkout sessions, paid status, payable balances, or live money movement.
- heavier creator monetization rollout
- fuller creator platform builder
- deeper room personalization
- request-to-speak / request-camera / request-seat flows with host approval queues
- stronger featured-vs-audience room controls and premium/ticketed room tooling
- instant payout / instant cash out lane foundations
- light compliant ad systems
- tighter monetization and creator-ops planning derived from `PRODUCT_DOCTRINE.md`

## Later Phase
- broader Game Live rollout after the Public v1 window
- Game Watch-Party after Game Live has traction
- larger premium or ticketed live-event formats with stronger audience tooling
- higher simultaneous live-seat capacity as media infrastructure improves
- advanced payouts and tax automation
- overseas creator payouts
- broader ad systems
- deeper creator-platform and premium-room expansion once moderation, compliance, and operations are ready

## Dependencies / Blockers / Compliance-Sensitive Areas
- Apple and Google billing constraints
- RevenueCat remains Premium subscription truth
- Public V1 Hardening H1A 18+ Signup Confirmation is pushed in `app/(auth)/signup.tsx`: new signup shows the 18+ copy, requires an active checkbox confirmation, blocks before `supabase.auth.signUp` if unchecked, preserves legal links and Sign In handoff, and does not persist age confirmation yet.
- H1B1 private legal acceptance schema foundation is pushed with local migration `supabase/migrations/202605070001_user_account_legal_acceptances.sql` and pure helper `_lib/accountLegalAcceptance.ts`.
- H1B1 defines private table `public.user_account_legal_acceptances` for `age_confirmed_at`, age version, terms/privacy acceptance timestamps, and versions. The table is intentionally separate from `user_profiles` and uses owner-only authenticated RLS.
- H1B2 legal acceptance storage is pushed and runtime-proved: remote migrations `202605070001` and `202605070002` are applied, generated database types are regenerated from the linked remote schema, signup writes legal acceptance after account creation succeeds with an authenticated session, anon reads to the legal acceptance table are denied, and May 17, 2026 disposable signup proof wrote/read one owner-only `public_v1_h1b` row while another authenticated user saw zero rows.
- H1B2 does not write legal acceptance to `user_profiles`, AsyncStorage, or auth metadata; does not collect full birthdate or ID verification; does not add first-use enforcement; and does not block existing users.
- Public V1 Hardening H2 upload/content lifecycle polish is pushed in `app/channel-settings.tsx` and `components/creator-media/creator-video-card.tsx`. It preserves existing picker/upload/storage/metadata/Open Player/Edit/Publish/Unpublish/Delete behavior, adds local-only upload lifecycle states, labels upload progress honestly without fake percentages, and shows backed owner `Published`/`Draft` plus `Media Ready`/`Media Unavailable`.
- H2 does not add fake processing, transcoding, archive, retry, storage billing, revenue, payout, or moderation states; it does not change RLS/storage policies, migrations, generated types, Player, Public Platform, Profile, Watch-Party, Live Stage, ads, RevenueCat, billing, or payout systems.
- H2 duplicate-upload UI correction is pushed: Platform Studio Content has one `Video Upload` form, no extra header/empty-state upload buttons, and `Upload Status` is inline inside the form rather than a second upload-looking box.
- Public V1 Hardening H3 security/compliance/moderation hardening is pushed in `_lib/moderation.ts`, `components/safety/report-sheet.tsx`, `app/settings.tsx`, and `components/system/support-screen.tsx`. It preserves backed `safety_reports` categories, adds clear category guidance for abuse/harassment/impersonation/copyright/safety/other, routes scam/fraud/unsafe product concerns through `Safety`, routes undisclosed sponsorship through `Other`, adds a Settings Support entry, and adds support copy for sponsorship/ad/scam concerns.
- May 17, 2026 Task 7 closeout proof passed for repo/runtime Settings, legal, account deletion, logout, Support, backend feedback queue, H1B2 disposable signup, and public apex legal/support URLs. Attorney approval, Play Data Safety/account deletion acceptance, release-route smoke, and outbound DKIM after real outbound provider setup remain external blockers.
- H3 does not add new report schema categories, fake resolve/dismiss/ban/delete actions, unsupported legal-compliance claims, RLS changes, migrations, generated-type edits, admin bypasses, or forbidden surface changes.
- Ads Launch Foundation V1A is pushed with `_lib/ads/adConfig.ts`, `_lib/ads/adEligibility.ts`, `_lib/ads/adProvider.ts`, `_lib/ads/providers/placeholder.ts`, `_lib/ads/adSession.ts`, `hooks/useAdEligibility.ts`, and `hooks/useActiveBrowsingTime.ts`; `app/admin.tsx` only shows read-only/foundation Admin Ads status.
- Ads Launch Foundation V1B is pushed with `components/ads/NativeAdSlot.tsx`, one Home native/feed placeholder foundation in `app/(tabs)/index.tsx`, native/feed long-use cap proof support in `_lib/ads/adSession.ts`, and read-only Admin Ads status copy in `app/admin.tsx`.
- Ads Launch Foundation V1C is pushed with `components/ads/InterstitialController.tsx`, a null-rendering placeholder interstitial controller mounted in `app/_layout.tsx`, and read-only Admin Ads status copy in `app/admin.tsx`.
- Ads Config V1D1 is pushed with optional normalized `app_configurations.config.adsLaunch` in `_lib/appConfig.ts` and read-only Admin Ads source/status copy in `app/admin.tsx`.
- Ads V1D2 is pushed with `hooks/useAdsLaunchConfig.ts`, Native/feed and Interstitial runtime fallback reads from normalized `app_configurations.config.adsLaunch`, and read-only Admin Ads source/status copy in `app/admin.tsx`.
- Ads V1A defaults `ads_enabled` to false, uses `placeholder` as the default provider, and has a placeholder provider that reports not connected and calls no SDK.
- Ads V1B default runtime remains hidden because `ads_enabled=false`; NativeAdSlot renders only after V1A eligibility passes, Premium/ad-free users never see it or increment counters, base native/feed session cap is 1, long-use unlock allows a second placement after 120 active browsing minutes, daily native/feed cap is 3, and forbidden routes/contexts stay blocked.
- Ads V1C default runtime remains hidden because `ads_enabled=false`; InterstitialController renders `null`, ignores first route mount, considers route transitions only, calls central eligibility with `placementKind: "interstitial"`, records placeholder interstitial shows only after eligibility and placeholder-provider success, respects 180-second first delay, 600-second spacing, session cap 3 plus long-use +2, daily cap 6, and forbidden routes/contexts.
- Ads V1D2 preserves default hidden runtime while wiring `NativeAdSlot` and `InterstitialController` fallback reads to normalized `app_config.adsLaunch`; Admin `runtimeControls.ads_enabled` is not used as the Ads Launch runtime source.
- No AppLovin, Unity LevelPlay, Unity Ads, or AdMob SDK dependency is installed for ads; no real ad IDs, real provider initialization, real ad rendering, CTV inventory, fake ad revenue, sponsor revenue, creator earnings, payout balances, invoices, or CTV revenue exist.
- AppLovin MAX is the primary ad mediation direction; Unity LevelPlay / Unity Ads may be added later through AppLovin MAX; do not build AdMob-only ads.
- All future real ad providers must go through the provider-neutral Chi'llywood ad wrapper; do not make direct screen-level SDK calls.
- launch ad caps: base active session 3 interstitial + 1 native/feed; after 2 active browsing hours +2 interstitial + 1 native/feed; daily cap 6 interstitial + 3 native/feed; Premium sees zero ads
- Ads must not appear inside active LiveKit rooms, during active video playback, while typing/commenting, during upload, on subscribe/payment screens, immediately at app launch, in Admin, in Platform Studio, in Chat, or in Profile/composer contexts unless explicitly redesigned later.
- Launch app ads are platform money at first; RevenueCat remains Premium subscription truth only and does not take ad revenue; Google Play does not take a subscription fee from AppLovin ad payouts.
- Creator-page ad revenue share is later at creator 70% net / Chi'llywood 30% net; creator-sold sponsor slots are later with brand paying Chi'llywood first and creator 80% net / Chi'llywood 20% net. Ledger Systems 4A-4D, Finance F2B/F2D/F2E, Network Billing Foundation, Network Invoice Draft Workflow + Overage Warning/Readout Foundation, Sponsor Checkout Foundation, Fraud Enforcement Foundation, Creator Revenue Share Ledger Foundation, Fraud Hold Enforcement Foundation, Real Source Revenue Import Foundation, Immutable Admin Audit Log Foundation, Creator Payout Read-Only Dashboard Foundation, Stripe Connect Schema Foundation, Stripe Connect Test-Mode Backend Functions, Payout Review Queue + Batch Draft Workflow Foundation, Provider Transfer Records Sync Foundation, Payout Transfer Preflight Foundation, and Payout Release Preflight Foundation add only foundation tables/read-only Admin counts/latest rows/schema/proof rows, provider-readiness schema, source-import readiness schema/skeleton, a read-only Platform Studio payout surface, backend-only test-mode functions, draft-only payout review/batch counts, backend-only status imports for existing local provider transfer records, or backend-only payout preflight refusal; no provider-backed creator ad revenue, real provider source revenue import, fake creator earnings, payable revenue-share balance, payout ledger entry creation from revenue-share rows, payout execution, payout approval/release, batch processing, transfer creation, sponsor checkout, sponsor approval action, payment link, payout split execution, invoice send, customer charge, live network billing action, live-mode Stripe call, production connected account, production onboarding link, transfer, payout, checkout session, fraud enforcement action, payout pause, monetization disable, upload restriction, live restriction, account restriction, fake fraud risk score, fake sponsor revenue, audit mutation UI, fake paid status, fake payable balance, fake earnings, fake unpaid balance, fake revenue, or CTV revenue system is active.
- Creator Platform monetization is a later/foundation direction: Premium creators can eventually choose free/paid content and set backed prices for their Platform from Platform Studio command-center surfaces; free viewers can buy/watch paid creator content without Premium; paid creator content split is creator 80% net / Chi'llywood 20% net; tips are 100% of the tip amount/net tip to creators with separate disclosed fees where allowed; merch/products/clothing sales need a future commerce/provider path; scheduled payouts are free; optional instant cash-out preferred direction is 1.5% with no default cap; and Stripe Connect or an equivalent marketplace provider is the preferred creator payout/commerce rail, not Premium entitlement truth.
- CTV ads are future-only for Chi'llywood Originals and network-style content and are not active now.
- Admin Command Center V1A is foundation-honest: no fake revenue, payout balances, invoices, sponsor revenue, network billing, fraud holds, fake live ad provider state, or fake kill switches.
- Admin Usage Metering Foundations 37-39 is pushed in `_lib/platformUsage.ts`, `app/admin.tsx`, and local migration `supabase/migrations/202605070003_platform_usage_metering_foundation.sql`. Admin Usage Provider Imports V1D3/V1D4 are pushed in `_lib/platformUsage.ts`, `app/admin.tsx`, `supabase/functions/provider-usage-import/index.ts`, `supabase/config.toml`, and migration `supabase/migrations/202605070012_provider_usage_import_idempotency.sql`. They keep Admin Usage read-only and honest: live/watch-party/uploads are DB estimates, storage is a metadata estimate, participant-minutes are a DB estimate, Cloudflare R2/Hetzner Object Storage/Hetzner Servers provider imports are server-side only, Hetzner Object Storage storage values are S3 inventory metadata estimates only, OVH remains not connected, bandwidth is not connected unless future real metering events exist, and no cost/billing/payout/revenue/fake provider-log truth was added.
- May 17, 2026 Task 10/11 closeout proof keeps Admin Usage and finance/monetization closed only as Public V1 foundations: Admin Usage is read-only/honest with a refreshed known non-admin denial proof, and the current Platform Studio Monetization tab renders blocked/read-only Premium, Revenue, Payouts, Stripe, store, and future-tool sections with no fake money state. Monetization/payment/Stripe guards pass, and live checkout, tips, payouts, production Connect, provider billing APIs, overage/customer billing, fraud enforcement hooks, KYC/tax/legal/accounting approval, refunds/chargebacks, and live money remain later blocked lanes.
- The usage metering migration has been applied remotely and generated database types include the usage tables. Future usage event writers must be separately authorized and proved.
- Ledger Systems 4A-4D finance foundation is pushed in `_lib/platformFinance.ts`, `app/admin.tsx`, and migration `supabase/migrations/202605070005_platform_finance_ledger_foundation.sql`. Finance remote proof confirmed aligned migrations/types, current-build Admin read-only/foundation panels, zero foundation rows, and RLS denial for anon/non-platform finance-ledger insert attempts. It keeps Admin Revenue/Payouts/Networks/Sponsors/Fraud read-only and honest: safe foundation row counts only, no live money totals, no payout buttons, no invoice actions, no sponsor checkout, no payout split execution, no fraud enforcement, and no provider keys or SDKs.
- The finance ledger migration is now applied remotely and generated database types include the finance foundation tables. Any finance provider imports, payout provider integration, network invoice actions, sponsor checkout, or fraud enforcement must still be separately authorized and proved.
- Finance F2B creator payout provider schema foundation is pushed in migration `supabase/migrations/202605080001_creator_payout_provider_foundation.sql` and generated database types. Finance F2D adds read-only Admin Payouts counts for future Stripe Connect or equivalent payout accounts, payout batches, provider transfers, payout holds, and immutable payout audit logs. Finance F2E adds deterministic non-live proof rows in migration `supabase/migrations/202605080002_creator_payout_provider_proof_rows.sql` with zero amounts and `not_active` status only. Creator Payout Read-Only Dashboard Foundation adds policy-only creator read access to own `creator_payout_ledger_entries`, now surfaced in Platform Studio through `Monetization > Payouts` instead of a separate top tab. Stripe Connect Schema Foundation adds migration `supabase/migrations/202605080008_stripe_connect_schema_foundation.sql` and generated database types for provider/config/capability/readiness fields, onboarding sessions, provider webhook events, payout eligibility/readiness records, and immutable audit linkage. Stripe Connect Test-Mode Backend Functions add deployed backend-only functions for account, onboarding-link, account-sync, and webhook flows; backend Edge Function secret names are configured by digest only; account/reuse, onboarding link, account sync, signed webhook handling, and duplicate-event idempotency have test-mode proof. Stripe payout preproduction readiness now proves an authenticated test creator can create a real Stripe test connected account, create a short-lived `connect.stripe.com` onboarding link, sync non-payable readiness, prove KYC/tax/payout capability blockers, and prove `$0` scheduled payout plus 1.5% no-cap instant cash-out fee math while live-money flags stay off. Admin Payout Provider Dashboard adds read-only Admin Payouts counts for payout-provider accounts, onboarding sessions, readiness rows, and webhook events. Payout Review Queue + Batch Draft Workflow Foundation adds migration `supabase/migrations/202605080009_payout_review_batch_foundation.sql`, generated types, and read-only Admin Payouts review/batch counts with zero-cent proof rows only. Provider Transfer Records Sync Foundation adds migration `supabase/migrations/202605080010_provider_transfer_sync_foundation.sql`, generated types, read-only Admin transfer sync counts, and deployed backend-only `stripe-connect-transfer-sync` status import for existing local provider records only. Payout Transfer Preflight Foundation adds deployed backend-only `stripe-connect-transfer-create` closed-gate preflight that returns `preconditions_required` and creates nothing. Payout Release Preflight Foundation adds deployed backend-only `payout-release-preflight` closed-gate preflight that returns `preconditions_required` and creates nothing. These add no live runtime payout behavior, no moderator access, no React Native Stripe SDK/keys/provider secrets, no production Connect account, no production payout approval/release, no batch processing, no transfer creation, no Mark Paid/Retry/Send Money/Release/Process action, and no payout/withdrawal/live money actions.
- Network Billing Foundation is pushed in migration `supabase/migrations/202605080003_network_billing_foundation.sql`, generated database types, `_lib/platformFinance.ts`, and read-only Admin Networks copy. It adds account display/contact fields, invoice month/subtotal/total fields, network plan records, account plan assignments, quota records, invoice line items, overage events, and billing audit logs, plus deterministic proof/foundation rows only. It adds no invoice send, customer charge, payment link, Stripe call, live overage approval, fake revenue, fake unpaid balance, real customer obligation, broad RLS access, SDK, or provider secret.
- Network Invoice Draft Workflow + Overage Warning/Readout Foundation is pushed in migration `supabase/migrations/202605080011_network_invoice_overage_foundation.sql`, `_lib/platformFinance.ts`, and read-only Admin Networks copy. It adds safe draft/readout labels and indexes for existing network invoice/line-item/overage tables, inserts zero-value foundation rows for an internal draft invoice, draft/foundation line items, a warning-only overage readout, and billing audit records, and shows invoice draft/overage warning/provider reconciliation status in Admin Networks. Draft invoices are internal only, no invoice can be sent, no customer can be charged, no payment links exist, overage warnings are read-only, billing execution is not active, provider reconciliation and trusted usage metering are required before real billing, and no fake revenue, fake unpaid balance, or real customer obligation exists.
- Sponsor Checkout Foundation is pushed in migration `supabase/migrations/202605080004_sponsor_checkout_foundation.sql`, generated database types, `_lib/platformFinance.ts`, and read-only Admin Sponsors copy. It adds/extends sponsor brands, sponsor deals, creatives, placements, disclosures, review logs, payment records, and payout split records, plus deterministic proof/foundation rows only. It adds no sponsor checkout, brand payment, payment link, Stripe Checkout, sponsor approval action, fake sponsor revenue, payable balance, payout split execution, broad RLS access, SDK, or provider secret.
- Sponsor Review/Disclosure/Payment Foundation is pushed in migration `supabase/migrations/202605080012_sponsor_monetization_foundation.sql`, generated database types, `_lib/platformFinance.ts`, and read-only Admin Sponsors copy. It adds sponsor review queue records, sponsor safety review records, disclosure required-before-live support, and sponsor payment test-mode planning fields, plus deterministic proof/foundation rows only. It adds no sponsor checkout, sponsor approval execution, sponsor activation, brand charge, payment link, creator payout release, payout split execution, fake sponsor revenue, fake payable balance, broad RLS access, SDK, or provider secret.
- Fraud Enforcement Foundation is pushed in migration `supabase/migrations/202605080005_fraud_enforcement_foundation.sql`, generated database types, `_lib/platformFinance.ts`, and read-only Admin Fraud copy. It expands `platform_fraud_holds` and adds fraud reasons, evidence records, planned actions, review notes, appeal placeholders, and fraud audit logs, plus deterministic proof/foundation rows only. It adds no payout pause, account restriction, upload restriction, live restriction, monetization disable, fraud risk score, active enforcement action, broad RLS access, SDK, provider secret, or payment credential.
- Creator Revenue Share Ledger / Fraud Hold Enforcement Foundation is pushed in migration `supabase/migrations/202605080013_revenue_share_fraud_hold_enforcement_foundation.sql`, generated database types, `_lib/platformFinance.ts`, and read-only Admin Revenue/Fraud copy. It adds creator revenue share rules, creator revenue share ledger rows, fraud enforcement policies, and non-executable fraud action readiness fields with deterministic zero-value/non-live proof rows only. It adds no real source revenue import, fake creator earnings, payable balance, payout ledger entry creation from revenue-share rows, payout execution, payout pause, monetization disable, upload restriction, live restriction, sponsor restriction, account restriction, fake risk score, broad RLS access, SDK, provider secret, or payment credential.
- Fraud Runtime Hook Planning/Spec is recorded with no code enforcement. Future runtime hook order is payout pause, monetization disable, sponsor restriction, network billing hold, upload restriction, then live restriction, each behind linked fraud hold/action rows, immutable audit, admin reason, review status, reversible state, appeal/review path, and affected-surface bypass proof.
- Real Source Revenue Import Foundation is pushed in migration `supabase/migrations/202605080015_revenue_source_import_foundation.sql`, generated database types, backend-only deployed `revenue-source-import` skeleton, `_lib/platformFinance.ts`, and read-only Admin Revenue source-import counts. It adds source-import readiness rows only and makes no provider calls. Future imports must start from provider-backed, idempotent source money records with reconciliation and audit proof before they can create non-foundation creator revenue-share ledger entries.
- Creator Revenue Dashboard Foundation is now represented inside `Monetization > Revenue` in `app/channel-settings.tsx` instead of a separate Platform Studio Revenue tab. It shows read-only revenue status, planned categories, and guardrails only. It reads no creator revenue rows, shows no source-money history, shows no fake earnings or payable balances, and adds no Withdraw/Cash Out or payout release. Future money-bearing Platform Studio revenue UI requires real source money imports, creator-read RLS, fraud/hold/review states, and honest non-payable empty states before showing any revenue data.
- Remaining Finance/Admin Lane Planning is recorded. Provider Billing Reconciliation Foundation is implemented/deployed as backend-only reconciliation over existing usage rows, Provider Billing API Import Preflight Foundation is implemented/deployed as backend-only provider billing import refusal, Sponsor Checkout Preflight Foundation is implemented/deployed as backend-only sponsor checkout refusal, Sponsor Brand Payment Preflight Foundation is implemented/deployed as backend-only brand payment refusal, Sponsor Reporting / Fraud Integration Preflight Foundation is implemented/deployed as backend-only sponsor reporting/fraud refusal, Fraud Review Queue Foundation is implemented as read-only review-state foundation, Real Source Revenue Import Foundation is implemented/deployed as backend-only no-provider-call source-import readiness, Creator Revenue Dashboard Foundation is implemented as inactive/no-earnings Platform Studio UI, Payout Transfer Preflight Foundation is implemented/deployed as backend-only closed-gate refusal, and Payout Release Preflight Foundation is implemented/deployed as backend-only closed-gate refusal. Actual provider billing API imports, actual test-mode transfer creation, actual production payout release, actual sponsor checkout, actual brand payment through Stripe, real sponsor reporting imports/fraud integration, real provider source imports, and creator revenue dashboards with money remain separate exact-prompt lanes with no transfer, payout, invoice send, customer charge, live checkout, fake paid state, fake sponsor performance, fake revenue, or enforcement active.
- Immutable Admin Audit Log Foundation is pushed in migration `supabase/migrations/202605080006_immutable_admin_audit_log_foundation.sql`, generated database types, `_lib/platformAudit.ts`, and read-only Admin Audit copy. It adds append-only `platform_admin_audit_logs`, owner/operator select/insert policies, no update/delete policy, a narrow update/delete prevention trigger, and deterministic proof/foundation rows only. It adds no audit row edit/delete/clear UI, payout approval/release, invoice send/charge, sponsor checkout/approval, fraud enforcement, account restriction, upload/live restriction, monetization disable, broad RLS access, SDK, provider secret, payment credential, or dangerous admin write.
- Owner/Admin Control Tools build on the owner superuser/scoped staff/legal foundation with `admin-owner-controls`, Audit Explorer, permission templates, temporary grants, Break Glass, consolidated Legal Intake/Evidence, Owner Security, Canary Checks, and Safety Dashboard. Owner normal app access remains unaudited unless Break Glass is active; approved Admin sensitive actions remain exact-permission, reason-required, and audited. `admin-owner-controls` is deployed as ACTIVE version 25, `admin-legal-evidence` is ACTIVE version 7, and physical Android owner-device Admin Canary proof returned `65 passed`, `0 manual required`, and `0 failed`, including automated legal readiness, public DMCA form/attachment/counter-notice checks, Legal Intake/Evidence list/create/open/status/timeline/evidence checks, Supabase redirect proof, owner-hidden proof, owner normal no-audit proof, admin self-grant denial, moderator grant denial, Live Ops dry-run flags, and proof role/grant cleanup. The Owner Security placeholder is replaced by a backed Owner Security Center using `owner_trusted_devices`, `security_audit_events`, temporary grant revoke actions, audit timeline, Live Ops flags, and backed checklist proof. Its hardening migration `202605230001_owner_security_center_hardening.sql` is applied, the Edge Function is deployed, backend non-owner denial and physical Android non-owner no-leak proof passed, and dangerous owner actions now require backend owner truth, backend-trusted current device, reason, typed confirmation, and audit write. Owner-session Android proof on `R5CR120QCBF` passed: the signed-in owner trusted the current device, Emergency Actions moved from `Locked` to `Available`, the exact-confirmation/reason untrust sheet was enforced, the device was marked untrusted again, Emergency Actions returned to `Locked`, and linked Supabase rows proved `owner_device_trusted` plus `owner_device_revoked` security audit events with the recorded proof reason. The May 22 Admin UI/UX law follow-up polished DMCA, Legal, Owner Security, Canary, Safety, Audit Explorer, Permission Templates, Break Glass, public copyright report, and uploader counter-notice surfaces with command-center panels, structured proof/details, exact disabled reasons, and no raw debug/fake-green UI without changing backend behavior. Canary status must stay real pass/fail/manual-required and never fake green.
- Admin V1B1 runtime controls config foundation is pushed in `_lib/featureFlags.ts`, `_lib/appConfig.ts`, and read-only `app/admin.tsx` Kill Switches copy. The typed defaults live under `app_configurations.config.runtimeControls`; Admin shows `Configured foundation` and `Not enforced yet`; no runtime enforcement, working toggles, migrations, generated database type edits, RLS changes, Supabase remote changes, or Premium gate weakening were added.
- Admin V1B2A is pushed in `app/(auth)/signup.tsx` and read-only `app/admin.tsx` copy. Signup now reads `runtimeControls.new_accounts_enabled` after email/password and 18+ confirmation and blocks before `supabase.auth.signUp` when false; default true preserves normal signup. No Admin write toggle, migration, generated database type edit, RLS/storage change, Supabase remote change, login/session change, or Premium gate change was added.
- Admin V1B2B is pushed in `app/channel-settings.tsx` and read-only `app/admin.tsx` copy. New creator-video upload submit now reads `runtimeControls.uploads_enabled` after existing missing-title, missing-file, and file-size checks and blocks before `uploadCreatorVideo` or storage work when false; default true preserves normal uploads. Existing video metadata edit, publish/unpublish/delete, Open Player, picker behavior, storage helpers, RLS, migrations, generated types, and one clear Platform Studio `Video Upload` form are unchanged.
- Admin V1B2C is pushed in `app/profile/[userId].tsx`, `app/player/[id].tsx`, and read-only `app/admin.tsx` copy. Backed Profile post comment/reply submit and creator-video comment/reply submit now read `runtimeControls.comments_enabled` after existing validation and block before backed comment create or comment attachment upload when false; default true preserves normal comments. Existing comment reads, deletes, reports, attachment picker selection, Watch-Party comments, Live Stage comments, Chi'lly Chat messages, Chi'lly Circle, profile privacy, Player controls/layout, RLS, migrations, generated types, and Supabase remote state are unchanged.
- Admin V1B2D is pushed in `app/profile/[userId].tsx` and read-only `app/admin.tsx` copy. Backed Profile post creation submit now reads `runtimeControls.profile_posting_enabled` after owner/busy, empty-body, and length checks and blocks before backed Profile post create or post attachment upload when false; default true preserves normal Profile posting. Existing Profile post reads, comments/replies, likes, deletes, reports, attachment picker selection, Chi'lly Circle, profile privacy, creator video upload, Platform Studio, Public Platform, Player, Watch-Party, Live Stage, Chat, RLS, migrations, generated types, and Supabase remote state are unchanged.
- Admin V1B2E is pushed in `app/channel-settings.tsx` and read-only `app/admin.tsx` copy. New creator event creation now reads `runtimeControls.creator_posting_enabled` and blocks before `createCreatorEvent` when false; default true preserves normal creator event creation. Existing `updateCreatorEvent` edits, creator-video upload, video metadata edit, publish/unpublish/delete, Profile posts, comments/replies, attachments, Platform Studio layout, Public Platform, Player, Watch-Party, Live Stage, Chat, RLS, migrations, generated types, and Supabase remote state are unchanged.
- Admin V1B2F is pushed in `app/profile/[userId].tsx`, `app/player/[id].tsx`, and read-only `app/admin.tsx` copy. Selected non-chat social attachments now read `runtimeControls.attachments_enabled` and block before parent post/comment create and attachment upload when false; default true preserves normal social attachments. Profile post attachments, Profile post comment/reply attachments, and creator-video comment/reply attachments are enforced. Text-only posts/comments, Chat attachments, room attachments, `_lib/socialAttachments.ts`, storage helpers, RLS, migrations, generated types, and Supabase remote state are unchanged.
- Admin V1B2G is pushed in `app/chat/[threadId].tsx` and read-only `app/admin.tsx` copy. Selected standalone Chi'lly Chat attachments now read `runtimeControls.chat_attachments_enabled` and block before optimistic message insertion and before `sendChatMessage` when false; default true preserves normal chat attachments. Text-only messages, room attachments, `_lib/chat.ts`, `_lib/socialAttachments.ts`, storage helpers, RLS, migrations, generated types, and Supabase remote state are unchanged.
- Admin V1B2H is pushed in `app/watch-party/[partyId].tsx`, `app/watch-party/live-stage/[partyId].tsx`, and read-only `app/admin.tsx` copy. Selected room attachments now read `runtimeControls.chat_attachments_enabled` and block before room message insert and attachment upload when false; default true preserves normal room attachments. Text-only room comments, room layouts, LiveKit behavior, Premium gates, `_lib/watchParty.ts`, `_lib/socialAttachments.ts`, storage helpers, RLS, migrations, generated types, and Supabase remote state are unchanged.
- Admin V1B2I-A is pushed in `app/chat/[threadId].tsx`, `app/chat/index.tsx`, `app/profile/[userId].tsx`, and read-only `app/admin.tsx` copy. Standalone Chi'lly Chat message sends and thread calls read `runtimeControls.chat_enabled` and block before new chat writes/calls when false; Rachi Help thread creation and non-self Profile-to-chat entry also block before direct thread creation. `/chat` and `/chat/[threadId]` remain readable. Room comments, room invites, `chat_attachments_enabled`, `_lib/chat.ts`, `_lib/watchParty.ts`, storage helpers, RLS, migrations, generated types, and Supabase remote state are unchanged.
- Admin V1B2I-B is pushed in `components/chat/internal-invite-sheet.tsx` and read-only `app/admin.tsx` copy. Room invite direct-message sends read `runtimeControls.chat_enabled` and block before `sendDirectInviteMessage` when false; system share fallback remains available. Room text comments, room layouts, LiveKit behavior, Premium gates, `chat_attachments_enabled`, `_lib/chat.ts`, `_lib/watchParty.ts`, storage helpers, RLS, migrations, generated types, and Supabase remote state are unchanged.
- Admin V1B2I-C is pushed in `app/watch-party/[partyId].tsx`, `app/watch-party/live-stage/[partyId].tsx`, and read-only `app/admin.tsx` copy. Watch-Party and Live Stage room-native text/comment submits read `runtimeControls.chat_enabled` and block before `sendPartyMessageRecord` when false; default true preserves normal room comments. Existing room reads, room layouts, LiveKit behavior, Premium gates, invite behavior/system share, `chat_attachments_enabled`, attachment picker behavior, `_lib/watchParty.ts`, `_lib/chat.ts`, `_lib/socialAttachments.ts`, storage helpers, RLS, migrations, generated types, and Supabase remote state are unchanged.
- Admin V1B2J is pushed in `_lib/premiumWatchPartyAccess.ts`, `_lib/creatorVideos.ts`, existing live/watch-party entry surfaces, Platform Studio upload-size validation, and read-only `app/admin.tsx` copy. `live_first_enabled`, `live_watch_party_enabled`, and `watch_party_live_enabled` now pause scoped entry flows when false while preserving Premium requirements when true. `max_upload_size_mb` now gates creator-video upload size before storage work. Admin remains read-only; `runtimeControls.ads_enabled` is not the current Ads Launch config owner; `premium_required_for_live` and `premium_required_for_watch_party` are separate Premium helper truth, not runtime switches.
- storage doctrine: Cloudflare R2 for public/high-download media; Hetzner Object Storage for source/original uploads/drafts/backups/archive/private-held-deleted media; Hetzner/OVH boxes for LiveKit and real-time live/watch-party traffic
- Stripe Connect or equivalent marketplace payout layer for creator cash-out
- creator payout operations and reconciliation from net receipts, not gross sticker price
- Finance F2 payout-provider foundation is closed out as schema/read-only Admin counts/non-live proof rows only, Creator Payout Read-Only Dashboard Foundation is closed out as read-only Platform Studio payout readiness plus own-ledger read policy only, Stripe Connect Schema Foundation is closed out as additive provider/onboarding/webhook/eligibility schema only, Stripe Connect Test-Mode Backend Functions are closed out as deployed backend-only test-mode functions with account creation/reuse, onboarding link creation, account sync, signed webhook, and duplicate-event idempotency proof complete, Admin Payout Provider Dashboard is closed out as read-only provider-status counts, Creator-facing payout setup UI is pushed as backend-only/test-mode setup/onboarding/status refresh, and Stripe payout preproduction readiness is closed for app testing of Connect setup, non-payable KYC/tax blocker states, owner-approval copy, and cash-out fee math. Payout Review Queue + Batch Draft Workflow Foundation is closed out as read-only/draft-only Admin foundation counts with zero-cent proof rows, Provider Transfer Records Sync Foundation is closed out as backend-only/admin-operator/test-mode status import for existing local provider transfer records only, Payout Transfer Preflight Foundation is closed out as backend-only/admin-operator transfer refusal, and Payout Release Preflight Foundation is closed out as backend-only/admin-operator release refusal. KYC, tax forms, provider-completed payout capability, payout provider production writes, payout approval/release, actual transfer creation, batch processing, fake paid status, fake payable balances, and live payouts remain later.
- Network Billing Foundation and Network Invoice Draft Workflow + Overage Warning/Readout Foundation are closed out as schema/read-only Admin counts/non-live proof rows only; Provider Billing Reconciliation Foundation can upsert reconciliation rows from already-backed usage data, and Provider Billing API Import Preflight Foundation refuses provider import until future gates are proved. Actual provider billing API imports, invoice send, payment links, customer charging, Stripe/network billing execution, and live overage billing remain later.
- Fraud Enforcement Foundation is closed out as schema/read-only Admin counts/non-live proof rows only, and Fraud Runtime Hook Planning/Spec is recorded with no runtime hook connected; review workflow, confirmation/reason, appeal path, legal/compliance review, affected-surface proof, and any live enforcement remain later. Immutable Admin Audit Log Foundation is also closed out as append-only schema/read-only Admin latest rows/non-live proof rows only; future dangerous writes must use it before/when they execute.
- tax reporting and finance operations
- moderation, abuse handling, and auditability for monetized surfaces
- country rollout and geography-specific constraints
- explicit separation between app-store subscription billing, RevenueCat entitlement handling, and creator payout infrastructure
