name: Temporary Live room entry source repair

on:
  push:
    branches:
      - "chatgpt/live-room-entry-end-to-end-v1"

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo ready
