import { ChannelStudioScreen } from "../channel-settings";

// Preferred Platform Studio route. The screen implementation still lives in
// app/channel-settings.tsx so /channel-settings can remain a compatibility route.
export default ChannelStudioScreen;
