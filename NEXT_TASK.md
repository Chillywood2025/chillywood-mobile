# NEXT TASK

## Exact Next Recommended Lane
Discovery Feed + Spectator System D3-D10 foundation is pushed at `e68251a`, D7B Live Spectator Broadcast Schema Foundation is pushed at `1e54530`, D7C Live Spectator Broadcast Backend Start/Stop Skeleton is pushed at `e36eb67`, the D7C output-config alias source update is pushed at `8ba557b`, the D7-D10 proof closeout / D7G metadata-only ad-policy label is pushed at `1a1ddf0`, the D7D private test Egress path is pushed at `b87dde3`, and D7D private start/status/stop proof completed on May 11, 2026. D3 Home Feed V1 now renders real backed rails for Live Now, Channels You Follow, From Your Chi'lly Circle, Upcoming Events, and Latest Public Uploads with honest empty states; D4 Public Channel has separate Live Now and Upcoming Events shelves; D5 Profile live/activity behavior is source-proofed as already privacy-gated/backed with no runtime code change; D6 metadata-only `/spectate/[itemId]` is pushed; D8 Watch-Party spectator eligibility helper is pushed; D9 is production notification/activity complete for Android; D10 deterministic ranking helpers use real signals only; D7C functions are deployed; D7D allows real Egress ids/private HLS playlist locations only for private `D7D_TEST_` proof rows; and D7G adds only metadata ad/CTV policy labels on `/spectate/[itemId]`. The D7D proof used a private `D7D_TEST_` room with one CLI proof participant, called LiveKit `StartRoomCompositeEgress` and `StopEgress`, wrote a real private Egress id, verified cleanup, returned no HLS URL, enabled no public/spectator playback, created no full-room spectator token, and revoked the temporary proof-account operator grant afterward. D7E Hetzner Object Storage public-safe HLS delivery proof passed on May 12, 2026 with physical Android device `R5CR120QCBF` publishing real camera/mic tracks into a private proof LiveKit room. Supabase used S3/LiveKit secrets server-side without exposing values, Egress returned a real Egress id, public playlist fetch reached `200` with a real `.m3u8` marker, and at least one segment fetched outside LiveKit with `200` and `video/mp2t`. The temporary public base, exact-prefix policy, proof function, nonce, local publisher token, proof room, and operator/probe access were cleaned up. `PUBLIC_HLS_BASE_URL` remains unset. D7F is now runtime-proved as an eligible public-safe HLS gate: `spectator_hls_playback_records` is server/admin-written, `spectator-playback` resolves state and proxies playlists/segments through a controlled endpoint, and `/spectate/[itemId]` renders watch-only playback only when that resolver returns `available`. Remote migrations `202605120001` and `202605120002` are applied, `spectator-playback` is deployed, a fresh Android-published HLS proof object produced `available`, the controlled playlist proxy returned `200` with `#EXTM3U`, and the controlled segment proxy returned `200` with `video/mp2t`. Private/proof/non-public/protected/title-rights-blocked/ticketed/Premium-full-room/failed/disabled/missing-HLS states returned blocked or unavailable without URL/token leakage, temporary proof rows were cleaned up, temporary operator access was revoked with `403 operator_required` after revoke, no full spectator LiveKit token was minted, and no raw Hetzner URL was returned to normal app users.

D9 production notifications/activity is closed for Android. Remote migration `202605120003` is applied, `notification-device-tokens` and `notification-dispatch` are deployed, Settings owns Android permission/preference/token registration UI, and the backend creates backed notification/activity records plus delivery attempts for followed creator live, Circle friend live, event starts soon, public upload, and replay later. Expo/EAS production Push Notifications FCM V1 is assigned for `com.chillywood.mobile` to `expo-fcm-v1-d9@chillywood-app.iam.gserviceaccount.com`; no service-account JSON, provider private key, raw token, service role, or operator JWT was committed or documented. Physical Android device `R5CR120QCBF` with safe token fingerprint `c1ab24247f5d` received real posted Chi'llywood notifications from backend dispatch, and all five trigger classes returned `sent` delivery attempts with Expo provider message ids present. `notification-dispatch` now targets the app-created Android `default` channel so production pushes land on Chi'llywood's high-importance Activity channel. A fresh background push appeared under Alerting notifications and tapping it opened `/spectate/36307717-be77-4a16-99e1-0e69e421a56d` safely with metadata-only spectator copy, no mic/camera controls, no full LiveKit room token, and no HLS URL. Non-recipient/non-follower/non-Circle checks had zero notification rows for the checked user, and private/proof, private upload, and title-rights-blocked replay checks returned blocked/no-send states. D9 proof discovery/event rows were cleaned from public discovery after proof, leaving zero public D9 proof items. iOS/APNs remains later unless separately scoped.

Ops alert automation safety gate is implemented under `ops/alert-automation` for backend/server operations only. It is dry-run by default, stores persistent jobs and JSONL audit logs, supports Alertmanager webhook HMAC, requires approval tokens for approval/denial endpoints, and blocks destructive LiveKit admin actions unless approval plus `ALLOW_LIVE_ACTIONS=true` plus `DRY_RUN=false` are all present. Network shaping remains blocked unless approval plus `ALLOW_NET_SHAPING=true` plus `DRY_RUN=false` are all present. The second-layer visibility pass adds sanitized `GET /jobs` and `GET /jobs/:id` APIs, optional `OPS_ADMIN_READ_TOKEN` read protection, optional SMTP domain-email notifications, and an Admin Command Center Ops Alerts tab. Email is disabled by default, notification-only, and cannot approve or execute actions. Mobile Admin Approve/Deny remains disabled pending a secure server-side admin proxy because `OPS_APPROVAL_TOKEN` must not be stored in client code. Local proof passed `npm run typecheck`, `npm run test`, `npm run build`, root typecheck, mock-email runtime webhook/approval checks, and Admin source proof; no real LiveKit delete/remove, TURN mutation, `tc` shaping, real email, Supabase change, Hetzner HLS change, production secret commit, or non-admin user-facing route change occurred. Future production activation must keep the approval gate, read-token boundary, SMTP secrets, and safety flags server-side until an explicit operator-approved incident run.

Admin Live Cost Guard foundation is now the repo-side product/admin control plane for LiveKit/TURN cost pressure. It adds owner/operator-only settings/events/actions tables, an Admin Live Cost Guard tab, server-side Alertmanager webhook/action scaffolding, audited manual controls, and a narrow token-issuance guard for Live Watch-Party and Watch-Party Live that remains inert by default. TURN Spike Protection / LiveKit Cost Safety is a non-user-facing infra/admin hardening lane; `docs/TURN_SPIKE_PROTECTION_RUNBOOK.md` plus `scripts/infra/turn-egress-snapshot.sh`, `scripts/infra/turn-burn-rate-estimator.sh`, and `scripts/infra/turn-emergency-cap.sh` document/read out the repo-side operator response path. `LIVE_COST_GUARD_WEBHOOK_SECRET` is now configured in Supabase Edge Function secrets, and direct observe-only webhook delivery proof is closed: invalid secret denied, valid fake warning payload inserted one `TEST_LIVE_COST_GUARD_OBSERVE_ONLY` event, inserted zero actions, and settings remained `enabled=false` / `mode=observe_only`. Current truth remains metrics not connected yet, TURN cap request-only, no SSH/host/firewall/coturn mutation, no automatic kicking/throttling/room blocking, and no normal-user behavior change. Next Live Cost Guard follow-up, if needed, is production monitoring proof: connect Prometheus/Alertmanager metrics, prove owner/operator-only access/RLS on live backend, and test manual/auto-protect with safe proof rooms before enabling any production remediation. Do not claim production server caps or active alerts until applied and proved.

Platform Owner/Admin/Moderator role hardening is live-proof closed. Remote migrations `202605140008_platform_staff_role_management.sql` and `202605140009_platform_staff_role_management_null_actor_guard.sql` are applied; Owner `rob2008gn@gmail.com` and Admin `chillywood92@gmail.com` both exist as auth users and resolve through server-side role checks, with internal `operator` remaining the database role for public-facing Admin. Live SQL/RPC proof passed for Owner add/remove Admin, Owner add/remove Moderator, Admin add/remove Moderator, Admin blocked from Owner/Admin-only actions, Moderator blocked from staff management, regular user blocked from staff RPCs/direct role writes, revoked-role loss of access, last-active-Owner protection, and grant/revoke audit rows. The `202605140009` follow-up fixed a NULL actor-role permission fallthrough found during proof. Future role work should be release/Admin UI smoke only unless new permissions are separately scoped; do not create fake auth users, client-only role checks, or service-role client access.

Refresh/video/data cost policy is implemented in repo. Live publish capture defaults are capped at 720p / 30fps with v1 max 30fps, full live access remains behind existing strict Premium helpers, room heartbeat is 15 seconds with a 45-second active membership window, Watch-Party Live snapshot fallback refresh is 30 seconds, Live Stage comment fallback refresh is 15 seconds while realtime remains primary, Chat read receipts are throttled, and Home/Profile/Public Channel/Platform Studio remain load-on-open/focus/manual surfaces. VOD quality ladder/resolver foundation is now repo-backed with `video_renditions`, `resolve_video_playback`, Player resolver integration, creator-facing rendition status, and media-storage rendition authorization; it creates no fake renditions and does not transcode. Full live VOD enforcement remains pending until real 360p/480p/720p/1080p files are generated, rows are written by a trusted worker/service, and delivery plus Premium entitlement proof passes. No Player UI redesign, Watch-Party UI layout, Live Stage UI layout, LiveKit token/registry/drain behavior, D7F spectator HLS path, Hetzner/HLS config, billing, payouts, ads, or finance behavior changed.

Live VOD enforcement is no longer blocked on Supabase linked DB auth. On May 14, 2026, linked migration list and linked schema lint passed, dry-run showed only `202605140010_vod_quality_ladder_resolver.sql`, `202605140010` was applied remotely, post-apply migration list showed local/remote alignment, post-apply dry-run said the remote database is up to date, and `media-storage` was deployed as ACTIVE version 37. Function smoke proved OPTIONS `200` and no-auth POST `401 missing_auth`. The next blocker is real VOD proof data and auth: create or ingest real safe rendition files, insert trusted `video_renditions` rows only for files that exist, use real viewer/owner auth contexts plus backed Premium entitlement where available, and prove Free 360p/480p access, Free HD denial, Premium HD access or fail-closed entitlement blocking, original/master privacy, and processing/failed denial. Do not fake transcoding or mark live VOD enforcement closed before that proof passes.

Existing standalone Player proof video candidate `84c486e9-a62e-4121-8e70-ee79e17b1bf0` is safe to use only after an operator can provide a real auth/storage proof path. It is public/clean and previously opened in the standalone Player, but it has no ready rendition rows today; anonymous `resolve_video_playback` returns `pending_renditions`, anonymous `video_renditions` direct access is denied, and anonymous `media-storage` signing is denied. Next smallest safe step: use a real owner/test session or server-side operator proof path to sign/download the source, generate real 360p/480p renditions without upscaling, upload them through the approved storage path, insert trusted rows only for files that exist, then rerun resolver/media-storage proof for free, Premium/fail-closed, owner, original/private, and processing/failed states.

Free VOD rendition proof remains blocked before source download. The current shell has `ffmpeg` and `ffprobe`, but no owner/test auth session for owner `4b5e7761-5bf1-4e18-9eb7-d6037a0eb32f`, no safe service-role/operator source-download path, no S3 storage credentials, and no usable linked database password. Do not insert ready `video_renditions` rows or create processing/failed proof rows until a safe authenticated source path exists and the matching files are real. The smallest unblocker is to provide a safe owner/test session or existing server-side operator proof path that can sign/download the selected S3 source without exposing secrets.

Creator monetization systems foundation is live-schema installed as the safe default-off money foundation, but not live money. Migration `202605140011_creator_monetization_systems_foundation.sql` is applied remotely on the linked Chi'llywood project and defines default-off monetization flags, creator pricing/access-control tables, product/order foundation, tip transaction foundation, append-only creator earnings ledger, payout request foundation, webhook idempotency, audit logs, and fail-closed RPCs. Post-apply proof shows migration list local/remote alignment, dry-run up to date, linked lint clean, default live-money flags false, and anon direct money-table writes denied. `_lib/creatorMonetization.ts` owns client helpers for safe preflight/readout calls, Player checks the paid-creator-content resolver before signing creator-video playback, Platform Studio has a disabled/foundation `Monetize` tab, and the public Channel route is copy-framed as the creator Mini Platform with a no-checkout commerce shelf state. `_lib/paymentRailPolicy.ts`, `supabase/functions/_shared/payment-rail-policy.ts`, and `npm run guard:payment-rail-policy` now keep the rail split explicit: Premium stays Google Play/RevenueCat, Android in-app digital creator paid content cannot silently use Stripe, tips cannot unlock digital perks/access, merch can use Stripe/commerce only after provider proof, and payout/cash-out remains Stripe Connect-only when later enabled. Premium remains `$9.99/month` through RevenueCat entitlement `premium`, and Premium revenue belongs to Chi'llywood without creator splitting. Paid creator checkout, tips, merch checkout, cash-out, payouts, production Stripe Connect, and Premium purchase proof remain blocked until provider/legal/security proof passes. Do not create fake purchases, tips, orders, earnings, balances, checkout sessions, payment intents, transfers, or payout releases.

Google Play + RevenueCat Premium setup has advanced to the next safe CLI boundary. RevenueCat project `projc5629a24`, Android Play Store app `appd24db94dd8`, package `com.chillywood.mobile`, Android public SDK key, entitlement `premium`, and offering `premium` are API-proved. `EXPO_PUBLIC_REVENUECAT_ANDROID_PUBLIC_SDK_KEY` is configured in EAS production env, and key-injected runtime validation reports RevenueCat configured. EAS production AAB build `aedded50-3818-4d7b-8bcf-9bb9c2d36513` completed for versionCode `2`, and that first real AAB was manually uploaded to Google Play Internal Testing as `Internal test v2 - RevenueCat Premium setup`. After the upload, Android Publisher API package visibility for `com.chillywood.mobile` returns `200`; subscription list returns `204`; `premium_subscription` still returns `404 Subscription not found`; and a Play subscription create attempt returns `400 FAILED_PRECONDITION: Cannot create a subscription without first registering a payments profile for the developer account`. RevenueCat product `premium_subscription` is still missing/unattached, and RevenueCat credentials remain attention-needed until the Play subscription exists and attaches to entitlement/offering `premium`. New EAS production build `96e60082-00b1-48dc-843b-132ce82a7710` completed after removing the public Admin sign-in link, loaded the production RevenueCat SDK key, used the existing remote keystore, incremented versionCode from 2 to 3, produced an AAB artifact, and EAS submitted it to the internal track only as submission `920c452f-1b9f-401c-94c9-616801684939`. Next smallest safe steps: register the Play developer account payments profile in Play Console, create/activate the Google Play subscription `premium_subscription` with monthly $9.99 base plan, attach/import the product in RevenueCat, and then run real license-tester purchase/restore/entitlement proof. Do not fake Premium or purchase proof.

LiveKit Server Registry + Room Router + Drain Mode is remote-activated and proved for the current one-box Hetzner reality. Remote migration `202605120004_livekit_server_registry_router.sql` is applied, `livekit-registry` and the routed `livekit-token` are deployed, `LIVEKIT_REGISTRY_HEARTBEAT_SECRET` is set server-side, and `chillywood-prod-01` is active with a fresh heartbeat. New `watch-party-live`, `live-stage`, and `chat-call` token paths route through the registry to `wss://live.chillywoodstream.com`; repeated room requests reuse assignments; routing audit and assignment rows are written; draining blocks only new room assignments while existing assigned rooms still resolve; reactivation restores new-room routing; standby/offline/full/stale proof states fail safely; and dummy proof servers are disabled after proof. There are still no new Hetzner/OVH boxes, autoscaling, provisioning, active-room migration, room deletion, participant disconnect behavior, Watch-Party Live UI change, Live Stage UI change, Player UI change, D7F spectator HLS/token change, D9 notification change, or ops alert automation behavior change.

May 13, 2026 LiveKit production proof follow-up passed the non-secret network and denial checks that were possible from the local/dev-device environment: `live.chillywoodstream.com` resolves to the intended Hetzner host, TLS verifies, the LiveKit `/rtc` WebSocket path is reachable and rejects invalid authorization, deployed `livekit-token`/`livekit-registry` secret names are present by name only, unauthenticated registry/list/heartbeat/token requests fail safe, anon REST cannot read registry/assignment/audit tables, and D7F spectator playback still uses the controlled HLS resolver/proxy without issuing spectator LiveKit tokens. Local firewall probes confirmed public HTTPS/signaling reachability and UDP send reachability to the expected realtime/TURN/range ports, but full STUN/TURN allocation could not be completed from this machine because no TURN client or host-side allocation proof environment was available. Physical Android devices were connected with the big phone on cellular and the smaller phone on Wi-Fi; the smaller Wi-Fi phone proved the existing Premium gate blocks Live Watch-Party before room/session/token/connect. Full Watch-Party Live and Live Stage two-device media proof, leave/rejoin proof, and 10-participant readiness remain unclosed because the connected accounts have no active Premium entitlement and real RevenueCat/Google Play billing is still externally blocked.

Next recommended blocker: RevenueCat Android production public SDK key / Google Play real purchase and restore proof remains the known launch blocker surfaced by the current local environment as `revenueCatAndroidPublicKeyConfigured: false`. The May 14 Premium/Admin Usage/Finance/Ops control-plane proof audit did not fake Premium billing and did not activate live money: Premium gates remain entitlement-backed, Admin Usage remains read-only except the existing creator-video upload/daily-rollup writer foundation, finance/payout/sponsor/provider-billing paths remain closed-gate or foundation-only, and ops alert approve/deny remains server-side only. For the LiveKit proof lane, the exact next unblocker is a valid entitlement-backed Premium test account or real store-backed Premium setup, plus a TURN allocation proof environment. Do not reopen D9 unless Android push regresses.

Profile / Public Channel / Platform Studio runtime proof plus Creator Media runtime proof is closed as of May 13, 2026. The proof used one physical Android device, `R5CR120QCBF`, with a real creator/owner account and a separate backed viewer/non-owner account by account switching; no second device was required for this lane. Profile post create/delete, public clean read, private/draft/deleted hiding, comments, replies, likes, clickable links, supported attachments, report UI, share-safe fallback, and Android composer behavior passed with real backed rows and no fake counts. Public Channel owner, signed-in viewer, signed-out viewer, and blocked-viewer states passed; Follow/Following mutation worked for the backed viewer account, signed-out Follow showed the auth handoff, stats rendered safely, and owner-only controls did not appear to viewers. The only runtime blocker found was signed-out public channel profile loading; it was fixed narrowly through the sanitized `read_public_channel_profile` RPC plus `_lib/userData.ts` fallback, without broad `user_profiles` RLS access. Platform Studio owner access, metadata edit persistence, publish/unpublish/delete lifecycle, non-owner denial, and safe Android form behavior passed. Creator Media proof passed through the existing upload path with non-zero real storage/table rows, public playback, missing-media safe shell, draft/public/deleted/non-owner states, comments/replies/clickable links/supported attachments, invalid/too-large attachment failures, and storage object remove/delete proof. The new proof video was unpublished, deleted, and its object removal verified after lifecycle proof; no fake data, storage secret, raw signed URL, service-role key, D7F spectator change, LiveKit routing change, billing/finance change, or unrelated UI redesign was added.

Live Stage role/access hardening is pushed at `4b90e09`, Watch-Party Party Room polish is pushed at `e0116d3`, Watch-Party Live local participant pinning is pushed at `c4b16ca`, and route-local modern UI modernization is pushed at `15b07953fe6b4343a061ce3c3fb2f463afc48db2`. Live Stage mode switching is host-only, route-param hybrid/live-watch attempts cannot let non-hosts mutate mode, Live First viewers cannot self-promote into camera/speaker seats, and `setPartyParticipantState` requires the authenticated room host. Watch-Party Live route separation remains intact and title/player-based; Party Room remains `/watch-party/[partyId]`, and Live Stage remains `/watch-party/live-stage/[partyId]`. Party Room pinning is local-only inside the existing Party Feeds tile area: long-press reveals inline Pin/Unpin, the pinned participant stays first for that viewer, and the pin persists locally until unpinned or the participant is absent. It does not change Live Stage, Chi'lly Chat calls, Player controls, LiveKit token/join logic, host/viewer authority, playback sync, comments/composer/attachments, waiting-room flow, party codes, Premium gates, or other users' views. Premium gates remain `requireLiveFirstPremium`, `requireLiveWatchPartyPremium`, and `requireWatchPartyLivePremium`. No 10-participant proof is claimed yet. Keep live/watch-party follow-ups separate from D3: 10-participant LiveKit proof first; any future live-room control behavior change needs a separate exact prompt plus live/two-device proof when behavior changes.

Communication compatibility route ownership cleanup is pushed at `fc2894658c25ea744f4be6e579083bc4aed27289`. `/chat` owns the canonical Chi'lly Chat inbox and `/chat/[threadId]` owns canonical direct-thread behavior. `app/communication/index.tsx` and `app/communication/[roomId].tsx` are compatibility-only; `/communication/[roomId]` no longer owns standalone communication room UI. It resolves active communication room ids to canonical Chat threads when possible, redirects mapped rooms to `/chat/[threadId]?openCall=1`, and redirects unmapped rooms safely to `/chat`. `openCall=1` may open an existing active call panel only and must not start a new call. Chat privacy/member checks remain canonical. Bottom-tab Chat remains deferred because `app/(tabs)/chat.tsx` would conflict with canonical `/chat` ownership.

Route-tree utility exposure cleanup is now pushed and should not be reopened as generic cleanup. `app/data/titles.ts` and `app/lib/_supabase.ts` were unreferenced exposed route-tree utility files and are removed. Active Watch-Party helper modules are preserved outside the Expo Router app tree at `_lib/watch-party/room-shared.ts` and `_lib/watch-party/waiting-room-shared.ts`. Future helper-only code must stay outside `app/` unless it intentionally owns an Expo route. Remaining log-readiness follow-up is release/preview proof for dependency or environment warnings that are not fixed by app runtime code, including Expo AV migration planning, Firebase console receipt proof, React Native dev-launcher soft exceptions, and release log audit.

Support email confirmation is pushed, receipt-proof closed, and baseline anti-spoofing DNS is hardened at repo level. `support@chillywoodstream.com` is the canonical public support contact, legal pages no longer show the old Gmail address, and `app.config.ts` provides the same client-safe fallback while still allowing `EXPO_PUBLIC_SUPPORT_EMAIL` to override it. Cloudflare Email Routing for `chillywoodstream.com` is enabled and ready, the support destination address is verified in Cloudflare, and an enabled custom routing rule forwards `support@chillywoodstream.com` to that destination. On May 13, 2026, a real test message with subject `Chi'llywood Support Receipt Proof - 2026-05-13` was sent to `support@chillywoodstream.com`, and the operator confirmed it arrived in the destination inbox. Public DNS now shows Cloudflare MX records, SPF `v=spf1 include:_spf.mx.cloudflare.net ~all`, and DMARC baseline `v=DMARC1; p=none; rua=mailto:support@chillywoodstream.com; adkim=r; aspf=r; fo=1`. DKIM remains pending until Chi'llywood configures a real outbound mail provider for `@chillywoodstream.com` and that provider issues DKIM selector records; no fake DKIM selector was added. No screenshots, raw headers, inbox exports, private email contents, sender private details, credentials, tokens, DKIM private key, or email secrets are committed.

Public launch legal / creator rules / copyright / trust-safety policy drafting is ready for attorney/external follow-up, and backed/Admin DMCA operational tooling is now live-proof closed. `docs/legal/` contains the launch policy set for Terms, Privacy, Creator Rules, Community Guidelines, Copyright/DMCA, copyright takedown workflow, account deletion, refunds/subscriptions, creator payouts, fraud/forfeiture, sponsor disclosure, banned content, moderation/reporting, and the legal launch checklist. `/creator-rules` is a public legal route and is linked from Settings and Support alongside Terms, Privacy, Community Guidelines, Copyright/DMCA, Account Deletion, and Support. Public legal/support URLs are now live on `chillywoodstream.com`, including the account deletion URL candidate for Google Play at `https://chillywoodstream.com/account-deletion` and public support at `https://chillywoodstream.com/support`; receipt proof for `support@chillywoodstream.com` is closed by operator-confirmed inbox receipt, and MX/SPF/DMARC baseline DNS is publicly proved. DMCA designated agent public contact posting and U.S. Copyright Office registration are recorded complete under registration `DMCA-1072720`. Formal copyright notice intake exists at `/copyright-report`; generic copyright reports link users to it; Admin owner/operator tooling can review DMCA cases, record statuses/actions, hide/disable/restore supported content types, record counter-notices/forwarding/court-action/restore eligibility, add/remove strikes, open repeat-infringer review, and view audit history. Live proof with disposable reporter/uploader/admin/viewer accounts and safe profile-post/comment/creator-video content passed for notice submission, invalid notice rejection, Admin list/detail/status, profile-post and creator-video hide/restore, public visibility blocking/restoration, strike/repeat-infringer review, rejected no-strike behavior, counter-notice deadlines, court-action restore blocking, RLS/private-data denial, generic report compatibility, and proof-content cleanup. Remaining blockers are attorney approval, Google Play account deletion/Data Safety entry acceptance, uploader-facing counter-notice submission if required, outbound email automation if required, and DKIM after real outbound-provider setup. Do not claim payouts, sponsor payments, refunds, automated deletion, broad legal compliance, or Google Play deletion compliance until those systems are proved.

Public legal URL surface repo generation is now available through `public-site/legal-site/`. Build it with `npm run legal-site:build` and serve it locally with `npm run legal-site:serve -- --port 4177`; the generated output under `public-site/legal-site/site/` covers `/terms`, `/privacy`, `/creator-rules`, `/community-guidelines`, `/copyright`, `/account-deletion`, `/refunds`, `/sponsor-disclosure`, `/banned-content`, `/moderation`, and `/support`. Cloudflare Pages project `chillywood-legal` is live at `https://chillywood-legal.pages.dev` and all 11 paths returned HTTP 200 there. The apex custom domain `chillywoodstream.com` is active on Cloudflare Pages. `https://chillywoodstream.com/` returns HTTP/2 200; `https://chillywoodstream.com/terms`, `/privacy`, `/account-deletion`, `/copyright`, and `/support` redirect with HTTP/2 308 to their trailing-slash paths and then return HTTP/2 200 with real static legal/support content. No LiveKit fallback was reached; the Privacy page mentions LiveKit only as policy text. Support inbox receipt proof for `support@chillywoodstream.com` has passed, and domain email MX/SPF/DMARC baseline DNS is publicly proved. The next external legal/store steps are to enter and have Play Console accept the account deletion URL, complete Data Safety, finish attorney review, and keep the DMCA designated-agent registration/contact current.

Google Play Android developer/package verification is complete for `com.chillywood.mobile`. Play Console accepted the ADI verification, and the selected signing certificate fingerprint is verified as `2B:A7:84:1B:70:6B:E8:69:E8:0F:EA:DA:43:8D:14:33:34:D6:E8:70:99:63:FA:B9:63:94:07:4E:9D:D0:95:43`. The ADI verification APK is archive proof only and is no longer needed for operations. This closes package ownership verification only; it does not close Play Data Safety/account deletion acceptance, production AAB/internal testing, RevenueCat billing, attorney review, release smoke, or any signing-secret placement.

RevenueCat Android production readiness hardening is pushed at `dd66545b364a8e727aab7d2b2cdc03fa8b1f70f5`. Repo-side readiness is complete: Android package truth is `com.chillywood.mobile`; the production public SDK key path is `EXPO_PUBLIC_REVENUECAT_ANDROID_PUBLIC_SDK_KEY` through app config/runtime config into `_lib/revenuecat.ts`; `/subscribe` exposes safe configured/key/offering/entitlement/store/provider-blocked readouts; Premium offering fallback remains configured `premium` first and RevenueCat fallback only for `premium_subscription`; the old dev-only Premium bypass env path is removed; and full live/watch-party Premium gates plus Premium ad-free behavior remain intact. Google Cloud project `chillywood-app` has the required Android Publisher, Play Developer Reporting, Pub/Sub, and Monitoring APIs enabled. Service account `chillywood-revenuecat-play@chillywood-app.iam.gserviceaccount.com` exists, its JSON remains outside the repo under `/Users/loverslane/secrets/chillywood/`, and it has the scoped `roles/pubsub.editor` and `roles/monitoring.viewer` project IAM roles only. The first real versionCode `2` AAB was manually uploaded to Internal Testing and Android Publisher API package visibility now returns `200`; the subscription list returns `204`, but `premium_subscription` still returns `404 Subscription not found`, and Play subscription create returns `400 FAILED_PRECONDITION` because the developer payments profile is not registered yet. RevenueCat `Credentials need attention` remains unresolved until the Play subscription/base plan exists and is attached/imported. Current local validation does not have the Android production public SDK key, while EAS production env does. A versionCode `3` AAB build completed after the public Admin sign-in link removal, and EAS submitted it to the internal track only as submission `920c452f-1b9f-401c-94c9-616801684939`. The next external proof lane is RevenueCat/Google Play only: register the Play developer payments profile, create/activate the Play subscription `premium_subscription` monthly $9.99 base plan, attach/import the product in RevenueCat entitlement/offering `premium`, then use a Play license tester to prove Premium purchase, restore, active entitlement, expired/revoked block, free-user full live/watch-party denial, and Premium ad-free state. Do not claim Google Play purchase proof until a real purchase/restore/subscription proof has run.

Modern UI/UX route-local visual modernization is pushed at `15b07953fe6b4343a061ce3c3fb2f463afc48db2`. The current major app surfaces now carry the modern premium dark streaming-platform visual treatment while preserving Chi'llywood's existing background identity: Home, Explore, Player, Watch-Party Party Room, Live Stage, Public Channel, Profile, Platform Studio/settings, Spectator, Chi'lly Chat, and Admin. This lane was visual/layout-only and changed no route ownership, backend behavior, data sources, Supabase remote state, migrations, generated database types, RLS/storage policy, LiveKit server config, Stripe/RevenueCat/Google Play/AppLovin setup, finance/live-money behavior, Premium gate, role boundary, package/native config, global theme, background asset, or app-wide background identity. No fake data, fake counts, fake messages, fake participants, fake revenue, fake payouts, fake sponsor money, fake network billing, fake trending, or fake recommendations were added. Physical Android dev-client proof on device `R5CR120QCBF` opened Home, Explore, Player, Watch-Party entry, Party Room safe route, Live Stage safe route, Public Channel, Profile, Platform Studio/settings, Spectator, Chat, and Admin from the current Metro bundle with no app/React crash; screenshots/logs remain untracked in `artifacts/ui-proof/`. The Player overlay crowding found during proof was fixed before push. Validation passed `npm run typecheck`, `npm run guard:watch-party-livekit`, `npm run validate:runtime`, and `git diff --check`; `npm run lint` still reflects the existing lint baseline and should be a separate cleanup lane if desired. Next UI proof should be a release/internal build smoke, not a new product redesign.

Chi'llywood Native Room / Screen UI doctrine is now locked into repo truth for future generated routes and route changes. Every new or materially changed room/screen must define the owner system, primary experience, top/middle/bottom/More placement, role/privacy/Premium gates, empty/loading/error/locked states, native mobile safe-area/keyboard/no-overlap proof, and background-preservation plan. Generic Expo/demo/admin-looking consumer screens, stacked-card piles without hierarchy, route ownership drift, fake data/counts/participants/money/messages, Premium-gate weakening, role leakage, and global background/theme changes are incomplete and forbidden. This pass made no runtime code changes because the existing major surfaces already carry the route-local modern UI treatment and were source-audited for native layout patterns.

LiveKit Egress repo-side software scaffold is pushed as backend/media infrastructure only. `infra/hetzner/docker-compose.livekit.yml` includes optional `livekit-egress` and localhost-only `livekit-redis` services behind the explicit Docker Compose `egress` profile; `infra/hetzner/livekit.env.example` includes non-secret Egress/Redis placeholders; `infra/hetzner/livekit-egress.yaml.example` is the server-side config template; and `docs/LIVEKIT_PRODUCTION_READINESS_RUNBOOK.md` records the activation checklist. Egress is not installed in the mobile app and normal LiveKit compose startup without `--profile egress` does not start Egress or Redis. The external D7D server proof is complete. D7E Hetzner Object Storage public-safe HLS delivery proof is complete with a physical Android publisher, real playlist fetch, and real segment fetch outside LiveKit. D7F runtime proof is complete with remote migrations applied, `spectator-playback` deployed, a fresh eligible public-safe HLS row resolving to `available`, controlled playlist/segment proxy `200` results, blocked-state proof, no raw URL/token leak, and temporary rows/access cleaned up. `PUBLIC_HLS_BASE_URL` remains unset because D7F uses the controlled `spectator-playback` resolver/proxy instead of a client-known public base.

The May 11 external Egress host proof is complete. Host SSH works; LiveKit, Egress, and Redis run on `chillywood-prod-01`; LiveKit and Egress share Redis; Supabase Edge Function LiveKit URL/key/secret were synced to the host secret store; and the Egress host config was adjusted for the 2-vCPU proof worker with `room_composite_cpu_cost: 2.0` plus an Egress-only restart. The proof account first returned `403 operator_required`, received a temporary operator grant only for proof, then returned `403 operator_required` again after that grant was revoked. A private `D7D_TEST_` room with one CLI proof participant/publisher started, `spectator-broadcast-start` returned `test_started` with `livekitApiCalled: true` and a real private Egress id, `spectator-broadcast-stop` returned `test_stopped` with `livekitApiCalled: true`, and LiveKit listed the Egress as `EGRESS_COMPLETE`. No public playback, spectator playback, HLS URL return, full spectator token, D7E playback UI, or D7F Admin broadcast control was enabled during D7D. The May 12 D7E Hetzner HLS delivery proof is complete with physical Android publisher, public playlist fetch, and public segment fetch outside LiveKit. D7F is now runtime-proved as controlled watch-only playback for eligible public-safe records; normal users still receive only controlled resolver/proxy URLs, never full LiveKit spectator tokens or raw Hetzner HLS URLs.

Payout Transfer Preflight Foundation is pushed and deployed. The next payout lane must still not create test-mode transfers until provider readiness, fraud clear state, KYC/tax readiness or a safe test equivalent, admin review, batch draft, immutable audit, idempotency, and explicit transfer-lane approval are all proved. Still no live mode, transfers, payouts, checkout sessions, creator withdrawals, payout approval/release, fake paid states, fake payable balances, fake earnings, or live money movement.

Payout Release Preflight Foundation is pushed and deployed. Production payout release remains closed: legal/accounting, production provider approval, KYC/tax, fraud review, admin review, hold-period, immutable audit, support/dispute policy, and staged-rollout gates must be proved before any release lane can exist. Still no payout release, payout approval, transfers, payouts, checkout sessions, fake paid states, fake payable balances, fake earnings, or live money movement.

Real Source Revenue Import Foundation and Creator Revenue Dashboard Foundation are pushed. The next creator-revenue lane must not add money-bearing creator revenue UI unless real provider-backed source money exists. It must not import AppLovin/ad money, Stripe sponsor money, tips, paid content, or network billing money unless real provider source records/events exist and are idempotent. Still no creator earnings, payable balances, payout ledger writes from revenue-share rows, Withdraw/Cash Out, payout approval/release, transfers, payouts, checkout, invoice send, customer charge, KYC/tax UI, fake balances, live fraud enforcement, or live money movement.

Provider Billing Reconciliation Foundation is pushed and deployed. Commit `12b5ddb` adds backend-only `provider-billing-reconciliation`, registers it in `supabase/config.toml`, updates `_lib/platformUsage.ts` readouts, and updates Admin Usage/Networks copy. The function requires internal Supabase bearer auth plus owner/operator role, reads only existing `provider_usage_daily` and `usage_daily_summaries`, upserts `provider_usage_reconciliation`, and writes immutable admin audit rows. It does not read provider secrets, call provider billing APIs, import provider bills, send invoices, charge customers, create payment links, trigger overage billing, create fake provider bills, create fake revenue, or execute network billing. Deployment proof showed OPTIONS `200`, GET `405`, and no-auth POST `401`.

Provider Billing API Import Preflight Foundation is pushed and deployed. Commit `a868cfb` adds backend-only `provider-billing-import-preflight` and registers it in `supabase/config.toml`. The function requires internal Supabase bearer auth plus owner/operator role, rejects provider credential, invoice, payment link, customer charge, currency, and amount instructions, returns `preconditions_required`, and writes immutable requested/blocked audit rows only for authorized operators. It reads no provider secrets, calls no provider APIs, imports no provider bills, creates/sends no invoices, charges no customers, creates no payment links, executes no overage billing, creates no fake provider bills, and moves no money. Deployment proof showed OPTIONS `200`, GET `405`, and no-auth POST `401`.

Sponsor Checkout Preflight Foundation is pushed and deployed. Commit `d1c754b` adds backend-only `sponsor-checkout-preflight` and registers it in `supabase/config.toml`. The function requires internal Supabase bearer auth plus owner/operator role, rejects checkout, payment, provider credential, customer, charge, currency, amount, and Stripe instructions, returns `preconditions_required`, and writes immutable requested/blocked audit rows only for authorized operators. It creates no Stripe Checkout session, payment link, brand charge, sponsor approval, sponsor activation, creator payout split execution, creator payout release, fake sponsor revenue, or live money movement. Deployment proof showed OPTIONS `200`, GET `405`, and no-auth POST `401`.

Sponsor Brand Payment Preflight Foundation is pushed and deployed. Commit `da43bac` adds backend-only `sponsor-brand-payment-preflight` and registers it in `supabase/config.toml`. The function requires internal Supabase bearer auth plus owner/operator role, rejects payment, checkout, provider credential, customer, charge, currency, amount, and Stripe instructions, returns `preconditions_required`, and writes immutable requested/blocked audit rows only for authorized operators. It creates no payment intent, Stripe Checkout session, payment link, brand charge, sponsor paid status, payable creator split, creator payout release, fake sponsor revenue, or live money movement. Deployment proof showed OPTIONS `200`, GET `405`, and no-auth POST `401`.

Sponsor Reporting / Fraud Integration Preflight Foundation is pushed and deployed. Commit `d127bdb` adds backend-only `sponsor-reporting-fraud-preflight` and registers it in `supabase/config.toml`. The function requires internal Supabase bearer auth plus owner/operator role, rejects provider credentials, fake performance, revenue, payment, checkout, charge, risk-score, and enforcement instructions, returns `preconditions_required`, and writes immutable requested/blocked audit rows only for authorized operators. It imports no sponsor reports, creates no fake sponsor performance or fake sponsor revenue, applies no fraud enforcement, restricts no creators or sponsor deals, activates no sponsors, charges no brands, releases no creator payouts, and moves no money. Deployment proof showed OPTIONS `200`, GET `405`, and no-auth POST `401`.

Fraud Review Queue Foundation is pushed and applied. Migration `supabase/migrations/202605080014_fraud_review_queue_foundation.sql` adds `fraud_review_queue_records`, generated database types, owner/operator RLS, foundation proof rows, immutable admin audit proof, and read-only Admin Fraud queue counts. It is review-state only: no payout pause, monetization disable, upload restriction, live restriction, sponsor restriction, account restriction, strike, ban, fake risk score, hold release, or runtime enforcement hook exists.

Admin Usage Provider Imports V1D3/V1D4 are pushed and proofed. The server-side `provider-usage-import` Supabase Edge Function is deployed, migration `202605070012_provider_usage_import_idempotency.sql` is applied, Admin Usage reads provider import status rows, missing auth returns `401`, and owner/operator proof completed Cloudflare R2, Hetzner Object Storage, and Hetzner Server import paths with the existing signed-in device session token in memory only. Hetzner Object Storage imports only S3-compatible bucket inventory metadata (`s3_inventory_storage_bytes` and `s3_inventory_object_count`) through server-side secrets, not traffic, billing, overage, invoice, revenue, payout, or storage-billing truth. No mobile provider API calls, provider secrets in repo, fake bandwidth, fake bills, overages, invoices, revenue, payouts, sponsor money, or Admin import buttons were added.

Finance Foundation Remote Proof is also complete. Migration history is aligned through `202605070011`, generated database types include the finance foundation tables, current-build Android Admin proved Revenue/Payouts/Networks/Sponsors/Fraud open as read-only/foundation with zero foundation rows, and non-platform/anon insert attempts into the finance ledger were denied by RLS. The proof did not insert money rows, did not add payout/invoice/sponsor/fraud actions, and did not change app runtime code, RLS, Supabase storage, LiveKit, ads, RevenueCat, billing execution, payout execution, sponsor execution, Player, Watch-Party, Live Stage, Platform Studio, Profile, or Public Channel.

Finance F2B creator payout provider schema foundation is pushed and applied. Migration `supabase/migrations/202605080001_creator_payout_provider_foundation.sql` and generated database types now define future `creator_payout_accounts`, `creator_payout_batches`, `creator_payout_provider_transfers`, `creator_payout_holds`, and `creator_payout_audit_log`. This is schema-only for future Stripe Connect or equivalent payout-provider work: no payout-provider runtime UI changed, no payout-provider creator self-read exists, no moderator access exists, no Stripe SDK/keys/provider calls were added, and no payout/withdrawal/live money action is active. The later Creator Payout Read-Only Dashboard Foundation adds only own-ledger read access to `creator_payout_ledger_entries`.

Finance F2C payout-provider schema/RLS proof is complete. Remote migration history includes `202605080001`, linked schema lint is clean, generated types include the payout-provider tables, and anonymous insert into `creator_payout_accounts` is denied. Non-empty read-denial and owner/operator visibility proof are still reserved for the safe proof-row lane.

Finance F2D payout provider Admin foundation readout is pushed. `_lib/platformFinance.ts` reads safe counts for payout accounts, batches, provider transfers, holds, and audit rows, and `/admin` Payouts shows those counts as read-only foundation status only. No live payout action, payout button, withdrawal button, Stripe onboarding, provider sync, provider call, KYC flow, or creator-facing balance exists.

Finance F2E payout provider foundation proof rows are pushed and applied. Migration `supabase/migrations/202605080002_creator_payout_provider_proof_rows.sql` inserts deterministic `not_active` proof/foundation rows marked `finance_foundation_proof: true`, `created_by: codex_finance_f2e_proof`, and `not_live_money: true` with zero amounts and no real creator, bank, card, provider, invoice, sponsor, or payment data. Anonymous reads see zero proof rows and anonymous insert remains denied. Finance F2 payout-provider closeout is complete and must remain foundation-only until a later Stripe Connect/provider integration lane is explicitly scoped.

Network Billing Foundation is pushed and applied. Migration `supabase/migrations/202605080003_network_billing_foundation.sql` expands existing network billing account/invoice foundation, adds plan, assignment, quota, invoice line item, overage event, and billing audit log tables, refreshes generated database types, inserts non-live foundation/proof rows, and updates `/admin` Networks as read-only counts only. Network billing remains not active: no invoices can be sent, no customers can be charged, no payment links or Stripe calls exist, no live overage approval exists, and proof rows must never be treated as payable customer obligations or live revenue.

Network Invoice Draft Workflow Foundation and Network Overage Warning/Readout Foundation are pushed and applied. Migration `supabase/migrations/202605080011_network_invoice_overage_foundation.sql` expands safe draft/readout labels and indexes for existing network invoice and overage tables, inserts zero-value foundation proof rows for an internal draft invoice, draft/foundation line items, warning-only overage readout, and billing audit records, and updates `/admin` Networks as read-only/foundation with invoice draft workflow, overage warning readout, and provider reconciliation copy. Draft invoices are internal only, no invoice can be sent, no customer can be charged, no payment links exist, overage warnings are read-only, billing execution is not active, provider reconciliation and trusted usage metering are required before real billing, and no fake revenue, fake unpaid balance, or real customer obligation exists.

Sponsor Checkout Foundation is pushed and applied. Migration `supabase/migrations/202605080004_sponsor_checkout_foundation.sql` extends sponsor deals and adds sponsor brand, creative, placement, disclosure, review log, payment record, and payout split record foundation tables. Generated database types are refreshed, `_lib/platformFinance.ts` reads sponsor foundation counts, and `/admin` Sponsors is read-only/foundation only. Proof rows are foundation-only with zero money action markers. Sponsor checkout is not live: no checkout URL, payment link, Stripe Checkout, brand payment, sponsor approval action, fake sponsor revenue, payable balance, or creator payout split execution exists. Later sponsor money requires platform review, disclosure rules, safe product/scam review, fraud/abuse review, payout hold/review, and audit trail. Future model remains: brand pays Chi'llywood first; creator-sold sponsor slots pay creator 80% net / Chi'llywood 20% net; platform-served creator-page ads pay creator 70% net / Chi'llywood 30% net.

Sponsor Deal Review Queue Foundation, Sponsor Disclosure / Moderation Foundation, and Sponsor Payment Test-Mode Foundation are pushed and applied. Migration `supabase/migrations/202605080012_sponsor_monetization_foundation.sql` adds `sponsor_review_queue_records`, `sponsor_safety_review_records`, `sponsor_disclosure_records.required_before_live`, and sponsor payment planning fields (`test_mode_enabled`, `checkout_provider`, `checkout_session_reference`, `payment_mode`). Generated database types are refreshed, `_lib/platformFinance.ts` reads the new sponsor foundation counts, and `/admin` Sponsors remains read-only/foundation only. Sponsor review queue is foundation-only, disclosure/moderation is foundation-only, and sponsor payment foundation is not checkout. No sponsor can be approved, no sponsor can be activated, no brand can be charged, no checkout exists, no payment link exists, no creator payout split can execute, no fake sponsor revenue exists, and no fake payable balance exists. Disclosure and safety review are required before sponsor deals can go live later. Sponsor payment records and payout split records are proof/foundation only.

Fraud Enforcement Foundation is pushed and applied. Migration `supabase/migrations/202605080005_fraud_enforcement_foundation.sql` expands `platform_fraud_holds` and adds fraud reasons, evidence records, planned actions, review notes, appeal placeholders, and fraud audit logs. Generated database types are refreshed, `_lib/platformFinance.ts` reads fraud foundation counts, and `/admin` Fraud is read-only/foundation only. Proof rows are foundation-only and have no live enforcement action markers. Fraud enforcement is not live: no payout pause, account restriction, upload restriction, live restriction, monetization disable, fraud risk score, or enforcement action exists. Future fraud enforcement requires immutable admin audit logs, review workflow, confirmation/reason, appeal path, and legal/compliance review. Future fraud reasons include invalid traffic, fake engagement, fake followers, fake views, fake ad activity, scams, undisclosed sponsorship, stolen content, chargebacks, refund abuse, policy violation, illegal conduct, suspicious payout behavior, and network overage abuse.

Creator Revenue Share Ledger Foundation and Fraud Hold Enforcement Foundation are pushed and applied. Migration `supabase/migrations/202605080013_revenue_share_fraud_hold_enforcement_foundation.sql` adds `creator_revenue_share_rules`, `creator_revenue_share_ledger_entries`, and `fraud_enforcement_policy_records`, expands `fraud_action_records` with enforcement policy linkage, target area, execution status, and executed-at fields, refreshes generated database types, and updates `/admin` Revenue/Fraud read-only counts. Creator revenue share rules and ledger rows are foundation-only. No real source money has been imported, no fake creator earnings exist, no payable balances exist, and no payout ledger entries are created from revenue share foundation rows. Fraud enforcement policies/actions are foundation-only, runtime enforcement hooks are not connected, no payouts are paused, no monetization is disabled, no uploads are restricted, no live access is restricted, no sponsor deals are restricted, and no account restrictions are active. Future creator revenue share requires real source money from ads/sponsors/tips/paid content. Future fraud enforcement requires runtime hook implementation, review workflow, confirmation/reason, appeal path, and immutable audit rows.

Real Source Revenue Import Foundation is pushed, remote-applied, type-refreshed, and deployed as a backend-only no-provider-call skeleton. Migration `supabase/migrations/202605080015_revenue_source_import_foundation.sql` adds `creator_revenue_source_import_records` with owner/operator RLS, duplicate-protection fields, zero-cent foundation proof rows, and immutable admin audit proof. The `revenue-source-import` function returns `not_configured`, requires internal bearer auth plus owner/operator role, reads no provider secrets, performs no provider calls, and writes no revenue import rows at runtime. Future revenue imports must be provider-backed and idempotent before they can feed creator revenue share: AppLovin/ad revenue import later after provider reports exist; Stripe sponsor payment import later after sponsor checkout test-mode proof and signed payment events; tips and paid-content import later only after those product flows exist; network billing import later only for platform finance/reconciliation, not creator earnings unless product explicitly defines it. Current state remains no real source money, no fake revenue, no creator earnings, no payout ledger writes from revenue-share rows, no payable balances, and no live money movement.

Creator Revenue Dashboard Foundation is pushed in Platform Studio. It adds a read-only inactive Revenue tab and Home card with source status, planned share-rule labels, and guardrails only. It does not read creator-owned revenue share rows, does not show imported source-money history, does not show fake earnings or payable balances, and does not add Withdraw/Cash Out or payout release. A future money-bearing Platform Studio revenue dashboard may only read creator-owned revenue share rows after real source money imports exist and creator-read RLS is scoped. Required future sections are source connection status, imported source-money history, revenue-share ledger rows, fraud/hold/review state, payout readiness, and help/rules.

Remaining Finance/Admin Lane Planning is recorded for transfer, payout release, sponsor checkout/payment/reporting, provider billing APIs, and fraud review. Test-mode payout transfer creation remains blocked until provider account readiness, KYC/tax readiness or safe test equivalent, fraud clear state, admin review, batch draft, immutable audit, idempotency, and explicit transfer-lane approval are all proved; no transfer is created now. Production payout release remains blocked by legal/accounting approval, tax/KYC readiness, fraud review, production Stripe readiness, support/dispute policy, staged rollout, and explicit product approval. Provider billing reconciliation and provider billing API import preflight are now backend-only foundations; actual provider billing API imports must stay server-side, provider-backed, idempotent, and reconciled to trusted usage/provider snapshots before any import rows can matter. No invoice send, customer charge, fake provider bill, overage billing, or payment link is allowed. Sponsor checkout preflight, sponsor brand payment preflight, and sponsor reporting/fraud preflight are now backend-only foundations; actual sponsor checkout, brand payment, reporting import, and fraud integration still require review/disclosure/safety/fraud/audit proof and explicit product approval before any test provider flow. Live brand charge remains blocked. Brand payment through Stripe must keep brand money paid to Chi'llywood first, creator split held until review/hold/provider proof, and no creator payout release. Sponsor reporting/fraud integration must not punish creators automatically or fake sponsor performance. Fraud review queue may add review states later, but no enforcement, risk score, strike, ban, payout pause, monetization disable, upload restriction, or live restriction is allowed from planning rows.

Fraud Runtime Hook Planning/Spec is recorded, with no runtime enforcement implemented. Future enforcement hooks must be built in this order: payout pause gate first because payout execution is not active; monetization disable gate second; sponsor deal restriction third; network billing hold fourth; upload restriction and live restriction last because they affect core creator/live surfaces. Every future hook requires a linked `platform_fraud_holds` row, `fraud_action_records` action, immutable `platform_admin_audit_logs` row, admin reason, review status, reversible state, appeal/review path, and affected-surface bypass proof. Current app behavior remains unchanged: no payouts are paused, no monetization is disabled, no uploads are restricted, no live access is restricted, no sponsor deals are restricted, no invoices are held, no account restrictions are active, and no fake risk score exists.

Immutable Admin Audit Log Foundation is pushed and applied. Migration `supabase/migrations/202605080006_immutable_admin_audit_log_foundation.sql` adds append-only `platform_admin_audit_logs`, generated database types are refreshed, `_lib/platformAudit.ts` reads counts/latest rows only, and `/admin` Audit is read-only/foundation with latest immutable rows plus the existing derived audit summary. Proof rows are foundation-only and record finance, network billing, sponsor checkout, fraud enforcement, and audit-foundation readiness only. Audit rows must not contain secrets, raw passwords, provider tokens, card data, or bank data. No audit row edit/delete/clear UI exists. Dangerous admin actions remain disabled: no payout approval/release, invoice send/charge, sponsor checkout/approval, fraud enforcement, account restriction, upload/live restriction, monetization disable, or admin role write is active. Future dangerous finance/fraud/admin writes must write immutable audit rows.

Creator Payout Read-Only Dashboard Foundation is pushed and applied. Migration `supabase/migrations/202605080007_creator_payout_dashboard_read_policy.sql` is policy-only and adds narrow creator read access to `creator_payout_ledger_entries` where `creator_user_id = auth.uid()::text`; regular creators can read only their own payout ledger rows and cannot insert/update/delete payout rows, anon cannot insert payout rows, and no broad payout access was added. No generated database type change was needed. `_lib/creatorPayouts.ts` is read-only with no writes, no service-role usage, no Stripe/provider calls, and no payout calculations implying payable balance. Platform Studio now has a read-only Payouts tab after Insights and before Brand, plus a Home Payouts card that switches to the Payouts tab. `/channel-settings` compatibility remains and `/channel-studio` remains the owner Platform Studio route. Creator payouts are not active yet: no Withdraw, Cash Out, Connect Stripe, KYC live flow, tax form flow, payout approval, payout release, transfer creation, fake payable balance, fake earnings, or live money movement exists. Runtime visual proof should be rerun later on a current signed-in build; source/helper/DB/typecheck proof passed.

Stripe Connect Schema Foundation (Payout S2) is pushed and applied. Migration `supabase/migrations/202605080008_stripe_connect_schema_foundation.sql` adds only additive schema for future Stripe Connect-style onboarding: payout account provider environment/configuration/capability/readiness fields, onboarding session records without storing long-term onboarding URLs, provider webhook event records with provider/environment/event id uniqueness, creator payout eligibility/readiness records, and immutable admin audit linkage fields. Generated database types are refreshed. Creators may read only their own eligibility/readiness rows and cannot insert/update/delete payout-provider rows; anon has no access; owner/operator access follows platform-role policies. No Stripe SDK, Stripe keys, provider calls, Stripe accounts, onboarding links, transfers, payouts, Connect Stripe UI, Withdraw/Cash Out, KYC/tax live flow, payout approval/release, fake payable balance, fake earnings, or live money movement exists.

Payout S3B - Stripe Connect Edge Function skeletons is pushed. S3B added the source-only skeleton shape for `stripe-connect-account`, `stripe-connect-onboarding-link`, `stripe-connect-account-sync`, and `stripe-connect-webhook`, with matching `supabase/config.toml` entries, internal auth validation where applicable, and no provider behavior.

Payout S3C - Stripe Connect test-mode backend functions is pushed and deployed. The four Stripe Connect functions are ACTIVE version 1 on Supabase project `bmkkhihfbmsnnmcqkoly`; they are backend-only, test-mode guarded, and still add no app UI. Account/link/sync functions validate Supabase bearer auth and creator self-service shape; account creation creates/reuses only server-owned Stripe test-mode Express account references when `STRIPE_SECRET_KEY` is configured; onboarding links require allowlisted return/refresh URLs and do not store long-term URLs; account sync updates provider readiness fields; webhook processing requires `STRIPE_WEBHOOK_SECRET`, verifies signatures, stores idempotent provider event rows, processes only safe `account.updated`, and writes immutable audit rows where provider actions can run. Remote Edge Function secrets now include `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` by name/digest only; secret values are not in repo truth. Test-mode provider proof passed for connected-account creation, account reuse, onboarding-link creation, account sync, and signed webhook/idempotency handling; provider readiness remains non-payable and no payout execution is active. Invalid webhook signatures are rejected. Signed webhook proof passed via Stripe CLI/Dashboard resend on May 8, 2026: a valid test-mode `account.updated` event reached the deployed webhook, stored exactly one provider event row as `ignored` because no matching local creator payout account existed, wrote received/processed immutable audit rows, and duplicate resend did not process a second time. After the webhook signing secret was exposed in chat, the Stripe test webhook endpoint was recreated, the old endpoint was deleted, the Supabase `STRIPE_WEBHOOK_SECRET` digest changed, and rotated-webhook proof passed again. The exposed Stripe test API secret still needs Dashboard rotation. No Stripe SDK/package dependency, Stripe key in repo, provider secret in repo, React Native Stripe integration, Connect Stripe UI, creator-facing setup button, KYC/tax UI, transfer, payout, checkout session, withdrawal, payout approval/release, fake payable balance, fake earnings, live mode, or live money movement exists.

Admin Payout Provider Dashboard is pushed. `_lib/platformFinance.ts` now reads existing payout-provider account, onboarding-session, eligibility-readiness, and webhook-event counts; `/admin` Payouts shows these as read-only provider-status rows. It adds no provider calls, no migrations, no generated type changes, no app UI outside Admin, no Connect Stripe button, no onboarding link creation, no transfers, no payouts, no checkout, no approval/release, no fake payable balances, and no live money movement.

Creator-facing payout setup UI is pushed. Platform Studio Payouts can show setup/readiness states, create/reuse a Stripe Connect test-mode payout account through backend Edge Functions, request/open a short-lived backend-created onboarding link, and refresh provider status through the backend. It stays separate from Premium/RevenueCat, validates creator self-service ownership through the Edge Functions, uses allowlisted return/refresh URLs, hides raw provider ids and raw provider requirement JSON, and keeps payouts inactive. It does not add Withdraw, Cash Out, available balance, payable balance, payout approval/release, transfer creation, payout creation, checkout session, KYC/tax UI, fake earnings, fake payable balances, live mode, or live money movement.

Payout Review Queue Foundation and Payout Batch Draft Workflow Foundation are pushed and applied. Migration `supabase/migrations/202605080009_payout_review_batch_foundation.sql` adds `creator_payout_review_records`, `creator_payout_review_notes`, and `creator_payout_batch_items`, extends `creator_payout_batches` with `batch_status`, `batch_type`, and `total_amount_cents`, refreshes generated database types, and inserts only zero-cent foundation proof rows marked `live_money_action: false`. `/admin` Payouts remains read-only/foundation and may show counts for review records, review notes, batches, batch items, provider transfers, holds, accounts, ledger entries, and audit rows. Payout review is foundation-only, payout batch workflow is draft-only, and no payout can be approved, rejected, released, processed, marked paid, transferred, or treated as payable. No Stripe payout, Connect Stripe button, fake payable balance, fake earnings, provider secret, payment credential, transfer creation, payout execution, KYC/tax live flow, or live money movement exists.

Provider Transfer Records Sync Foundation is pushed and applied. Migration `supabase/migrations/202605080010_provider_transfer_sync_foundation.sql` adds `provider_status` and `last_provider_sync_at` to `creator_payout_provider_transfers`, adds sync indexes, keeps sync statuses test/foundation labeled, refreshes generated database types, and inserts only a zero-cent foundation proof row with no fake provider transfer/payout id plus an immutable audit proof row. The backend-only `stripe-connect-transfer-sync` Edge Function is deployed ACTIVE version 1, requires internal Supabase bearer auth plus admin/operator role, accepts only local transfer record ids, rejects client-supplied provider transfer/payout ids, reads `STRIPE_SECRET_KEY` only from Edge Function secrets, and imports status only for existing local test-mode provider transfer records. `/admin` Payouts remains read-only/foundation and may show Provider Transfer Records, Sync Required, Synced/Test, and Failed Sync counts. Provider transfer sync does not create transfers, payouts, checkout sessions, payable balances, fake paid status, Mark Paid, Retry Transfer, Send Money, Release Payout, Process Batch, Connect Stripe UI, or live money movement. No existing real Stripe test-mode provider transfer id was available, so no provider transfer was synced and no transfer was created for proof.

Payout Transfer Preflight Foundation is pushed and deployed. `stripe-connect-transfer-create` is backend-only/admin-operator/test-mode and returns `preconditions_required`; it exists to prove the dangerous transfer boundary stays closed. It uses internal Supabase bearer auth plus owner/operator checks, rejects client-supplied provider transfer/payout ids, rejects amount/currency/batch/ledger-entry money instructions, reads no Stripe secrets, makes no Stripe/provider calls, writes no provider transfer rows, and writes immutable audit rows only for authorized requested/blocked preflight attempts. It creates no transfers, payouts, checkout sessions, paid state, payable balance, payout release, Connect Stripe UI, or live money movement.

Payout Release Preflight Foundation is pushed and deployed. `payout-release-preflight` is backend-only/admin-operator and returns `preconditions_required`; it exists to prove production payout release stays closed. It rejects client-supplied money amounts, provider references, approval flags, and release flags, reads no provider secrets, makes no provider calls, writes no payout/provider rows, and writes immutable audit rows only for authorized requested/blocked preflight attempts. It creates no payout release, payout approval, transfers, payouts, checkout sessions, paid state, payable balance, or live money movement.

The next finance/admin lanes are:

1. Real source revenue import foundation, still no fake revenue or creator earnings.
2. Creator-facing revenue share dashboard money/data expansion only after real source money exists.
3. Sponsor checkout test-mode only after review/disclosure/fraud/audit proof and explicit product approval, still no live checkout.
4. Test-mode payout transfer creation only after provider/fraud/KYC/tax/audit proof and explicit transfer-lane approval.
5. Production payout release only after legal/accounting/provider approval.

The store/external readiness audit remains a valid launch-readiness lane, but payout work must stay non-production and non-transfer until backed write permissions, dangerous-action confirmation, reason capture, RLS, fraud review, KYC/tax, provider proof, immutable audit proof, and explicit product approval are all designed and proved.

Store / External Launch Readiness Audit remains an audit/spec lane only when selected:

- audit App Store / Google Play readiness without changing runtime code
- treat Android package verification for `com.chillywood.mobile` as complete, and verify iOS bundle identifier truth before any iOS store work
- verify public Terms, Privacy, Community Guidelines, Support, DMCA/Copyright, and account-deletion surfaces/URLs
- verify Data Safety and privacy disclosure inputs from existing product behavior
- verify RevenueCat/Google Play external setup and real Android purchase/restore proof now that repo-side production-readiness readouts are pushed
- verify AppLovin readiness gaps without installing SDKs or adding ad IDs
- do not install SDKs, add real IDs, change package/native config, touch store dashboards from code, apply migrations, or touch Supabase remote state unless a later exact prompt scopes it

AppLovin MAX SDK integration is intentionally paused until external store/AppLovin account/app/ad-unit setup is ready.

Product direction:

- Public V1 Hardening H1A 18+ signup confirmation is pushed.
- Public V1 Hardening H1B2 legal acceptance storage is pushed. New signups with authenticated sessions write age/terms/privacy acceptance timestamps plus versions to `user_account_legal_acceptances`.
- Public V1 Hardening H2 upload/content lifecycle polish is pushed.
- H2 keeps the existing creator-video picker/upload/storage/metadata path, adds honest local upload lifecycle states, clarifies backed draft/published and media-ready/unavailable status, and does not add fake processing/transcoding/archive/retry/storage-billing states.
- H2 duplicate-upload UI correction is pushed. Platform Studio Content has one clear `Video Upload` form, no duplicate upload boxes/buttons, and `Upload Status` is inline inside that form.
- Public V1 Hardening H3 security/compliance/moderation hardening is pushed. Settings has a direct Support entry, Support includes sponsorship/ad/scam concern copy, the Report Sheet explains existing backed categories, and no new report schema/action system was added.
- Ads V1A/V1B/V1C are pushed as provider-neutral, no-SDK, no-real-rendering infrastructure.
- Ads V1C added only placeholder interstitial decision/controller foundation. It did not install SDKs, add real IDs, initialize AppLovin/Unity/AdMob, render real ads, add CTV inventory, show fake ad revenue, or change forbidden surfaces.
- Ads Config V1D1 app-config `adsLaunch` resolver foundation is pushed. `_lib/appConfig.ts` now normalizes optional `app_configurations.config.adsLaunch` through code-owned `ADS_LAUNCH_CONFIG_DEFAULTS`, and `app/admin.tsx` reads `experienceConfig.adsLaunch` for read-only Admin Ads status copy.
- Ads V1D2 runtime-read foundation is pushed. `hooks/useAdsLaunchConfig.ts` reads cached/default app config first, then safely reads `readAppConfig()` and returns normalized `adsLaunch`; `NativeAdSlot` and `InterstitialController` use that source when no explicit proof/test config override is passed.
- The separate public Admin sign-in entry is removed. `app/(auth)/login.tsx` now presents normal Chi'llywood auth only, keeps no Admin Command Center link/copy, stores no credentials, hard-codes no account, adds no local grant or bypass, and `/admin` remains protected by signed-in plus backed platform-role checks after normal sign-in.
- Public V1 current-build release proof is complete. The attached Android current build opened Home, Explore, Settings, Support, Terms, Privacy, Community Guidelines, Platform Studio, `/channel-settings` compatibility, Rachi Profile, Rachi public Channel, Player `t1`, Watch-Party waiting room, and `/admin`; Home stayed free of visible ad placeholders while `ads_enabled=false`; no runtime fixes were needed.
- Candidate integration is pushed at `90ff081ff38b36165d2eb18023c018af821dc747`. RevenueCat Premium offering resolution prefers the configured `premium` offering and falls back only for `premium_subscription` to a RevenueCat current/default/first offering with purchasable packages; Premium access still requires RevenueCat/backend entitlement truth and no fake product/price/local unlock was added. Explore search is canonical as a client-side filter over loaded `titles` rows with honest empty states and no fake search data. The Chat tab candidate is deferred, not canonical, because `app/(tabs)/chat.tsx` would duplicate normalized `/chat` ownership with `app/chat/index.tsx`; `/chat` and `/chat/[threadId]` remain canonical Chi'lly Chat.
- Normal runtime must remain honest: ads stay disabled by default because `ads_enabled=false`.
- Runtime ad owners now read normalized `app_config.adsLaunch` with default-disabled fallback; normal runtime must remain hidden because normalized `ads_enabled=false` and the placeholder provider is not connected.
- Admin V1B1 runtime controls config foundation is pushed. Typed defaults live under `app_configurations.config.runtimeControls`; Admin Kill Switches shows read-only `Configured foundation` and `Not enforced yet`; no working toggles or runtime enforcement were added.
- Admin V1B2A new-account enforcement is pushed. Signup reads `runtimeControls.new_accounts_enabled` after email/password and 18+ confirmation pass, blocks before `supabase.auth.signUp` when false, preserves default true behavior, and updates Admin New Accounts copy as read-only `Enforced on signup`.
- Admin V1B2B upload enforcement is pushed. Platform Studio compatibility route `app/channel-settings.tsx` reads normalized `runtimeControls.uploads_enabled` from existing app config and blocks only new creator-video upload submit before storage/upload work when false. Default true and config-read fallback true preserve normal uploads. Existing video metadata edit, publish/unpublish/delete, Open Player, picker behavior, storage helpers, RLS, migrations, generated types, and the single `Video Upload` form remain unchanged. Admin Uploads copy is read-only `Enforced on upload`; no working Admin toggle was added.
- Admin V1B2C comments enforcement is pushed. Profile post comment/reply submit and creator-video comment/reply submit read normalized `runtimeControls.comments_enabled` after existing validation and before backed comment create or comment attachment upload. Default true preserves normal comments. Existing comment reads, deletes, reports, attachment picker selection, Watch-Party comments, Live Stage comments, Chi'lly Chat messages, Chi'lly Circle, profile privacy, Player controls/layout, RLS, migrations, generated types, and Supabase remote state are unchanged. Admin Comments copy is read-only `Enforced on comments`; no working Admin toggle was added.
- Admin V1B2D profile posting enforcement is pushed. Profile post creation submit reads normalized `runtimeControls.profile_posting_enabled` after owner/busy, empty-body, and length checks and before backed Profile post create or post attachment upload. Default true preserves normal Profile posting. Existing Profile post reads, comments/replies, likes, deletes, reports, attachment picker selection, Chi'lly Circle, profile privacy, creator video upload, Platform Studio, Public Channel, Player, Watch-Party, Live Stage, Chat, RLS, migrations, generated types, and Supabase remote state are unchanged. Admin Profile Posting copy is read-only `Enforced on profile posts`; no working Admin toggle was added.
- Admin V1B2E creator posting enforcement is pushed. Platform Studio compatibility route `app/channel-settings.tsx` reads normalized `runtimeControls.creator_posting_enabled` from existing app config and blocks only new creator event creation before `createCreatorEvent` when false. Default true preserves normal creator event creation. Existing creator event edits through `updateCreatorEvent`, creator-video upload, video metadata edit, publish/unpublish/delete, Profile posts, comments/replies, attachments, Platform Studio layout, Public Channel, Player, Watch-Party, Live Stage, Chat, RLS, migrations, generated types, and Supabase remote state are unchanged. Admin Creator Posting copy is read-only `Enforced on creator events`; no working Admin toggle was added.
- Admin V1B2F social attachment enforcement is pushed. `app/profile/[userId].tsx` and `app/player/[id].tsx` read normalized `runtimeControls.attachments_enabled` and block only selected non-chat social attachments before parent post/comment create and before attachment upload. Profile post attachments, Profile post comment/reply attachments, and creator-video comment/reply attachments are enforced. Text-only Profile posts/comments and creator-video comments remain governed by their existing posting/comment controls. Chat attachments, room attachments, `_lib/socialAttachments.ts`, `_lib/profilePosts.ts`, `_lib/creatorVideoComments.ts`, storage helpers, RLS, migrations, generated types, and Supabase remote state are unchanged. Admin Attachments copy is read-only `Enforced on social attachments`; no working Admin toggle was added.
- Admin V1B2G standalone Chi'lly Chat attachment enforcement is pushed. `app/chat/[threadId].tsx` reads normalized `runtimeControls.chat_attachments_enabled` and blocks only selected attachments before optimistic message insertion and before `sendChatMessage` when false. Text-only Chi'lly Chat messages remain allowed. Existing signed-in/thread gates, reads, realtime updates, mark-read behavior, attachment picker selection, room attachments, `_lib/chat.ts`, `_lib/socialAttachments.ts`, storage helpers, RLS, migrations, generated types, and Supabase remote state are unchanged. Admin Chat Attachments copy is read-only `Enforced on chat attachments`; no working Admin toggle was added.
- Admin V1B2H room attachment enforcement is pushed. `app/watch-party/[partyId].tsx` and `app/watch-party/live-stage/[partyId].tsx` read normalized `runtimeControls.chat_attachments_enabled` and block only selected room attachments before `sendPartyMessageRecord` and before `createSocialAttachmentForSurface` when false. Text-only room comments remain allowed. Existing room layouts, LiveKit behavior, Premium gates, message reads, room routes, attachment picker selection, `_lib/watchParty.ts`, `_lib/socialAttachments.ts`, storage helpers, RLS, migrations, generated types, and Supabase remote state are unchanged. Admin Chat Attachments copy is read-only `Enforced on chat and room attachments`; no working Admin toggle was added.
- Admin V1B2I-A standalone Chi'lly Chat enforcement is pushed. `app/chat/[threadId].tsx` reads normalized `runtimeControls.chat_enabled` and blocks only standalone message send before optimistic insertion and `sendChatMessage`, and standalone thread call start before `startChatThreadCall`, when false. `app/chat/index.tsx` blocks only the Rachi official starter thread before `getOrCreateDirectThread`, and `app/profile/[userId].tsx` blocks only non-self Profile-to-chat entry before direct thread creation. `/chat` and `/chat/[threadId]` remain readable. Existing inbox reads, thread reads, realtime refresh, mark-read, reports, profile opens, signed-in/thread gates, attachment picker selection, `chat_attachments_enabled`, room comments, room invites, `_lib/chat.ts`, `_lib/watchParty.ts`, storage helpers, RLS, migrations, generated types, and Supabase remote state are unchanged. Admin Chat copy is read-only `Enforced on standalone chat`; no working Admin toggle was added.
- Admin V1B2I-B chat invite enforcement is pushed. `components/chat/internal-invite-sheet.tsx` reads normalized `runtimeControls.chat_enabled` and blocks only room invite direct-message sends before `sendDirectInviteMessage` when false. System share fallback remains available. Existing room text comments, room layouts, LiveKit behavior, Premium gates, invite sheet search, `chat_attachments_enabled`, `_lib/chat.ts`, `_lib/watchParty.ts`, storage helpers, RLS, migrations, generated types, and Supabase remote state are unchanged. Admin Chat copy is read-only `Enforced on chat and invites`; no working Admin toggle was added.
- Admin V1B2I-C room comment enforcement is pushed. `app/watch-party/[partyId].tsx` and `app/watch-party/live-stage/[partyId].tsx` read normalized `runtimeControls.chat_enabled` and block only room-native text/comment submits before `sendPartyMessageRecord` when false. Existing room message reads, room layouts, LiveKit behavior, Premium gates, invite behavior/system share, `chat_attachments_enabled`, attachment picker behavior, `_lib/watchParty.ts`, `_lib/chat.ts`, `_lib/socialAttachments.ts`, storage helpers, RLS, migrations, generated types, and Supabase remote state are unchanged. Admin Chat copy is read-only `Enforced on chat, invites, and room comments`; no working Admin toggle was added.
- Admin V1B2J remaining runtime-control enforcement is pushed. `live_first_enabled`, `live_watch_party_enabled`, and `watch_party_live_enabled` are enforced through the central Premium live/watch-party helper layer on scoped Live First, Live Watch-Party hybrid, and Watch-Party Live entry points; disabled controls show operations-paused copy and do not weaken Premium gates. `max_upload_size_mb` is enforced before creator-video storage upload with default 5120 MB preserving the existing 5 GB limit. Admin Kill Switches remains read-only with no working toggle/config write. Admin `runtimeControls.ads_enabled` is still not the Ads Launch source of truth, and `premium_required_for_live` / `premium_required_for_watch_party` remain Premium helper truth rather than runtime switches.
- Admin Usage Metering Foundations 37-39 and Admin Usage Metering Foundation V1 remote/provider schema are pushed. `_lib/platformUsage.ts` reads existing backed Admin Usage data and safe schema counts, migrations `supabase/migrations/202605070003_platform_usage_metering_foundation.sql` and `supabase/migrations/202605070004_admin_usage_metering_v1.sql` have been applied to the linked Supabase remote, and `supabase/database.types.ts` now includes `platform_usage_metering_events`, `platform_usage_daily_rollups`, `usage_meter_events`, `usage_daily_summaries`, `usage_monthly_summaries`, `provider_accounts`, `provider_usage_imports`, `provider_usage_daily`, `provider_billing_snapshots`, and `provider_usage_reconciliation`.
- Admin Usage Writer V1A0/V1A1/V1A2 is pushed and runtime-proved for successful creator-video upload usage only. V1A0 adds secure idempotent RPC `record_creator_video_upload_usage(target_video_id text)`. V1A1 adds `_lib/platformUsage.ts` helper `recordCreatorVideoUploadUsage` and calls it from `_lib/creatorVideos.ts` only after the `videos` row insert succeeds. Runtime proof showed one `video_uploaded` usage row, one `storage_bytes` metadata row, and replay skipping without double-counting. V1A2 adds remote-applied RPC `rollup_creator_video_upload_usage_daily(target_usage_date date default null)` and helper `rollupCreatorVideoUploadUsageDaily` to upsert already-backed upload/storage rows into `usage_daily_summaries`; V1A2 proof rolled up May 7, 2026 rows and replayed idempotently. Metering failure is non-fatal to upload success.
- Admin Usage Provider Imports V1D3/V1D4 are pushed. V1D3 added server-side Edge Function `provider-usage-import`, migration `supabase/migrations/202605070012_provider_usage_import_idempotency.sql`, provider import readouts in `_lib/platformUsage.ts`, and read-only Admin Usage provider rows in `app/admin.tsx`. V1D4 added Hetzner Object Storage S3 inventory import through the same server-side function. Cloudflare R2, Hetzner Object Storage, and Hetzner Servers can import provider facts only when server-side secrets are configured and an owner/operator invokes the function. Hetzner Object Storage values are metadata estimates only. OVH Object Storage and OVH Servers remain `Not connected yet` until later exact API lanes. No provider secrets are stored in repo or mobile app.
- Admin Usage remains read-only. Live/watch-party rooms and uploads are DB estimates; storage is a metadata estimate from existing video/attachment rows; participant-minutes are a DB estimate from room membership rows; internal usage/provider import/reconciliation rows are schema foundation only; bandwidth remains `Not connected yet` unless future real metering events exist. Do not treat these values as billing, payout, revenue, cost, LiveKit invoice, provider bill, overage, customer invoice, or provider-log truth.
- Ledger Systems 4A-4D finance foundation is pushed and its migration `supabase/migrations/202605070005_platform_finance_ledger_foundation.sql` has now been applied remotely to unblock ordered usage V1A2 migration inclusion. Generated database types include the finance foundation tables. Admin finance remains read-only/foundation only: no live money totals, payout buttons, invoice actions, sponsor checkout, payout split execution, fraud enforcement, provider integrations, or fake revenue exists.
- Sponsor Checkout Foundation is pushed. Admin Sponsors may show foundation counts for sponsor brands, sponsor deals, creatives, placements, disclosures, review logs, payment records, and payout split records only. It must not show live money totals, checkout buttons, payment links, sponsor approval actions, fake sponsor revenue, payable balances, or payout split execution.
- Immutable Admin Audit Log Foundation is pushed. Admin Audit may show read-only/latest immutable audit rows from `platform_admin_audit_logs` and the separate derived role/safety summary. Audit rows are append-only and must not contain secrets, raw passwords, provider tokens, card data, or bank data. No audit row edit/delete/clear UI exists.
- Admin Revenue, Payouts, Networks, Sponsors, and Fraud remain read-only/foundation. They may show row counts if future tables are readable, but they must not show live money totals, payout buttons, invoice actions, sponsor checkout, payout split execution, fraud enforcement, provider keys, payment credentials, or fake revenue.

Required proof before the next Admin V1B2 runtime-control enforcement:

- choose exactly one target control and prove the affected app surface already has a safe read path or can receive one without broad rewrites
- block only the exact submit/action path when false; do not hide or duplicate existing surfaces unless explicitly scoped
- preserve existing signed-in, beta, platform-role, RLS, Premium, privacy, and route gates
- preserve existing privacy, comment, attachment, upload, channel, profile, and room behavior unless the prompt explicitly scopes that surface
- do not expose working Admin write controls unless the prompt also scopes backed config save permissions and manual proof
- do not change migrations, generated database types, RLS, Supabase remote state, storage, LiveKit config, ads SDKs, RevenueCat setup, or forbidden surfaces
- if a runtime control cannot be read safely, leave it as `Configured foundation` / `Not enforced yet`

## Current Product Lane Order
1. Store/external launch readiness audit:
   - audit App Store / Google Play readiness without changing runtime code
   - use the completed Android package verification for `com.chillywood.mobile` as baseline, and verify iOS bundle identifier truth before iOS store work
   - verify public legal URLs, support/account-deletion links, Data Safety and privacy disclosure inputs, RevenueCat public setup, and AppLovin readiness gaps
   - do not install SDKs, add real ad IDs, change package/native config, or touch store dashboards from code unless a later exact prompt scopes it
2. Real AppLovin MAX readiness/integration planning:
   - later only after external AppLovin/store setup is ready
   - keep provider wrapper architecture
   - Unity LevelPlay / Unity Ads later through AppLovin MAX if needed
   - no AdMob-only path
3. Admin Usage later writers/importers:
   - usage remote migration/type refresh is complete for `202605070003_platform_usage_metering_foundation.sql` and `202605070004_admin_usage_metering_v1.sql`
   - creator-video upload usage writer V1A0/V1A1/V1A2 is pushed for `video_uploaded` and `storage_bytes` metadata only after successful backed video row creation, plus V1A2 daily rollup foundation into `usage_daily_summaries`
   - V1A2 runtime proof passed for daily rollup from already-backed creator-video usage/storage rows into `usage_daily_summaries`
   - Provider Imports V1D3/V1D4 are pushed for server-side Cloudflare R2, Hetzner Object Storage, and Hetzner Servers import foundation only
   - Hetzner Object Storage imports S3 inventory metadata estimates only; OVH Object Storage and OVH Servers remain not connected until separate exact provider API lanes
   - add bandwidth, participant-minute, provider-billing, reconciliation, customer billing, overage, payout, or revenue writers/jobs only in separate scoped lanes
   - prove writer/import behavior before relying on metering rows or provider rows as real usage/provider truth
   - keep Admin Usage read-only and honest: DB estimate, Metadata estimate, Schema connected, Not connected yet, or Not active yet
4. Finance proof and later writers:
   - finance foundation migration `supabase/migrations/202605070005_platform_finance_ledger_foundation.sql` is now applied remotely and generated database types include the finance tables
   - Finance F2B creator payout provider schema migration `supabase/migrations/202605080001_creator_payout_provider_foundation.sql` is applied remotely and generated database types include payout-provider foundation tables
   - Finance F2C schema/RLS proof is complete for migration/type alignment and anonymous insert denial
   - Finance F2D Admin payout-provider readout is pushed as read-only foundation counts only
   - Finance F2E proof rows are pushed as non-live `not_active` foundation data only
   - Network Billing Foundation migration `supabase/migrations/202605080003_network_billing_foundation.sql` is applied remotely and generated database types include network plans, assignments, quotas, invoice line items, overage events, and billing audit logs
   - Sponsor Checkout Foundation migration `supabase/migrations/202605080004_sponsor_checkout_foundation.sql` is applied remotely and generated database types include sponsor brands, deals, creatives, placements, disclosures, review logs, payment records, and payout split records
   - Fraud Enforcement Foundation migration `supabase/migrations/202605080005_fraud_enforcement_foundation.sql` is applied remotely and generated database types include fraud holds, fraud reasons, evidence records, planned actions, review notes, appeal placeholders, and fraud audit logs
   - Immutable Admin Audit Log Foundation migration `supabase/migrations/202605080006_immutable_admin_audit_log_foundation.sql` is applied remotely and generated database types include `platform_admin_audit_logs`
   - Creator Payout Read-Only Dashboard Foundation migration `supabase/migrations/202605080007_creator_payout_dashboard_read_policy.sql` is applied remotely as a policy-only migration; no generated database type change was needed
   - Stripe Connect Schema Foundation migration `supabase/migrations/202605080008_stripe_connect_schema_foundation.sql` is applied remotely and generated database types include future payout account provider/config/capability/readiness fields, onboarding session records, provider webhook event records, creator payout eligibility/readiness records, and immutable audit linkage fields
   - Stripe Connect Test-Mode Backend Functions (Payout S3C) is pushed in `supabase/functions/stripe-connect-account`, `supabase/functions/stripe-connect-onboarding-link`, `supabase/functions/stripe-connect-account-sync`, `supabase/functions/stripe-connect-webhook`, and shared backend-only helper `supabase/functions/_shared/stripe-connect.ts`; the four functions are deployed ACTIVE version 1, use internal auth where applicable, are test-mode guarded, write immutable audit rows where provider actions can run, verify webhooks only when `STRIPE_WEBHOOK_SECRET` is configured, and add no Stripe SDK/package dependency, React Native Stripe integration, Connect Stripe UI, transfers, payouts, checkout sessions, or live money actions. Account creation/reuse, onboarding-link creation, account sync, and signed webhook/idempotency proof have test-mode provider proof; duplicate test events are ignored idempotently.
   - Payout Review Queue + Batch Draft Workflow Foundation migration `supabase/migrations/202605080009_payout_review_batch_foundation.sql` is applied remotely and generated database types include payout review records, review notes, batch items, and the new batch draft/foundation fields
   - Provider Transfer Records Sync Foundation migration `supabase/migrations/202605080010_provider_transfer_sync_foundation.sql` is applied remotely, generated database types include provider transfer sync fields, and `stripe-connect-transfer-sync` is deployed ACTIVE version 1 as a backend-only/admin-operator/test-mode status import function
   - Payout Transfer Preflight Foundation is pushed in `supabase/functions/stripe-connect-transfer-create/index.ts` and `supabase/config.toml`; the function is deployed ACTIVE version 1, returns `preconditions_required`, reads no Stripe secrets, makes no Stripe calls, writes no provider transfer rows, and creates no transfers/payouts/checkout sessions
   - Payout Release Preflight Foundation is pushed in `supabase/functions/payout-release-preflight/index.ts` and `supabase/config.toml`; the function is deployed ACTIVE version 1, returns `preconditions_required`, reads no provider secrets, makes no provider calls, writes no payout/provider rows, and creates no payout release/approval/transfers/payouts/checkout sessions
   - Network Invoice Draft Workflow + Overage Warning/Readout Foundation migration `supabase/migrations/202605080011_network_invoice_overage_foundation.sql` is applied remotely; Admin Networks shows read-only invoice draft, draft line item, warning-only overage, and review-required overage counts; generated database types had no checked-in diff because no table shapes changed
   - Sponsor Review Queue / Disclosure / Moderation / Payment Test-Mode Foundation migration `supabase/migrations/202605080012_sponsor_monetization_foundation.sql` is applied remotely and generated database types include sponsor review queue records, sponsor safety review records, sponsor disclosure `required_before_live`, and sponsor payment planning fields
   - Finance F2 payout-provider, Network Billing, Sponsor Checkout, Sponsor Review/Disclosure/Payment Foundation, Fraud Enforcement, Immutable Admin Audit Log, Creator Payout Read-Only Dashboard, Stripe Connect Schema Foundation, S3B skeleton, S3C test-mode functions, Payout Review/Batch Draft, and Provider Transfer Sync closeouts are complete for foundation-only schema/readout/proof rows, creator read-only dashboard proof, additive provider-readiness schema, source skeletons, deployed backend-only test-mode functions, Admin read-only/draft payout review counts, or backend-only provider transfer status sync; moderator visibility proof still requires a later safe lane before operational reliance
   - Creator-facing Connect Stripe Setup Button Planning/Spec is recorded; future implementation needs a separate exact implementation prompt and must stay no Withdraw/Cash Out/payout release/transfer/payout/checkout/fake balance/live money
   - Fraud Runtime Hook Planning/Spec is recorded; no runtime enforcement hook is connected, and future hooks require fraud hold/action records, immutable audit, admin reason, reversible state, appeal/review path, and affected-surface proof
   - Real Source Revenue Import Foundation is pushed/deployed; no provider import, fake revenue, creator earnings, payable balance, or payout ledger write from revenue-share rows exists
   - Creator Revenue Dashboard Foundation is pushed; no fake earnings/payable balances may be shown
   - Remaining Finance/Admin Lane Planning is recorded for actual test-mode transfer creation, production payout release, provider billing APIs, sponsor checkout test-mode, brand payment through Stripe, real sponsor reporting/fraud integration after the preflight gate, and Fraud review queue
   - Provider Billing Reconciliation Foundation is pushed/deployed as backend-only owner/operator reconciliation over existing provider usage/internal summary rows, with no provider bill import or billing execution
   - Fraud Review Queue Foundation is pushed/applied as review-state/readout only, with no runtime enforcement, fake risk score, strike, ban, payout pause, monetization disable, upload restriction, live restriction, sponsor restriction, or account restriction
   - Creator-facing payout setup UI is pushed as backend-only/test-mode setup/onboarding/status refresh in Platform Studio, with no payout execution, fake balance, KYC/tax UI, checkout, transfer, payout, or live money movement
   - next recommended finance/admin implementation lane is still gated; transfer creation, production payout release, live sponsor checkout, brand charging, fake revenue, fake creator earnings, real provider source imports, money-bearing creator revenue dashboards, and fraud enforcement remain blocked
   - add provider billing APIs, Stripe Connect live integration, payout provider production writes, live payout actions, live network billing actions, sponsor checkout, or live fraud enforcement only in separate scoped lanes
   - keep Admin finance panels read-only and honest: counts only, Not connected yet, Foundation only, or Not active yet

## Current Pushed Truth To Preserve
- Premium gate is pushed.
- Free vs Premium full live/watch-party gate is pushed: all full Live First, Live Watch-Party, and Watch-Party Live access is Premium.
- Free users are blocked before full room/session/token/connect and receive no full LiveKit room/token/connect access.
- No free live/watch-party preview mode was added.
- Chi'lly Circle V1 is pushed.
- Chi'lly Circle profile privacy is pushed.
- Platform Studio Phase 1 rename/organization is pushed.
- Platform Studio Phase 2A shell is pushed at `/channel-studio`.
- `/channel-settings` compatibility is preserved.
- Platform Studio Home dashboard correction is pushed.
- Public Channel Phase 2B is pushed at `/channel/[userId]`.
- Public Channel visual polish and streaming-network correction are pushed.
- Role Visibility Compatibility fix is pushed: Profile backed identity now wins over route display params after load, and direct creator-video Player lookup now enforces public-or-owner visibility before returning non-public videos. No migrations, generated database types, RLS/storage policies, Supabase remote state, package/native config, LiveKit config, forbidden app surfaces, Admin bypass, money actions, fake balances, or Premium gate changes were added. Runtime visual proof for the full role matrix should still be run on a current build.
- Chi'lly Chat Role/Privacy Compatibility fix is pushed at `7fd4565`: chat helpers require current-user membership before returning normal thread state, message/attachment reads, sends, or stale-call cleanup. No chat UI redesign, call redesign, migrations, generated database type edits, Supabase remote/RLS/storage changes, LiveKit config changes, package changes, Stripe/payment/provider changes, admin bypass, fake data, or new messaging features were added. Direct-chat block policy after channel audience blocks remains a follow-up product decision.
- Live Stage role/access hardening is pushed at `4b90e09`: Live Stage mode switching is host-only, route-param hybrid/live-watch attempts cannot let non-hosts mutate mode, Live First viewers cannot self-promote into camera/speaker seats on stage entry, and `setPartyParticipantState` now requires the authenticated room host. Watch-Party Live route separation remains intact and title/player-based. Full live/watch-party Premium gates remain intact. No 10-participant proof has been claimed yet. The later visual modernization at `15b07953fe6b4343a061ce3c3fb2f463afc48db2` polished Live Stage UI hierarchy/control surfaces only and did not change LiveKit server config, Player controls, Watch-Party route ownership, Live Stage route ownership, migrations, generated database types, RLS/storage, Public Channel, Profile, Platform Studio, Chi'lly Circle, Admin, finance, ads, Stripe, RevenueCat, or Google Play setup.
- Modern UI/UX route-local visual modernization is pushed at `15b07953fe6b4343a061ce3c3fb2f463afc48db2`: Home, Explore, Player, Watch-Party Party Room, Live Stage, Public Channel, Profile, Platform Studio/settings, Spectator, Chi'lly Chat, and Admin now use the current premium dark streaming-platform visual treatment while preserving product behavior and Chi'llywood background identity. No route ownership, backend behavior, data source, Supabase remote state, migration, generated database type, RLS/storage policy, LiveKit server config, Stripe/RevenueCat/Google Play/AppLovin setup, finance/live-money behavior, Premium gate, role boundary, package/native config, global theme/background asset, or fake data/money/count/message/participant/recommendation was added. Android device proof on `R5CR120QCBF` passed for the touched routes through the current Metro dev-client bundle; release/internal-build visual smoke remains a later proof option.
- Watch-Party Party Room polish is pushed at `e0116d3`: Party Room remains `/watch-party/[partyId]`, title/player-based, and separate from Live Stage. The Party Room now uses clearer Party Screen/Party Feeds language, a compact Player/Invite/Report/Leave action dock, and confirmed Leave behavior that calls `leavePartyRoomSession` before returning. Camera/mic publishing remains in the shared Player path. No 10-participant proof is claimed, and no Live Stage, LiveKit server config, Player controls, Public Channel, Profile, Platform Studio, Admin, finance, ads, Stripe, RevenueCat, Google Play, migrations, generated database types, RLS/storage, package/native config, or Supabase remote state changed.
- Watch-Party Live local participant pinning is pushed at `c4b16ca`: Party Room owns the feature in `app/watch-party/[partyId].tsx` with `_lib/watchPartyPinning.ts` as local AsyncStorage helper. It is viewer-local, route-scoped, and only reorders/emphasizes the existing Party Feeds participant tile area after long-press plus inline Pin/Unpin. It clears safely when the pinned participant leaves, shows a one-time non-blocking coachmark, and does not affect Live Stage, Chi'lly Chat calls, Player controls, LiveKit token/join logic, host/viewer authority, playback sync, comments/composer/attachments, waiting-room flow, party codes, Premium gates, or any other user's room view.
- Discovery Feed Foundation D2 is pushed at `c2c6b04`: `discovery_feed_items` and `discovery_feed_item_blocks` are remote-applied, generated database types are refreshed, and `_lib/discoveryFeed.ts` is read-only helper foundation. Regular app users are not broadly granted feed reads in D2; admin/operator policies own the tables. `is_spectator_playback_enabled` defaults false and is constrained false. No Home/Profile/Channel feed UI, spectator playback, HLS/Egress, full-room spectator token, fake feed item, fake trending, fake viewer count, fake spectator count, public proof row, protected-content leakage, app UI change, LiveKit change, or forbidden surface was added.
- Discovery Feed + Spectator System D3-D10 is pushed at `e68251a`: D3 Home Feed V1, D4 Public Channel discovery shelves, D6 metadata-only Spectator view, D8 spectator eligibility helper, D9 production notification/activity Android proof, and D10 deterministic ranking helpers are implemented/proved where scoped; D5 Profile live/activity is source-proofed and already privacy-gated/backed; D7 live spectator broadcast playback remains blocked except for D7F-approved public-safe HLS records through the controlled resolver/proxy. The added feed policy is narrow authenticated SELECT only for public, clean, rights-safe, non-blocked discovery rows and adds no broad user writes. No protected/title/private/ticketed content is exposed as public spectator video, no spectator receives a full LiveKit room token, no raw playback URL/HLS/Egress path is exposed to normal app users, no fake feed item/trending/viewer count/spectator count is added, Premium gates remain intact, free public content may be ad-supported later, Premium remains ad-free, and CTV ads remain future-only for public/free TV-style inventory.
- D7B Live Spectator Broadcast Schema Foundation is pushed at `1e54530`, the D7D test transition is pushed at `b87dde3`, and D7D private start/stop proof passed on May 11, 2026: `room_broadcast_sessions` is admin/operator-owned. D7D real Egress ids/private HLS playlist locations are allowed only for private `D7D_TEST_` rows with `metadata.d7d_test_proof = true`. D7C functions are deployed with D7D test start/stop support, `D7D_TEST_EGRESS_ENABLED` and LiveKit/S3/Hetzner secret names are present by name only, method/no-auth proof passed, and the real admin/operator D7D start/stop proof passed with cleanup. D7E Hetzner Object Storage HLS delivery proof passed with public playlist/segment fetch outside LiveKit. D7F runtime proof passed for the controlled public-safe HLS spectator gate; no spectator receives a full LiveKit participant token, private/proof/protected/title/ticketed/Premium-full-room states remain blocked, and raw HLS URLs remain hidden from normal app users.
- Profile View Channel routes to `/channel/[userId]`.
- Platform Studio Preview Channel routes to `/channel/[ownUserId]`.
- Admin Command Center V1A is pushed at `/admin`.
- `/admin` is the canonical platform owner/operator route; do not create duplicate admin routes like `/admin-command-center`.
- Admin is separate from Platform Studio, Profile Settings, Public Channel, Chi'lly Circle, and future Room Control.
- Admin remains protected by existing signed-in plus beta/platform-role/backend permission checks. Route access, report visibility, and privileged-write boundaries were preserved, and no production admin bypass was added.
- Admin V1A sections are Home, Reports, Content, Roles, Audit, Rachi, Users, Premium, Kill Switches, Usage, Ads, Revenue, Payouts, Networks, Sponsors, Fraud, and System.
- Admin V1A keeps Reports, Content, Roles, Audit, and Rachi backed behavior working. Users, Premium, Kill Switches, Ads, Revenue, Payouts, Networks, Sponsors, Fraud, and parts of Usage/System are foundation or read-only where not backed.
- Admin V1A must not be expanded with fake money, fake usage, fake payouts, fake sponsor revenue, fake network invoices, fake fraud holds, fake live ad provider state, fake kill switches, or hard-coded credentials.
- Admin V1B1 runtime controls config foundation is pushed. `_lib/featureFlags.ts` owns typed defaults and normalization, `_lib/appConfig.ts` persists normalized `runtimeControls` under existing `app_configurations.config`, and `app/admin.tsx` updates Kill Switches copy only as read-only `Configured foundation` / `Not enforced yet`. No runtime behavior, Premium gate, migration, generated type, RLS, storage, Supabase remote, or Admin permission boundary changed.
- Admin V1B2A new-account enforcement is pushed. `app/(auth)/signup.tsx` blocks before `supabase.auth.signUp` if `runtimeControls.new_accounts_enabled` is false, after existing email/password and 18+ checks. Admin marks New Accounts as read-only `Enforced on signup`; no working toggle, login change, legal acceptance storage change, migration, generated type edit, RLS/storage change, Supabase remote change, or Premium gate change was added.
- Admin V1B2B upload enforcement is pushed. `app/channel-settings.tsx` blocks only new creator-video upload submit when `runtimeControls.uploads_enabled` is false, after existing missing-title, missing-file, and file-size checks and before `uploadCreatorVideo` or storage work. Existing video metadata edits, publish/unpublish/delete, Open Player, picker behavior, storage helpers, RLS, migrations, generated types, and one clear Platform Studio `Video Upload` form are unchanged. Admin marks Uploads as read-only `Enforced on upload`; no working toggle was added.
- Admin V1B2C comments enforcement is pushed. `app/profile/[userId].tsx` blocks only Profile post comment/reply submit when `runtimeControls.comments_enabled` is false, after signed-in, engageable-post, empty-body, and length checks and before `createProfilePostComment` or comment attachment upload. `app/player/[id].tsx` blocks only creator-video comment/reply submit when the same control is false, after creator-video route/source, signed-in, empty-body, and length checks and before `createCreatorVideoComment` or comment attachment upload. Admin marks Comments as read-only `Enforced on comments`; no working toggle was added.
- Admin V1B2D profile posting enforcement is pushed. `app/profile/[userId].tsx` blocks only Profile post creation submit when `runtimeControls.profile_posting_enabled` is false, after owner/busy, empty-body, and length checks and before `createProfilePost` or post attachment upload. Admin marks Profile Posting as read-only `Enforced on profile posts`; no working toggle was added.
- Admin V1B2E creator posting enforcement is pushed. `app/channel-settings.tsx` blocks only new creator event creation when `runtimeControls.creator_posting_enabled` is false, before `createCreatorEvent`. Existing `updateCreatorEvent` edits, creator-video upload, metadata edit, publish/unpublish/delete, Profile posts, comments/replies, attachments, Platform Studio layout, Public Channel, Player, Watch-Party, Live Stage, Chat, RLS, migrations, generated types, and Supabase remote state are unchanged. Admin marks Creator Posting as read-only `Enforced on creator events`; no working toggle was added.
- Admin V1B2F social attachment enforcement is pushed. `app/profile/[userId].tsx` and `app/player/[id].tsx` block selected non-chat social attachments when `runtimeControls.attachments_enabled` is false, before parent post/comment create and before attachment upload. Text-only posts/comments remain allowed where existing controls allow them. Admin marks Attachments as read-only `Enforced on social attachments`; no working toggle was added.
- Admin V1B2G standalone Chi'lly Chat attachment enforcement is pushed. `app/chat/[threadId].tsx` blocks selected attachments when `runtimeControls.chat_attachments_enabled` is false, before optimistic message insertion and before `sendChatMessage`. Text-only messages remain allowed. Admin marks Chat Attachments as read-only `Enforced on chat attachments`; no working toggle was added.
- Admin V1B2H room attachment enforcement is pushed. `app/watch-party/[partyId].tsx` and `app/watch-party/live-stage/[partyId].tsx` block selected room attachments when `runtimeControls.chat_attachments_enabled` is false, before room message insert and before attachment upload. Text-only room comments remain allowed. Admin marks Chat Attachments as read-only `Enforced on chat and room attachments`; no working toggle was added.
- Admin V1B2I-C room comment enforcement is pushed. `app/watch-party/[partyId].tsx` and `app/watch-party/live-stage/[partyId].tsx` block room-native text/comment submits when `runtimeControls.chat_enabled` is false, before `sendPartyMessageRecord`. Existing room reads, layouts, LiveKit, Premium gates, invite behavior/system share, attachment controls, RLS, migrations, generated types, and Supabase remote state are unchanged. Admin marks Chat as read-only `Enforced on chat, invites, and room comments`; no working toggle was added.
- Admin V1B2J remaining runtime-control enforcement is pushed. The formerly remaining configured controls are now scoped: `live_first_enabled`, `live_watch_party_enabled`, and `watch_party_live_enabled` gate their existing live/watch-party entry helper paths, and `max_upload_size_mb` gates creator-video upload size before storage work. Future Admin runtime-control work should be limited to backed write/toggle permissions and runtime false-config visual proof, not new money/provider behavior.
- Admin Usage Metering Foundations 37-39 plus Admin Usage Metering Foundation V1 remote/provider schema are pushed in `_lib/platformUsage.ts`, `app/admin.tsx`, migrations `supabase/migrations/202605070003_platform_usage_metering_foundation.sql` and `supabase/migrations/202605070004_admin_usage_metering_v1.sql`, and generated database types. Admin Usage Writer V1A0/V1A1/V1A2 is pushed for successful creator-video upload usage only. Admin Usage may show storage metadata estimates, participant-minute DB estimates, schema-connected internal usage/provider import/reconciliation counts, and provider import rows for Cloudflare R2, Hetzner Object Storage, and Hetzner Servers when real rows exist, but bandwidth remains not connected unless future real metering rows exist. Do not show fake cost, fake billing, fake revenue, fake payouts, fake provider logs, fake provider bills, fake overages, fake storage billing, fake traffic, or fake LiveKit billing truth.
- Ledger Systems 4A-4D finance foundation is pushed in `_lib/platformFinance.ts`, `app/admin.tsx`, and migration `supabase/migrations/202605070005_platform_finance_ledger_foundation.sql`. The migration is now applied remotely, and generated database types include the finance foundation tables. Admin finance panels may show safe foundation row counts only; no live money totals, payout execution, invoices, sponsor checkout, payout split execution, fraud enforcement, provider integrations, or fake revenue exists.
- Finance F2B creator payout provider schema foundation is pushed in migration `supabase/migrations/202605080001_creator_payout_provider_foundation.sql` and generated database types. It is remote-applied/type-refreshed schema only for future payout accounts, payout batches, provider transfers, payout holds, and immutable payout audit logs. No Stripe integration, payout buttons, withdrawal buttons, or live payout action exists.
- Network Billing Foundation is pushed in migration `supabase/migrations/202605080003_network_billing_foundation.sql`, generated database types, `_lib/platformFinance.ts`, and read-only `app/admin.tsx` Networks copy. It adds/expands network billing account display/contact fields, invoice month/subtotal/total fields, network plan records, account plan assignments, quota records, invoice line items, overage events, and billing audit logs. Admin Networks may show foundation counts only and must clearly preserve no invoice send, no customer charge, no payment link, no Stripe call, no live overage approval, no fake revenue, no unpaid balance, and no real customer obligation.
- Sponsor Checkout Foundation is pushed in migration `supabase/migrations/202605080004_sponsor_checkout_foundation.sql`, generated database types, `_lib/platformFinance.ts`, and read-only `app/admin.tsx` Sponsors copy. It includes sponsor brands, sponsor deals, creatives, placements, disclosures, review logs, payment records, and payout split records with foundation-only proof rows. No sponsor checkout, brand payment, payment link, Stripe Checkout, sponsor approval action, fake sponsor revenue, payable balance, or payout split execution exists.
- Fraud Enforcement Foundation is pushed in migration `supabase/migrations/202605080005_fraud_enforcement_foundation.sql`, generated database types, `_lib/platformFinance.ts`, and read-only `app/admin.tsx` Fraud copy. It includes fraud holds, fraud reasons, evidence records, planned actions, review notes, appeal placeholders, and fraud audit logs with foundation-only proof rows. No payout pause, account restriction, upload restriction, live restriction, monetization disable, fraud risk score, or active enforcement action exists.
- Creator Revenue Share Ledger / Fraud Hold Enforcement Foundation is pushed in migration `supabase/migrations/202605080013_revenue_share_fraud_hold_enforcement_foundation.sql`, generated database types, `_lib/platformFinance.ts`, and read-only `app/admin.tsx` Revenue/Fraud copy. It adds creator revenue share rules, creator revenue share ledger rows, fraud enforcement policies, and non-executable fraud action readiness fields with foundation-only proof rows. No real source money has been imported, no fake creator earnings exist, no payable balances exist, no payout ledger entries are created from revenue share foundation rows, runtime enforcement hooks are not connected, and no payouts, monetization, uploads, live access, sponsor deals, or accounts are restricted.
- Immutable Admin Audit Log Foundation is pushed in migration `supabase/migrations/202605080006_immutable_admin_audit_log_foundation.sql`, generated database types, `_lib/platformAudit.ts`, and read-only `app/admin.tsx` Audit copy. It adds append-only `platform_admin_audit_logs` and proof rows only. No audit row edit/delete/clear UI, payout approval/release, invoice send/charge, sponsor checkout/approval, fraud enforcement, account restriction, upload/live restriction, monetization disable, or admin role write exists.
- Creator Payout Read-Only Dashboard Foundation is pushed in `app/channel-settings.tsx`, `_lib/creatorPayouts.ts`, and policy-only migration `supabase/migrations/202605080007_creator_payout_dashboard_read_policy.sql`. Payouts live in Platform Studio after Insights and before Brand, the Home Payouts card switches to that tab, `/channel-settings` compatibility remains, and `/channel-studio` remains the owner Platform Studio route. Creator payouts are not active yet: no Withdraw, Cash Out, Connect Stripe, KYC live flow, tax form flow, payout approval, payout release, transfer creation, fake payable balance, fake earnings, or live money movement exists. Regular creators can read only their own payout ledger rows and cannot insert/update/delete payout rows; anon cannot insert payout rows; no broad payout access or generated type change was added.
- Stripe Connect Schema Foundation (Payout S2) is pushed in migration `supabase/migrations/202605080008_stripe_connect_schema_foundation.sql` and generated database types. It adds provider/config/capability/readiness fields, onboarding session records without long-term onboarding URL storage, provider webhook event records with idempotent uniqueness, creator payout eligibility/readiness records, and immutable audit linkage fields only. No Stripe SDK, Stripe keys, provider calls, account creation, onboarding link creation, transfer creation, payout execution, Connect Stripe UI, Withdraw/Cash Out, KYC/tax live flow, fake payable balance, fake earnings, or live money movement exists.
- Stripe Connect Test-Mode Backend Functions (Payout S3C) is pushed in `supabase/functions/stripe-connect-account`, `supabase/functions/stripe-connect-onboarding-link`, `supabase/functions/stripe-connect-account-sync`, `supabase/functions/stripe-connect-webhook`, and shared helper `supabase/functions/_shared/stripe-connect.ts`. The functions are deployed ACTIVE version 1, remain backend-only/test-mode only, and use internal Supabase bearer auth validation where applicable. Account creation/reuse, onboarding-link creation, account sync, and signed webhook/idempotency handling have test-mode provider proof; provider readiness remains non-payable and payout execution stays inactive. Invalid webhook signatures are rejected. A valid signed test-mode `account.updated` event was accepted through the deployed webhook, stored once as an ignored provider event because no matching local payout account existed, wrote received/processed audit rows, and duplicate resend did not process twice. They add no Stripe SDK/package dependency, Stripe key in repo, provider secret in repo, React Native Stripe integration, Connect Stripe UI, creator payout setup button, transfer, payout, checkout session, withdrawal, payout approval/release, fake payable balance, fake earnings, live mode, migration, generated database type change, or app UI. `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` are configured as backend Edge Function secret names by digest only.
- Payout Review Queue + Batch Draft Workflow Foundation is pushed in `_lib/platformFinance.ts`, `app/admin.tsx`, generated database types, and migration `supabase/migrations/202605080009_payout_review_batch_foundation.sql`. Admin Payouts remains read-only/foundation and shows payout review queue and batch draft counts only. No payout approval, release, batch processing, transfer creation, Stripe payout, Connect Stripe button, fake payable balance, fake earnings, or live money movement exists.
- Provider Transfer Records Sync Foundation is pushed in `supabase/functions/stripe-connect-transfer-sync`, `supabase/functions/_shared/stripe-connect.ts`, `_lib/platformFinance.ts`, `app/admin.tsx`, generated database types, and migration `supabase/migrations/202605080010_provider_transfer_sync_foundation.sql`. Admin Payouts remains read-only/foundation and shows provider transfer sync counts only. Provider transfer sync is backend-only/admin-operator/test-mode status import for existing local provider records; it creates no transfer, payout, checkout session, paid state, payable balance, or live money movement.
- Payout Transfer Preflight Foundation is pushed in `supabase/functions/stripe-connect-transfer-create/index.ts` and `supabase/config.toml`. It is a deployed backend-only/admin-operator/test-mode preflight that returns `preconditions_required`, rejects client provider references and money instructions, reads no Stripe secrets, makes no Stripe calls, writes no provider transfer rows, and creates no transfer, payout, checkout session, paid state, payable balance, payout release, or live money movement.
- Payout Release Preflight Foundation is pushed in `supabase/functions/payout-release-preflight/index.ts` and `supabase/config.toml`. It is a deployed backend-only/admin-operator closed-gate preflight that returns `preconditions_required`, rejects client money/provider/release instructions, reads no provider secrets, makes no provider calls, writes no payout/provider rows, and creates no payout release, payout approval, transfer, payout, checkout session, paid state, payable balance, or live money movement.
- Ads Launch Foundation V1A is pushed.
- Ads V1A is provider-neutral foundation only. No real ads render yet, no AppLovin SDK was installed, no Unity LevelPlay SDK was installed, no Unity Ads SDK was installed, no AdMob SDK was installed, no real ad IDs were added, no real provider initialization was added, and no CTV inventory was added.
- Ads V1A behavior to preserve: central defaults, `ads_enabled: false`, `ads_provider: placeholder`, placeholder provider not connected/no SDK calls, Premium/ad-free always ineligible, Premium/ad-free counters do not increment, forbidden routes/contexts block eligibility, active browsing time tracking exists, session caps exist, daily caps persist through AsyncStorage, and Admin Ads remains read-only/foundation.
- Ads V1B is pushed.
- Ads V1B added one native/feed placeholder placement on Home through `components/ads/NativeAdSlot.tsx` and `app/(tabs)/index.tsx`.
- Ads V1B behavior to preserve: normal runtime hides the placeholder because `ads_enabled=false`; `NativeAdSlot` renders only after V1A eligibility passes; Premium/ad-free users never see it and do not increment counters; placeholder native/feed records only when eligible; base native/feed session cap is 1; long-use unlock allows a second placement after 120 active browsing minutes; daily native/feed cap is 3; forbidden routes/contexts remain blocked; Admin Ads stays read-only/foundation.
- Ads V1B did not add real ad rendering, SDKs, real IDs, provider initialization, interstitials, CTV inventory, fake revenue, or payout/sponsor/creator earnings systems.
- Ads V1C is pushed.
- Ads V1C added `components/ads/InterstitialController.tsx`, mounted it in `app/_layout.tsx`, and updated Admin Ads read-only/foundation copy in `app/admin.tsx`.
- Ads V1C behavior to preserve: normal runtime shows no interstitial because `ads_enabled=false` and placeholder provider is not connected; the controller renders `null`, ignores first route mount, considers route transitions only, calls central eligibility with `placementKind: "interstitial"`, records placeholder interstitial shows only after eligibility and placeholder-provider success, blocks Premium/ad-free users, respects 180-second first delay, 600-second spacing, session cap 3 plus long-use +2 after 120 active browsing minutes, daily cap 6, and forbidden routes/contexts.
- Ads V1C did not add real ad rendering, SDKs, real IDs, provider initialization, CTV inventory, fake revenue, or payout/sponsor/creator earnings systems.
- Ads Config V1D1 is pushed.
- Ads Config V1D1 added optional normalized `adsLaunch` under `app_configurations.config`, defaulting through `ADS_LAUNCH_CONFIG_DEFAULTS`, and changed only Admin Ads read-only status copy to display the normalized source/values.
- Ads V1D2 is pushed.
- Ads V1D2 added `hooks/useAdsLaunchConfig.ts` and wired `NativeAdSlot` plus `InterstitialController` fallback runtime reads to normalized `app_config.adsLaunch`. It did not use Admin `runtimeControls.ads_enabled`, did not add working Admin ad toggles, and did not add SDKs, real IDs, provider initialization, real rendering, CTV inventory, fake revenue, migrations, generated database types, RLS changes, package/native config, or Supabase remote changes.
- Public V1 current-build release proof is complete. Do not redo it unless a new runtime/code change lands or an explicit prompt asks for a new current-build proof.
- Public V1 Hardening H1A 18+ Signup Confirmation is pushed.
- H1A added a no-migration checkbox gate to `app/(auth)/signup.tsx`: signup shows `Chi'llywood is for users 18 and older.`, requires `I confirm I am 18 or older.`, blocks before `supabase.auth.signUp` with the required alert if unchecked, and preserves legal links plus Sign In handoff.
- H1B2 legal acceptance storage is pushed.
- H1B2 applied remote migrations `202605070001` and `202605070002`, regenerated database types from the linked remote schema, wires signup writes through `_lib/accountLegalAcceptance.ts`, and keeps anon access to the legal acceptance table denied.
- H1B2 writes only after signup returns an authenticated session. If email confirmation returns no session, the app does not fake the write.
- Public V1 Hardening H2 upload/content lifecycle polish is pushed.
- H2 changed only `app/channel-settings.tsx` and `components/creator-media/creator-video-card.tsx`.
- H2 keeps existing picker/upload/storage/metadata/Open Player/Edit/Publish/Unpublish/Delete behavior, adds honest local upload lifecycle copy, clarifies backed `Published`/`Draft` and `Media Ready`/`Media Unavailable` states, and does not add fake processing/transcoding/archive/retry/storage-billing states.
- H2 follow-up duplicate-upload UI correction changed only `app/channel-settings.tsx`; keep one clear Platform Studio `Video Upload` form and do not reintroduce separate header/empty-state upload boxes.
- Public V1 Hardening H3 security/compliance/moderation hardening is pushed.
- H3 changed only `_lib/moderation.ts`, `components/safety/report-sheet.tsx`, `app/settings.tsx`, and `components/system/support-screen.tsx`.
- H3 preserves the existing backed `safety_reports` categories and explains them more clearly. Scam/fraud/unsafe product/ad concerns use `Safety`; undisclosed sponsorship uses `Other` until a later schema pass. H3 does not add new schema categories, fake moderation actions, fake legal claims, RLS changes, migrations, generated-type edits, or admin bypasses.

## Product Truth To Preserve
- Profile = person/social identity.
- Channel = public mini streaming platform/network.
- Platform Studio = owner-only creator operating system.
- Follow = channel audience.
- Chi'lly Circle = personal mutual friendship/connection.
- Subscribers = monetized channel supporters later.
- Public Channel must not leak owner-only controls to non-owners.
- Public Channel must not show drafts/private/unpublished content.
- Profile deep-link route params must remain fallback-only once backed identity loads. Creator-video Player deep links must remain public-or-owner; non-public creator videos must not resolve for non-owners.
- Platform Studio is owner-only.
- Admin is platform/operator authority, not creator/channel owner authority.
- `/admin` is the only Admin Command Center route.
- Admin must not expose Supabase service-role keys, LiveKit secrets, RevenueCat secrets, app-store keys, provider secret keys, or any other secret.
- Test admin credentials must never be stored, printed, logged, hard-coded, or committed.

## Locked Business Decisions
- Launch planned as 18+.
- H1A no-migration signup confirmation is pushed.
- H1B2 legal acceptance storage is pushed for new signups with authenticated sessions.
- Free users see ads at launch.
- Premium users see no ads.
- Planned Premium price: `$9.99/month` and `$99/year`.
- Premium uses Google Play plus RevenueCat entitlement `premium`; Premium subscription revenue belongs to Chi'llywood/platform, is not split with creators, and must not create a creator subscription revenue pool.
- Full live/watch-party access is Premium and must not be made free again without an explicit product decision.
- Free users may only get live/watch-party preview in a separate future pass if explicitly designed safely; no preview mode exists now.
- Ads support free browsing.
- Premium supports expensive live usage.
- RevenueCat remains Premium subscription truth. Premium offering lookup may use the canonical safe fallback for `premium_subscription` offer display/purchase selection, but entitlement truth still owns access and no public route may unlock Premium without a trusted active entitlement.
- AppLovin MAX is primary ad mediation direction; Unity LevelPlay / Unity Ads may come through AppLovin MAX later.
- Do not build AdMob-only ads.
- Do not make direct screen-level SDK calls; future real providers must use the Chi'llywood provider-neutral ad wrapper.
- Ads launch cap: base active session 3 interstitial + 1 native/feed; after 2 active browsing hours +2 interstitial + 1 native/feed; daily cap 6 interstitial + 3 native/feed; Premium sees zero ads.
- Ads must not appear inside active LiveKit rooms, during active video playback, while typing/commenting, during upload, on subscribe/payment screens, immediately at app launch, in Admin, in Platform Studio, in Chat, or in Profile/composer contexts unless explicitly redesigned later.
- Launch app ads are platform money at first. RevenueCat remains Premium subscription truth only and does not take ad revenue. Google Play does not take a subscription fee from AppLovin ad payouts.
- Creator-page ad revenue share is later: creator 70% net and Chi'llywood 30% net. Creator-sold sponsor slots are later: brand pays Chi'llywood first, creator 80% net, and Chi'llywood 20% net.
- No live creator ad revenue sharing, payout execution, sponsor deal execution, or CTV revenue system exists yet; finance and payout-provider tables are foundation-only.
- Creator mini-platform monetization remains later/foundation-only: Premium creators may eventually sell paid videos, rooms, posts/content, collections, events, VIP access, channel subscriptions/fan memberships, and merch/products/clothing on their Channel mini platform, managed from Platform Studio command-center surfaces, while free viewers may buy paid creator content/products without Premium. Paid creator content and creator merch/product sales are separate from Premium subscription revenue. Creator paid content split is creator 80% net / Chi'llywood 20% net. Tips remain creator 100% of the tip amount/net tip with separate disclosed service/platform/provider/cash-out fees where allowed. Scheduled payouts are free; optional instant cash-out preferred direction is 1.5% with no default cap. Do not add fake purchases, fake merch orders, fake earnings, fake balances, payout release, transfer creation, or live money movement.
- CTV ads are future-only for Chi'llywood Originals and network-style content and are not active now.
- Storage doctrine: Cloudflare R2 for public/high-download media; Hetzner Object Storage for source/original uploads, drafts, backups, archive, private/held/deleted media; Hetzner/OVH boxes for LiveKit and real-time live/watch-party traffic.

## Prompt Standard
Future Chi'llywood prompts must be production-grade and include exact product truth, exact scope, route/screen purpose, UI layout, buttons/actions, data sources, empty/loading/error states, permissions/gates, backend/RLS/storage limits, forbidden areas, validation/manual proof, and report format.

Do not proceed from vague prompts like "modernize", "polish", "add filters", "add route", or "improve dashboard" unless every behavior is spelled out.

## Validation Baseline
Use the lane-specific prompt for validation. Default baseline remains:

- `git status --short`
- branch and HEAD
- scoped diff check
- `npm run typecheck` for app/runtime code changes
- `git diff --check`
- targeted Android/manual proof when UI, routing, gates, or device behavior changed

## Staging Discipline
- Do not stage `artifacts/`.
- Do not stage `supabase/.temp/`.
- Stage only task-pure files.
- Before commit, show `git diff --cached --name-only` and confirm no unrelated files are staged.
