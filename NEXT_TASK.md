# NEXT TASK

## Exact Next Task
Carry forward the still-valid proved baseline exactly as recorded, including the locked Party / Live / Profile / Chi'lly Chat semantics, the preserved Rachi official-account foundation, the re-proved Live Waiting Room / Live Room / Live Stage path, and the now-corrected Watch-Party Live player surface on `app/player/[id].tsx`. Preserve as now true that the Android shared-player route no longer paints as a black sheet on the mounted Watch-Party flow, and that the refined participant dock remains side-by-side, floating on the branded background, and free of the old card-shell / white-border treatment. The exact next lane is a fresh two-phone Watch-Party Live runtime-proof pass that focuses only on the remaining honest surface items after this Android paint fix, especially active-playback auto-hide, reactions if exposed on the refined surface, and high-count participant-scale behavior.

## Current Plan
1. Preserve the carried-forward proved baseline exactly as recorded.
2. Preserve the now-landed refined Watch-Party Live player surface, the corrected floating participant dock, and the closed Android black-player fix on `app/player/[id].tsx`.
3. Re-run two-phone Watch-Party proof on that refined painted surface for only the remaining honest runtime items.
4. Reopen the player owner only if the fresh proof shows a real runtime regression.

## Exact Next Batch
- preserve the now-landed refined Watch-Party Live owner, corrected floating participant dock, and Android paint fix exactly as pushed
- run two-phone proof against the refined `Watch-Party Live` surface now that the mounted Android player paints correctly
- verify the remaining runtime items honestly on-device, especially active-playback auto-hide, reactions if exposed, and participant-scale behavior
- reopen `app/player/[id].tsx` only if that proof exposes a real regression

## Scope
This next pass should:
- preserve the carried-forward proved baseline exactly as recorded
- preserve the now-reconciled Watch-Party Live owner on `main`
- keep the next lane focused on runtime proof of `app/player/[id].tsx`, not renewed black-surface diagnosis
- avoid speculative additional player-room churn outside the active Watch-Party owner
- keep unrelated local dirt out of the checkpoint

## Out Of Scope
Do not:
- reopen transport/media/TURN/ICE debugging
- treat old-shell screenshots as proof of the current reconciled surface
- broaden beyond the active Watch-Party Live owner unless fresh proof first forces it
- reopen already-closed Live Stage clutter work without fresh regression proof
- broaden into Premium, analytics, or broader chat work
- mix unrelated local dirt into any future checkpoint

## Success Criteria
The next lane is successful when:
- the reconciled `app/player/[id].tsx` surface proves correctly on two real phones with the corrected floating participant dock intact
- the reconciled `app/player/[id].tsx` surface continues to paint real video on the mounted Android Watch-Party route instead of returning to a black sheet
- the remaining Watch-Party runtime items are closed or narrowed against the real intended layout without reopening participant-card chrome
- the repo can move forward without stale old-shell ambiguity in the Watch-Party player lane
