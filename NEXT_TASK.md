# NEXT TASK

## Exact Next Task
The next exact task is a narrow **Content Studio / creator-content management audit** on `main`, using `ROADMAP.md`, `CURRENT_STATE.md`, and this file as governing truth. Do not reopen `/profile/[userId]`, `/channel-settings`, `/chat`, watch-party/live route truth, or the landed Stage 5 summary surfaces unless a real regression is proved. The next chapter is now locked as creator/content management deepening, and the first pass should audit the current Content Studio foundation plus its public-surface consumers so the smallest honest first implementation batch can be chosen without drifting into full creator-platform scope.

## Current Plan
1. Re-read `ROADMAP.md`, `CURRENT_STATE.md`, `NEXT_TASK.md`, and the current Content Studio owners first.
2. Treat the current profile/channel chapter as closed unless a real regression is found.
3. Audit the existing content-management foundation in:
   - `app/admin.tsx`
   - `_lib/appConfig.ts`
   - `app/(tabs)/index.tsx`
   - `app/profile/[userId].tsx`
4. Identify which creator/content-management gaps are already supported by current title/config truth and which still rely on placeholders or local fallback assumptions.
5. Choose the single smallest first implementation batch for the chapter without widening into heavier creator-platform, monetization, or scheduling systems.
6. Preserve `/profile/[userId]`, `/channel-settings`, `/chat`, `/chat/[threadId]`, and all watch-party/live route truth unchanged.

## Exact Next Batch
- audit the current Content Studio / creator-content management foundation
- inventory real title/programming controls versus still-placeholder curation surfaces
- choose the smallest first implementation batch for creator/content management deepening
- keep `Chi'lly Chat`, `Watch-Party Live`, and `Live Watch-Party` doctrine unchanged
- keep unrelated local dirt out of the checkpoint

## Scope
This next pass should:
- stay inside current content-management owners and their direct public-surface consumers
- prove what current title/config/programming truth already supports
- identify the smallest honest first implementation lane for this chapter
- avoid reopening landed profile/channel work without a proved regression
- avoid inventing a full creator mini-platform before Public v1 needs it
- preserve all current route truth and all previously landed profile/channel stages
- keep unrelated local dirt out of the checkpoint

## Out Of Scope
Do not:
- reopen profile/channel scope because of preference churn
- widen into live/event scheduling, heavier monetization rollout, audience/analytics expansion, or deeper safety/admin work
- invent creator-platform routes or `/studio*` route truth
- fake content catalogs, fake programming, or fake analytics
- create `/studio*` routes
- touch runtime room/live-stage owners
- touch RBAC or Rachi control-plane work
- broaden into unrelated admin expansion
- broaden into other screen owners
- mix unrelated local dirt into the checkpoint

## Success Criteria
The next lane is successful when:
- the repo proves the current Content Studio / creator-content management foundation truthfully
- one exact first implementation batch is chosen for the new chapter without doctrine drift
- `/profile/[userId]`, `/channel-settings`, `/chat`, and live/watch-party route truth remain unchanged
- no fake future scope is introduced
