# NEXT TASK

## Exact Next Task
The next exact task is a narrow post-profile/channel roadmap selection pass on `main`, using `ROADMAP.md`, `CURRENT_STATE.md`, and this file as governing truth. Do not reopen `/profile/[userId]`, `/channel-settings`, `/chat`, watch-party/live route truth, or the landed Stage 5 summary surfaces unless a real regression is proved. The current profile/channel chapter is now complete under the active doctrine, so the next pass should identify and lock the next exact Public v1 execution lane outside this chapter instead of inventing more profile/channel scope.

## Current Plan
1. Re-read `ROADMAP.md`, `CURRENT_STATE.md`, and `NEXT_TASK.md` first.
2. Treat the current profile/channel chapter as closed unless a real regression is found.
3. Identify the next highest-value unfinished Public v1 lane that is not blocked by later-phase dependencies.
4. Convert that lane into one exact, current-structure implementation batch with clear owner files.
5. Preserve `/profile/[userId]`, `/channel-settings`, `/chat`, `/chat/[threadId]`, and all watch-party/live route truth unchanged.

## Exact Next Batch
- audit the now-closed profile/channel chapter against `ROADMAP.md`
- choose the next exact Public v1 execution lane outside profile/channel work
- keep `Chi'lly Chat`, `Watch-Party Live`, and `Live Watch-Party` doctrine unchanged
- keep unrelated local dirt out of the checkpoint

## Scope
This next pass should:
- prove that the profile/channel chapter is complete under current doctrine
- identify the next exact implementation lane from current Public v1 truth
- avoid reopening landed profile/channel work without a proved regression
- preserve all current route truth and all previously landed profile/channel stages
- keep unrelated local dirt out of the checkpoint

## Out Of Scope
Do not:
- reopen profile/channel scope because of preference churn
- fake a next lane that is blocked by later-phase dependencies
- create `/studio*` routes
- touch runtime room/live-stage owners
- touch RBAC or Rachi control-plane work
- broaden into unrelated admin expansion
- broaden into other screen owners
- mix unrelated local dirt into the checkpoint

## Success Criteria
The next lane is successful when:
- the repo records that the current profile/channel chapter is complete
- one exact next Public v1 execution lane is chosen without doctrine drift
- `/profile/[userId]`, `/channel-settings`, `/chat`, and live/watch-party route truth remain unchanged
- no fake future scope is introduced
