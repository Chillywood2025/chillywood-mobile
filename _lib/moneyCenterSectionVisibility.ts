/**
 * Money Center focus identifies the section a route or action opens. It must
 * never suppress that section's canonical accordion body.
 */
export const isMoneyCenterSectionBodyVisible = (expanded: boolean): boolean => expanded;
