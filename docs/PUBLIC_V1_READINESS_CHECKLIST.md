# Public V1 Readiness Checklist

Date: 2026-04-29

Purpose: track Public v1 launch readiness without treating static validation as runtime proof. Use this document with `CURRENT_STATE.md`, `NEXT_TASK.md`, `docs/FULL_APP_ROUTE_OWNER_BEHAVIOR_AUDIT.md`, `docs/FULL_APP_NON_ROOM_BEHAVIOR_AUDIT.md`, `docs/PUBLIC_V1_SMOOTHNESS_HARDENING_AUDIT.md`, `docs/PUBLIC_V1_AND_LATER_SYSTEMS_PLAN.md`, `docs/EXTERNAL_SETUP_PUBLIC_V1_CHECKLIST.md`, and `docs/DASHBOARD_SETUP_COMMAND_CENTER.md`.

Status key:

- Done: implemented and proof completed for the relevant launch path.
- Implemented / Proof Pending: code or docs exist, but runtime, device, store, live Supabase, or two-device proof is still required.
- External Setup Pending: app-side behavior exists or is defined, but store, legal, account, or release-console setup must be completed outside this codebase.
- Later Phase: intentionally post-v1.
- Blocked: a known repo or runtime blocker prevents launch readiness.

## Launch Rules

- Do not mark a system Done when Android, two-device, store, live Supabase, or release proof is still pending.
- Do not fake Premium, billing, moderation, Watch-Party, creator upload, audience role, monetization, or notification support.
- Backend/RLS truth must enforce sensitive behavior. Hidden buttons are not security.
- Runtime proof must save screenshots/logs under `/tmp` and summarize facts in repo control files.
- Native room/screen UI is a launch-quality requirement. New or materially changed routes must be Chi'llywood-native, preserve the dark cinematic background identity, declare owner and primary experience, place identity/status at top, primary experience in the middle, fast controls at bottom, secondary controls in More/sheets, and prove no overlap/clipping, role leakage, fake data, route drift, or Premium gate weakening.
- Profile is personal/social identity; Channel is the public mini streaming platform/network on `/channel/[userId]`; Channel Studio is owner-only on `/channel-studio`; `/channel-settings` remains compatibility.
- Public Channel must not show owner-only controls to non-owners or drafts/private/unpublished content.
- Launch is planned as 18+. H1A signup confirmation and H1B2 legal acceptance storage are pushed: signup shows the 18+ copy, requires an active checkbox confirmation before account creation is attempted, preserves legal links plus Sign In handoff, and writes backed age/terms/privacy acceptance after account creation succeeds with an authenticated session. Free users see ads at launch, Premium users see zero ads, and all full Live First, Live Watch-Party, and Watch-Party Live access is Premium.
- Native game streaming, paid creator videos, subscriber-only videos, tips/coins, live payouts, media comments, full Friends, close friends, friend-only privacy, full VIP/moderator roster, advanced creator studio, and automatic transcoding remain later. Creator Payout Read-Only Dashboard Foundation is pushed in Channel Studio as foundation-only readiness/ledger visibility with no Withdraw, Cash Out, Connect Stripe, KYC/tax live flow, payout approval/release, fake payable balance, fake earnings, or live money movement. Stripe Connect Test-Mode Backend Functions are deployed backend-only/test-mode, backend Stripe secret names are configured by digest only, account creation/reuse, onboarding-link creation, account sync, signed webhook handling, and duplicate-event idempotency have test-mode provider proof; invalid webhook signatures are rejected; Admin Payout Provider Dashboard is read-only provider-status counts only; and Provider Transfer Records Sync Foundation is backend-only/admin-operator/test-mode status import for existing local provider records only. They add no React Native Stripe SDK, Stripe keys in repo, Connect Stripe UI, transfer creation, payouts, checkout sessions, live mode, fake paid status, or live money movement. Payout Review Queue + Batch Draft Workflow Foundation is pushed in Admin Payouts as read-only/draft-only counts/proof rows only; no payout approval, batch processing, transfer creation, fake payable balance, fake earnings, or live money movement exists. Ads Launch Foundation V1A/V1B/V1C is pushed as no-SDK/no-real-rendering infrastructure. Home native/feed placeholder foundation and placeholder interstitial controller foundation exist but normal runtime hides them while `ads_enabled=false`; real ad serving and real AppLovin MAX integration remain separate lanes.
- Real Chi’llyfects AR processing remains post-v1. Do not add Snap Camera Kit, DeepAR, MediaPipe processing, custom WebRTC frame processors, or native AR SDKs before Public v1.

## May 13, 2026 Profile / Creator Media Runtime Proof

Profile / Public Channel / Channel Studio and Creator Media runtime proof is closed for this lane; this closeout supersedes older row-level Proof Pending notes for these surfaces below. Proof used one physical Android device, `R5CR120QCBF`, with a real creator/owner account and a separate backed viewer/non-owner account by account switching; a second physical device was not required for this lane. Profile proof passed for backed post create, owner delete, public clean read, draft/deleted hidden states, comments, replies, likes, clickable links, supported attachment display, safe report UI, Android composer keyboard behavior, and no fake counts/comments/users. Public Channel proof passed for owner, signed-in viewer, signed-out viewer, and blocked-viewer states; Follow/Following mutation worked for the viewer account, signed-out Follow produced the auth handoff, no owner-only controls appeared to viewers, channel stats rendered safely, and the signed-out public profile read blocker was fixed narrowly with `read_public_channel_profile` instead of broadening `user_profiles` RLS. Channel Studio proof passed for owner access, persisted metadata edit, publish/unpublish/delete lifecycle, Android editable form behavior, and non-owner denial through backed RLS/API proof.

Creator Media proof passed through the existing upload path with a real non-zero test upload, not a fake media row. The current zero-byte proof blocker was cleared by proving Studio now has a non-zero existing media item and by uploading a new non-zero creator video through Android document picker and backed storage/table writes; the lifecycle proof then unpublished and deleted that new proof video and removed its storage object. Public published playback opened in the branded Player, draft/private/unpublished/deleted media stayed unavailable to public viewers, missing media stayed in the safe shell, comments/replies/clickable links/supported attachments worked, owner/non-owner delete/mutation permissions were enforced by RLS, invalid attachment type and too-large attachment attempts failed safely, and no raw signed storage URL, storage secret, service-role key, credential, fake data, unsafe storage bypass, D7F spectator behavior change, billing change, LiveKit routing change, or unrelated UI redesign was added.

## May 13, 2026 Legal Policy Closeout Status

This table uses the launch-legal status vocabulary requested for the public policy lane. "Drafted pending attorney review" means repo policy text exists but is not legal approval. "Linked in app" means a public route or app link exists. "Externally blocked" means public hosting, store-console proof, inbox receipt, DMCA registration, or legal/support ownership is still outside repo code. "Not implemented" means no complete app/backend workflow exists.

| Item | Status | Notes |
| --- | --- | --- |
| Terms | Drafted pending attorney review / linked in app | `docs/legal/TERMS_OF_SERVICE.md`, `app/terms.tsx`, Settings and Support links. |
| Privacy | Drafted pending attorney review / linked in app | `docs/legal/PRIVACY_POLICY.md`, `app/privacy.tsx`, Settings and Support links. |
| Creator Rules | Drafted pending attorney review / linked in app | `docs/legal/CREATOR_RULES.md`, `app/creator-rules.tsx`, Settings and Support links. |
| Community Guidelines | Drafted pending attorney review / linked in app | `docs/legal/COMMUNITY_GUIDELINES.md`, `app/community-guidelines.tsx`, Settings and Support links. |
| DMCA/Copyright Policy | Drafted pending attorney review / linked in app / DMCA agent registered | `docs/legal/COPYRIGHT_DMCA_POLICY.md`, `app/copyright.tsx`; designated agent contact now uses registration `DMCA-1072720` and `support@chillywoodstream.com`. |
| DMCA agent public page/contact | Done / attorney review pending | Public copyright page lists Chi'llywood Copyright Agent / Chi'llywood, `support@chillywoodstream.com`, 9316 S Kimbark, Chicago, IL 60619, phone `3124879454`. |
| DMCA agent Copyright Office registration | Done | User-provided registration truth records Chi'llywood registration `DMCA-1072720`; public Copyright Office directory status checked as active effective May 13, 2026. |
| Copyright report flow | Implemented / attorney review and live end-to-end proof pending | `app/copyright-report.tsx` collects formal notice fields and writes backed DMCA cases through `submit_dmca_notice`; generic report sheet links copyright reports to the formal route. |
| Counter-notice workflow | Partially implemented / attorney review pending | Admin can record Support/email counter-notices, forwarding, 10-14 business-day windows, court action notices, and restore eligibility; direct uploader submission route remains pending. |
| Repeat infringer policy | Implemented for Admin tracking / attorney review pending | `dmca_strikes` tracks active/removed/disputed/expired strikes and can open repeat-infringer review; no automatic termination. |
| Account deletion in-app path | Linked in app / proof pending | Settings and Support reach account deletion; flow remains request-based. |
| Account deletion web link | Live URL candidate / Play acceptance pending | `https://chillywoodstream.com/account-deletion` is live and returns HTTP/2 200 after trailing-slash redirect; Play Console entry/acceptance is still pending. |
| Refund/subscription terms | Drafted pending attorney review / externally blocked | Store/RevenueCat proof remains external; no refund promise beyond law/store rules. |
| Creator payout rules | Drafted pending attorney review / not active | Payout policies drafted; payouts remain gated/not active. |
| Fraud/forfeiture rules | Drafted pending attorney review / partially implemented | Foundation/read-only fraud systems exist; live enforcement is not connected. |
| Sponsor disclosure rules | Drafted pending attorney review / not active for paid sponsor money | Sponsor policy drafted; live sponsor payments/activation remain gated. |
| Banned content policy | Drafted pending attorney review | `docs/legal/BANNED_CONTENT_POLICY.md`. |
| Moderation/reporting workflow | Drafted pending attorney review / partially implemented | Generic reports and admin moderation foundation exist; complete category/workflow tooling remains pending. |

## May 13, 2026 Public Legal URL Surface Status

The expanded public legal/support web surface exists in repo as a static site and is deployed on `chillywoodstream.com`. Store/legal acceptance work remains external.

| Path | Repo/local status | Deployment status | Notes |
| --- | --- | --- | --- |
| `/terms` | Generated from `docs/legal/TERMS_OF_SERVICE.md` | Live on `chillywoodstream.com` | `https://chillywoodstream.com/terms` redirects HTTP/2 308 to `/terms/` and then returns HTTP/2 200. |
| `/privacy` | Generated from `docs/legal/PRIVACY_POLICY.md` | Live on `chillywoodstream.com` | `https://chillywoodstream.com/privacy` redirects HTTP/2 308 to `/privacy/` and then returns HTTP/2 200. Privacy mentions LiveKit only as policy text. |
| `/creator-rules` | Generated from `docs/legal/CREATOR_RULES.md` | Live on `chillywoodstream.com` | App route also exists. |
| `/community-guidelines` | Generated from `docs/legal/COMMUNITY_GUIDELINES.md` | Live on `chillywoodstream.com` | App route also exists. |
| `/copyright` | Generated from `docs/legal/COPYRIGHT_DMCA_POLICY.md` | Live on `chillywoodstream.com` / DMCA agent registered / DMCA tooling added | `https://chillywoodstream.com/copyright` redirects HTTP/2 308 to `/copyright/` and then returns HTTP/2 200. Page lists DMCA registration `DMCA-1072720`, designated agent contact, formal notice intake, Admin case tracking, counter-notice recording, and strike tracking truth; attorney review and live end-to-end proof remain pending. |
| `/account-deletion` | Generated from `docs/legal/ACCOUNT_DELETION_POLICY.md` | Live URL candidate / Play acceptance pending | `https://chillywoodstream.com/account-deletion` redirects HTTP/2 308 to `/account-deletion/` and then returns HTTP/2 200. Google Play account deletion compliance is not complete until the URL is entered in Play Console and accepted. |
| `/refunds` | Generated from `docs/legal/REFUNDS_SUBSCRIPTIONS_POLICY.md` | Live on `chillywoodstream.com` | Store/provider refund handling only; no unauthorized refund promise. |
| `/sponsor-disclosure` | Generated from `docs/legal/SPONSOR_DISCLOSURE_POLICY.md` | Live on `chillywoodstream.com` | Sponsor money remains inactive unless separately proved. |
| `/banned-content` | Generated from `docs/legal/BANNED_CONTENT_POLICY.md` | Live on `chillywoodstream.com` | Severe-escalation operations still need owner/SLA approval. |
| `/moderation` | Generated from `docs/legal/MODERATION_REPORTING_WORKFLOW.md` | Live on `chillywoodstream.com` | Dedicated DMCA workflow tooling is added; general report lifecycle, fraud/sponsor workflows, outbound email automation, and end-to-end proof remain pending. |
| `/support` | Static support page generated from repo truth | Live / inbox receipt proof pending | `https://chillywoodstream.com/support` redirects HTTP/2 308 to `/support/` and then returns HTTP/2 200. Uses only confirmed `support@chillywoodstream.com`; no fake phone or mailing address. |

Static site owner: `public-site/legal-site/`. Build command: `npm run legal-site:build`. Local server command: `npm run legal-site:serve -- --port 4177`. Generated deployable output: `public-site/legal-site/site/`. Cloudflare Pages project: `chillywood-legal`. Proved public Pages host: `https://chillywood-legal.pages.dev`. Apex custom domain `chillywoodstream.com` is active, `https://chillywoodstream.com/` returns HTTP/2 200, and the tested legal/support paths return HTTP/2 200 after trailing-slash redirect. No LiveKit fallback was reached.

## Checklist

| Area | Status | Owner files/systems | Implemented truth | Still needs proof or setup | V1 or later | Next action |
| --- | --- | --- | --- | --- | --- | --- |
| Auth / Session | Implemented / Proof Pending | `app/_layout.tsx`, `app/(auth)/*`, `_lib/session.tsx`, `_lib/supabase.ts`, `_lib/accountLegalAcceptance.ts`, `supabase/migrations/202605070001_user_account_legal_acceptances.sql`, `supabase/migrations/202605070002_harden_user_account_legal_acceptance_grants.sql` | Root auth gate protects tab shell; login/signup redirect support exists; signup now shows Terms/Privacy/Community Guidelines acceptance links before account creation; H1A signup confirmation shows `Chi'llywood is for users 18 and older.`, requires `I confirm I am 18 or older.`, blocks before `supabase.auth.signUp` when unchecked, and preserves the existing-account Sign In handoff. H1B2 applies the private `user_account_legal_acceptances` table remotely, regenerates generated database types, and writes age/terms/privacy acceptance after account creation succeeds with an authenticated session. Settings signs out through Supabase auth. | H1A current-build Android dev-client proof passed for unchecked block, checked fallback to existing signup validation, legal links, and Sign In handoff. H1B2 passed typecheck/diff-check, remote migration alignment, generated type inspection, and anon REST denial proof. Final runtime proof with a safe disposable signup account is still needed to verify the real insert path, plus Android/release route smoke for signed-out/signed-in redirects, logout, protected deep links, real existing-account login, and release-build signup/legal behavior. | V1 required | Run safe disposable-account signup proof for the H1B2 backend write, then continue auth/release smoke. |
| Profile / Public Channel / Channel Studio | Implemented / Proof Pending | `app/profile/[userId].tsx`, `app/channel/[userId].tsx`, `app/channel-studio/index.tsx`, `app/channel-settings.tsx`, `_lib/userData.ts`, `_lib/channelReadModels.ts`, `_lib/channelAudience.ts`, `_lib/profilePosts.ts`, `_lib/socialAttachments.ts`, `supabase/migrations/202604290001_public_v1_social_basics.sql`, `supabase/migrations/202604290002_profile_post_engagement.sql`, `supabase/migrations/202604290003_social_replies_links_attachments.sql` | Profile is person/social identity. Public Channel is the creator's mini streaming/network route at `/channel/[userId]` with hero, backed Channel Pulse stats, Featured, Latest Uploads, Live & Upcoming, and About. Channel Studio is the owner-only creator operating system at `/channel-studio`; `/channel-settings` remains compatibility. Profile View Channel routes to `/channel/[userId]`; Studio Preview Channel routes to `/channel/[ownUserId]`. Public Channel shows public creator videos/events/about only and does not show owner controls, drafts, private, or unpublished videos to non-owners. Public Channel perfection polish at `4873f08` adds safe-area-aware header/bottom spacing and keeps the required Followers / Videos / Events stat row visible without inventing fake follower counts when the follower-count query is unavailable. Profile backed identity now wins over route display params once loaded, so public profile viewing cannot keep stale/spoofed deep-link identity copy after the profile row resolves. Profile has backed Posts/status updates, Channel entry, and clear owner/public top actions. Profile posts are separate from Channel creator videos. Profile Posts now use "Post" copy, keyboard-aware composer scrolling, backed comments/replies, backed single likes/counts, route-safe share, clickable links, optional 250 MB social attachments, owner/post-owner deletes where backed, and no fake counts. Remote schema/RLS proof confirms `profile_posts` exists through `202604290001`; the Profile engagement and social attachment/reply migrations are local/pending remote application. Runtime proof for the earlier public Channel visual correction passed on Android owner and non-owner channels; the later safe-area/stat polish passed source/typecheck/diff-check proof and still needs manual device proof. | Android proof still needed for Profile post create/delete/public clean reads/report UI, Profile post comments/replies/likes/share/links/attachments, Android keyboard visibility, upload-submit proof for the Profile creator-video composer, remaining draft hiding/public empty-state proof, public Follow/Following action mutation proof, latest Public Channel safe-area/stat polish visual proof, and full role-matrix visual proof with safe test data. Deep threaded replies, automatic link previews, richer reactions, malware-scanning claims, full Friends beyond Chi'lly Circle V1, and subscriber-only media remain later. | V1 required | Preserve `/channel/[userId]`, `/channel-studio`, and `/channel-settings`; apply/prove social migrations remotely before Profile engagement proof. |
| Creator Media | Implemented / Proof Pending | `app/channel-studio/index.tsx`, `app/channel-settings.tsx`, `app/channel/[userId].tsx`, `app/profile/[userId].tsx`, `app/player/[id].tsx`, `_lib/creatorVideos.ts`, `_lib/creatorVideoLinks.ts`, `_lib/creatorVideoComments.ts`, `components/creator-media/creator-video-card.tsx`, `supabase/migrations/202604280001_raise_creator_video_movie_upload_limit.sql`, `supabase/migrations/202604290001_public_v1_social_basics.sql` | Upload, metadata save, draft/public visibility model, owner management, public Channel listing, standalone Player handoff, modern thumbnail/fallback creator-video cards, and text-only creator-video comments exist. Owner Profile composer and Channel Studio management both use the backed `_lib/creatorVideos.ts` upload/storage/metadata path. Creator videos live in public Channel media sections rather than the personal Posts lane. Channel Studio has creator-video load retry, clearer picker cancel/unsupported-file copy, safer upload/manage failure notices, and owner Content visibility fixes. Creator-video uploads verify that non-empty selected files create non-empty Storage objects before metadata is accepted. Public V1 Hardening H2 upload/content lifecycle polish is pushed: Channel Studio Content shows one clear `Video Upload` form, honest local upload lifecycle states for no file selected, file selected, ready to upload, uploading, upload succeeded, upload failed, saving metadata, and editing metadata; it does not fake percent progress when the upload API does not report it. The H2 duplicate-upload UI correction removed extra header/empty-state upload buttons and keeps `Upload Status` inline inside the single form. Creator-video cards show backed owner `Published`/`Draft` and `Media Ready`/`Media Unavailable` from existing source availability. The false 50 MiB app-side cap is removed; the current channel movie upload intent is 5 GiB with honest storage-limit copy, and the `creator-videos` bucket row is applied/proved at 5 GiB. Public clean/reported creator videos can be route-shared without exposing signed Storage URLs. Remote schema/RLS proof confirms authenticated users can create/delete their own text comments on public clean creator videos, anon can read public clean comments, anon cannot insert/delete comments, hidden comment insert is blocked, and `creator_video_comment` reports insert. One-device owner proof confirms owner surfaces and public Channel render modern no-thumbnail/fallback states with owner controls only in owner surfaces. H2 current-build Android no-mutation proof confirmed `/channel-studio?tab=content` opens for a disposable signed-in proof account, summary cards and empty Creator Library render, and the upload panel shows `Upload Status`, `No File Selected`, honest no-file guidance, `Choose Video File`, title/description fields, and no fake processing/transcoding/archive/retry states. | Public/draft visibility, Android text comment create/read/delete/report UI proof, Profile composer picker/upload runtime proof with the original 54.7 MB `Chillywoodtest.mp4`, proving the separate Supabase Storage global limit is not capped at 50 MB, edit, publish/unpublish, delete/unpublish confirmation proof with safe media, non-owner denial, Storage API delete/remove proof, repair/re-upload for the zero-byte current proof video, and thumbnail-present card proof if data exists. | V1 required | Prove creator-video comments and reports on Android, confirm global Storage limit, retry uncompressed `Chillywoodtest.mp4`, then continue focused Android public/draft, safe publish/unpublish/delete confirmation, non-owner, and Supabase/Storage proof. |
| Player | Implemented / Proof Pending | `app/player/[id].tsx`, `_lib/mediaSources.ts`, `_lib/watchPartyContentSources.ts`, `_lib/creatorVideoLinks.ts`, `_lib/creatorVideoComments.ts`, `_lib/socialAttachments.ts` | Platform titles and creator videos share the premium standalone Player shell. Missing platform ids and missing creator sources show honest unavailable states. Creator videos now require explicit `source=creator-video`; bare `/player/[id]` no longer resolves creator videos by fallback. Direct creator-video Player reads now enforce public-or-owner visibility in `_lib/creatorVideos.ts`, so drafts/private/unpublished videos do not resolve for non-owners even if broader moderation/admin RLS can read them elsewhere. One-device proof confirms valid `/player/t1`, invalid platform id, explicit creator-video route ownership, invalid creator-video source, and bare creator-video id denial. Creator-video playback hides title-only save/like/share relationship controls, keeps backed Report, exposes route-safe Share only for public shareable creator videos, shows a Discussion panel for creator videos only, supports comments/replies, clickable links, and optional 250 MB social attachments when the local migration is applied, and now shows repair/re-upload guidance when a standalone source fails to load from storage. Remote RLS proof for creator-video comment create/read/delete/report passed through PostgREST. | Playable source sanity for the current creator video is blocked by a zero-byte Storage object; after repair/re-upload, rerun valid creator-video playback and confirm no sample/platform fallback. Creator-video comment/reply/link/attachment Android UI proof and explicit non-owner draft/private deep-link denial proof are pending. | V1 required | Prove creator-video comments/replies/links/attachments in Android Player, then repair/re-upload current creator media before marking valid creator-video playback passed. |
| Watch-Party platform titles | Implemented / Proof Pending | `app/watch-party/index.tsx`, `app/watch-party/[partyId].tsx`, `_lib/watchParty.ts`, `_lib/premiumWatchPartyAccess.ts`, `components/live/live-effects-sheet.tsx`, `docs/LIVE_WATCH_PARTY_LAYOUT_LOCK.md` | Platform/admin title Watch-Party uses normal waiting room and content-first Party Room, not Live Stage. The Party Room keeps a shared source card at the top, a host/viewer feed grid below it with five bubbles across and two visible rows before scroll, clearer Party Screen/Party Feeds language, and a compact Player/Invite/Report/Leave action dock. Leave is confirmed and records the user as muted/camera-off before returning. The current visible comments placement is layout-locked and must not be replaced by menu-only comments. Chi’llyfects UI is branded and foundation-only. Route/source hardening blocks no-source Party Room creation and invalid platform-title fallback. Full Watch-Party Live access is Premium and free users are blocked before room/session/token/connect. | Final valid Premium platform room smoke, invalid title smoke, no-source waiting room smoke, comments send/read smoke, layout proof, leave/rejoin smoke, and Chi’llyfects foundation proof. Real entitlement-backed Premium account proof remains pending. No 10-participant runtime proof is claimed. | V1 required | Prove valid Premium paths with an entitlement-backed Premium account; preserve the layout lock and keep Party Room separate from Live Stage. |
| Creator-video Watch-Party | Implemented / Proof Pending | `app/player/[id].tsx`, `app/watch-party/index.tsx`, `app/watch-party/[partyId].tsx`, `_lib/watchParty.ts`, `_lib/watchPartyContentSources.ts`, `docs/LIVE_LAYOUT_AND_EFFECTS_SYSTEM.md`, `docs/LIVE_WATCH_PARTY_LAYOUT_LOCK.md` | Creator video Watch-Party linking is built; one-device Android proof passed for normal Party flow, creator-video source, and no Live Stage/sample/platform fallback. Creator-video rooms share the same content-first Party Room layout as platform titles, with the source card above the five-across/two-visible-row host/viewer feed grid plus the Player/Invite/Report/Leave Party Room action dock. The current visible comments placement is layout-locked and must not be replaced by menu-only comments. Player Watch-Party Live now rejects live room ids and preserves source context on waiting-room fallback. | Two-device join/rejoin, draft/private blocked state, hidden/removed blocked state, signed-out/non-premium blocked state, cross-surface direct-open smoke, comments smoke, leave/rejoin smoke, and layout proof. No 10-participant runtime proof is claimed. | V1 if proof completes | Run two-device creator-video Watch-Party proof later; preserve the layout lock and keep Player controls unchanged. |
| Live Stage / LiveKit | Implemented / Proof Pending | `app/watch-party/live-stage/[partyId].tsx`, `components/watch-party-live/livekit-stage-media-surface.tsx`, `_lib/liveEffects.ts`, `_lib/premiumWatchPartyAccess.ts`, `components/live/live-effects-sheet.tsx`, `docs/LIVEKIT_PRODUCTION_READINESS_RUNBOOK.md`, `docs/LIVE_LAYOUT_AND_EFFECTS_SYSTEM.md`, `docs/LIVE_WATCH_PARTY_LAYOUT_LOCK.md`, `docs/CHILLYFECTS_SNAP_CAMERA_KIT_AUDIT.md` | Current main preserves two-device Live First and Live Watch-Party proof; Live Stage remains separate from title/content Party Room and now rejects non-live Party Room ids. Live Stage role/access hardening is pushed: mode switching is host-only, route-param hybrid/live-watch attempts cannot let non-hosts mutate mode, Live First viewers cannot self-promote into camera/speaker seats on stage entry, and `setPartyParticipantState` requires the authenticated room host. Full Live First and Live Watch-Party access is Premium. Free users are blocked before full room/session/token/connect, no full LiveKit room/token/connect access is granted, and no free preview mode was added. The deployed `livekit-token` function has redacted remote proof that valid live-stage and watch-party-live token requests succeed while cross-surface mismatches return `409 room_surface_mismatch`; remote-proved registry/router/drain mode now routes new RTC tokens through `chillywood-prod-01` only when the server is active, fresh-heartbeating, and under capacity, while existing assignments stay pinned and draining blocks only new assignments. May 13 production proof passed DNS/TLS/WebSocket reachability, unauthenticated token/admin denial, secret-name-only Supabase checks, and non-Premium Live Watch-Party gate proof; D7F spectator safety remained unchanged with no spectator LiveKit token and no raw HLS URL returned. The layout foundation makes Live Stage people-first, keeps Live First free of the Chi'lly Party Members box, keeps the Chi'lly Party Members remote-feed grid in Live Watch-Party only, excludes the current user's own preview from that grid, keeps real other host/viewer feeds visible there on viewer devices, shows other live feeds three across/two visible rows before scroll, and adds honest Chi’llyfects catalog UI. The current visible comments placement is layout-locked and must not be replaced by menu-only comments. Approved overlay behavior is locked: entry from Live Room and host mode switches arm the same 10-second overlay/comments auto-hide timer, while Lock Controls keeps it visible. Stream/media fixes must not change the locked UI. `Off` is the only real active Chi’llyfect/camera state; no outgoing camera processing exists. Snap Camera Kit audit completed as post-v1 planning input only. Public v1 must not add Snap Camera Kit, DeepAR, MediaPipe processing, custom WebRTC frame processors, native AR SDKs, or real AR processing. | TURN/firewall allocation proof, cellular/Wi-Fi media, sustained two-phone Premium live/watch retention, audio, release log proof, layout proof, request-to-join proof, remote-feed grid proof, overlay auto-hide proof, Premium app-route token success, 10-participant LiveKit proof, and Chi’llyfects foundation proof. Real entitlement-backed Premium account proof remains pending; May 13 connected devices had no active Premium entitlement, so media join and 10-participant readiness were not claimed. Any real AR / processed-output proof is post-v1. | V1 required for live launch; real AR is post-v1 | Follow `docs/LIVE_WATCH_PARTY_LAYOUT_LOCK.md`, `docs/LIVE_LAYOUT_AND_EFFECTS_SYSTEM.md`, and `docs/LIVEKIT_PRODUCTION_READINESS_RUNBOOK.md`, then use a bounded two-phone Premium-account proof runner plus TURN allocation proof. Do not claim 10-participant readiness until real devices or a controlled synthetic/load equivalent proves it. Do not start a Snap/AR POC before Public v1. |
| Chat | Done | `app/chat/index.tsx`, `app/chat/[threadId].tsx`, `app/communication/index.tsx`, `app/communication/[roomId].tsx`, `_lib/chat.ts`, `_lib/socialAttachments.ts`, `supabase/migrations/202605130002_chat_call_thread_membership_guard.sql`, `supabase/migrations/202605130003_chat_realtime_publication.sql`, `docs/chilly-chat-final-behavior-proof.md` | Native Chi'lly Chat owns inbox/thread behavior: `/chat` owns the canonical inbox and `/chat/[threadId]` owns canonical threads. `/communication/index.tsx` and `/communication/[roomId].tsx` are compatibility-only; `/communication/[roomId]` resolves active communication room ids to canonical Chat threads when possible, redirects mapped rooms to `/chat/[threadId]?openCall=1`, and redirects unmapped rooms safely to `/chat`. `openCall=1` may open an existing active call panel only and must not start a new call. Chat privacy/member checks remain canonical. Signed-out inbox/thread states provide Sign In handoffs. Thread messages render clickable links and can attach one selected file/image up to 250 MB through private `social_attachments`; chat attachments are readable only to thread participants. May 13 physical Android proof passed signed-in inbox/send/read persistence, post-migration realtime UI delivery without duplicate rows, signed-out physical-device auth handoff for inbox/thread/call-compatible deep links, non-member thread denial, attachment recipient read/privacy, regular voice-call connect/audio/hangup, video-call connect/remote media/camera toggle/mic toggle/hangup, LiveKit registry compatibility for `chat-call`, and invalid `chat-call` safe failure. A proof-blocking call privacy gap was fixed by requiring chat-thread membership before joining chat-attached communication rooms, chat realtime tables are now added to the Supabase Realtime publication when present, and Realtime broadcast/untrack waits in call-session cleanup are bounded so hangup cleanup is not held by a stale Realtime operation. | No Chat proof blocker remains. Continue normal release smoke later; the current known unrelated validation warning is RevenueCat Android billing setup. | V1 required | Preserve the existing Chat design and rerun scoped smoke only if Chat/auth/LiveKit routing changes. |
| Premium / Access Gates | Implemented / Proof Pending | `_lib/monetization.ts`, `_lib/premiumEntitlements.ts`, `_lib/premiumWatchPartyAccess.ts`, `_lib/roomRules.ts`, `_lib/watchParty.ts`, components access sheet, `app/subscribe.tsx`, `app/settings.tsx`, `app/(tabs)/index.tsx`, `app/watch-party/index.tsx`, `app/watch-party/[partyId].tsx`, `app/watch-party/live-stage/[partyId].tsx`, `app/title/[id].tsx`, `app/player/[id].tsx` | Backend entitlement model exists; route/access checks do not rely on local-only Premium rows; `/subscribe` and Settings now provide public-facing account-owned status/purchase/restore copy that stays locked when store setup is unavailable. RevenueCat Premium offering resolution prefers the configured `premium` offering and can fall back for `premium_subscription` only to a RevenueCat current/default/first offering with purchasable packages; entitlement truth still controls access and no fake product/price/local Premium unlock exists. All full Live First, Live Watch-Party, and Watch-Party Live access is Premium. Helper exports include `canUseLiveFirst`, `canUseLiveWatchParty`, `canUseWatchPartyLive`, `requireLiveFirstPremium`, `requireLiveWatchPartyPremium`, and `requireWatchPartyLivePremium`. Strict live/watch-party gates ignore dev Premium bypass behavior where needed so free-user blocking can be proved. Free-user runtime proof showed Home live entry displays the Premium sheet and does not route into `/watch-party`, generate a room code, or request/connect LiveKit; direct `/watch-party?mode=live` is blocked with the same gate. No free preview mode, 60/120-second preview, low-quality preview token, or read-only live preview was added. | Store product configuration, real purchase path, restore path, expired/revoked proof, release-like signed-out/deep-link proof, and real entitlement-backed Premium account proof. Existing Premium paths were preserved by implementation and need proof with a real Premium account when available. | V1 required for Premium features | Run store setup/proof and real Premium account live/watch-party proof later. |
| Billing / Subscription Entitlement | External Setup Pending | `_lib/revenuecat.ts`, `_lib/monetization.ts`, `_lib/premiumEntitlements.ts`, `app/subscribe.tsx`, billing migrations | RevenueCat owner, subscription target, restore/manage calls, backend entitlement table, and safe Premium offering fallback exist. UI blocks when store/offers are unavailable and does not grant Premium locally. | RevenueCat product IDs, store credentials, real purchase validation, restore validation, refund/revocation proof. | V1 required if Premium gates ship | Follow the Lane 1 runbook in `docs/EXTERNAL_SETUP_PUBLIC_V1_CHECKLIST.md`, then run store proof. |
| Ads Launch Foundation | Implemented / Proof Pending | `_lib/ads/adConfig.ts`, `_lib/ads/adEligibility.ts`, `_lib/ads/adProvider.ts`, `_lib/ads/providers/placeholder.ts`, `_lib/ads/adSession.ts`, `hooks/useAdEligibility.ts`, `hooks/useActiveBrowsingTime.ts`, `components/ads/NativeAdSlot.tsx`, `components/ads/InterstitialController.tsx`, `app/(tabs)/index.tsx`, `app/_layout.tsx`, `app/admin.tsx` | Ads Launch Foundation V1A/V1B/V1C is pushed as provider-neutral foundation only. Central ad config defaults exist with `ads_enabled: false` and `ads_provider: placeholder`; the placeholder provider reports not connected and calls no SDK; Premium/ad-free users are ineligible and do not increment counters; forbidden routes/contexts block eligibility; active browsing time tracking, session caps, daily caps, and AsyncStorage daily persistence exist. V1B adds one Home native/feed placeholder foundation through `NativeAdSlot`, but default runtime keeps it hidden because `ads_enabled=false`. V1C adds a null-rendering placeholder interstitial controller in the root layout, but default runtime keeps it hidden because `ads_enabled=false` and the placeholder provider is not connected. V1C ignores first route mount, considers route transitions only, calls central eligibility with `placementKind: "interstitial"`, records placeholder interstitial shows only after eligibility and placeholder-provider success, and respects 180-second first delay, 600-second spacing, session cap 3 plus long-use +2, daily cap 6, and forbidden routes/contexts. Admin Ads remains read-only/foundation. No AppLovin, Unity LevelPlay, Unity Ads, or AdMob SDK was installed; no real ad IDs, provider initialization, real rendering, CTV inventory, fake ad revenue, sponsor revenue, creator earnings, payout balances, invoices, or CTV revenue were added. | Real AppLovin MAX integration later after external setup. Final release smoke must confirm no ads in LiveKit rooms, playback, typing/commenting, upload, subscribe/payment, immediate app launch, Admin, Channel Studio, Chat, or Profile/composer contexts. | V1 foundation / Post-v1 integration later | Do not integrate real ads until AppLovin MAX external setup is ready; keep provider wrapper architecture and no AdMob-only path. |
| Moderation / Safety | Implemented / Proof Pending | `_lib/moderation.ts`, `components/safety/report-sheet.tsx`, `app/profile/[userId].tsx`, `app/player/[id].tsx`, `app/admin.tsx`, `supabase/migrations/202604290001_public_v1_social_basics.sql`, `supabase/migrations/202604290002_profile_post_engagement.sql`, `supabase/migrations/202604290003_social_replies_links_attachments.sql` | Creator-video reports, profile-post reports, profile-post-comment reports, creator-video-comment reports, local/pending-remote social-attachment report target, reason picker, admin queue visibility, hide/remove/restore actions, and moderation status filters exist. Public V1 Hardening H3 is pushed: the Report Sheet now explains the existing backed categories (`abuse`, `harassment`, `impersonation`, `copyright`, `safety`, `other`) without adding new schema categories; scam/fraud/unsafe product or ad concerns route through `Safety`; undisclosed sponsorship routes through `Other` until a later schema pass. Remote PostgREST proof confirms `safety_reports` accepts `profile_post`, `creator_video_comment`, and existing `creator_video` target types through `202604290001`; `profile_post_comment` is local/pending remote application in `202604290002`, and `social_attachment` is local/pending remote application in `202604290003`. Current-build H3 proof opens a backed title Report Sheet and confirms backed category chips plus honest later-schema copy. Admin role-alignment proof confirms signed-out denial and backend non-operator/local-helper denial with no destructive creator-video controls. H3 proof also confirmed a non-admin account is denied from `/admin`. Temporary-operator proof confirms backend owner/operator moderation can hide a safe creator video, Player blocks it while hidden, Restore Clean returns it to allowed status, and cleanup removes the temporary role and restores denial. Admin UI requires confirmation for Hide/Remove/Restore and a safety reason for Hide/Remove. | Android report-sheet submission proof for creator_video/profile_post/profile_post_comment/creator_video_comment, social attachment report/moderation workflow proof if UI is added, hidden/removed public Profile/Channel/comment/attachment proof, release-like Admin Command Center smoke, and longer report-queue workflow proof. No malware scanning claim exists yet. | V1 required | Run UGC report submission proof with safe test content; keep any admin action proof scoped to safe test content and backend role truth. |
| Admin / Operator | Implemented / Proof Pending | `app/admin.tsx`, `_lib/moderation.ts`, `_lib/officialAccounts.ts`, platform-role tables | Admin Command Center V1A is pushed on canonical `/admin`; do not create `/admin-command-center`. Admin remains separate from Channel Studio, Profile Settings, Public Channel, Chi'lly Circle, and future Room Control. Access remains signed-in plus beta/platform-role/backend protected, with route access, report visibility, and privileged-write boundaries preserved and no production admin bypass. Admin Home shows Platform Snapshot and Needs Attention. Backed sections preserve Reports, Content, Roles, Audit, and Rachi. New/foundation sections are Home, Reports, Content, Roles, Audit, Rachi, Users, Premium, Kill Switches, Usage, Ads, Revenue, Payouts, Networks, Sponsors, Fraud, and System. Users search is not connected; Premium is read-only and RevenueCat remains truth; Kill Switches are foundation rows; Usage uses DB estimates where backed and says bandwidth/LiveKit/participant-minute metering is not connected; Ads records AppLovin MAX direction and launch caps without SDKs/ad IDs/provider initialization/rendering; Revenue/Payouts/Networks/Sponsors/Fraud are foundation-only with no fake money, payouts, invoices, sponsor checkout, network billing, fraud holds, balances, or risk scores. Admin V1B1 typed runtime controls config foundation is pushed; Admin V1B2A marks New Accounts read-only `Enforced on signup`; Admin V1B2B marks Uploads read-only `Enforced on upload` for new creator-video upload submit only; Admin V1B2C marks Comments read-only `Enforced on comments` for backed Profile post and creator-video comment submit only; Admin V1B2D marks Profile Posting read-only `Enforced on profile posts` for backed Profile post creation submit only; Admin V1B2E marks Creator Posting read-only `Enforced on creator events` for new creator event creation only; Admin V1B2F marks Attachments read-only `Enforced on social attachments` for selected non-chat social attachments only; Admin V1B2G/V1B2H mark Chat Attachments read-only `Enforced on chat and room attachments` for selected standalone Chi'lly Chat and room attachments only; Admin V1B2I-A/V1B2I-B/V1B2I-C mark Chat read-only `Enforced on chat, invites, and room comments` for standalone Chi'lly Chat writes/call starts/profile-entry/starter-thread, room invite direct-message sends, and Watch-Party/Live Stage room-native text/comment submits only. Admin V1B closeout records `live_first_enabled`, `live_watch_party_enabled`, `watch_party_live_enabled`, and `max_upload_size_mb` as foundation-only; Admin `runtimeControls.ads_enabled` as separate from Ads Launch config; and `premium_required_for_live` / `premium_required_for_watch_party` as Premium helper truth rather than runtime switches. No working Admin kill-switch toggles or config-write controls were added. Rachi is managed as official public platform presence only, not Admin authority. Android runtime proof confirmed signed-out denial, admin/operator access through backend membership, all Admin V1A tabs opening, foundation honesty, no fake money/action systems, and no forbidden-surface file changes. | Release-like admin smoke, a separate non-admin denial proof with a known non-admin account, longer report-queue workflow proof, and optional false-config runtime proof for `uploads_enabled`, `comments_enabled`, `profile_posting_enabled`, `creator_posting_enabled`, `attachments_enabled`, `chat_attachments_enabled`, and `chat_enabled` with safe accounts/media/rooms. Any future live/ads/upload-size/Premium-required enforcement requires a separate source-of-truth and app-surface proof. Test admin credentials may remain active for future proof sessions but must never be stored, printed, logged, hard-coded, or committed. | V1 required | Keep final admin smoke bounded, protect secrets, preserve backend role truth, and keep Admin write controls out unless separately scoped. |
| Supabase / RLS | Implemented / Proof Pending | Supabase migrations, `supabase/database.types.ts`, `_lib/supabase.ts`, `docs/SUPABASE_REMOTE_PUBLIC_V1_RUNBOOK.md` | Local and remote migration history is aligned through `202605070002`. H1B2 applied `202605070001_user_account_legal_acceptances.sql` and `202605070002_harden_user_account_legal_acceptance_grants.sql`; generated database types include `user_account_legal_acceptances`; anon REST reads to that table return permission denied; authenticated access remains owner-only through RLS. Remote schema dump confirms the `202604290001` tables, RLS, policies, indexes, the creator-video comment foreign key, and the widened `safety_reports_target_type_check`. Sanitized PostgREST proof confirms authenticated own post/comment create-delete, anon public clean reads, anon write/delete denial, draft/hidden filtering, and safety report inserts for `profile_post`, `creator_video_comment`, and existing `creator_video`. The `creator-videos` bucket row is private with `file_size_limit = 5368709120` and the expected video MIME allowlist. `livekit-token` is deployed ACTIVE version 8, and redacted remote function proof passed for valid live-stage/watch-party-live token issuance plus invalid surface mismatch rejection. | Android runtime proof is still needed for Profile posts, Profile post comments/replies/likes/share/links/attachments, following feed, creator-video comments/replies/attachments, Chat attachments, and report-sheet flows. Full live Supabase proof is still needed for separate Storage global limit, legal acceptance insert with a real safe signup account, public/draft/non-owner creator media, Storage API delete/remove, admin writes, entitlement states, access gates, and Android app-route proof against the deployed token function. | V1 required | Run safe signup legal-acceptance insert proof and remaining focused Supabase/API proof; keep `supabase/.temp/` uncommitted. |
| Settings / Account | Implemented / Proof Pending | `app/settings.tsx`, `app/account-deletion.tsx`, `app/support.tsx`, `app/creator-rules.tsx` | Settings links to Profile, Channel Settings, Premium, legal/support, account deletion, logout, and direct legal/support entries. The Legal & Support card now includes Privacy Policy, Terms of Use, Community Guidelines, Creator Rules, Copyright/DMCA, Support, and Request Account Deletion. Account deletion is an honest request/help path, not fake deletion completion. | Android proof for logout, Creator Rules route smoke, and account-deletion support request flow. | V1 required | Include remaining account actions and legal links in final route smoke. |
| Legal / Support / Content Policy | Apex legal URLs live / DMCA agent registered / DMCA tooling added / Inbox Receipt Proof Pending / Attorney Review Pending / Play Acceptance Pending | `app/privacy.tsx`, `app/terms.tsx`, `app/account-deletion.tsx`, `app/community-guidelines.tsx`, `app/copyright.tsx`, `app/copyright-report.tsx`, `app/creator-rules.tsx`, `components/safety/report-sheet.tsx`, `components/system/support-screen.tsx`, `_lib/accountLegalAcceptance.ts`, `_lib/dmca.ts`, `supabase/migrations/202605140001_dmca_operational_tooling.sql`, `docs/legal/*`, `docs/ACCOUNT_LEGAL_DATA_SAFETY_RUNBOOK.md`, `public-site/legal-site/*` | Bundled Privacy, Terms, Account Deletion, Community Guidelines, Copyright/DMCA, Creator Rules, Support, and formal Copyright Report pages exist; Settings and Support link to legal pages, and generic copyright reports route users to the formal copyright report form. Signup links Terms/Privacy/Guidelines before account creation, H1A adds explicit 18+ signup confirmation, and H1B2 wires private legal acceptance storage for authenticated signup sessions. `docs/legal/` contains the drafted policy set. `public-site/legal-site/` generates the full static surface; `https://chillywood-legal.pages.dev/*` is live/proved for all required paths, and `chillywoodstream.com` is active on Cloudflare Pages. `https://chillywoodstream.com/` returns HTTP/2 200; `https://chillywoodstream.com/terms`, `/privacy`, `/account-deletion`, `/copyright`, and `/support` redirect with HTTP/2 308 to trailing-slash paths and then return HTTP/2 200 with real static legal/support content. No LiveKit fallback was reached; Privacy mentions LiveKit only as policy text. `https://chillywoodstream.com/account-deletion` is now the live Google Play account deletion web URL candidate, and `https://chillywoodstream.com/support` is the live public support URL. DMCA designated agent registration is recorded complete under `DMCA-1072720`, the public copyright page lists the designated agent contact, and the backed DMCA migration adds case/counter-notice/content-action/strike/audit tables plus owner/operator RPCs. Public copy does not claim attorney review completion, Google Play deletion compliance, live payouts, sponsor payments, or refund systems beyond store/provider rules. Public support contact is `support@chillywoodstream.com`; Cloudflare Email Routing is configured but receipt proof remains pending. | Attorney/legal approval, app URL env update if product wants Settings to prefer apex URLs, `support@chillywoodstream.com` inbox receipt proof, Play Store policy URL proof, Play Data Safety/account deletion entry acceptance, safe disposable-account H1B2 write proof, release-route proof, support feedback queue proof, live end-to-end DMCA proof with safe test data/admin-uploader-reporter accounts, uploader-facing counter-notice route, and outbound email automation if required before launch. | V1 required | Enter the live account deletion URL in Play Console and wait for acceptance, send a real support test email, complete attorney review separately, and run safe DMCA end-to-end proof. Keep the DMCA agent registration current and do not claim Google Play account deletion compliance until Play acceptance is verified. |
| Home / Discovery | Implemented / Runtime Proof Pending | `app/(tabs)/index.tsx`, `app/(tabs)/explore.tsx`, `app/(tabs)/my-list.tsx`, `app/channel/[userId].tsx`, `app/spectate/[itemId].tsx`, `_lib/channelAudience.ts`, `_lib/creatorVideos.ts`, `_lib/liveEvents.ts`, `_lib/discoveryFeed.ts`, `_lib/spectatorAccess.ts`, `_lib/spectatorPlayback.ts`, `_lib/notifications.ts`, `supabase/migrations/202605090001_discovery_feed_foundation.sql`, `supabase/migrations/202605090002_discovery_feed_user_safe_read.sql`, `supabase/migrations/202605090003_room_broadcast_sessions_foundation.sql`, `supabase/migrations/202605090004_room_broadcast_sessions_d7d_test_transition.sql`, `supabase/functions/spectator-broadcast-start/index.ts`, `supabase/functions/spectator-broadcast-stop/index.ts`, `supabase/functions/spectator-broadcast-status/index.ts` | Discovery Feed Foundation D2, D3-D10 foundation, D7B broadcast-session schema foundation, D7C backend functions, D7D private test transition, D7E Hetzner delivery proof, D7F controlled public-safe spectator playback gate, and D7G metadata-only ad policy labels are pushed/proved where scoped. Home D3 renders real public-safe rails with honest empty states; Explore filters already-loaded platform `titles` rows client-side with honest no-match/empty states and no fake search rows; Public Channel D4 separates Live Now and Upcoming Events; D5 Profile live/activity remains backed/privacy-gated; D6 `/spectate/[itemId]` is metadata-first and renders watch-only HLS only when the controlled `spectator-playback` resolver returns `available` for an eligible public-safe record. D8 adds spectator eligibility classification; D9 now has real production notification/activity Android proof; D10 ranking helpers use real signals only. D7D permits real Egress ids/private HLS playlist locations only for private `D7D_TEST_` rows with `metadata.d7d_test_proof = true`; D7F permits server/admin-approved public-safe rows through `spectator_hls_playback_records` only. D7E public playlist/segment fetch proof passed outside LiveKit, and D7F runtime proof passed with controlled playlist/segment proxy `200` results, blocked-state proof, no raw Hetzner URL to normal app users, no full spectator LiveKit token, `PUBLIC_HLS_BASE_URL` unset, and temporary proof rows/access cleaned up. No protected/title/private/ticketed public spectator video, fake feed item, fake trending, fake viewer count, fake spectator count, fake HLS URL, fake Egress id, fake notification completion, or full-room spectator token exists. | Runtime smoke for Home rails, Channel shelves, Profile live/activity, and `/spectate/[itemId]` with real future public-safe events; blocked/private/protected feed-row proof with safe real rows; following/Circle privacy proof with real relationships; and broader discovery route proof. Global creator-video search/recommendations remain later. | V1 required, global creator search later | Keep discovery source-backed only. Do not add fake search, fake trends, fake counts, fake HLS URLs, fake Egress ids, public protected-content playback, full-room spectator tokens, fake notification completion, or public playback outside D7F-approved public-safe records. |
| Search | Partial Explore Title Filter Current / Full Search Later | No dedicated full-search route | Explore provides browse plus backed client-side filtering for platform title rows already loaded by Explore. | Full user/title/channel/video search is not implemented. | Post-v1 unless required | Do not add fake global search, fake recommendations, or fake result rows before source truth is designed. |
| Notifications / Reminders | Implemented / Android Production Proof Passed | `_lib/notifications.ts`, `app/settings.tsx`, `app/_layout.tsx`, `app.config.ts`, `app.json`, `supabase/migrations/202605120003_d9_notifications_activity_production.sql`, `supabase/functions/notification-device-tokens/index.ts`, `supabase/functions/notification-dispatch/index.ts`, `notifications`, `notification_preferences`, `user_push_tokens`, `notification_delivery_attempts`, `notification_event_dedupes`, `creator_events`, `event_reminders` | D9 is production-complete for Android, not foundation-only. Android notification permission UX, Settings notification preferences, Activity list/read/dismiss/deep-link handling, authenticated device-token registration/revocation/status, server-side dispatch, event dedupe, delivery-attempt logging, and five trigger classes are wired: followed creator went live, Circle friend went live, event starts soon, public upload, and replay later. Remote migration `202605120003` is applied; `notification-device-tokens` and `notification-dispatch` are deployed; Expo/EAS FCM V1 is assigned for `com.chillywood.mobile`; physical Android device `R5CR120QCBF` registered a real Expo Android token with safe fingerprint `c1ab24247f5d`; all five trigger classes produced backed activity rows and `sent` delivery attempts with Expo provider message ids present; a fresh background push appeared under Alerting notifications and tapping it opened `/spectate/[itemId]` safely. `notification-dispatch` targets the app-created Android `default` notification channel so pushes use Chi'llywood's high-importance Activity channel. Private/proof/non-public/protected/title-rights-blocked/ticketed/Premium-full-room, duplicate, non-follower, non-Circle, blocked-user, disabled/failed/missing-target, and preference-ineligible states stayed silent, skipped, or blocked. Raw tokens are not shown in UI/docs/logs and provider secrets are not committed. | iOS/APNs remains later unless separately scoped. Continue normal release-build notification smoke during Android release candidate testing, but D9 Android provider receipt/tap proof is no longer an external blocker. | V1 required / Android passed | Do not add mock push, fake notification completion, raw token logs, client-side service credentials, or private/ineligible notification leakage. |
| Account Deletion | Live URL Candidate / Play Acceptance Pending | `app/account-deletion.tsx`, `app/settings.tsx`, support flow, `docs/ACCOUNT_LEGAL_DATA_SAFETY_RUNBOOK.md`, `public-site/legal-site/*` | Public information page and support handoff exist; Settings links to the deletion page or configured destination. Deletion is request-based and now documents Profile, Channel, uploaded videos, chat, room, billing/subscription, and moderation/report record handling. `https://chillywoodstream.com/account-deletion` redirects with HTTP/2 308 to `/account-deletion/` and then returns HTTP/2 200 with real static account-deletion content; no LiveKit fallback was reached. | Backend deletion/de-identification and retention runbook approval, support process/SLA confirmation, and Play Console account deletion URL acceptance. | V1 required | Enter `https://chillywoodstream.com/account-deletion` in Play Console when ready, confirm legal/support/backend retention process, and do not claim Google Play account deletion compliance until Play acceptance is verified. |
| Support Feedback | Implemented / Proof Pending | `components/system/support-screen.tsx`, `_lib/betaProgram.tsx` | Signed-in support feedback route exists; signed-out users get sign-in prompt. | Proof that feedback lands in expected backend queue for public-v1 environment. | V1 required | Run support feedback proof later. |
| Android Release Build | External Setup Pending | `app.json`, `app.config.ts`, `eas.json`, package scripts, `docs/ANDROID_RELEASE_EAS_RUNBOOK.md`, `docs/PRODUCTION_ENV_SECRETS_RUNBOOK.md` | Android package, icon/splash, Firebase/RevenueCat/Supabase runtime config owners and production EAS AAB profile exist. The Android release/EAS runbook documents signing/build handoff, and the production env runbook now maps runtime env, EAS env, and secret boundaries. | EAS Android credential verification, versionCode strategy, strict production env validation, preview build, production AAB build, internal testing install/upload, release route smoke, permissions review, release log audit, Play pre-launch report, Android vitals/crash/ANR monitoring, slow/frozen-frame review, and LiveKit battery/performance observation. | V1 required | Follow `docs/PRODUCTION_ENV_SECRETS_RUNBOOK.md` before builds, then `docs/ANDROID_RELEASE_EAS_RUNBOOK.md` for preview/production build proof. |
| Store Readiness | External Setup Pending | Play Store console, runtime config, legal/support URLs, `docs/PLAY_STORE_LISTING_CONTENT_RATING_RUNBOOK.md` | App-side policy pages and Premium UX are ready to point at configured store/legal surfaces. The store listing/content-rating runbook now documents draft copy, app category guidance, asset gaps, screenshot plan, content-rating prep, Data Safety consistency, UGC policy readiness, expanded policy-page status, and signup acceptance posture. | Store listing approval, Play-ready icon export, feature graphic, screenshots, support/legal URLs, privacy declarations, content rating, target audience, UGC policy answers, attorney/legal approval, billing product setup. | V1 required | Follow `docs/PLAY_STORE_LISTING_CONTENT_RATING_RUNBOOK.md`, then complete Play Console setup and prove in release build/internal testing. |
| Firebase Crashlytics / Performance | Implemented / Proof Pending | `app.config.ts`, `firebase.json`, `google-services.json`, `_lib/firebaseCrashlytics.ts`, `_lib/firebasePerformance.ts`, `_lib/firebaseAnalytics.ts`, `_lib/firebaseRemoteConfig.ts`, `app/_layout.tsx`, `docs/FIREBASE_CRASHLYTICS_PERFORMANCE_RUNBOOK.md` | Firebase Android config, packages, config plugins, app-shell bootstrap, Crashlytics non-fatal owner, Performance bootstrap trace, and dev-only proof helpers exist. | Firebase Console proof from preview/release-like Android build, approved Crashlytics non-fatal or crash receipt, Performance dashboard receipt, Privacy/Data Safety reconciliation, and release log audit. | V1 required diagnostics | Follow `docs/FIREBASE_CRASHLYTICS_PERFORMANCE_RUNBOOK.md` after a preview/release build is available; do not force a crash without explicit approval. |
| Production Logging | Implemented / Proof Pending | `_lib/logger.ts`, `_lib/firebaseCrashlytics.ts`, `_lib/firebaseApp.ts`, `_lib/revenuecat.ts`, `_lib/reactNativeRuntime.ts`, route/player/upload logs, `app/player/[id].tsx` | Player source/Watch-Party debug lines use the dev-only logger, creator-video upload logs stay dev-only without signed URL output, Live Stage per-render debug spam was removed, Live Stage verbose lifecycle logs are default-off behind a local flag, Firebase default-app dev warnings are deduped, known dev RevenueCat Play Store `canMakePayments` SDK noise is suppressed in the dev log handler, New Architecture layout-animation no-op warnings are gated, and runtime error/Crashlytics handoff redacts common token, signed URL, API key, bearer, and JWT patterns. | Release build log audit for noisy native SDK logs, tokens, signed URLs, RevenueCat receipts, LiveKit participant tokens, React Native soft exceptions, Expo AV deprecation/migration posture, and production logging posture. | V1 required | Re-audit logs during release/preview build proof. |
| Final Smoke Test | Implemented / Proof Pending | Whole app | Route ownership is documented; high-priority route hardening is already fixed, including the 2026-04-28 no-source Party Room guard, invalid platform-title fallback removal, Live Stage non-live-room rejection, Player live-room rejection, LiveKit token surface mismatch guard, explicit creator-video Player source ownership, signup Sign In handoff, signed-out Chat Sign In handoffs, and `/communication/[roomId]` compatibility redirect ownership. Route-tree exposure cleanup is pushed: unreferenced exposed `app/data/titles.ts` and `app/lib/_supabase.ts` were removed, and active Watch-Party helpers now live outside `app/` at `_lib/watch-party/room-shared.ts` and `_lib/watch-party/waiting-room-shared.ts`. | Signed-out/signed-in/admin/premium/creator/live/chat/settings final smoke plus direct cross-surface room smoke and non-room source smoke. | V1 required | Run after remaining proof lanes and include route-tree/log warning re-audit in release/preview proof. |

## Current Non-Proof Closeout Classification

Category A, completed now without runtime proof or external setup:

- Checklist status taxonomy now uses only Done, Implemented / Proof Pending, External Setup Pending, Later Phase, and Blocked.
- Public discovery empty states no longer expose developer setup instructions.
- Player route/source and Watch-Party handoff diagnostics now use the dev-only logger instead of raw production console output.
- Home/Discovery, Notifications/Reminders, Production Logging, Android Release Build, and Final Smoke Test rows now have exact launch statuses instead of vague Partial or Proof Pending-only states.
- Non-room behavior audit completed in `docs/FULL_APP_NON_ROOM_BEHAVIOR_AUDIT.md`; signup, Chat signed-out handoffs, and Player source ownership were hardened without changing major layouts or starting monetization.
- Smoothness hardening completed in `docs/PUBLIC_V1_SMOOTHNESS_HARDENING_AUDIT.md`; Home/Explore/My List, Creator Media, Premium copy, and runtime logging redaction were tightened without runtime proof or later-phase implementation.

Category B, requires Android/runtime, two-device, live Supabase, or release proof later:

- Auth/session redirects, logout, and protected deep links.
- Profile/Channel public-vs-owner behavior and no Chi'llywood Originals/platform-title filler inside creator Channels.
- Creator Media public/draft visibility, owner/non-owner denial, thumbnail/fallback visual smoke, edit, publish/unpublish, delete, and Storage API remove.
- Player source smoke for a repaired/re-uploaded playable creator video; valid/invalid platform routes, invalid creator-video route, bare creator-video id denial, and zero-byte error copy now have one-device proof.
- Platform title Watch-Party and creator-video Watch-Party two-device join/rejoin.
- Live Stage/LiveKit sustained proof.
- Live layout/Chi’llyfects proof for people-first Live Stage, content-first Party Room, current visible comments placement, Live Stage overlay auto-hide behavior, and foundation-only Chi’llyfects UI.
- Chi'lly Chat final behavior proof is closed for inbox/thread send, post-migration realtime UI, signed-out handoff, attachment privacy, regular calls, video calls, and `chat-call` registry compatibility.
- Real entitlement-backed Premium account proof for full Live First, Live Watch-Party, and Watch-Party Live; free-user Home live and `/watch-party?mode=live` blocked-state proof is already captured.
- Admin Command Center V1A runtime proof passed for signed-out denial, admin/operator access, tab opening, foundation honesty, and route smoke. A separate non-admin denial proof with a known non-admin account is still pending, plus release-like admin smoke and longer report-queue workflow proof.
- Ads Launch Foundation V1A helper/unit proof and Admin Ads foundation proof passed. Ads V1B native/feed helper proof and current-build Android runtime proof passed for default hidden Home state, safe test eligibility, Premium no-ad behavior, caps, forbidden routes/contexts, Admin Ads read-only/foundation copy, and route smoke. Ads V1C helper proof passed for default hidden runtime, initial route mount blocking, safe free-user eligibility after 180 active browsing seconds, Premium/ad-free blocking, 600-second spacing, base/long-use/daily caps, forbidden routes, forbidden contexts, unknown context blocking, background blocking, and provider-not-connected blocking. Final release smoke is still pending.
- H1A current-build signup proof passed for 18+ unchecked blocking, checked fallback to existing signup validation, Terms/Privacy/Guidelines links, and Sign In handoff. H1B2 passed typecheck/diff-check, remote migration alignment, generated type inspection, and anon REST denial proof. Runtime signup write proof with a safe disposable account, release-build signup proof, real existing-account login proof, and Settings/support/legal route smoke remain pending.
- H2 upload/content lifecycle polish passed `npm run typecheck`, `git diff --check`, scoped forbidden-file checks, and current-build Android no-mutation proof for Channel Studio Content summary cards, empty Creator Library, upload panel, `Upload Status`, `No File Selected`, honest no-file guidance, `Choose Video File`, title/description fields, and absence of fake percent progress, processing, transcoding, archive, or retry states. The duplicate-upload UI correction was pushed after typecheck/diff-check and leaves one clear `Video Upload` form. Safe-media publish/unpublish/delete confirmation proof remains pending because the disposable proof account had no videos.
- Android release build and final route smoke.

Category C, requires store/legal/account/release setup outside this pass:

- RevenueCat product IDs, offerings, store credentials, purchase validation, restore validation, refund/revocation proof, and Play Store billing setup.
- Legal review for signup acceptance, Privacy, Terms, account deletion, Community Guidelines, Copyright/DMCA, DMCA agent/contact process, and support; public legal/support URLs are live on `chillywoodstream.com`.
- Store listing, feature graphic, screenshots, content rating, target audience, Data Safety consistency, Play acceptance of the live support URL and account-deletion URL, and UGC policy declarations.
- Release runtime environment confirmation for Supabase, LiveKit, RevenueCat, Firebase, support, and legal links, tracked in `docs/PRODUCTION_ENV_SECRETS_RUNBOOK.md`.
- LiveKit production DNS/TLS/TURN/firewall/token endpoint proof from `docs/LIVEKIT_PRODUCTION_READINESS_RUNBOOK.md`.

Category D, later phase by product decision:

- Full global search across users, channels, creator videos, and platform titles.
- Push notification delivery and a full notification center.
- Native game/video streaming.
- Real Chi’llyfects AR processing, including Snap Camera Kit, DeepAR, MediaPipe processing, custom WebRTC frame processors, and native AR SDK work.
- Paid creator videos, subscriber-only media, tips, coins, live creator payouts, tax/KYC, and earnings ledgers. The Channel Studio payout dashboard is read-only/foundation only and must not be treated as live payout availability; Stripe Connect Test-Mode Backend Functions and Provider Transfer Records Sync Foundation are backend-only proof infrastructure, not creator-facing payout availability; Admin Payouts review/batch/transfer-sync rows are foundation/draft/status-import only and cannot approve, process, create transfers, retry transfers, mark paid, send money, release payouts, or mark payouts payable.
- Real ad SDK integration, ad revenue imports, creator ad revenue share, CTV ads, and sponsor/ledger systems.
- Full Audience Role Roster, VIPs, channel moderators, creator teams, and durable co-host roles.
- Comment media upload, advanced creator studio, and automatic transcoding.

## External Setup Pending

- External setup details, dashboard/manual owners, proof requirements, and exact next actions now live in `docs/EXTERNAL_SETUP_PUBLIC_V1_CHECKLIST.md`.
- Dashboard-by-dashboard setup order, login/manual actions, secret placement, and proof gates now live in `docs/DASHBOARD_SETUP_COMMAND_CENTER.md`.
- Play Store listing assets and content rating prep now lives in `docs/PLAY_STORE_LISTING_CONTENT_RATING_RUNBOOK.md`; it is not Play Console proof.
- RevenueCat products, offerings, public SDK keys, and store-side subscription setup.
- Google Play listing metadata, policy declarations, screenshots, and Play Console entry/acceptance for the live support, privacy, terms, and account-deletion URLs.
- Final legal review of bundled policy pages and public hosted URLs.
- Release runtime environment values for Supabase, LiveKit, legal, support, RevenueCat, and Firebase; use `docs/PRODUCTION_ENV_SECRETS_RUNBOOK.md` and strict validation before builds.
- LiveKit production domain/TURN/TLS/network proof and token denial proof.
- Android EAS signing credentials, versionCode strategy, preview build, production AAB, and Play internal-testing handoff tracked in `docs/ANDROID_RELEASE_EAS_RUNBOOK.md`.

## Runtime Proof Pending

- Creator Media public/draft and owner/non-owner proof.
- Repair/re-upload the current zero-byte creator-video Storage object and rerun creator-video Player playback/source sanity.
- Confirm the separate Supabase Storage global file-size limit is not capped at 50 MB, then retry original 54.7 MB `Chillywoodtest.mp4` without compression; the `creator-videos` bucket row itself is already proved at 5 GiB.
- Creator-video thumbnail-present card proof when data exists, public/non-owner Profile/Channel proof, and remaining owner upload/manage proof.
- Profile text posts, following feed, and creator-video text comments have remote Supabase schema/RLS proof through `202604290001`; Profile post comments/likes/share are implemented with local migration `202604290002` pending remote application/proof. Android runtime proof is still pending, including Profile composer keyboard visibility. Creator-video likes/saves, media comments, nested replies, richer reactions, and fake engagement counts remain later lanes.
- Creator-video two-device Watch-Party join/rejoin proof.
- Draft/private and hidden/removed creator-video Watch-Party blocked-state proof.
- Real entitlement-backed Premium account proof and remaining signed-out/deep-link gate proof.
- Store purchase/restore proof if Premium ships live.
- Admin Command Center V1A runtime proof passed for signed-out denial, admin/operator access, tab opening, foundation honesty, and route smoke. A separate non-admin denial proof with a known non-admin account is still pending, plus release-like admin smoke and longer report-queue workflow proof.
- Storage API delete/remove proof for creator videos.
- Settings/logout/legal/support route smoke.
- Non-room route smoke for signup handoff and Player explicit source ownership. Chat signed-out physical-device handoff is already covered by the May 13 Chi'lly Chat final behavior proof.
- Smoothness route smoke for Home/Explore safe errors, My List empty/error actions, Creator Media picker/load/upload states, Premium copy, and release logging redaction.
- Final Android release build and route smoke.

## Later-Phase Items

- Native game/video streaming.
- Paid creator videos and subscriber-only media.
- Tips, coins, live creator payouts, tax/KYC, and earnings ledgers. The pushed Channel Studio payout dashboard is read-only/foundation only, with no payable balance or payout action; Stripe Connect Test-Mode Backend Functions and Provider Transfer Records Sync Foundation are deployed backend-only/test-mode but do not add payout setup UI, transfer creation, payouts, checkout, fake paid status, or live provider money actions; Payout Review Queue + Batch Draft Workflow Foundation is Admin read-only/draft-only and adds no approval, processing, transfer, payable balance, or live money action.
- Real ad SDK integration, ad revenue imports, creator ad revenue share, CTV ads, and sponsor/ledger systems.
- Full Audience Role Roster, VIPs, channel moderators, creator teams, and durable co-host roles.
- Media comments and media Profile posts.
- Full Friends, friend requests, close friends, and friend-only privacy.
- Full search across users, channels, creator videos, and platform titles.
- Push notification delivery and notification center.
- Advanced creator studio and automatic transcoding.
