# CURRENT STATE

## Hot-Path Control Rule
This file is intentionally compact. Keep current truth here, keep detailed proof output in artifacts or `/tmp`, and do not load archived checkpoint history during normal preflight unless a historical reconciliation task explicitly asks for it.

Full checkpoint history through April 24, 2026 is preserved at `docs/archive/current-state-history-through-2026-04-24.md`. Later detailed proof history is available in git history and task artifacts; this hot-path file should carry only the current governing facts future sessions must not undo.

## Current Checkpoint
Current `main` is production-grade Public v1 hardening with Admin Command Center V1A, Admin V1B1 runtime controls config foundation, Admin V1B2A new-account kill switch enforcement, Admin V1B2B upload kill switch enforcement, Admin V1B2C comments kill switch enforcement, Admin V1B2D profile posting kill switch enforcement, Admin V1B2E creator posting kill switch enforcement, Admin V1B2F social attachment kill switch enforcement, Admin V1B2G standalone Chi'lly Chat attachment kill switch enforcement, Admin V1B2H room attachment kill switch enforcement, Admin V1B2I-A standalone Chi'lly Chat runtime-control enforcement, Admin V1B2I-B chat invite runtime-control enforcement, Admin V1B2I-C room comment runtime-control enforcement, Ads Launch Foundation V1A/V1B/V1C, Public V1 Hardening H1A 18+ Signup Confirmation, H1B2 legal acceptance storage, H2 upload/content lifecycle polish, H3 security/compliance/moderation hardening, Admin Usage Metering Foundations 37-39, Admin Usage Writer V1A0/V1A1/V1A2 creator-video upload usage and daily rollup foundation, Admin Usage Provider Imports V1D3/V1D4 server-side import foundation, and Ledger Systems 4A-4D finance foundation with remote schema/type refresh plus read-only Admin proof complete. The Admin V1A checkpoint is committed at `2690367912e4f10309d09d08433be25662028ed3`; the Admin V1B1 runtime controls config foundation checkpoint is committed at `2f6fd450e00fd5f522ad94799416fecfa54bd641`; the Admin V1B2A new-account kill switch checkpoint is committed at `9fca92bd1d47507eaacb7494e945040a032666c9`; the Admin V1B2B upload kill switch checkpoint is committed at `6d3c24e10f77702199603d6ea2cf8239986872f7`; the Admin V1B2C comments kill switch checkpoint is committed at `6b821d61b2e3894aec28335e181985b6b4065df8`; the Admin V1B2D profile posting checkpoint is committed at `ccb1afdd492c19f3b8e32fe395f38a634bbe6f75`; the Admin V1B2E creator posting checkpoint is committed at `51463522580c40192fe2dd586e5597e6cebf73b9`; the Admin V1B2F social attachment checkpoint is committed at `d1637054c0d3c51dc2be40a915c837c79bcf05d4`; the Admin V1B2G standalone Chi'lly Chat attachment checkpoint is committed at `ed5038358c5be5f3ab0eacfc94d5b43033e65c56`; the Admin V1B2H room attachment checkpoint is committed at `9d3465347c0586aa14e3feec1829d6d635273cae`; the Admin V1B2I-A standalone Chi'lly Chat runtime-control checkpoint is committed at `116ff6eb0d6a4ab80867e5de2dda94c269d907be`; the Admin V1B2I-B chat invite checkpoint is committed at `f75a0da81fa53821971b671eaa4cc3f413326621`; the Admin V1B2I-C room comment checkpoint is committed at `d98e4fbc5425a39a8add2868d9a58569c4cd12dd`; the Ads V1A checkpoint is committed at `3bfe82328b4200e86ff15955c95bac7a1e218013`; the Ads V1B native/feed placeholder checkpoint is committed at `c8a21b93d01f8af5d6ed5b229f03a594b9a59832`; the Ads V1C placeholder interstitial controller checkpoint is committed at `2c1af252da391fe0e43a80bd7eaf9ea562fc1293`; the H1A 18+ signup confirmation checkpoint is committed at `b7f9c53a2e505375aa8c04ec4669acf310d78233`; the H1B1 legal acceptance schema foundation checkpoint is committed at `e052dc816c12f58d056e01f93867199304bca467`; the H1B2 legal acceptance storage checkpoint is committed at `2ecbd1f4d2ef66a4be77fba03c6f54d299413cf8`; the H2 upload/content lifecycle polish checkpoint is committed at `d5a4a9143b9443828cd4a81289b68f20b73d4119`; the H2 duplicate-upload UI correction is committed at `3e76cf00aea1bece0910759aefcea27c3f4f6a08`; the H3 public safety/support hardening checkpoint is committed at `0ea39d2af54002e5a47e8577df4ea276c36d7697`; the Admin Usage Metering Foundations 37-39 checkpoint is committed at `bd4cba0e278758ce90b56f53801156e53b8ed8d0`; the Admin Usage Provider Imports V1D3 checkpoint is committed at `6f01a837c720e20604049bd6e2ca9b1ed7ea21f8`; the Admin Usage Provider Imports V1D4 Hetzner Object Storage checkpoint is committed at `0ca201eee182923889a74844a202881947aa2b4e`; the Ledger Systems 4A-4D finance foundation checkpoint is committed at `32b863e5b94530126fdda820a71a9cdaa4ae4e22`; the Admin Usage Writer V1A2 daily rollup/finance remote type-refresh checkpoint is committed at `b0f273070444ee95f2d20cf73b7ab76b58c4b3fd`.

Ads Config V1D1 app-config `adsLaunch` resolver foundation is pushed at `e7b8ecb3fbe39d5625edb29edf95315999d79678`.
Ads V1D2 runtime-read foundation is pushed at `92818cf0bd6b9c073408ab0a432df1ea35532e06`.
Admin Command Center sign-in entry is pushed at `91c1f7b978d32814266a9b83b1f95d864ab8696a`.
Admin Usage Metering Foundations 37-39 is pushed at `bd4cba0e278758ce90b56f53801156e53b8ed8d0`.
Admin Usage Metering Foundation V1 remote/provider schema is pushed at `7ca3a994af94c4e5d4329b0ff87c942904495676`.
Admin Usage Writer V1A0 creator-video upload RPC foundation is pushed at `7a05e9f23584aa56359789cfd43e6f1a37b585a1`.
Admin Usage Writer V1A1 creator-video upload success wire-up is pushed at `47a346da968c1c8c1a9ed7886af5bc5a086dcd48`.
Admin Usage Writer V1A1 runtime proof/Admin Usage copy closeout is pushed at `f638970b6ee34e17bfabcfc8565ae266d84ac4c3`.
Admin Usage Writer V1A2 daily rollup RPC foundation and finance/usage type refresh is pushed at `b0f273070444ee95f2d20cf73b7ab76b58c4b3fd`.
Admin Usage Provider Imports V1D3 server-side provider import foundation is pushed at `6f01a837c720e20604049bd6e2ca9b1ed7ea21f8`.
Admin Usage Provider Imports V1D4 Hetzner Object Storage S3 inventory import is pushed at `0ca201eee182923889a74844a202881947aa2b4e`.
Provider Billing Reconciliation Foundation is pushed at `12b5ddb`; it is backend-only, deployed, and does not import provider bills or execute network billing.
Ledger Systems 4A-4D finance foundation is pushed at `32b863e5b94530126fdda820a71a9cdaa4ae4e22`.
Finance F2B creator payout provider schema foundation is pushed at `1e0c3f65c44ab3e21f88e734755f9557a3458e00`.
Finance F2D payout provider Admin foundation readout is pushed at `a0b5e63eeb23ccb16ce900583264018921e0b10c`.
Finance F2E payout provider foundation proof rows are pushed at `0caa02f39802335e5bb55c89577482c80795b9ee`. Finance F2 payout-provider closeout truth is current through schema, RLS proof, read-only Admin counts, and non-live proof rows.
Network Billing Foundation is pushed at `193916e2d53102115b4031f3d29b6f91ccf8c36d`.
Sponsor Checkout Foundation is pushed at `febc520420d69c03614b446995840776fc039624`.
Sponsor Review Queue + Disclosure / Moderation + Payment Test-Mode Foundation is pushed at `3031ce1851828817ca90b34eb789b4059e43fdc4`.
Fraud Enforcement Foundation is pushed at `62d7c4c6743cc5554dd771186ff5643957540136`.
Creator Revenue Share Ledger + Fraud Hold Enforcement Foundation is pushed at `e936f05614254b4884ca948a6711d3524c1e6002`.
Fraud Review Queue Foundation is pushed at `171894e`; it is review-state/readout only and no runtime enforcement hook is connected.
Real Source Revenue Import Planning/Spec is recorded as repo truth; no provider import code is implemented.
Creator-Facing Revenue Share Dashboard Planning/Spec is recorded as repo truth; no creator revenue dashboard is implemented.
Remaining Finance/Admin Lane Planning is recorded as repo truth for test-mode transfers, production payout release, sponsor checkout/payment/reporting, and provider billing APIs; provider billing reconciliation foundation and fraud review queue foundation are now implemented as safe backend/readout foundations, while the remaining planned lanes still require separate exact implementation prompts.
Immutable Admin Audit Log Foundation is pushed at `f7951470a476c738897d20d51c753a3afce94d28`.
Creator Payout Read-Only Dashboard Foundation is pushed at `fb4faf0f75b0d6878b8fd86d17d1d472fd2d9fdd`.
Stripe Connect Schema Foundation (Payout S2) is pushed at `1e6d5caa6dd8cbbf3d32193d19d8d5a05269ae68`.
Stripe Connect Edge Function Skeletons (Payout S3B) is pushed at `246243028680b2e7d7a6da03fcf788f589dd04af`.
Stripe Connect Test-Mode Backend Functions (Payout S3C) is pushed at `4ce5c632931bf9f88c7a118cc3685f85eb61c229`.
Admin Payout Provider Dashboard is pushed at `b7bb9e1`.
Creator-facing payout setup UI is pushed at `c641e8d`; it is backend-only/test-mode and does not activate payouts.
Payout Review Queue + Batch Draft Workflow Foundation is pushed at `9b760b8a09431bfa427176c1c6c90f1bec712cf1`.
Public V1 current-build release proof is complete as of May 7, 2026. No release-proof blockers were found, and no runtime code fixes were needed.

Already pushed and to be preserved:

- Premium gate is pushed: all full live/watch-party access is Premium. Live First is no longer free; Live Watch-Party and Watch-Party Live remain Premium. Free users are blocked before full room/session/token/connect, receive no full LiveKit room/token/connect access, and no free preview mode was added. Premium users keep the existing full live/watch-party flows.
- Chi'lly Circle V1 is pushed: request, accept, decline, cancel, remove, My Chi'lly Circle management, Follow separation, and channel-audience block override.
- Chi'lly Circle profile privacy is pushed: Everyone, Chi'lly Circle Only, and Private, with backend/RLS tightening so locked-profile posts, comments, and attachments do not leak through old read paths.
- Channel Studio Phase 1 is pushed: Manage Channel was renamed and organized as Channel Studio.
- Channel Studio Phase 2A is pushed: `/channel-studio` is the preferred owner Studio shell with Home, Content, Live, Audience, Insights, and Brand tabs; `/channel-settings` remains a compatibility route.
- Channel Studio Home dashboard correction is pushed: compact dashboard-first Home, Today's Snapshot before Quick Actions, compact Quick Actions, Needs Attention, Latest Content, and minor Later/Roadmap at the bottom.
- Public Channel Phase 2B is pushed: `/channel/[userId]` is the public viewer-facing Channel route, Profile View Channel routes to `/channel/[userId]`, Studio Preview Channel routes to `/channel/[ownUserId]`, not-found/unavailable states exist, and the public Channel has hero, Featured, Latest Uploads, Live & Upcoming, and About sections.
- Public Channel visual polish and streaming-network visual correction are pushed: the public route is a media/network destination with a channel hero, backed Channel Pulse row, Featured spotlight, Latest Uploads shelf, programming-style Live & Upcoming, and final About card. Public Channel uses viewer-facing `Play`, not owner management labels, never exposes Upload/Edit/Publish/Unpublish/Delete to non-owners, never shows drafts/private/unpublished videos, and may show owner-only Open Channel Studio only to the channel owner.
- Centralized live/watch-party Premium access runs through the existing Premium access helper layer, including `canUseLiveFirst`, `canUseLiveWatchParty`, `canUseWatchPartyLive`, `requireLiveFirstPremium`, `requireLiveWatchPartyPremium`, and `requireWatchPartyLivePremium`. Strict live/watch-party gates ignore dev Premium bypass behavior where needed so free-user blocking can be proved.
- Admin Command Center V1A is pushed on the canonical `/admin` route. Do not create duplicate admin routes such as `/admin-command-center`. Admin is platform owner/operator authority and must remain separate from Channel Studio, Profile Settings, Public Channel, Chi'lly Circle, and future Room Control.
- Admin remains protected by existing signed-in plus beta/platform-role/backend permission checks. Route access, report visibility, and privileged-write boundaries were preserved. Admin V1A did not add a production admin bypass.
- Login now supports a normal Admin Command Center sign-in entry. `app/(auth)/login.tsx` can open in an admin-intent state with `redirectTo=/admin`, but it stores no credentials, prints no credentials, hard-codes no admin account, adds no production bypass, and still relies on the canonical `/admin` backend platform-role checks after sign-in.
- `/admin` now presents as `Admin Command Center` with Home, Reports, Content, Roles, Audit, Rachi, Users, Premium, Kill Switches, Usage, Ads, Revenue, Payouts, Networks, Sponsors, Fraud, and System sections. Home includes Platform Snapshot and Needs Attention.
- Existing backed admin areas remain real: Reports uses the existing safety report/admin moderation surfaces; Content preserves creator-video moderation, title programming, and admin content behavior; Roles preserves platform role visibility; Audit now includes Immutable Admin Audit Log Foundation readout plus the existing derived role/safety summary; Rachi identity/admin display remains present, but Rachi does not grant operator permissions.
- Admin V1A foundation areas must stay honest: Users search is not connected yet; Premium is read-only/foundation and RevenueCat remains Premium truth; Kill Switches lists foundation rows only; Usage may show DB-estimate live/watch-party/upload signals, storage metadata estimates, and participant-minute DB estimates where backed, but must label them honestly and must still say bandwidth and LiveKit/provider billing metering are not connected yet unless real metering rows/provider data exist; Ads is foundation only with AppLovin MAX direction and no SDK/ad IDs/provider init/rendering; Revenue, Payouts, Networks, Sponsors, and Fraud must not show fake money, fake balances, fake invoices, fake sponsor revenue, fake network billing, fake holds, or working payout/sponsor/fraud flows.
- Admin V1B1 runtime controls config foundation is pushed. `_lib/featureFlags.ts` owns typed `APP_RUNTIME_CONTROL_DEFAULTS` and `resolveAppRuntimeControls`; `_lib/appConfig.ts` stores normalized `runtimeControls` under the existing `app_configurations.config` JSON shape; `app/admin.tsx` shows Kill Switches rows as read-only `Configured foundation` / `Not enforced yet` status only. No working admin toggles, no runtime enforcement, no migrations, no generated database type edits, no RLS changes, no Supabase remote changes, and no Premium gate weakening were added.
- Admin V1B2A new-account kill switch enforcement is pushed. `app/(auth)/signup.tsx` reads normalized `runtimeControls.new_accounts_enabled` after email/password and 18+ confirmation pass, and blocks before `supabase.auth.signUp` with `New accounts are paused` when the control is false. The default remains true, so normal signup behavior is unchanged. `app/admin.tsx` updates the New Accounts Kill Switches row as read-only `Enforced on signup`; no working Admin toggle, config write control, migration, generated type edit, RLS/storage change, Supabase remote change, login/session change, legal acceptance storage change, or Premium gate weakening was added.
- Admin V1B2B upload kill switch enforcement is pushed. `app/channel-settings.tsx` reads normalized `runtimeControls.uploads_enabled` from the existing `readAppConfig` load and blocks only new creator-video upload submit before `uploadCreatorVideo`, storage upload, or metadata insert when the control is false. Default true and config-read fallback true preserve normal upload behavior. Existing missing-title, missing-file, and file-size checks still run first; existing video metadata edit, publish/unpublish/delete, Open Player, picker behavior, storage helpers, RLS, migrations, generated types, and Channel Studio's single `Video Upload` form are unchanged. `app/admin.tsx` updates the Uploads Kill Switches row as read-only `Enforced on upload`; no working Admin toggle or config write control was added.
- Admin V1B2C comments kill switch enforcement is pushed. `app/profile/[userId].tsx` reads normalized `runtimeControls.comments_enabled` from the existing app config state and blocks only Profile post comment/reply submit after signed-in, engageable-post, empty-body, and length checks and before `createProfilePostComment` or comment attachment upload. `app/player/[id].tsx` reads the same control and blocks only creator-video comment/reply submit after creator-video route/source, signed-in, empty-body, and length checks and before `createCreatorVideoComment` or comment attachment upload. Default true and config-read fallback true preserve normal comments. Existing comment reads, deletes, reports, attachment picker selection, Watch-Party comments, Live Stage comments, Chi'lly Chat messages, Chi'lly Circle, profile privacy, Player controls/layout, `_lib/profilePosts.ts`, `_lib/creatorVideoComments.ts`, RLS, migrations, generated types, and Supabase remote state are unchanged. `app/admin.tsx` updates the Comments Kill Switches row as read-only `Enforced on comments`; no working Admin toggle or config write control was added.
- Admin V1B2D profile posting kill switch enforcement is pushed. `app/profile/[userId].tsx` reads normalized `runtimeControls.profile_posting_enabled` from the existing app config state and blocks only Profile post creation submit after owner/busy, empty-body, and length checks and before `createProfilePost` or post attachment upload. Default true and config-read fallback true preserve normal Profile posting. Existing Profile post reads, comments/replies, likes, deletes, reports, attachment picker selection, Chi'lly Circle, profile privacy, creator video upload, Channel Studio, Public Channel, Player, Watch-Party, Live Stage, Chat, `_lib/profilePosts.ts`, RLS, migrations, generated types, and Supabase remote state are unchanged. `app/admin.tsx` updates the Profile Posting Kill Switches row as read-only `Enforced on profile posts`; no working Admin toggle or config write control was added.
- Admin V1B2E creator posting kill switch enforcement is pushed. `app/channel-settings.tsx` reads normalized `runtimeControls.creator_posting_enabled` from the existing app config load and blocks only new creator event creation before `createCreatorEvent` when the control is false. Default true and config-read fallback true preserve normal creator event creation. Existing creator event edits through `updateCreatorEvent`, creator-video upload, video metadata edit, publish/unpublish/delete, Profile posts, comments/replies, attachments, Channel Studio layout, Public Channel, Player, Watch-Party, Live Stage, Chat, RLS, migrations, generated types, and Supabase remote state are unchanged. `app/admin.tsx` updates the Creator Posting Kill Switches row as read-only `Enforced on creator events`; no working Admin toggle or config write control was added.
- Admin V1B2F social attachment kill switch enforcement is pushed. `app/profile/[userId].tsx` and `app/player/[id].tsx` read normalized `runtimeControls.attachments_enabled` and block only selected non-chat social attachments before parent post/comment create and before attachment upload. The enforced surfaces are Profile post attachments, Profile post comment/reply attachments, and creator-video comment/reply attachments. Text-only Profile posts, Profile comments, and creator-video comments still submit when their existing posting/comment controls allow them. Existing attachment picker selection, reads, deletes, reports, Profile privacy, Chi'lly Circle, Chat attachments, room attachments, `_lib/socialAttachments.ts`, `_lib/profilePosts.ts`, `_lib/creatorVideoComments.ts`, storage helpers, RLS, migrations, generated types, and Supabase remote state are unchanged. `app/admin.tsx` updates the Attachments row as read-only `Enforced on social attachments`; no working Admin toggle or config write control was added.
- Admin V1B2G standalone Chi'lly Chat attachment kill switch enforcement is pushed. `app/chat/[threadId].tsx` reads normalized `runtimeControls.chat_attachments_enabled` and blocks only selected attachments before optimistic message insertion and before `sendChatMessage` when the control is false. Default true and config-read fallback true preserve normal chat attachments. Text-only Chi'lly Chat messages still send through the existing path. Existing signed-in/thread gates, reads, realtime updates, mark-read behavior, attachment picker selection, room attachments, `_lib/chat.ts`, `_lib/socialAttachments.ts`, storage helpers, RLS, migrations, generated types, and Supabase remote state are unchanged. `app/admin.tsx` updates the Chat Attachments row as read-only `Enforced on chat attachments`; no working Admin toggle or config write control was added.
- Admin V1B2H room attachment kill switch enforcement is pushed. `app/watch-party/[partyId].tsx` and `app/watch-party/live-stage/[partyId].tsx` read normalized `runtimeControls.chat_attachments_enabled` from their existing app config state and block only selected room attachments before `sendPartyMessageRecord` and before `createSocialAttachmentForSurface` when the control is false. Default true and config-read fallback true preserve normal room attachments. Text-only room comments still send through the existing room paths. Existing room layouts, LiveKit behavior, Premium gates, message reads, room routes, attachment picker selection, `_lib/watchParty.ts`, `_lib/socialAttachments.ts`, storage helpers, RLS, migrations, generated types, and Supabase remote state are unchanged. `app/admin.tsx` updates the Chat Attachments row as read-only `Enforced on chat and room attachments`; no working Admin toggle or config write control was added.
- Admin V1B2I-A standalone Chi'lly Chat runtime-control enforcement is pushed. `app/chat/[threadId].tsx` reads normalized `runtimeControls.chat_enabled` and blocks standalone message send before optimistic insertion and before `sendChatMessage`, and blocks standalone thread voice/video call start before `startChatThreadCall` when the control is false. `app/chat/index.tsx` blocks the Rachi official starter thread before `getOrCreateDirectThread`, and `app/profile/[userId].tsx` blocks non-self Profile-to-chat entry before direct thread creation when false. Default true and config-read fallback true preserve normal standalone Chi'lly Chat. `/chat` and `/chat/[threadId]` remain readable; inbox reads, thread reads, realtime refresh, mark-read, reports, profile opens, existing signed-in/thread gates, attachment picker selection, `chat_attachments_enabled`, room comments, room invites, `_lib/chat.ts`, `_lib/watchParty.ts`, RLS, migrations, generated types, and Supabase remote state are unchanged. `app/admin.tsx` updates the Chat row as read-only `Enforced on standalone chat`; no working Admin toggle or config write control was added.
- Admin V1B2I-B chat invite runtime-control enforcement is pushed. `components/chat/internal-invite-sheet.tsx` reads normalized `runtimeControls.chat_enabled` and blocks room invite direct-message sends before `sendDirectInviteMessage` when the control is false. Default true and config-read fallback true preserve normal room invite direct-message behavior. System share fallback remains available. Watch-Party room text comments, Live Stage text comments, room layouts, LiveKit behavior, Premium gates, `chat_attachments_enabled`, `_lib/chat.ts`, `_lib/watchParty.ts`, RLS, migrations, generated types, and Supabase remote state are unchanged. `app/admin.tsx` updates the Chat row as read-only `Enforced on chat and invites`; no working Admin toggle or config write control was added.
- Admin V1B2I-C room comment runtime-control enforcement is pushed. `app/watch-party/[partyId].tsx` and `app/watch-party/live-stage/[partyId].tsx` read normalized `runtimeControls.chat_enabled` and block room-native text/comment submits before `sendPartyMessageRecord` when the control is false. Default true and config-read fallback true preserve normal room comments. Existing room message reads, room layouts, LiveKit behavior, Premium gates, invite behavior/system share, `chat_attachments_enabled`, attachment picker behavior, `_lib/watchParty.ts`, `_lib/chat.ts`, `_lib/socialAttachments.ts`, storage helpers, RLS, migrations, generated types, and Supabase remote state are unchanged. `app/admin.tsx` updates the Chat row as read-only `Enforced on chat, invites, and room comments`; no working Admin toggle or config write control was added.
- Admin V1B runtimeControls closeout truth is recorded. The enforced controls are scoped only to the pushed surfaces: `new_accounts_enabled`, `uploads_enabled`, `comments_enabled`, `attachments_enabled`, `chat_enabled`, `chat_attachments_enabled`, `creator_posting_enabled`, and `profile_posting_enabled`. The configured foundation-only controls are `live_first_enabled`, `live_watch_party_enabled`, `watch_party_live_enabled`, and `max_upload_size_mb`; they are not enforced yet. Admin `runtimeControls.ads_enabled` is not the Ads Launch config source of truth; Ads V1D2 runtime owners read normalized `app_config.adsLaunch` and remain disabled by default while `ads_enabled=false`. `premium_required_for_live` and `premium_required_for_watch_party` are not runtime switches; Premium gates are enforced separately through the Premium access helpers and must not be weakened.
- Admin Usage Metering Foundations 37-39 plus Admin Usage Metering Foundation V1 remote/provider schema are pushed. They add migrations `supabase/migrations/202605070003_platform_usage_metering_foundation.sql` and `supabase/migrations/202605070004_admin_usage_metering_v1.sql`, helper `_lib/platformUsage.ts`, generated usage/provider table types in `supabase/database.types.ts`, and read-only Admin Usage updates in `app/admin.tsx`. Both usage migrations have been applied to the linked Supabase remote. Existing `platform_usage_metering_events` and `platform_usage_daily_rollups` remain future internal metric tables; new `usage_meter_events`, `usage_daily_summaries`, `usage_monthly_summaries`, `provider_accounts`, `provider_usage_imports`, `provider_usage_daily`, `provider_billing_snapshots`, and `provider_usage_reconciliation` are schema foundation for internal usage, provider imports, provider billing snapshots, and future reconciliation.
- Admin Usage Writer V1A0/V1A1/V1A2 is pushed and runtime-proved for creator-video upload usage only. V1A0 adds the secure idempotent `record_creator_video_upload_usage(target_video_id text)` RPC; V1A1 adds `_lib/platformUsage.ts` helper `recordCreatorVideoUploadUsage` and calls it from `_lib/creatorVideos.ts` only after a `videos` row insert succeeds. Runtime proof on the current Android build created a safe draft creator video, recorded exactly one `video_uploaded` usage row and one `storage_bytes` metadata row, and proved a second RPC call returned `skipped` while counts stayed 1/1. V1A2 adds remote-applied RPC `rollup_creator_video_upload_usage_daily(target_usage_date date default null)` and helper `rollupCreatorVideoUploadUsageDaily` to roll already-backed `video_uploaded` and `storage_bytes` rows into `usage_daily_summaries`. V1A2 proof used a direct database session with Supabase auth context for an existing operator, rolled up the existing May 7, 2026 creator-video usage rows, and proved replay stayed idempotent. Automatic rollup scheduling is not wired yet. Metering failure is non-fatal to upload success and logs only a dev upload event. Provider Billing Reconciliation Foundation is pushed/deployed as `provider-billing-reconciliation`; it is owner/operator, backend-only, reads already-backed `provider_usage_daily` plus `usage_daily_summaries`, upserts `provider_usage_reconciliation`, writes immutable admin audit rows, and updates Admin Usage/Networks readouts. It does not call provider billing APIs, read provider secrets, import provider bills, create invoices, charge customers, create payment links, trigger overage billing, or create fake provider bills. No bandwidth writer, participant-minute writer, customer billing, overage execution, payout, revenue, or fake provider-value writer exists yet.
- Admin Usage Provider Imports V1D3/V1D4 are pushed. V1D3 added the server-side Supabase Edge Function `provider-usage-import`, idempotency migration `supabase/migrations/202605070012_provider_usage_import_idempotency.sql`, provider import readouts in `_lib/platformUsage.ts`, and read-only Admin Usage provider rows in `app/admin.tsx`. V1D4 added active Hetzner Object Storage import through S3-compatible bucket inventory metadata. The migration is applied remotely and the Edge Function is deployed. The mobile app never calls Cloudflare, Hetzner, or OVH APIs directly. Cloudflare R2, Hetzner Object Storage, and Hetzner Servers can show real provider-imported rows only after server-side secrets are configured and an owner/operator import succeeds. Current proof with server-side secrets configured completed Cloudflare R2 with zero rows for the selected day, completed Hetzner Object Storage with `s3_inventory_storage_bytes` and `s3_inventory_object_count`, and completed Hetzner Servers with provider network metrics. Hetzner Object Storage values are S3 inventory metadata estimates only, not traffic, billing, overage, invoice, revenue, payout, or storage-billing truth. OVH Object Storage and OVH Servers remain `Not connected yet` until later exact provider API lanes. No provider secrets, provider tokens, real bills, overages, invoices, revenue, payouts, sponsor money, fake bandwidth, fake provider rows, or Admin import buttons were added.
- Admin Usage now reads existing backed data through `_lib/platformUsage.ts`: active live rooms and active watch-party rooms are DB estimates from `watch_party_rooms`; uploads today is from `videos.created_at`; storage is a metadata estimate from `videos.file_size_bytes` and `social_attachments.size_bytes`; participant-minutes are a DB estimate from `watch_party_room_memberships` and `communication_room_memberships` membership timestamps clamped to the current day. Bandwidth remains `Not connected yet` unless future real `platform_usage_metering_events` rows exist. None of these values are billing, payout, revenue, LiveKit invoice, provider-log, or cost truth.
- Ledger Systems 4A-4D finance foundation is pushed and its schema is now applied to Supabase remote. It adds migration `supabase/migrations/202605070005_platform_finance_ledger_foundation.sql`, helper `_lib/platformFinance.ts`, generated finance table types in `supabase/database.types.ts`, and read-only Admin Revenue/Payouts/Networks/Sponsors/Fraud status updates in `app/admin.tsx`. The migration defines future `platform_finance_ledger_events`, `creator_payout_ledger_entries`, `network_billing_accounts`, `network_invoice_records`, `sponsor_deal_records`, and `platform_fraud_holds`. These remain foundation/read-only tables only: no provider-backed finance imports, payout execution, invoices, sponsor checkout, payout split execution, fraud enforcement, provider integration, or live money totals are active.
- Finance F2B creator payout provider schema foundation is pushed, remote-applied, and type-refreshed. It adds migration `supabase/migrations/202605080001_creator_payout_provider_foundation.sql` and generated database types for `creator_payout_accounts`, `creator_payout_batches`, `creator_payout_provider_transfers`, `creator_payout_holds`, and `creator_payout_audit_log`. These are future Stripe Connect or equivalent payout-provider foundation tables only. Owner/operator RLS is scoped for future platform operations; audit rows have no update/delete policy; no moderator access, Stripe integration, provider calls, provider secrets, payout buttons, withdrawal buttons, or live payouts were added. Creator self-read now exists only for own `creator_payout_ledger_entries` through the later Creator Payout Read-Only Dashboard Foundation, not for payout-provider account/transfer tables.
- Finance F2D payout provider Admin foundation readout is pushed. `_lib/platformFinance.ts` now reads safe counts for payout accounts, payout batches, provider transfers, payout holds, and payout audit log rows, and `app/admin.tsx` shows them in the Payouts tab as read-only foundation status. It adds no buttons, balances, payout approval, Stripe onboarding, provider sync, provider API calls, KYC flow, withdrawal flow, live transfer action, creator-facing payout setup, or runtime payout behavior.
- Finance F2E payout provider foundation proof rows are pushed and remote-applied through migration `supabase/migrations/202605080002_creator_payout_provider_proof_rows.sql`. The rows are deterministic `not_active` foundation/proof records marked `finance_foundation_proof: true`, `created_by: codex_finance_f2e_proof`, and `not_live_money: true`. Amount fields are `0`; they use no real creator, bank, card, provider account, payout account, invoice, sponsor, or customer payment data. Anonymous REST proof shows zero visible proof rows and anon insert remains denied. These rows must never be treated as payable balance, payout obligation, Stripe state, KYC state, creator earnings, or live money.
- Network Billing Foundation is pushed and remote-applied through migration `supabase/migrations/202605080003_network_billing_foundation.sql`. It expands existing `network_billing_accounts` and `network_invoice_records` with display/contact and invoice month/subtotal/total fields, adds `network_plan_records`, `network_account_plan_assignments`, `network_quota_records`, `network_invoice_line_items`, `network_overage_events`, and `network_billing_audit_logs`, refreshes generated database types, and updates Admin Networks read-only counts. The migration inserts deterministic foundation/proof rows for one network account, one Network Starter plan, one plan assignment, storage/bandwidth/live participant-minute/team-seat quotas, one invoice draft, one line item, one overage foundation event, and one billing audit row. These rows are proof-only and must never be treated as live billing, payable balance, customer obligation, invoice send state, Stripe state, or real overage truth.
- Network Invoice Draft Workflow Foundation and Network Overage Warning/Readout Foundation are pushed and remote-applied through migration `supabase/migrations/202605080011_network_invoice_overage_foundation.sql`. It expands safe draft/readout status vocabularies for `network_invoice_records`, `network_invoice_line_items`, and `network_overage_events`, adds readout indexes, and inserts deterministic zero-value foundation proof rows for an internal draft invoice, draft/foundation line items, a warning-only overage readout, and billing audit records. `_lib/platformFinance.ts` reads draft invoice, draft line item, warning-only overage, and review-required overage counts. Admin Networks remains read-only/foundation with Network Billing Foundation, Invoice Draft Workflow, Overage Warning Readout, and Provider Reconciliation sections. Draft invoices are internal only; no invoice can be sent, no customer can be charged, no payment links exist, overage warnings are read-only, billing execution is not active, provider reconciliation and trusted usage metering are required before real billing, and no fake revenue, fake unpaid balance, or real customer obligation exists.
- Sponsor Checkout Foundation is pushed and remote-applied through migration `supabase/migrations/202605080004_sponsor_checkout_foundation.sql`. It extends existing sponsor deals and adds sponsor brands, creatives, placements, disclosures, review logs, payment records, and payout split records. Generated database types were refreshed, `_lib/platformFinance.ts` reads safe sponsor counts, and Admin Sponsors is read-only/foundation only. Deterministic proof rows are marked `sponsor_checkout_foundation_proof: true`, `created_by: codex_sponsor_checkout_foundation`, `foundation_only: true`, and `live_money_action: false`. No sponsor checkout exists, no brand can pay yet, no payment link exists, no Stripe Checkout is connected, no sponsor approval action exists, no fake sponsor revenue is shown, and no creator payout split can execute. Future sponsor money requires platform review, disclosure review, safe product/scam review, fraud/abuse review, payout hold/review, and audit trail. Future model remains: brand pays Chi'llywood first; creator-sold sponsor slots pay creator 80% net / Chi'llywood 20% net; platform-served creator-page ads pay creator 70% net / Chi'llywood 30% net; paid partnership disclosure is required before any sponsor deal can go live.
- Sponsor Deal Review Queue Foundation, Sponsor Disclosure / Moderation Foundation, and Sponsor Payment Test-Mode Foundation are pushed and remote-applied through migration `supabase/migrations/202605080012_sponsor_monetization_foundation.sql`. It adds `sponsor_review_queue_records` and `sponsor_safety_review_records`, adds `sponsor_disclosure_records.required_before_live`, adds sponsor payment planning fields `test_mode_enabled`, `checkout_provider`, `checkout_session_reference`, and `payment_mode`, refreshes generated database types, updates `_lib/platformFinance.ts` sponsor counts, and keeps `/admin` Sponsors read-only/foundation. Proof rows are foundation-only and explicitly mark no approval execution, no checkout, no brand charge, no live enforcement, no live money action, and no creator payout release. Sponsor review queue, disclosure/moderation, and sponsor payment foundation are not checkout: no sponsor can be approved, no sponsor can be activated, no brand can be charged, no payment link exists, no checkout exists, no creator payout split can execute, no fake sponsor revenue exists, and no fake payable balance exists. Disclosure and safety review are required before sponsor deals can go live later.
- Fraud Enforcement Foundation is pushed and remote-applied through migration `supabase/migrations/202605080005_fraud_enforcement_foundation.sql`. It expands existing `platform_fraud_holds` for foundation review state and adds fraud reasons, evidence records, planned actions, review notes, appeal placeholders, and fraud audit logs. Generated database types were refreshed, `_lib/platformFinance.ts` reads safe fraud counts, and Admin Fraud is read-only/foundation only. Deterministic proof rows are marked `fraud_enforcement_foundation_proof: true`, `created_by: codex_fraud_enforcement_foundation`, `foundation_only: true`, and `live_enforcement_action: false`. No payout pause exists, no account restriction exists, no upload restriction exists, no live restriction exists, no monetization disable exists, no fraud risk scores exist, and no enforcement action is active. Future fraud enforcement requires immutable admin audit logs, review workflow, confirmation/reason, appeal path, and legal/compliance review. Future fraud reasons include invalid traffic, fake engagement, fake followers, fake views, fake ad activity, scams, undisclosed sponsorship, stolen content, chargebacks, refund abuse, policy violation, illegal conduct, suspicious payout behavior, and network overage abuse.
- Fraud Review Queue Foundation is pushed and remote-applied through migration `supabase/migrations/202605080014_fraud_review_queue_foundation.sql`. It adds `fraud_review_queue_records`, generated database types, proof rows marked `live_enforcement_action: false`, immutable admin audit proof, and read-only Admin Fraud queue counts for pending-later, needs-evidence-later, escalated-later, enforcement-planned-later, and appealed-later states. The queue is owner/operator scoped and review-state only. It does not pause payouts, disable monetization, restrict uploads, restrict live access, restrict sponsor deals, restrict accounts, apply strikes, ban users, set risk scores, release holds, or connect runtime enforcement hooks.
- Creator Revenue Share Ledger Foundation and Fraud Hold Enforcement Foundation are pushed and remote-applied through migration `supabase/migrations/202605080013_revenue_share_fraud_hold_enforcement_foundation.sql`. It adds `creator_revenue_share_rules`, `creator_revenue_share_ledger_entries`, and `fraud_enforcement_policy_records`, expands `fraud_action_records` with enforcement policy linkage, target area, execution status, and executed-at fields, refreshes generated database types, and updates Admin Revenue/Fraud read-only counts. Creator revenue share rules and ledger rows are foundation-only: no real source money has been imported, no fake creator earnings exist, no payable balances exist, and no payout ledger entries are created from revenue share foundation rows. Fraud enforcement policies/actions are foundation-only: runtime enforcement hooks are not connected, no payouts are paused, no monetization is disabled, no uploads are restricted, no live access is restricted, no sponsor deals are restricted, and no account restrictions are active. Future creator revenue share requires real source money from ads/sponsors/tips/paid content. Future fraud enforcement requires runtime hook implementation, review workflow, confirmation/reason, appeal path, and immutable audit rows.
- Real Source Revenue Import Planning/Spec is recorded, with no import job, provider call, provider SDK, fake revenue, creator earnings, payable balance, payout ledger write from revenue-share rows, or live money movement. Future imports must be provider-backed, idempotent, reconciled, and audited before any creator revenue-share ledger row can become meaningful.
- Creator-Facing Revenue Share Dashboard Planning/Spec is recorded, with no runtime code or UI change. Future Channel Studio revenue share UI requires real source money imports, creator-read RLS, fraud/hold/review states, and honest empty states. No fake earnings, payable balances, provider revenue, Withdraw/Cash Out, or payout release exists.
- Remaining Finance/Admin Lane Planning is recorded, with no runtime code, migrations, provider calls, deployments, or UI actions. Test-mode transfer creation, production payout release, provider billing imports/reconciliation, sponsor checkout test-mode, brand payment through Stripe, sponsor reporting/fraud integration, and fraud review queue all require separate exact implementation prompts. No transfers, payouts, checkout sessions, invoice sends, customer charges, payment links, fraud enforcement, fake sponsor performance, fake provider bills, fake paid status, or live money movement exists.
- Immutable Admin Audit Log Foundation is pushed and remote-applied through migration `supabase/migrations/202605080006_immutable_admin_audit_log_foundation.sql`. It adds `platform_admin_audit_logs` as the append-only foundation table for who/what/when/why/target/before/after metadata; generated database types were refreshed; `_lib/platformAudit.ts` reads counts/latest rows only; and Admin Audit shows a read-only immutable log section plus the existing derived role/safety summary. Proof rows are marked `admin_audit_foundation_proof: true`, `created_by: codex_admin_audit_foundation`, `foundation_only: true`, and `live_action: false`. No audit row edit/delete/clear UI exists. Dangerous admin actions remain disabled/foundation only: no payout approval/release, invoice send/charge, sponsor checkout/approval, fraud enforcement, account restriction, upload/live restriction, monetization disable, or admin role write was added. Future dangerous finance/fraud/admin writes must write immutable audit rows.
- Creator Payout Read-Only Dashboard Foundation is pushed and remote-applied through policy-only migration `supabase/migrations/202605080007_creator_payout_dashboard_read_policy.sql`. It adds a narrow creator read policy on `creator_payout_ledger_entries` where `creator_user_id = auth.uid()::text`, with no creator insert/update/delete and no broad payout access. `supabase/database.types.ts` did not change after linked typegen because the migration is policy-only. `_lib/creatorPayouts.ts` is read-only and uses the current signed-in creator only, with no service-role usage, no writes, no Stripe/provider calls, and no payout calculations implying payable balance. Channel Studio now has a read-only Payouts tab after Insights and before Brand, plus a Home Payouts card that switches to that tab. `/channel-settings` compatibility remains and `/channel-studio` remains the owner Studio route. Creator payouts are not active yet: no Withdraw, Cash Out, Connect Stripe, KYC live flow, tax form flow, payout approval, payout release, transfer creation, fake payable balance, fake earnings, or live money movement exists. Future payout requirements remain payout account setup, Stripe Connect later, KYC, tax forms, fraud review before release, admin approval, 7-30 day hold, minimum payout undecided, and Immutable Admin Audit Log before dangerous payout actions. No proof rows were inserted because no safe test creator was identified; runtime visual proof should be rerun later on a current signed-in build, while source/helper/DB/typecheck proof passed.
- Stripe Connect Schema Foundation (Payout S2) is pushed and remote-applied through additive migration `supabase/migrations/202605080008_stripe_connect_schema_foundation.sql`. It expands payout account provider/config/capability/readiness fields, adds onboarding session records without long-term onboarding URL storage, adds provider webhook event records with provider/environment/event id uniqueness for idempotency, adds creator payout eligibility/readiness records, and links payout-provider foundation tables to immutable admin audit rows where appropriate. Generated database types were refreshed. Creators may read only their own eligibility/readiness rows; creators cannot insert/update/delete payout-provider rows; anon has no access; owner/operator access stays behind platform-role policies. No Stripe SDK, Stripe keys, provider calls, Stripe accounts, onboarding links, transfers, payouts, Connect Stripe UI, Withdraw/Cash Out, KYC/tax live flow, payout approval/release, fake payable balance, or live money movement was added.
- Stripe Connect Edge Function Skeletons (Payout S3B) was pushed source-only in `supabase/functions/stripe-connect-account`, `supabase/functions/stripe-connect-onboarding-link`, `supabase/functions/stripe-connect-account-sync`, `supabase/functions/stripe-connect-webhook`, and matching `supabase/config.toml` entries. The S3B pass did not deploy functions or touch Supabase remote state. The skeletons returned `not_configured`, used internal Supabase bearer auth validation where applicable, and marked audit as required later. `verify_jwt=false` entries exist only because these functions use the repo's internal auth validation pattern. S3B added no Stripe SDK, Stripe keys, `STRIPE_*` env reads, Stripe API calls, connected accounts, onboarding links, transfers, payouts, checkout sessions, provider writes, webhook storage, Connect Stripe UI, creator-facing payout setup buttons, or live money actions. S3C is the later backend-only test-mode function pass recorded below.
- Stripe Connect Test-Mode Backend Functions (Payout S3C) is pushed and the four Stripe Connect Edge Functions are deployed as ACTIVE version 1 on project `bmkkhihfbmsnnmcqkoly`: `stripe-connect-account`, `stripe-connect-onboarding-link`, `stripe-connect-account-sync`, and `stripe-connect-webhook`. The functions are backend-only and test-mode guarded. They use internal Supabase bearer auth for account/link/sync, reject client-supplied provider account ids, refuse non-test Stripe secrets, use server-side Stripe REST calls only from Edge Functions, write/update payout-provider rows only through backend validation, write immutable audit rows where provider actions can run, verify webhook signatures when `STRIPE_WEBHOOK_SECRET` is configured, store webhook events idempotently when processing is possible, and keep return/refresh URL allowlisting for onboarding links. Remote Edge Function secrets now include `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` by name/digest only; secret values must never enter React Native, repo docs, commits, or logs. Test-mode account creation/reuse, onboarding-link creation, account sync proof, and signed webhook/idempotency proof passed after Connect sandbox enablement; provider readiness remains non-payable and payout execution inactive. Invalid webhook signatures are rejected. A signed test-mode `account.updated` event was accepted by the deployed webhook, stored once in `creator_payout_provider_webhook_events` as `ignored` because it targeted a Stripe account with no matching local creator payout account, wrote received/processed immutable audit rows, and duplicate resend of the same event did not create a second provider event or second processing audit. After the signing secret was exposed in chat, the Stripe test webhook endpoint was recreated, the old webhook endpoint was deleted, the Supabase `STRIPE_WEBHOOK_SECRET` digest changed, and rotated-webhook proof passed again with a signed test-mode `account.updated` event plus duplicate resend idempotency. The exposed Stripe test API secret still must be rotated in the Stripe Dashboard because Stripe API-key rotation is Dashboard-owned. No Stripe SDK/package dependency, Stripe key in repo, provider secret in repo, React Native Stripe integration, Connect Stripe UI, KYC/tax UI, transfer, payout, checkout session, withdrawal, payout approval/release, fake payable balance, fake earnings, live mode, or live money movement exists.
- Admin Payout Provider Dashboard is pushed in `_lib/platformFinance.ts` and `app/admin.tsx`. Admin Payouts now reads existing payout-provider account counts, test-mode account counts, onboarding sessions/link-created counts, payout eligibility/readiness counts, and Stripe Connect webhook event counts by status. It remains read-only/operator-facing only and adds no provider calls, no migration, no generated database type change, no app UI outside Admin, no Connect Stripe creator button, no onboarding link creation, no transfer creation, no payout creation, no checkout session, no payout approval/release, no fake payable balance, no fake earnings, and no live money movement.
- Creator-facing payout setup UI is pushed in `app/channel-settings.tsx` and `_lib/creatorPayouts.ts`. Channel Studio Payouts can now show provider setup/readiness states, create/reuse a Stripe Connect test-mode payout account through backend Edge Functions, request a short-lived onboarding link through the backend, open that link, and refresh provider status through the backend. This UI does not read Stripe secrets, does not accept client-owned provider account ids, does not show raw provider ids or raw requirement JSON, and does not add KYC/tax UI, Withdraw, Cash Out, payable balance, fake earnings, payout approval/release, transfer creation, payout creation, checkout sessions, live mode, or live money movement. It keeps copy that payouts are not active yet.
- Payout Review Queue + Batch Draft Workflow Foundation is pushed and remote-applied through migration `supabase/migrations/202605080009_payout_review_batch_foundation.sql`. It adds `creator_payout_review_records`, `creator_payout_review_notes`, and `creator_payout_batch_items`, and extends `creator_payout_batches` with `batch_status`, `batch_type`, and `total_amount_cents`. Generated database types were refreshed, `_lib/platformFinance.ts` reads the new safe counts, and Admin Payouts remains read-only/foundation. The payout review queue is foundation-only, the payout batch workflow is draft-only, and proof rows are deterministic foundation rows with zero cents and `live_money_action: false`. Admin may show counts for payout review records, review notes, batches, batch items, provider transfers, holds, accounts, ledger entries, and audit rows, but no payout can be approved, rejected, released, processed, marked paid, or transferred. No Stripe payout, Connect Stripe button, fake payable balance, fake earnings, transfer creation, payout execution, KYC/tax live flow, provider SDK, provider secret, payment credential, or live money movement exists. Future payout actions still require Immutable Admin Audit Log, fraud review, Stripe provider proof, KYC/tax readiness, admin approval/reason/audit, and explicit product approval.
- Provider Transfer Records Sync Foundation is pushed and remote-applied through migration `supabase/migrations/202605080010_provider_transfer_sync_foundation.sql`. It adds provider transfer sync fields `creator_payout_provider_transfers.provider_status` and `creator_payout_provider_transfers.last_provider_sync_at`, broadens the local transfer status vocabulary with test/foundation sync labels, adds safe sync indexes, inserts a zero-cent foundation-only local transfer proof row with no fake provider transfer/payout id, and records an immutable audit proof row. `stripe-connect-transfer-sync` is deployed ACTIVE version 1 and is backend-only/test-mode only: POST plus OPTIONS, internal Supabase bearer auth, admin/operator permission required, local transfer record id required, client-supplied provider transfer/payout ids rejected, `STRIPE_SECRET_KEY` read only from Edge Function secrets, status imported only for existing local provider records, and immutable audit rows written for sync attempts where possible. Admin Payouts remains read-only/foundation and may show Provider Transfer Records, Sync Required, Synced/Test, and Failed Sync counts. Provider transfer sync does not create transfers, payouts, checkout sessions, payable balances, fake paid status, manual money movement, Mark Paid, Retry Transfer, Send Money, Release Payout, Process Batch, Connect Stripe UI, or live money movement. Webhook transfer/payout event processing was not expanded in this lane; production transfer sync still requires Stripe test-mode proof, webhook signature verification, idempotency, immutable audit rows, fraud/KYC/tax/admin review checks, and explicit payout execution lane approval.
- Admin finance panels remain read-only/foundation. They may show safe foundation row counts when the future tables are readable; otherwise they show `Not connected yet`, `Foundation only`, or `Not active yet`. They must not show live money totals, payout buttons, invoice actions, sponsor checkout, payment links, sponsor approval actions, payout split execution, fraud enforcement, provider keys, Stripe/AppLovin/RevenueCat secrets, fake sponsor revenue, fake fraud risk scores, or fake revenue.
- Test admin account access may remain active for future proof sessions, but credentials must never be stored, printed, logged, hard-coded, or committed. Admin must never expose Supabase service-role keys, LiveKit secrets, RevenueCat secrets, app-store keys, provider secret keys, or any other secret.
- Ads Launch Foundation V1A is pushed as provider-neutral, no-SDK, no-real-rendering infrastructure. It adds `_lib/ads/adConfig.ts`, `_lib/ads/adEligibility.ts`, `_lib/ads/adProvider.ts`, `_lib/ads/providers/placeholder.ts`, `_lib/ads/adSession.ts`, `hooks/useAdEligibility.ts`, and `hooks/useActiveBrowsingTime.ts`; `app/admin.tsx` was updated only for read-only/foundation Admin Ads status.
- Ads V1A central config defaults exist with `ads_enabled: false`, `ads_provider: placeholder`, interstitial/native enablement defaults, launch caps, 180-second first-interstitial delay, 600-second interstitial spacing, Premium ad-free, and future CTV/creator-page/sponsor flags disabled. The placeholder provider reports not connected and does not call any SDK. Premium/ad-free users are always ineligible and do not increment ad counters. Eligibility blocks forbidden routes/contexts, active browsing time tracking exists for future decisions, session caps exist, and daily caps persist through AsyncStorage.
- Ads V1A did not add AppLovin, Unity LevelPlay, Unity Ads, or AdMob SDKs; did not add real ad IDs, provider initialization, real ad rendering, CTV inventory, fake ad revenue, sponsor revenue, creator earnings, payout balances, invoices, or CTV revenue.
- Ads Launch Foundation V1B is pushed as native/feed placeholder placement foundation only. It adds `components/ads/NativeAdSlot.tsx`, places one native/feed placeholder slot on the Home browsing surface in `app/(tabs)/index.tsx`, updates `_lib/ads/adSession.ts` so placeholder records can use explicit active-browsing seconds for long-use cap proof, and updates Admin Ads copy in `app/admin.tsx` as read-only/foundation status.
- Ads V1B default runtime still hides the Home placeholder because `ads_enabled` defaults false. The placeholder renders only after V1A eligibility passes, Premium/ad-free users never see it and do not increment counters, placeholder native/feed shows record only when eligible, base native/feed session cap is 1, long-use unlock allows a second native/feed placement after 120 active browsing minutes, daily native/feed cap is 3, and forbidden routes/contexts stay blocked.
- Ads V1B did not add real ad rendering, AppLovin/Unity/AdMob SDKs, real ad IDs, provider initialization, interstitials, CTV inventory, fake ad revenue, sponsor revenue, creator earnings, payout balances, invoices, or CTV revenue.
- Ads Launch Foundation V1C is pushed as placeholder interstitial controller foundation only. It adds `components/ads/InterstitialController.tsx`, mounts it in `app/_layout.tsx`, and updates `app/admin.tsx` read-only/foundation Ads copy with `Interstitial placement: placeholder controller foundation` and `No real interstitial ads are live.`
- Ads V1C default runtime still shows no interstitial because `ads_enabled` defaults false and the placeholder provider is not connected. The controller renders `null`, ignores the first route mount so there is no app-launch ad, considers route transitions only, calls central eligibility with `placementKind: "interstitial"`, and records placeholder interstitial shows only after eligibility and placeholder-provider success.
- Ads V1C preserves Premium/ad-free zero ads, 180-second first-interstitial delay, 600-second interstitial spacing, base session cap 3, long-use extra +2 after 120 active browsing minutes, daily cap 6, forbidden route/context blocking, provider-neutral wrapper doctrine, and no direct screen-level SDK calls.
- Ads V1C did not add real ad rendering, AppLovin/Unity/AdMob SDKs, real ad IDs, provider initialization, CTV inventory, fake ad revenue, sponsor revenue, creator earnings, payout balances, invoices, or CTV revenue.
- Ads Config V1D1 app-config `adsLaunch` resolver foundation is pushed. `_lib/appConfig.ts` now includes normalized `adsLaunch` under the existing `app_configurations.config` JSON shape, defaulting through code-owned `ADS_LAUNCH_CONFIG_DEFAULTS` with `ads_enabled: false`; `app/admin.tsx` reads `experienceConfig.adsLaunch` for read-only Admin Ads status copy.
- Ads V1D2 runtime-read foundation is pushed. `hooks/useAdsLaunchConfig.ts` reads cached/default app config first, then safely reads `readAppConfig()` and returns normalized `adsLaunch`; `components/ads/NativeAdSlot.tsx` and `components/ads/InterstitialController.tsx` use that hook when no explicit proof/test config override is provided. Normal runtime remains hidden by default because normalized `ads_enabled` remains false and the placeholder provider is not connected. V1D2 does not use Admin `runtimeControls.ads_enabled`, does not add working Admin toggles, and does not add SDKs, real ad IDs, provider initialization, real rendering, CTV inventory, fake revenue, migrations, generated database type edits, RLS changes, package/native config changes, or Supabase remote changes.
- Public V1 Hardening H1A 18+ Signup Confirmation is pushed as a no-migration signup-only gate in `app/(auth)/signup.tsx`. New account creation now shows `Chi'llywood is for users 18 and older.` and requires the user to actively check `I confirm I am 18 or older.` before `supabase.auth.signUp` is called.
- H1A preserves existing email/password validation order, loading state, closed-beta copy, Terms of Service link, Privacy Policy link, Community Guidelines link, and existing Sign In handoff. If the checkbox is not confirmed, signup shows `18+ confirmation required` with `Confirm you are 18 or older before creating a Chi'llywood account.` and does not call account creation.
- H1A blocks unchecked signup before account creation. H1B2 now provides backed legal acceptance storage for new signups when Supabase returns an authenticated signup session.
- Public V1 Hardening H1B1 private legal acceptance schema foundation is pushed. It adds local migration `supabase/migrations/202605070001_user_account_legal_acceptances.sql` and pure helper `_lib/accountLegalAcceptance.ts`.
- H1B1 creates the intended private table `public.user_account_legal_acceptances` with `user_id`, nullable age/terms/privacy acceptance timestamps and versions, and created/updated timestamps. RLS is enabled with authenticated users allowed to select, insert, and update only their own row. There is no public read, and the table is intentionally separate from `user_profiles` because profile rows are broader social/profile data and are not the right owner for private legal acceptance timestamps.
- Public V1 Hardening H1B2 legal acceptance storage is pushed. Remote Supabase migrations `202605070001_user_account_legal_acceptances.sql` and `202605070002_harden_user_account_legal_acceptance_grants.sql` are applied, `supabase/database.types.ts` was regenerated from the linked remote schema, and signup now writes the accepted age/terms/privacy timestamp plus version to `user_account_legal_acceptances` after account creation succeeds with an authenticated session.
- H1B2 keeps legal acceptance storage out of `user_profiles`, AsyncStorage, and auth metadata. It does not collect full birthdate or ID verification, does not add first-use enforcement, does not block existing signed-in/admin/test users, and does not change login behavior. If Supabase signup returns no authenticated session because email confirmation is required, the app does not fake the write and records no backend acceptance until a later first-use/sign-in persistence lane is explicitly designed.
- H1B2 hardens public access: anon REST reads to `user_account_legal_acceptances` return `401 permission denied`; authenticated users keep owner-only select/insert/update through RLS.
- Public V1 Hardening H2 upload/content lifecycle polish is pushed. It touches `app/channel-settings.tsx` and `components/creator-media/creator-video-card.tsx` only.
- H2 keeps the existing creator-video picker, upload, storage, metadata save, Open Player, Edit, Publish/Unpublish, and Delete paths. It adds local-only Channel Studio Content lifecycle status for `No File Selected`, `File Selected`, `Ready To Upload`, `Uploading...`, `Upload Succeeded`, `Upload Failed`, `Saving Metadata`, and `Editing Metadata`, with honest non-percent upload copy because backed upload progress is not available.
- H2 creator video cards now show owner-facing `Published`/`Draft` status from existing visibility truth and `Media Ready`/`Media Unavailable` from existing source availability. It does not add fake processing, transcoding, archive, retry, storage-billing, payout, revenue, or moderation states; it does not move storage, change RLS/storage policies, change migrations/generated types, or alter Player/Public Channel/Profile/Watch-Party/Live Stage/ads/RevenueCat behavior.
- H2 follow-up duplicate-upload UI correction is pushed. Channel Studio Content now has one clear `Video Upload` form, no extra header/empty-state upload buttons, and `Upload Status` is inline inside that single form instead of reading as a second upload box. The correction touched `app/channel-settings.tsx` only.
- Public V1 Hardening H3 security/compliance/moderation hardening is pushed. It touches `_lib/moderation.ts`, `components/safety/report-sheet.tsx`, `app/settings.tsx`, and `components/system/support-screen.tsx` only.
- H3 keeps the existing backed `safety_reports` categories (`abuse`, `harassment`, `impersonation`, `copyright`, `safety`, `other`) and adds clearer user-facing category descriptions. Scam/fraud/unsafe product/ad concerns are routed honestly through `Safety`; undisclosed sponsorship concerns route through `Other` until a later schema pass. It does not add new report categories, fake moderation actions, fake resolve/dismiss/ban/delete buttons, RLS changes, migrations, generated-type edits, admin bypasses, or unsupported legal-compliance claims.
- H3 adds a direct Settings `Support` entry in the Legal & Support card and adds sponsorship/ad/scam concern copy to Support. Settings still links Privacy Policy, Terms of Use, Community Guidelines, Copyright/DMCA, Account Deletion, and Support; account deletion remains request/help only, not fake destructive deletion.

## Product Truth
- Chi'llywood is production-grade now; future Codex prompts must be exact and scoped, not vague.
- Profile = person/social identity.
- Channel = public mini streaming platform/network.
- Channel Studio = owner-only creator operating system.
- Follow = channel audience.
- Chi'lly Circle = personal mutual friendship/connection.
- Subscribers = monetized channel supporters later.
- Keep Profile, Channel, Channel Studio, Chi'lly Circle, Follow, Subscribers, and Admin concepts separate.
- Public Channel must never show owner-only controls to non-owners.
- Public Channel must never show drafts, private videos, or unpublished content.
- Channel Studio is owner-only.
- `/admin` is the canonical platform owner/operator Admin Command Center route.
- `/channel/[userId]` is the public Channel route.
- `/channel-studio` is the preferred owner Studio route.
- `/channel-settings` remains compatibility and must continue to resolve.

## Locked Business Decisions
- Launch is planned as 18+.
- H1A no-migration signup confirmation is pushed: new signup requires an explicit 18+ checkbox before account creation is attempted.
- H1B2 legal acceptance storage is pushed for new signups with authenticated sessions. The private table is applied remotely, generated database types are regenerated, signup writes age/terms/privacy acceptance timestamps plus versions, and anon table access is denied.
- Free users see ads at launch.
- Premium users see no ads.
- Planned Premium price is `$9.99/month` and `$99/year`.
- Full live/watch-party access is Premium and must not be made free again without an explicit product decision.
- Free users do not get full Live First, Live Watch-Party, Watch-Party Live, or full LiveKit room/token/connect access.
- Free preview was not added. Any future live/watch-party preview must be a separate, explicitly scoped, limited, low-cost, separately gated pass.
- Ads support free browsing.
- Premium supports expensive live usage.
- RevenueCat remains the Premium subscription truth owner.
- AppLovin MAX is the primary ad mediation direction.
- Unity LevelPlay / Unity Ads may be added through AppLovin MAX later.
- Do not build an AdMob-only ad system.
- All future real ad providers must go through the provider-neutral Chi'llywood ad wrapper. Do not make direct screen-level SDK calls.
- Ads launch cap: base active session allows 3 interstitial plus 1 native/feed; after 2 active browsing hours allow +2 interstitial plus +1 native/feed; daily cap is 6 interstitial plus 3 native/feed; Premium sees zero ads.
- Ads must not appear inside active LiveKit rooms, during active video playback, while typing/commenting, during upload, on subscribe/payment screens, immediately at app launch, in Admin, in Channel Studio, in Chat, or in Profile/composer contexts unless explicitly redesigned later.
- Launch app ads are platform money at first. RevenueCat remains Premium subscription truth only and does not take ad revenue. Google Play does not take a subscription fee from AppLovin ad payouts.
- Creator-page ad revenue share is later: creator 70% net and Chi'llywood 30% net. Creator-sold sponsor slots are later: brand pays Chi'llywood first, creator 80% net, and Chi'llywood 20% net. No live creator ad revenue sharing, payout execution, sponsor deal execution, or CTV revenue system exists yet; finance and payout-provider tables are foundation-only.
- CTV ads are future-only for Chi'llywood Originals and network-style content; no CTV inventory is active now.
- Storage doctrine: Cloudflare R2 for public/high-download media; Hetzner Object Storage for source/original uploads, drafts, backups, archive, and private/held/deleted media; Hetzner/OVH boxes for LiveKit and real-time live/watch-party traffic.

## Prompt Standard
All future Codex prompts for Chi'llywood must be production-grade:

- exact product truth
- exact scope
- exact route/screen purpose
- exact UI layout
- exact buttons/actions
- exact data sources
- exact empty/loading/error states
- exact permissions/gates
- exact backend/RLS/storage limits
- exact forbidden areas
- exact validation/manual proof
- exact report format

Do not accept vague prompts such as "modernize", "polish", "add filters", "add route", or "improve dashboard" unless every behavior is spelled out.

## Current Next Action
Recommended next lanes, in order:

1. Store/external launch readiness audit: review App Store / Google Play account readiness, package/bundle identifier truth, public legal/support URLs, Data Safety and privacy disclosure inputs, RevenueCat public setup, and AppLovin readiness gaps without installing SDKs, adding real IDs, touching dashboards from code, or changing runtime behavior.
2. Real AppLovin MAX readiness/integration planning later: only after external AppLovin account/app/ad units are ready; keep provider wrapper architecture, use Unity LevelPlay / Unity Ads later through AppLovin MAX if needed, and do not create an AdMob-only path.
3. Admin Usage later writers/importers only in separate scoped lanes: creator-video upload usage writer V1A0/V1A1/V1A2 is pushed for successful creator-video uploads and daily rollup foundation only, Provider Imports V1D3/V1D4 are pushed as a server-side Edge Function plus read-only Admin provider rows, and Provider Billing Reconciliation Foundation is pushed/deployed as a backend-only reconciliation lane over already-backed provider usage/internal summary rows. Cloudflare R2, Hetzner Object Storage, and Hetzner Servers are callable only server-side; Hetzner Object Storage imports only S3 inventory metadata estimates, not traffic or billing truth. OVH remains not connected. No bandwidth writer, participant-minute writer, provider-bill import, customer billing, overage execution, payout, or revenue writer/job exists yet. Keep Admin Usage read-only and honest: DB estimate, Metadata estimate, Provider import, Reconciliation foundation, Schema connected, or Not connected yet.
4. Finance/admin later writers only in separate scoped lanes: Finance Foundation Remote Proof is complete for migration alignment, generated types, current-build Admin read-only/foundation panels, and anon/non-platform finance-ledger insert denial. Finance F2 payout-provider foundation is complete through schema, RLS proof, read-only Admin counts, and non-live proof rows. Network Billing Foundation and Network Invoice Draft Workflow + Overage Warning/Readout Foundation are complete through remote schema, type/typegen check, read-only Admin Networks counts, and non-live proof rows. Sponsor Checkout Foundation plus Sponsor Review Queue / Disclosure / Moderation / Payment Test-Mode Foundations are complete through remote schema, type refresh, read-only Admin Sponsors counts, and non-live proof rows. Fraud Enforcement Foundation, Fraud Review Queue Foundation, and Creator Revenue Share Ledger / Fraud Hold Enforcement Foundation are complete through remote schema, type refresh, read-only Admin Revenue/Fraud counts, and non-live proof rows. Immutable Admin Audit Log Foundation is complete through remote schema, type refresh, read-only Admin Audit counts/latest rows, append-only trigger/RLS, and anon REST denial proof. Creator Payout Read-Only Dashboard Foundation is complete through the policy-only creator read migration, read-only creator helper, and Channel Studio Payouts tab/card. Stripe Connect Schema Foundation (Payout S2) is complete through additive schema, RLS, remote migration alignment, and generated type refresh only. Stripe Connect Edge Function Skeletons (Payout S3B) is complete as source-only skeleton code, Stripe Connect Test-Mode Backend Functions (Payout S3C) is pushed/deployed as backend-only test-mode functions with account creation/reuse, onboarding-link creation, account sync, signed webhook, and idempotency proof complete, Admin Payout Provider Dashboard is complete as a read-only Admin Payouts provider-status readout, Creator-facing payout setup UI is complete as backend-only/test-mode setup/onboarding/status refresh in Channel Studio, Payout Review Queue + Batch Draft Workflow Foundation is complete through schema, generated types, read-only Admin counts, and foundation-only zero-cent proof rows, Provider Transfer Records Sync Foundation is complete as backend-only/admin-operator/test-mode status import for existing local provider transfer records only, and Provider Billing Reconciliation Foundation is complete as backend-only/admin-operator reconciliation over existing usage rows. Backend Edge Function secrets now include `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` by name/digest only; no values belong in repo docs, logs, commits, or React Native. These remain non-production payout/billing/revenue/fraud foundations with no runtime payout, live billing, invoice send, customer charge, sponsor checkout, sponsor approval, payment link, fake sponsor revenue, fake unpaid balance, fake revenue, fake creator earnings, revenue-share payable balance, payout split execution, payout ledger write from revenue-share rows, fraud enforcement action, payout pause, account restriction, upload restriction, live restriction, monetization disable, fraud risk score, payout approval/release, Withdraw/Cash Out/KYC/tax live flow, fake paid status, fake payable balance, fake earnings, live mode, transfer creation, payout creation, checkout session, or dangerous admin write. Next recommended finance/admin lanes: real source revenue import implementation only after provider-backed source records exist; creator-facing revenue share dashboard only after real source money exists; test-mode payout transfer creation only after provider/fraud/KYC/tax/audit proof; production payout release only after legal/accounting/provider approval. Add provider billing APIs, payout provider production writes, live network billing actions, sponsor checkout, moderator fraud-row proof, live enforcement, admin role writes, real source revenue imports, creator revenue dashboards, or live payout actions only when separately scoped.

## Validation Truth
Latest pushed live access work was checked with `npm run typecheck` and `git diff --check` before commit/push. Free-user runtime proof showed the Home live entry displays the Premium sheet and does not route into `/watch-party`, generate a room code, or request/connect LiveKit; direct `/watch-party?mode=live` was blocked with the same Premium gate. Existing Premium paths were intentionally preserved, but a real entitlement-backed Premium account proof should still be done later when available.

Admin Command Center V1A was checked with `npm run typecheck` and `git diff --check`, then runtime-smoked on Android. Proof passed for signed-out denial, admin/operator access through backend membership, default Home, all Admin V1A tabs opening, foundation-only honesty, no fake money/action systems, System not exposing secrets in UI, and route smoke for Channel Studio, Channel Settings, public Channel, Profile, Player, Watch-Party, and Live Stage. Separate non-admin denial proof with a known non-admin account remains pending.

Admin V1B2B upload kill switch enforcement was checked with `npm run typecheck`, `git diff --check`, source proof that the false branch returns before `uploadCreatorVideo`, scoped forbidden-file checks, and added-line secret scanning. Runtime false-config upload proof with a safe media account remains optional follow-up; default true preserves normal upload behavior.

Admin V1B2C comments kill switch enforcement was checked with `npm run typecheck`, `git diff --check`, source proof that false branches return before `createProfilePostComment` and `createCreatorVideoComment`, scoped forbidden-file checks, and added-line secret scanning. Runtime false-config comment proof with a safe account remains optional follow-up; default true preserves normal comment behavior.

Admin V1B2D profile posting kill switch enforcement was checked with `npm run typecheck`, `git diff --check`, source proof that the false branch returns before `createProfilePost`, scoped forbidden-file checks, and added-line secret scanning. Runtime false-config Profile post proof with a safe account remains optional follow-up; default true preserves normal Profile posting.

Admin V1B2E creator posting kill switch enforcement was checked with `npm run typecheck`, `git diff --check`, source proof that the false branch returns before `createCreatorEvent` and does not block the `updateCreatorEvent` edit branch, scoped forbidden-file checks, and added-line secret scanning. Runtime false-config creator-event proof with a safe account remains optional follow-up; default true preserves normal creator event creation.

Admin V1B2F social attachment kill switch enforcement was checked with `npm run typecheck`, `git diff --check`, source proof that the false branches return before `createProfilePost`, `createProfilePostComment`, and `createCreatorVideoComment`, scoped forbidden-file checks, and added-line secret scanning. Runtime false-config attachment proof with safe accounts/media remains optional follow-up; default true preserves normal social attachment behavior.

Admin V1B2G standalone Chi'lly Chat attachment kill switch enforcement was checked with `npm run typecheck`, `git diff --check`, source proof that the false branch returns before optimistic message insertion and before `sendChatMessage`, scoped forbidden-file checks, and added-line secret scanning. Runtime false-config chat attachment proof with a safe account/thread remains optional follow-up; default true preserves normal chat attachment behavior.

Admin V1B2H room attachment kill switch enforcement was checked with `npm run typecheck`, `git diff --check`, source proof that false branches return before `sendPartyMessageRecord` and before `createSocialAttachmentForSurface`, scoped forbidden-file checks, added-line secret scanning, and the Watch-Party LiveKit camera guard. Runtime false-config room attachment proof with a safe room remains optional follow-up; default true preserves normal room attachment behavior.

Admin V1B2I-A standalone Chi'lly Chat runtime-control enforcement was checked with `npm run typecheck`, `git diff --check`, source proof that false branches return before optimistic message insertion, `sendChatMessage`, `startChatThreadCall`, and `getOrCreateDirectThread`, scoped forbidden-file checks, added-line secret scanning, and the Watch-Party LiveKit camera guard. Runtime false-config standalone Chat proof with a safe account/thread remains optional follow-up; default true preserves normal standalone Chi'lly Chat behavior.

Admin V1B2I-B chat invite runtime-control enforcement was checked with `npm run typecheck`, `git diff --check`, source proof that the false branch returns before `sendDirectInviteMessage`, scoped forbidden-file checks, added-line secret scanning, and the Watch-Party LiveKit camera guard. Runtime false-config invite proof with a safe room/invite target remains optional follow-up; default true preserves normal invite direct-message behavior and system share fallback remains available.

Admin V1B2I-C room comment runtime-control enforcement was checked with `npm run typecheck`, `git diff --check`, source proof that false branches return before `sendPartyMessageRecord` in Watch-Party and Live Stage room comment submitters, scoped forbidden-file checks, added-line secret scanning, and the Watch-Party LiveKit camera guard. Runtime false-config room comment proof with a safe room remains optional follow-up; default true preserves normal room-native text/comment behavior.

Admin V1B runtimeControls closeout was a docs/control truth pass only. It records that `live_first_enabled`, `live_watch_party_enabled`, `watch_party_live_enabled`, and `max_upload_size_mb` are configured foundation-only; Admin `runtimeControls.ads_enabled` is separate from Ads Launch config; and `premium_required_for_live` / `premium_required_for_watch_party` are not runtime switches because Premium gates are enforced through Premium access helpers.

Admin Usage Metering Foundations 37-39 and Admin Usage Metering Foundation V1 remote/provider schema were checked with `npm run typecheck`, `git diff --check`, `supabase db push --dry-run`, `supabase db push`, `supabase gen types typescript --linked --schema public`, `supabase db lint --linked --schema public --fail-on error`, scoped forbidden-file and secret/fake-money scans, anon insert-denial proof, and current-build Android Admin proof. Runtime proof opened `/admin`, selected Usage, and showed `Internal Usage Metering` as schema connected, `Provider Usage Imports` as schema connected with Cloudflare/Hetzner/OVH not connected, `Reconciliation` as not active, plus existing Live Now/Watch-Parties/Uploads, `Storage Metadata Estimate`, `Participant-Minutes DB Estimate`, `Bandwidth Metering`, and cost-risk copy. Proof showed storage as a metadata estimate, participant-minutes as a DB estimate, bandwidth/LiveKit/provider billing metering as not connected, and no fake provider usage, bills, overages, revenue, or usage event writers. Finance migration is now applied and finance proof is closed out separately below.

Admin Usage Writer V1A0/V1A1/V1A2 creator-video upload usage was checked with `supabase db lint --linked --schema public --fail-on error`, `npm run typecheck`, `git diff --check`, source proof, scoped forbidden-file scans, anon RPC-denial proof for V1A0, and current-build Android safe-upload proof for V1A1. V1A1 runtime proof showed upload success, `usage_metering_recorded`, one `video_uploaded` usage row, one `storage_bytes` metadata row, and idempotent replay returning `skipped` without double-counting. V1A2 remote migration/type refresh was checked with linked migration history alignment, linked schema lint, regenerated database types, `npm run typecheck`, and `git diff --check`; runtime proof used a direct database session with Supabase auth context for an existing operator. The proof called `rollup_creator_video_upload_usage_daily` for May 7, 2026, saw two `video_uploaded` source rows and two `storage_bytes` source rows, upserted two upload daily summary rows and two storage daily summary rows, replayed idempotently with the same row counts/quantities, and confirmed an unauthenticated call returns `unauthenticated` without rolling up. Admin Usage remains read-only/honest and now states that only the creator-video upload writer is active; broader app usage writers remain not connected.

Admin Usage Provider Imports V1D3/V1D4 were checked with `npm run typecheck`, `git diff --check`, `supabase db push --dry-run`, `supabase db push`, `supabase functions deploy provider-usage-import`, scoped forbidden-file scans, package/native diff checks, and secret-string scans. The deployed function rejects missing auth with `401`. V1D4 proof used the existing signed-in device session token in memory only and printed no token/credentials/secrets. Owner/operator proof completed Hetzner Object Storage dry-run and write import, inserted two provider metadata rows for `s3_inventory_storage_bytes` and `s3_inventory_object_count`, and `all_configured` dry-run included Cloudflare R2, Hetzner Object Storage, and Hetzner Servers. Admin Usage remains read-only and honest: Cloudflare R2, Hetzner Object Storage, Hetzner Servers, OVH Object Storage, and OVH Servers show provider-import state without fake bills, overages, revenue, invoices, payouts, sponsor money, fake bandwidth, or traffic/billing claims.

Ledger Systems 4A-4D finance foundation was checked with `npm run typecheck`, `git diff --check`, scoped forbidden-file scans, and secret/provider-key scans in the original foundation pass. The finance migration was later applied remotely only to unblock ordered usage V1A2 migration inclusion, and generated database types were refreshed from the linked remote schema. Finance Foundation Remote Proof then confirmed migration history aligned through `202605070011`, generated types include the finance tables, linked schema lint has no public-schema errors, current-build Android Admin opens Revenue/Payouts/Networks/Sponsors/Fraud as read-only/foundation with zero foundation rows, and anon/non-platform finance-ledger insert attempts are denied by RLS. Because no money rows were inserted for proof, non-empty anon read-denial and moderator fraud-row visibility remain future safe-seed/test-lane proof items. No package/native SDK files changed, no Stripe/AppLovin/RevenueCat provider keys were added, and no real payout, invoice, sponsor checkout, fraud enforcement, provider integration, or live money totals were added.

Finance F2B creator payout provider schema foundation was checked with `supabase db push`, `supabase migration list`, `supabase gen types typescript --linked --schema public`, `npm run typecheck`, `git diff --check`, and scoped secret/provider scans. Migration `202605080001_creator_payout_provider_foundation.sql` is applied remotely, generated database types include the payout-provider foundation tables, and no runtime app/Admin/creator UI files changed in that pass. No Stripe SDK, Stripe keys, provider calls, payout buttons, withdrawal buttons, live payout execution, payout-provider creator self-read, moderator access, package/native SDK files, or forbidden surfaces were added.

Finance F2C payout-provider schema/RLS proof confirmed migration history includes `202605080001`, linked schema lint reports no public-schema errors, generated database types include all five payout-provider tables, and anonymous REST insert into `creator_payout_accounts` is denied by RLS/grants. Anonymous empty-table reads return no row data; non-empty row visibility denial and owner/operator row proof remain scoped to the later safe proof-row lane. No files changed during F2C except repo-truth docs.

Finance F2D payout provider Admin foundation readout was checked with `npm run typecheck`, `git diff --check`, scoped file checks, and a scoped secret/provider scan. Only `_lib/platformFinance.ts` and `app/admin.tsx` changed. Admin Payouts remains read-only/foundation and now shows payout-provider foundation counts without live money totals, payout buttons, withdrawal buttons, provider integration, Stripe keys, KYC flow, provider calls, or creator-facing balances.

Finance F2E payout provider foundation proof rows were checked with `supabase db push --dry-run`, `supabase db push`, `supabase migration list`, `supabase db lint --linked --schema public --fail-on error`, `npm run typecheck`, `git diff --check`, scoped secret/provider scans, and anonymous REST proof. Remote migration history includes `202605080002`; anonymous reads see zero proof rows and anonymous insert remains denied. No generated database types, app runtime files, Admin action buttons, Stripe/provider SDKs, provider keys, live payout actions, payout balances, invoices, sponsor checkout, or forbidden surfaces were added.

Network Billing Foundation was checked with `supabase db push --dry-run`, `supabase db push`, `supabase migration list`, `supabase gen types typescript --linked --schema public`, `npm run typecheck`, `git diff --check`, scoped secret/payment-action scans, and anonymous REST proof. Remote migration history includes `202605080003`; dry-run reports the remote database is up to date. Admin Networks remains read-only/foundation and shows counts only for accounts, plans, assignments, quotas, invoice drafts, line items, overage events, and audit rows. No invoice send action, charge action, payment link, Stripe SDK/key, provider secret, fake revenue, fake unpaid balance, live overage approval, customer billing, sponsor checkout, payout execution, or forbidden surface change was added. Linked DB lint remains blocked by the known Supabase CLI login-password auth issue, not by migration SQL.

Network Invoice Draft Workflow Foundation and Network Overage Warning/Readout Foundation were checked with `supabase db push --dry-run`, `supabase db push`, `supabase migration list`, `supabase gen types typescript --linked --schema public`, `supabase db lint --linked --schema public --fail-on error`, `npm run typecheck`, `git diff --check`, scoped SDK/secret/payment-action scans, and anonymous REST proof. Remote migration history includes `202605080011`; post-apply dry-run reports the remote database is up to date and linked public-schema lint has no errors. Generated database types had no checked-in diff because the migration changed constraints/indexes/proof rows, not table shapes. Anonymous REST reads return no invoice/line-item/overage rows, and anonymous insert into `network_invoice_records` is denied by RLS. Admin Networks remains read-only/foundation with invoice draft and overage warning counts only. Proof rows are zero-value foundation rows marked `live_money_action: false`, `customer_charge: false`, `invoice_sent: false`, and/or `billing_execution: false`. Runtime visual Admin proof should be rerun later on a current signed-in build. No invoice send, customer charge, payment link, Stripe Billing/Checkout/SDK, provider SDK/secret, fake revenue, fake unpaid balance, real customer obligation, real overage billing, broad RLS access, unrelated migration, unrelated database type change, or forbidden surface change was added.

Sponsor Checkout Foundation was checked with `supabase db push --dry-run`, `supabase db push`, `supabase migration list`, `supabase gen types typescript --linked --schema public`, `supabase db lint --linked --schema public --fail-on error`, `npm run typecheck`, `git diff --check`, scoped secret/payment-action scans, and anonymous REST proof. Remote migration history includes `202605080004`; dry-run reports the remote database is up to date, linked public-schema lint has no errors, and anonymous REST reads see zero visible sponsor foundation rows. Admin Sponsors remains read-only/foundation and shows counts only for sponsor brands, sponsor deals, creatives, placements, disclosures, review logs, payment records, and payout split records. No sponsor checkout, payment link, Stripe Checkout, sponsor approval action, brand charge, fake sponsor revenue, payable balance, creator payout split execution, provider SDK/key, broad RLS access, or forbidden surface change was added.

Fraud Enforcement Foundation was checked with `supabase db push --dry-run`, `supabase db push`, `supabase migration list`, `supabase gen types typescript --linked --schema public`, `npm run typecheck`, `git diff --check`, scoped secret/enforcement-action scans, and anonymous REST proof. Remote migration history includes `202605080005`; post-apply dry-run reports the remote database is up to date. Linked DB lint is currently blocked by the known Supabase CLI `cli_login_postgres` password-auth issue, not by migration SQL. Anonymous REST reads see zero visible fraud foundation rows and anonymous insert into `fraud_reason_records` is denied. Admin Fraud remains read-only/foundation and shows counts only for fraud holds, fraud reasons, evidence records, planned actions, review notes, appeal placeholders, and fraud audit logs. No payout pause, account restriction, upload restriction, live restriction, monetization disable, fraud risk score, enforcement action, provider SDK/key, broad RLS access, or forbidden surface change was added.

Creator Revenue Share Ledger Foundation + Fraud Hold Enforcement Foundation was checked with `supabase db push --dry-run`, `supabase db push`, `supabase migration list`, `supabase gen types typescript --linked --schema public`, `supabase db lint --linked --schema public --fail-on error`, `npm run typecheck`, `git diff --check`, scoped SDK/secret/fake-earnings/enforcement-action scans, staged-file proof, source/helper/DB proof, and anonymous REST denial proof for the new revenue-share and fraud enforcement policy tables. Remote migration history includes `202605080013`; post-apply dry-run reports the remote database is up to date and linked public-schema lint has no errors. Admin Revenue and Admin Fraud remain read-only/foundation with counts only. Revenue-share proof rows are zero cents and marked no source money, no live money action, and no payable balance created. Fraud hold enforcement proof rows are marked no live enforcement and no runtime hook connected. Runtime visual Admin proof should be rerun later on a current signed-in build. No SDK, provider secret, payment credential, fake earnings, payable balance, payout execution, payout approval/release, payout ledger write from revenue-share rows, fraud enforcement, payout pause, monetization disable, upload restriction, live restriction, sponsor restriction, account restriction, fake risk score, broad RLS access, unrelated migration, unrelated generated database type change, or forbidden surface change was added.

Immutable Admin Audit Log Foundation was checked with `supabase db push --dry-run`, `supabase db push`, `supabase migration list`, `supabase gen types typescript --linked --schema public`, `supabase db lint --linked --schema public --fail-on error`, `npm run typecheck`, `git diff --check`, scoped secret/dangerous-action scans, and anonymous REST proof. Remote migration history includes `202605080006`; post-apply dry-run reports the remote database is up to date, linked public-schema lint has no errors, and anonymous REST read/insert/update/delete attempts against `platform_admin_audit_logs` return `401 permission denied`. Admin Audit remains read-only/foundation and can show latest immutable rows if connected while preserving the derived audit summary separately. No audit edit/delete/clear UI, payout approval/release, invoice send/charge, sponsor checkout/approval, fraud enforcement, account restriction, upload/live restriction, monetization disable, provider SDK/key, broad RLS access, or forbidden surface change was added.

Creator Payout Read-Only Dashboard Foundation was checked with `npm run typecheck`, `git diff --check`, `supabase db push --dry-run`, `supabase migration list`, Supabase lint, scoped SDK/config/secret/live-payout scans, policy/source inspection, and database/source/helper proof. Remote migration history includes `202605080007`; the migration is policy-only and `supabase/database.types.ts` did not change after linked typegen. Channel Studio has a read-only Payouts tab/card, and `_lib/creatorPayouts.ts` reads only current creator payout foundation rows. No proof rows were inserted because no safe test creator was identified. Runtime visual proof was attempted but should be rerun later on a current signed-in build. No Stripe SDK/key, provider SDK/secret, service-role usage, payout execution, transfer creation, withdrawal/cash-out/Connect Stripe/KYC/tax flow, payout approval/release, fake payable balance, fake earnings, broad payout access, or forbidden surface change was added.

Stripe Connect Schema Foundation (Payout S2) was checked with `supabase db push --dry-run`, `supabase db push`, `supabase migration list`, `supabase gen types typescript --linked --schema public`, `npm run typecheck`, `git diff --check`, scoped SDK/config/secret/live-payout scans, and staged-file proof. Remote migration history includes `202605080008`; post-apply dry-run reports the remote database is up to date. Linked DB lint was blocked by the known Supabase CLI temp-role auth throttle, not by migration SQL. No app runtime/Admin/Channel Studio files changed. No Stripe SDK/key, provider SDK/secret, provider call, service-role usage, Stripe account creation, onboarding link creation, transfer creation, payout execution, Connect Stripe button, Withdraw/Cash Out, KYC/tax live flow, payout approval/release, fake payable balance, fake earnings, broad RLS access, package/native config, or forbidden surface change was added.

Stripe Connect Edge Function Skeletons (Payout S3B) was checked with `npm run typecheck`, `git diff --check`, scoped SDK/config/secret/provider-call scans, staged-file proof, and direct TypeScript/static syntax validation for the four new Edge Function files. Only `supabase/config.toml` and `supabase/functions/stripe-connect-account`, `supabase/functions/stripe-connect-onboarding-link`, `supabase/functions/stripe-connect-account-sync`, and `supabase/functions/stripe-connect-webhook` changed. No functions were deployed, no Supabase remote state was touched, no migration or generated database type changed, and no app UI changed. No Stripe SDK/key, `STRIPE_*` env read, provider SDK/secret, provider call, service-role client, Stripe account creation, onboarding link creation, webhook storage, transfer creation, payout execution, checkout session, Connect Stripe button, creator-facing payout setup button, fake payable balance, fake earnings, package/native config, or forbidden surface change was added.

Stripe Connect Test-Mode Backend Functions (Payout S3C) was checked with `npm run typecheck`, `git diff --check`, a direct TypeScript/static syntax validation pass for the Edge Function sources, scoped SDK/config/secret/live-money scans, `supabase migration list`, `supabase functions deploy` for only the four Stripe Connect functions, `supabase functions list`, `supabase secrets list`, and remote function smoke proof. The deployed functions are ACTIVE version 1. No-auth account/link/sync calls return `401`, wrong-method calls return `405`, OPTIONS returns `ok`, and webhook returned `not_configured` before the webhook secret was configured. Remote Edge Function secrets now include `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` by name/digest only; values are not recorded in repo truth. A follow-up test-mode provider proof rerun passed for connected-account creation, account reuse, onboarding-link creation, and account sync through the deployed backend functions; provider readiness stayed non-payable (`eligible_for_payouts: false`, `minimum_payout_met: false`, no payout execution active), audit rows were written by the function flows, and invalid webhook signatures are rejected. Signed webhook/idempotency proof later passed with Stripe CLI/Dashboard resend: the valid test-mode `account.updated` event was accepted, one provider event row was stored with test-mode metadata and status `ignored` because no matching local payout account existed, received/processed audit rows were written, and duplicate resend of the same event did not create a second event row or second processing audit. After secret exposure, the webhook signing secret was rotated by recreating the test webhook endpoint, deleting the old endpoint, updating the Supabase Edge Function secret, and rerunning signed-event/idempotency proof successfully. No migrations, generated database types, app UI, package/native SDK config, React Native Stripe SDK, Stripe key in repo, provider secret in repo, Connect Stripe UI, Withdraw/Cash Out, KYC/tax UI, payout approval/release, fake payable balance, fake earnings, live mode, transfer, payout, checkout session, or live money action was added.

Payout Review Queue + Batch Draft Workflow Foundation was checked with `npm run typecheck`, `git diff --check`, `supabase db push --dry-run`, `supabase migration list`, Supabase lint, generated database type refresh, scoped SDK/config/secret/live-payout scans, staged-file proof, source/helper/DB proof, and anonymous REST denial proof for the new review/batch tables. Remote migration history includes `202605080009`; post-apply dry-run reports the remote database is up to date. Admin Payouts remains read-only/foundation and can show payout review records, review notes, batches, batch items, provider transfers, holds, accounts, ledger entries, and audit rows as counts/foundation rows only. Proof rows are foundation-only, zero cents, and marked `live_money_action: false`; runtime visual Admin proof should be rerun later on a current signed-in build. No package/native SDK config, Stripe SDK/key, provider SDK/secret, payment credential, Connect Stripe UI, Withdraw/Cash Out, KYC/tax UI, payout approval/release, batch processing, transfer creation, payout execution, fake payable balance, fake earnings, broad RLS access, unrelated migration, unrelated generated database type change, or forbidden surface change was added.

Provider Transfer Records Sync Foundation was checked with `npm run typecheck`, `git diff --check`, pre-apply `supabase db push --dry-run`, `supabase db push`, `supabase migration list`, linked schema lint, generated database type refresh, `supabase functions deploy stripe-connect-transfer-sync`, `supabase functions list`, `supabase secrets list`, remote no-auth/wrong-method/OPTIONS function smoke proof, anonymous REST read/insert/update/delete proof, scoped SDK/config/secret/live-money scans, and staged-file proof. Remote migration history includes `202605080010`; post-apply dry-run was retried but hit the known intermittent Supabase CLI `cli_login_postgres` SASL auth error while `supabase migration list` showed local/remote alignment through `202605080010`. Deno is not installed locally; Edge Function syntax proof came from the successful Supabase deploy plus a TypeScript fallback check whose only remaining failures were Deno/npm import ambient types. `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` are configured as backend Edge Function secret names by digest only; values are not recorded. No existing real Stripe test-mode provider transfer id was available, so no provider transfer was synced and no transfer was created for proof. Admin Payouts remains read-only/foundation; provider transfer status imports do not move money. No React Native Stripe SDK, Stripe key in repo, provider secret in repo, package/native SDK config, Connect Stripe UI, Withdraw/Cash Out, KYC/tax UI, payout approval/release, batch processing, transfer creation, payout creation, checkout session, fake paid status, fake payable balance, fake earnings, live mode, or live money movement was added.

Ads Launch Foundation V1A was checked with `npm run typecheck` and `git diff --check`, helper-level eligibility/cap proof, and Admin Ads foundation proof. It remains no-SDK, no-real-ad, no-real-provider, no-real-revenue foundation only.

Ads Launch Foundation V1B was checked with `npm run typecheck`, `git diff --check`, helper-level native/feed eligibility/cap proof, current-build Android runtime proof for default Home hidden state, Admin Ads read-only/foundation status, and route smoke for Channel Studio, Channel Settings, public Channel, Profile, Player, Watch-Party, and Live Stage. It remains no-SDK, no-real-ad, no-real-provider, no-real-revenue foundation only.

Ads Launch Foundation V1C was checked with `npm run typecheck`, `git diff --check`, scoped forbidden-file/dependency scans, and helper-level placeholder interstitial proof. Proof passed for default hidden runtime, initial route mount blocked, safe free-user eligibility after 180 active browsing seconds, Premium/ad-free blocking, 600-second spacing, base and long-use session caps, daily cap, forbidden routes, forbidden contexts, unknown context blocking, background app blocking, and provider-not-connected blocking. It remains no-SDK, no-real-ad, no-real-provider, no-real-revenue foundation only.

Ads Config V1D1 app-config `adsLaunch` resolver foundation was checked with `npm run typecheck`, `git diff --check`, scoped forbidden-file scans, and source proof before commit/push. It adds optional normalized `app_configurations.config.adsLaunch` defaults and read-only Admin Ads copy only.

Ads V1D2 runtime-read foundation was checked with `npm run typecheck`, `git diff --check`, scoped forbidden-file/dependency scans, Android current-build Home proof for default hidden ads, and Android `/admin` Ads tab proof for normalized-source read-only copy. It wires `NativeAdSlot` and `InterstitialController` to normalized `app_config.adsLaunch` through `hooks/useAdsLaunchConfig.ts` while keeping explicit config overrides for proof harnesses. Normal runtime remains hidden by default because `ads_enabled=false` and the placeholder provider is not connected.

Admin Command Center sign-in entry was checked with `npm run typecheck`, `git diff --check`, current-build Android dev-client proof, and live `/admin` proof with an operator account. Runtime proof confirmed the Login screen can enter `Admin Command Center Sign In`, routes to `/admin` after normal sign-in, preserves backend platform-role denial/allow behavior, and does not store, print, or hard-code credentials.

Public V1 current-build release proof was completed on an attached Android Samsung SM-N986U1 through the Expo dev client on Metro 8081. Proof opened Home, Explore, Settings, Support, Terms, Privacy, Community Guidelines, Channel Studio, `/channel-settings` compatibility, Rachi Profile, Rachi public Channel, Player `t1`, Watch-Party waiting room, and `/admin`. Home showed no visible ad placeholder while `ads_enabled=false`; `/admin` showed the Admin Command Center tabs; Settings showed legal/support entries; Player and Watch-Party opened without crash. The signed-in auth state redirected away from the Login screen, so the Admin sign-in entry was source-proved in `app/(auth)/login.tsx` rather than logging out or printing credentials. No runtime files were changed during proof.

Public V1 Hardening H1A 18+ Signup Confirmation was checked with `npm run typecheck`, `git diff --check`, and current-build Android dev-client proof. Runtime proof confirmed the signup screen shows the 18+ copy and unchecked checkbox, pressing Sign Up while unchecked shows the required 18+ alert before account creation, checking the box falls through to the existing signup validation path, Terms/Privacy/Community Guidelines links still open, and the Sign In handoff still returns to Login. Real existing-account login proof and release-build signup proof remain pending.

Public V1 Hardening H1B1 private legal acceptance schema foundation was checked with `npm run typecheck` and `git diff --check` before commit/push. It added only the local migration and pure helper, did not apply remote migrations, did not hand-edit generated database types, and did not change runtime app behavior.

Public V1 Hardening H1B2 legal acceptance storage was checked with `npm run typecheck`, `git diff --check`, `supabase migration list`, generated type inspection, and anon REST denial proof. Remote migrations `202605070001` and `202605070002` are applied. Anon REST reads to `user_account_legal_acceptances` return `401 permission denied`. Runtime signup write proof with a safe disposable account is still pending because no test signup identity was provided in the implementation/proof pass.

Public V1 Hardening H2 upload/content lifecycle polish was checked with `npm run typecheck`, `git diff --check`, scoped forbidden-file diff checks, and current-build Android dev-client no-mutation proof. Runtime proof confirmed a disposable signed-in proof account could open `/channel-studio?tab=content`, the Content tab rendered summary cards and empty Creator Library truth, the upload panel showed `Upload Status`, `No File Selected`, honest no-file guidance, `Choose Video File`, title/description fields, and no fake percent progress or processing/transcoding/archive/retry states. The proof account had no videos, so publish/unpublish/delete confirmation modals still need safe-media proof later.

Public V1 Hardening H3 security/compliance/moderation hardening was checked with `npm run typecheck`, `git diff --check`, scoped forbidden-file and secret-string scans, and current-build Android dev-client smoke. Runtime proof confirmed Settings shows Privacy Policy, Terms of Use, Community Guidelines, Copyright/DMCA, Support, and Request Account Deletion; Support opens and shows sponsorship/ad/scam concern copy; a backed title Report Sheet opens with backed category chips and honest later-schema copy; and a non-admin account is denied from `/admin` without exposing Admin Command Center content or privileged actions. Longer report submission and admin queue workflow proof remain pending.

## Staging Discipline
- Work on current `main` only.
- Keep unrelated local dirt out of the checkpoint.
- Never stage `artifacts/`.
- Never stage `supabase/.temp/`.
- Stage only task-pure files for the active lane.
