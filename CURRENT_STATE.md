# CURRENT STATE

## Current Checkpoint
An April 20, 2026 Content Studio programming snapshot pass now lands the fifth creator/content-management implementation batch on `main` without changing schema, remote DB state, route truth, or any public surface files. `app/admin.tsx` now reads `created_at` alongside the existing title programming signals and renders a compact Current Programming Snapshot inside Experience Config that resolves the current Home hero outcome from the active hero strategy (`latest`, `hero_flag`, or `manual_title`), shows the real title that currently wins that resolution, reports the configured Top Picks source plus its real backing-title count, and makes guardrail fallback truth explicit whenever the configured source is currently unbacked. This keeps the improvement operator-facing only, gives Content Studio a direct at-a-glance view of current Home programming truth without leaving admin, and passes `npm run typecheck`. The next exact lane is a narrow Content Studio manual hero selection workflow batch on `main`.

An April 20, 2026 Content Studio publication-state consistency pass now lands the fourth creator/content-management implementation batch on `main` without changing schema, remote DB state, or route truth. `app/admin.tsx` now normalizes publication truth before title edits persist: editor open/save flows and publication patch paths now reconcile `status`, `is_published`, and `release_at` into one coherent state, scheduled titles without a usable future `release_at` now fall back safely to real `draft` or `published` truth, and stale scheduling timestamps are cleared whenever a title is saved as `draft`, `published`, or `archived`. This preserves the current Content Studio shape, keeps downstream Home and `/profile/[userId]` consumers honest, and passes `npm run typecheck`. The next exact lane is a narrow Content Studio source-of-truth closeout audit on `main`.

An April 20, 2026 Content Studio programming-guardrails pass now lands the third creator/content-management implementation batch on `main` without changing schema, remote DB state, or route truth. `app/admin.tsx` now hardens the Content Studio experience-config save path so weak programming states are normalized before they persist: stale `manualHeroTitleId` values now clear safely, invalid manual hero strategy now falls back to real `hero_flag` or `latest` truth, `hero_flag` now falls back to `latest` when no actual hero-flagged title exists, and `topPicksSource` now resets to `recent` when `featured`, `trending`, or `top_row` no longer have real matching titles. This preserves the current config structure, keeps Home and `/profile/[userId]` consumers honest when curated pools are absent, and passes `npm run typecheck`. The next exact lane is a narrow Content Studio publication-state consistency batch on `main`.

An April 20, 2026 profile content programming-consumption pass now lands the second creator/content-management implementation batch on `main` without changing schema, remote DB state, or route truth. `app/profile/[userId].tsx` now reads the already-landed title/programming truth and app-config hero truth so the canonical profile/channel content surface can speak from real content signals instead of placeholder-only doctrine cards: the route now loads current titles with hero/featured/trending/top-row flags plus config-driven hero selection, folds that truth into the `Content` tab cards and related public content framing, and keeps honest fallback copy when real programming truth is absent. This preserves `/profile/[userId]` as the canonical profile/channel route, avoids fake creator catalogs or fake uploads, keeps `Chi'lly Chat`, `Watch-Party Live`, and `Live Watch-Party` doctrine unchanged, and passes `npm run typecheck`. The next exact lane is a narrow Content Studio publication-and-programming audit on `main`.

An April 20, 2026 Home programming-consumption pass now lands the first creator/content-management implementation batch on `main` without changing schema, remote DB state, or route truth. `app/(tabs)/index.tsx` now consumes more of the already-landed Content Studio and app-config programming truth by honoring config-driven hero selection (`latest`, `hero_flag`, `manual_title`) beneath the existing continue-watching override, wiring the top picks rail to the real configured source (`recent`, `top_row`, `featured`, or `trending`) with honest fallback to recent titles when a configured source is empty, rendering Home rails in the configured order, surfacing the continue-watching rail when it is both enabled and backed by real progress data, and using the configured hero kicker instead of a hardcoded string. This keeps Home honest when programming truth is absent, preserves current route doctrine, avoids any profile/channel reopening, and passes `npm run typecheck`. The next exact lane is a narrow profile content programming-consumption batch on `main`.

An April 20, 2026 roadmap-selection pass now locks **creator/content management deepening** as the next major focus after the completed profile/channel chapter. This choice is grounded in current Public v1 repo truth: `app/admin.tsx` already owns a real Content Studio foundation for titles, release windows, hero/featured/trending/top-row curation, home-rail controls, and creator grants; `_lib/appConfig.ts` already owns the shared home/config surface those controls drive; and Home, title, and profile/channel surfaces already contain honest content- and curation-facing hooks that can benefit from deeper real programming without reopening route doctrine. By contrast, live/event scheduling lacks a canonical product owner, heavier access/monetization rollout stays blocked by external billing/compliance truth, audience/analytics expansion beyond current summary truth still lacks backend aggregation, and deeper safety/admin work has lower immediate product leverage than better content programming. No product code, UI, schema, routes, or runtime behavior changed in this pass. The next exact lane is a narrow Content Studio / creator-content management audit on `main`.

An April 19, 2026 final Stage 5 `/channel-settings` audience-visibility surface pass now lands the real visibility summary layer on `main` without changing schema, remote DB state, or route truth. `app/channel-settings.tsx` now renders the backed audience-visibility truth from `readChannelAudienceSummary(channelUserId)` for `publicActivityVisibility`, `followerSurfaceEnabled`, and `subscriberSurfaceEnabled`, while unsupported deeper audience-role controls like VIP/mod/co-host still stay explicitly later instead of being fabricated. The existing Stage 5 audience, analytics, and safety/admin summary structure remains intact, no unrelated UI owners changed, and `npm run typecheck` passes. This closes the current profile/channel chapter cleanly on the active doctrine, and the next exact lane is a narrow post-profile/channel roadmap selection pass on `main`.

An April 19, 2026 audience-visibility truth pass now lands the minimum honest schema + helper foundation for the remaining Stage 5 audience-visibility blocker on `main`. `user_profiles` now owns canonical truth for `public_activity_visibility`, `follower_surface_enabled`, and `subscriber_surface_enabled` through a narrow migration and generated schema update, and `readChannelAudienceSummary(channelUserId)` now returns those values from real profile-backed visibility truth instead of reporting them as missing. No UI code changed, no remote DB state changed in this pass, and route doctrine stayed intact across `/profile/[userId]`, `/channel-settings`, `/chat`, and all watch-party/live owners. `npm run typecheck` passes. This clears the last backing-truth blocker behind audience visibility work and makes the next exact lane a narrow `/channel-settings` audience-visibility surface pass on `main`.

An April 19, 2026 Stage 5 `/channel-settings` summary-surface pass now lands the honest audience, analytics, and safety/admin summary layer on `main` without changing schema, remote DB state, or route truth. `app/channel-settings.tsx` now consumes the landed `readChannelAudienceSummary(channelUserId)`, `readCreatorAnalyticsSummary(channelUserId)`, and `readChannelSafetyAdminSummary(channelUserId)` helpers to render the current Stage 5 owner-safe summary surface on the canonical studio-equivalent route. Audience now shows real follower, subscriber, pending-request, and blocked-audience counts; analytics now shows only the real hosted room/session metrics plus latest activity and audience-linked signals; safety/admin now shows current role/access/report summary truth. Unsupported audience-role, audience-visibility, profile-visit, attendance, content-performance, and conversion fields remain explicitly unavailable or later instead of being zeroed or fabricated. This closes the Stage 5 summary-surface lane cleanly, but full Stage 5 still stops short of real audience visibility controls because that backing truth is still missing. `npm run typecheck` passes. The next exact lane is a narrow audience-visibility truth pass on `main`.

An April 19, 2026 creator-analytics blocker-resolution pass now lands the minimal honest creator analytics helper summary on `main` without changing UI code, schema, remote DB state, or route truth. The existing channel read-model owner now exposes `readCreatorAnalyticsSummary(channelUserId)` and returns only the creator metrics that are fully supported by current repo/schema truth: `watchPartySessionsHosted`, `liveSessionsHosted`, `communicationRoomsHosted`, `activeHostedRooms`, `latestHostedActivityAt`, `followerCount`, and `subscriberCount`. Unsupported creator metrics like `profileVisits`, `liveAttendanceTotal`, `contentLaunches`, `continueWatchingReturns`, and `gatedSurfaceViews` remain explicitly `null` with `missing` field status instead of being fabricated. This closes the creator-analytics blocker-resolution lane cleanly, keeps Stage 5 UI honest about what is and is not available, and makes the next exact lane a narrow `/channel-settings` Stage 5 summary-surface pass on `main`. `npm run typecheck` passes.

An April 19, 2026 Stage 5 helper read-model pass now lands the minimal honest owner-scoped summary readers for channel audience and channel safety/admin on `main` without changing UI code, schema, remote DB state, or route truth. A new helper owner now exposes `readChannelAudienceSummary(channelUserId)` from the landed audience relationship foundation and `readChannelSafetyAdminSummary(channelUserId)` from the existing moderation/platform-role truth. The audience reader only returns real counts that are now fully supported by schema (`channel_followers`, `channel_subscribers`, `channel_audience_requests`, `channel_audience_blocks`) and explicitly returns `null` plus `missing` status for still-unsupported fields like VIP/mod/co-host counts and audience visibility surfaces. The safety/admin reader stays aligned with current operator/admin doctrine by combining `getModerationAccess(...)`, `readMyPlatformRoleMemberships()`, official-account truth, and the existing safety-report queue. Creator-facing analytics aggregates remain honestly blocked. `npm run typecheck` passes. This closes the helper read-model lane cleanly and makes the next exact lane a narrow creator-analytics blocker-resolution pass on `main`.

An April 19, 2026 Stage 5 audience-relationship foundation pass now lands the minimum canonical backend/schema truth for channel followers, creator/channel subscribers, pending audience requests, and blocked audience on `main` without changing UI code, remote DB state, or route truth. The repo now has narrow relationship tables and RLS posture for `channel_followers`, `channel_subscribers`, `channel_audience_requests`, and `channel_audience_blocks`, and the durable profile/channel implementation spec now records their exact ownership rules, status fields, indexes, and current-vs-later doctrine fit. This clears the audience-schema half of the Stage 5 blocker cleanly while keeping creator-facing analytics aggregates honestly blocked. No Stage 5 UI ships in this lane, no fake audience counts or creator analytics are introduced, and `npm run typecheck` passes. This makes the next exact lane a narrow helper read-model pass for channel audience summary and channel safety/admin summary on `main`.

An April 19, 2026 Stage 5 prerequisite doctrine/data-model pass now lands the canonical blocker truth for profile/channel Stage 5 on `main` without changing UI code, schema, remote DB state, or route truth. The current repo can already support a later helper-only `ChannelSafetyAdminReadModel` from `platform_role_memberships` and `safety_reports`, and it can later derive a narrow owner-scoped room/session analytics slice from `watch_party_rooms` and `communication_rooms`, but full Stage 5 remains blocked because the canonical audience and creator-analytics truth is still missing at the backend/schema layer. There is still no channel-scoped truth for followers, creator/channel subscribers, pending audience requests, blocked audience, or creator-facing aggregate metrics like profile visits, follows, subscriber counts, and content performance. The durable implementation spec now records the canonical `ChannelAudienceReadModel`, `CreatorAnalyticsReadModel`, and `ChannelSafetyAdminReadModel` plus exact field-level support status. This closes the Stage 5 prerequisite doctrine/data-model lane cleanly and makes the next exact lane a narrow backend/schema foundation pass for channel audience relationships on `main`.

An April 19, 2026 Stage 4 profile/channel implementation pass now lands the access & monetization surface layer on `main` without changing schema, remote DB state, or canonical route truth. The current route owners stay intact: `app/profile/[userId].tsx` now uses existing `user_profiles` defaults and `creator_permissions` truth to make channel access posture visible on the canonical public/owner profile surface, while `app/channel-settings.tsx` now exposes an explicit Access & Monetization section that summarizes current watch-party defaults, communication defaults, and creator grant availability on the existing studio-equivalent route. The implementation stays honest to current repo truth by reusing existing `party_pass` / `premium` capability signals instead of inventing unsupported purchase behavior, preserves `/chat` and `/chat/[threadId]` as the canonical Chi'lly Chat owners, preserves locked `Watch-Party Live` / `Live Watch-Party` semantics, introduces no `/studio*` route drift, and passes `npm run typecheck`. This closes Stage 4 cleanly and makes Stage 5 audience / analytics / safety the next exact lane.

An April 19, 2026 Stage 3 profile/channel implementation pass now lands the `/channel-settings` studio-equivalent expansion on `main` without changing schema, remote DB state, or canonical route truth. `app/channel-settings.tsx` remains the single owner of the current private creator control center and now reads as a structured sectioned surface instead of a narrow defaults page: the route now exposes an explicit section map for Identity, Design, Layout, Content, Access & Monetization, Audience, Analytics, and Safety/Admin, while the current route keeps Channel Identity, Watch Party Defaults, and Communication Defaults intact and adds near-term section entry points for Design, Layout, and Content on the same route. `/profile/[userId]` remains the shared public + owner-facing identity/channel surface, `/chat` and `/chat/[threadId]` remain the canonical Chi'lly Chat routes, no `/studio*` route truth was introduced, and `npm run typecheck` passes. This closes Stage 3 cleanly and makes Stage 4 access & monetization implementation the next exact lane.

An April 19, 2026 Stage 2 profile/channel implementation pass now lands owner mode on the same canonical `/profile/[userId]` route on `main` without changing schema, remote DB state, or route truth. `app/profile/[userId].tsx` remains the single owner of the unified public profile/channel surface, and self-view now adds an owner-only ribbon layer on that same route: owner stats stay visible without leaking to visitor mode, owner quick actions now keep `Manage Channel` pointing to `/channel-settings`, and owner setup prompts now make the next channel-shaping steps explicit without splitting the creator experience into a separate app. The Stage 1 public tabbed surface remains intact for visitors and owners alike, Chi'lly Chat remains the canonical messaging handoff, `Watch-Party Live` / `Live Watch-Party` doctrine remains unchanged, and `npm run typecheck` passes. This closes Stage 2 cleanly and makes Stage 3 `/channel-settings` studio-equivalent expansion the next exact lane.

An April 19, 2026 Stage 1 profile/channel implementation pass now lands the public unified `/profile/[userId]` profile/channel foundation on `main` without changing schema, remote DB state, or canonical route truth. The canonical owner remains `app/profile/[userId].tsx`, but the route now reads as one unified public profile/channel surface instead of the earlier profile-vs-channel split: the hero section stays intact, the identity/stats row remains visible, the primary action row keeps canonical Chi'lly Chat plus linked live/watch-party handoff semantics, and a new public tab strip now owns `Home`, `Content`, `Live`, `Community`, and `About` on the same route. The tab content now follows the landed implementation spec's MVP order and public responsibilities while preserving locked `Watch-Party Live` / `Live Watch-Party` meaning, preserving Chi'lly Chat route ownership, and avoiding any `/studio*` route drift. `npm run typecheck` passes. This closes Stage 1 cleanly and makes Stage 2 owner-mode implementation on the same canonical `/profile/[userId]` route the next exact lane.

An April 19, 2026 doctrine-only profile/channel translation pass now lands `docs/profile-channel-implementation-spec.md` as the durable repo-aligned implementation spec for Chi'llywood's profile/channel system on `main`. The new spec translates the full product blueprint into current route truth without changing UI code, schema, remote DB state, or active route ownership: `/profile/[userId]` stays the single canonical public-facing profile/channel surface, owner mode stays on that same route, `/channel-settings` stays the current studio-equivalent control center, `/chat` and `/chat/[threadId]` remain the canonical Chi'lly Chat routes, and `Watch-Party Live` / `Live Watch-Party` label doctrine remains locked. The spec now defines exact ownership by current files/routes, exact public-vs-owner responsibilities, exact `/channel-settings` section architecture, explicit current-vs-later route truth, the required view-model layer, and a five-stage implementation order. This closes the doctrine/spec translation lane cleanly and makes Stage 1 implementation of the public unified `/profile/[userId]` profile/channel surface the next exact execution lane.

An April 19, 2026 narrow typed player title-hydration pass now lands generated DB typing for the DB-facing `titles` hydration in `app/player/[id].tsx` on `main` without changing runtime behavior, live schema, or remote DB state. The player's DB-facing `TitleRow` now relies on generated `Tables<>` / `Pick<>` schema truth plus a tiny local compatibility extension for non-DB fields, the advanced/base title reads and the by-name `id` lookup now use typed `.returns<...>()`, and the remaining `as TitleRow` casts are removed. The existing local fallback behavior, nullable handling, monetization gate behavior, and rendering stay unchanged. `npm run typecheck` passes. This closes the narrow typed player title-hydration batch without widening into profile/channel work, other screens/helpers, UI behavior, or schema work.

An April 19, 2026 narrow typed Home screen pass now lands generated DB typing for the direct DB reads in `app/(tabs)/index.tsx` on `main` without changing runtime behavior, live schema, or remote DB state. The local title and watch-party live-metadata row shims in that owner are now generated `Tables<>` / `Pick<>`-based aliases, the direct title and live-metadata result casts are removed in favor of the typed client surface, and the existing title ordering, local fallback behavior, nullable handling, live-room metadata aggregation, and rendering stay unchanged. `npm run typecheck` passes. This closes the narrow typed Home screen batch without widening into title detail, profile/channel work, helpers, UI behavior, or schema work.

An April 19, 2026 narrow typed title-details screen pass now lands generated DB typing for the direct DB reads in `app/title/[id].tsx` on `main` without changing runtime behavior, live schema, or remote DB state. The local title and watch-party live-metadata row shims in that owner are now generated `Tables<>` / `Pick<>`-based aliases, the direct `as TitleRow` / watch-party metadata casts are removed in favor of the typed client surface, and the existing exact-id / exact-title resolution, local fallback behavior, nullable-schema handling, and rendering stay unchanged. `npm run typecheck` passes. This closes the narrow typed title-details screen batch without widening into Home, profile/channel work, helpers, UI behavior, or schema work.

An April 19, 2026 narrow typed title-list screen pass now lands generated DB typing for the direct title-list reads in `app/(tabs)/explore.tsx` and `app/(tabs)/my-list.tsx` on `main` without changing runtime behavior, live schema, or remote DB state. The local `TitleRow` shims in those two owners are now generated `Tables<>` / `Pick<>`-based aliases, the direct `as TitleRow[]` casts are removed in favor of the typed client surface, and the existing title ordering, local fallback behavior, and rendering stay unchanged. `npm run typecheck` passes. This closes the narrow typed title-list screen batch without widening into home/title detail owners, helpers, UI behavior, or schema work.

An April 19, 2026 narrow typed watch-party sync-and-message pass now lands generated DB typing for the remaining sync/message seams in `_lib/watchParty.ts` on `main` without changing runtime behavior, live schema, or remote DB state. The watch-party sync-event insert path now uses the same generated `TablesInsert<>`-based ownership as the rest of the helper, the message insert path now uses generated insert typing, and the message read path now relies on a generated `watch_party_room_messages` row shape via the typed client instead of `Record<string, unknown>` mapping. The already-landed watch-party room/membership read-side and write-side typing, compatibility fallback behavior, and returned room/membership models stay unchanged, and `npm run typecheck` passes. This closes the remaining typed watch-party helper seam without widening into screen owners, UI behavior, or schema work.

An April 19, 2026 narrow typed watch-party room-and-membership write-side pass now lands generated DB typing for `_lib/watchParty.ts` room and membership write payloads on `main` without changing runtime behavior, live schema, or remote DB state. The remaining direct insert, upsert, and update shapes for `watch_party_rooms` and `watch_party_room_memberships` now rely on generated `TablesInsert<>` / `TablesUpdate<>` typing, including the membership upsert payload, the current room-create payload, the legacy-compatible fallback room-create payload, the room-policy update payload, the participant-state update payload, and the playback update payload. The already-landed watch-party room/membership read-side result shapes, compatibility fallback behavior, sync-event logic, message logic, and returned room/membership models stay unchanged, and `npm run typecheck` passes. This closes the narrow typed watch-party room-and-membership write-side batch without widening into watch-party sync/message typing, screen owners, UI behavior, or schema work.

An April 19, 2026 narrow typed watch-party room-and-membership read-side pass now lands generated DB typing for `_lib/watchParty.ts` room and membership result modeling on `main` without changing runtime behavior, live schema, or remote DB state. The helper-local room and membership row shims are now generated `Tables<>` / `Pick<>`-based aliases, the room read model now distinguishes the full current room-select shape from the legacy fallback base shape, and the old read-side casts across room fetches, room-create selected returns, membership lists, room-policy selected returns, participant-state selected returns, and membership upsert selected returns are removed in favor of the typed client surface plus typed null/string guards. Compatibility fallback behavior and returned watch-party room/membership shapes remain unchanged, and `npm run typecheck` passes. This closes the narrow typed watch-party room-and-membership read-side batch without widening into watch-party write payloads, message typing, screen owners, UI behavior, or schema work.

An April 19, 2026 narrow typed communication write-side pass now lands generated DB typing for `_lib/communication.ts` write payloads on `main` without changing runtime behavior, live schema, or remote DB state. The remaining direct insert, upsert, and update shapes for `communication_rooms` and `communication_room_memberships` now rely on generated `TablesInsert<>` / `TablesUpdate<>` typing, including the current room-create payload, the legacy-compatible fallback create payload, the membership session upsert payload, the member touch/update payload, and the room end/update payload. The already-landed read-side room and membership result shapes, compatibility fallback behavior, realtime/media/config/auth helper behavior, and returned communication models stay unchanged, and `npm run typecheck` passes. This closes the narrow typed communication write-side batch without widening into watch-party helpers, screen owners, UI behavior, or schema work.

An April 19, 2026 narrow typed communication read-side pass now lands generated DB typing for `_lib/communication.ts` read modeling on `main` without changing runtime behavior, live schema, or remote DB state. The helper-local room and membership row shims are now generated `Tables<>` / `Pick<>`-based aliases, the room read model now distinguishes the full current room-select shape from the legacy fallback base shape, and the old read-side casts across room fetches, code lookups, linked-room lookups, membership lists, and selected-return parsing are removed in favor of the typed client surface plus a typed null guard. Compatibility fallback behavior and returned shapes remain unchanged, and `npm run typecheck` passes. This closes the narrow typed communication read-side batch without widening into communication write payloads, watch-party helpers, or UI owners.

An April 19, 2026 narrow typed chat write-side pass now lands generated DB typing for `_lib/chat.ts` write payloads on `main` without changing runtime behavior, live schema, or remote DB state. The remaining direct insert and update shapes for `chat_threads`, `chat_thread_members`, and `chat_messages` now rely on generated `TablesInsert<>` / `TablesUpdate<>` typing, including the direct-thread member-row builder, thread insert path, message insert path, and thread/member update paths. The already-landed read-side chat result shapes and realtime callbacks stay unchanged, and `npm run typecheck` passes. This closes the helper-only typed chat lane without widening into communication/watch-party helpers or UI owners.

An April 19, 2026 narrow typed chat read-side pass now lands generated DB typing for `_lib/chat.ts` read modeling on `main` without changing runtime behavior, live schema, or remote DB state. The helper-local row shims for `chat_thread_members`, `chat_messages`, and the chat-facing `user_profiles` search result are now generated `Tables<>`-based aliases, the thread read model now uses a local nested `chat_threads` plus `members` select type instead of an ad hoc row shape, and the old read-side casts across thread lists, single-thread reads, message reads, search reads, and parsed message/thread results are removed in favor of the typed client surface. `npm run typecheck` passes. This closes the narrow typed chat read-side batch without widening into chat write payloads, communication/watch-party helpers, or UI owners.

An April 19, 2026 typed user-data helper pass now lands generated DB typing in `_lib/userData.ts` on `main` without changing runtime behavior, live schema, or remote DB state. The remaining handwritten row shims for `user_profiles` and `watch_history` are replaced with generated row `Pick<>` shapes that match the actual selected fields, `user_profiles`, `user_list`, and `watch_history` writes now rely on generated insert typing, and the old `Record<string, unknown>[]` parsing path for remote my-list merges is removed in favor of the typed client surface. `npm run typecheck` passes. This closes the typed user-data helper batch without widening into chat, watch-party, communication, player, title, or other UI-facing owners.

An April 19, 2026 typed beta-and-moderation helper pass now lands generated DB typing in `_lib/betaProgram.tsx` and `_lib/moderation.ts` on `main` without changing runtime behavior, live schema, or remote DB state. The handwritten beta membership row shim is replaced by repo-owned generated table typing, beta feedback and safety-report writes now use generated insert typing, platform-role and safety-report reads now rely on generated table rows instead of loose `Array<Record<string, unknown>>` parsing, and beta RPC result handling stays behavior-preserving while narrowing onto the typed client surface. `npm run typecheck` passes. This closes the next helper-only typed-schema adoption batch without widening into chat, communication, watch-party, player, room, or title owners.

An April 19, 2026 typed config-and-monetization helper pass now lands generated DB typing in `_lib/appConfig.ts` and `_lib/monetization.ts` on `main` without changing runtime behavior, live schema, or remote DB state. The handwritten `AppConfigRow` and `CreatorPermissionRow` shims are replaced by repo-owned generated table types, write payloads for `app_configurations`, `creator_permissions`, `user_subscriptions`, and `watch_party_pass_unlocks` now use generated insert typing, and the remaining loose casts in those two helpers are reduced to the typed client surface instead of ad hoc local row definitions. `npm run typecheck` passes. This closes the smallest helper-only typed-schema adoption batch without widening into chat, communication, watch-party, player, or room owners.

An April 19, 2026 typed-schema foundation pass now lands repo-owned database types and typed shared Supabase clients on `main` without changing live schema or remote DB state. Repo truth now includes `supabase/database.types.ts` generated from the normalized current schema truth, `_lib/supabase.ts` and `app/lib/_supabase.ts` now instantiate `createClient<Database>(...)`, and the exact compatibility fixes proved by typecheck are now closed in `_lib/betaProgram.tsx`, `_lib/moderation.ts`, `_lib/userData.ts`, `_lib/watchParty.ts`, and `app/admin.tsx`. The lane preserved runtime behavior while aligning the checked-in client surface with actual schema truth: JSON context payloads now conform to typed `Json`, watch-history upserts use the typed insert shape, the invalid `titles.view_count` / `watch_count` assumption is removed to match the real schema, sync-event writes now use the typed `watch_party_sync_events` fields, DB-facing title ids are narrowed to strings, and `npm run typecheck` passes. This closes the typed-schema foundation blocker without touching migrations, UI behavior, or live schema state.

An April 19, 2026 remote migration-history normalization pass now closes the baseline-cutover bookkeeping lane on `main` without changing live remote schema. The active local bootstrap path remains the single baseline file `supabase/migrations/202604190004_baseline_current_schema_truth.sql`, the old chain remains archived unchanged in `supabase/migrations_legacy/`, and remote migration history is now normalized to that active path: the legacy pre-baseline versions were marked reverted in bookkeeping only, `202604190004` was marked applied, `supabase migration list` now matches local-vs-remote on the single active baseline version, and `supabase db push --dry-run` now reports `Remote database is up to date.` This closes the history/bookkeeping blocker without applying schema SQL in the lane.

An April 19, 2026 baseline-cutover pass now makes the canonical future bootstrap path a single baseline file on `main` without changing live remote DB state. Repo-side bootstrap truth now lives in `supabase/migrations/202604190004_baseline_current_schema_truth.sql` as the only active migration in `supabase/migrations/`. The prior migration chain is preserved unchanged in `supabase/migrations_legacy/` as legacy-only history, including both duplicate `202603270009` files and the three reconciliation migrations for `app_configurations`, `creator_permissions`, and `user_profiles`. This closes the bootstrap-path cutover lane itself while explicitly deferring remote migration-history normalization / bookkeeping to a later separate lane.

An April 19, 2026 schema-reconciliation pass now closes the remaining `public.user_profiles` remote-schema truth lane on `main`. Repo-side migration truth now lives in `supabase/migrations/202604190003_reconcile_user_profiles_remote_truth.sql`. Remote apply and verification are complete for `public.user_profiles`: the live table still exists with `user_id`, `username`, `avatar_index`, `display_name`, `avatar_url`, `tagline`, `channel_role`, `likes_visibility`, `shares_visibility`, the default room-policy columns, and `updated_at`; the primary key on `user_id` and `user_profiles_updated_at_idx` on `updated_at desc` both exist; RLS stays enabled; authenticated select plus self-owned insert/update behavior are still intact; anon read is blocked; and non-self authenticated writes are blocked. The table-level grant posture is also now narrower because anon grants are revoked. This closes the remaining direct schema-reconciliation lane for the three active repo-dependent tables. Historical/bootstrap migration drift is still the remaining schema housekeeping concern, and generated DB types are still deferred until that follow-up is intentionally handled.

An April 19, 2026 schema-reconciliation checkpoint now closes the first remote-schema truth lane on `main`. Commit `f96c503` (`reconcile app config and creator permissions schema truth`) saves the repo-side migration truth for `public.app_configurations` and `public.creator_permissions` in `supabase/migrations/202604190001_reconcile_app_configurations_remote_truth.sql` and `supabase/migrations/202604190002_reconcile_creator_permissions_remote_truth.sql`. Remote apply and verification are complete for both tables: `app_configurations` now keeps public read while effective public writes are removed, operator-only writes are present, and the sanitize function plus before-write trigger exist; `creator_permissions` now revokes anon grants, removes the old permissive public policies, keeps self/operator read plus operator-only write policies, blocks anon read, and blocks non-operator authenticated writes. This schema lane is now closed. The remaining schema-reconciliation lane is `public.user_profiles`. Historical/bootstrap migration drift still exists as a later cleanup concern, but it is not the active task.

An April 18, 2026 narrow mainline runtime-fix pass now removes the duplicate `RTCVideoView` crash on clean `main` without reopening watch-party layout, route doctrine, or visual design. Exact owner proof showed the crash came from eager legacy `react-native-webrtc` loading alongside LiveKit RTC ownership in the same JS runtime: `components/watch-party-live/livekit-stage-media-surface.tsx` loads the LiveKit RTC path, while `app/watch-party/live-stage/[partyId].tsx`, `components/communication/communication-participant-grid.tsx`, `components/communication/communication-preview-card.tsx`, and `hooks/use-communication-room-session.ts` were still touching the legacy RTC module before fallback was actually active. The fix is intentionally narrow and durable: legacy RTC loading is now gated behind active fallback use only, so `main` no longer registers duplicate native `RTCVideoView` owners. Real-device proof from `main` now re-confirms that Party Room renders, `Watch-Party Live` renders, and Live Stage renders again, with the approved branded/background visual truth visible instead of the prior redbox.

A mainline consolidation pass on April 18, 2026 now carries the durable LiveKit watch-party/runtime foundation onto clean `main` without discarding the newer approved player/live-stage layout line that was already present there. The active owners added in this chunk are `_lib/livekit/bootstrap.ts`, `_lib/livekit/dom-exception-polyfill.ts`, `_lib/livekit/join-boundary.ts`, `_lib/livekit/token-contract.ts`, `_lib/runtimeConfig.ts`, `components/watch-party-live/livekit-stage-media-surface.tsx`, `app/watch-party/[partyId].tsx`, `app/watch-party/live-stage/[partyId].tsx`, `app/player/[id].tsx`, and `supabase/functions/livekit-token/index.ts`. Repo truth on this consolidated base is now that `Watch-Party Live` remains the player/title-owned shared-player layer inside Party flow, `Live First` / `Live Watch-Party` remain distinct live-stage modes, Party Room still stays on `/watch-party/[partyId]`, Live Room / Live Stage still stay on `/watch-party/live-stage/[partyId]`, and the LiveKit room/token/runtime wiring now lives on top of that newer room/player shell instead of on a stranded side branch.

The same April 18, 2026 consolidation now also carries the durable LiveKit token-function auth fix and the Firebase monitoring foundation together on `main`. `supabase/config.toml` now owns `[functions.livekit-token] verify_jwt = false`, while `supabase/functions/livekit-token/index.ts` remains the server-side auth/token issuer that verifies callers inside the function body. Firebase Analytics and Remote Config remain the existing analytics/config owners, `_lib/firebaseCrashlytics.ts` is the Crashlytics owner, `_lib/firebasePerformance.ts` is the Performance Monitoring owner, `_lib/logger.ts` remains the shared non-fatal runtime-error mirror, `app/_layout.tsx` remains the monitoring bootstrap owner, `components/dev/dev-debug-overlay.tsx` remains the dev-only proof owner, `app.config.ts` remains the native Firebase plugin owner, and `firebase.json` remains the checked-in Crashlytics collection config. Repo truth still remains that there is no checked-in Sentry integration to remove on this branch, and native/device Firebase console proof is still pending.

A narrow Watch-Party participant-scale pass on April 13, 2026 now makes the two Watch-Party participant surfaces structurally large-count capable on clean `main` without reopening transport, route doctrine, or broader room semantics. The active owners were only `app/player/[id].tsx` and `app/watch-party/live-stage/[partyId].tsx`. Exact owner proof showed that both surfaces were still rendering participant collections through raw `ScrollView` mapping, which was honest for small groups but not a truthful high-count structure. The owner-local fix now virtualizes both surfaces: `Watch-Party Live` on `app/player/[id].tsx` uses a horizontal `FlatList` for the shared-player participant rail, and `Live Watch-Party` on `app/watch-party/live-stage/[partyId].tsx` uses a row-virtualized `FlatList` for the hybrid community panel instead of raw mapped scroll content. This closes the structural participant-scale blocker for scrolling through very large participant counts on both Watch-Party surfaces while preserving the rest of each surface exactly as-is. `npm run typecheck` passed in the clean worktree before push. Repo truth remains honest: this is large-count participant browsing support, not proof of 500 simultaneous equal live video feeds, and the reusable communication-room RTC transport still caps active live seats at four.

A narrow Watch-Party Live Android player-surface pass on April 13, 2026 now closes the black shared-player regression on the active owner `app/player/[id].tsx` without reopening launch or warm-intent routing. Exact proof showed the `/player/...` route was still reaching JS on both real Android phones, but the shared Watch-Party playback surface could stay black after route handoff even while analytics and source logs were correct. The owner-local fix now switches only the Android `Watch-Party Live` playback path to `expo-video` `VideoView` with `surfaceType="textureView"` while preserving the existing seek/play/pause/rate control contract through a narrow wrapper, and keys the native video view by source identity so the bundle-to-remote source swap remounts cleanly instead of hitting the earlier `Cannot set prop 'player'` failure. Two-phone runtime proof on the canonical shared-player route `/player/f0d03df8-ced8-433f-a5c0-e2b930813eb0?partyId=5RUUH4` now shows actual video frames, the refined `Comments` / `Menu` dock, and the participant rail on both physical Android phones instead of a black sheet. This closes the Android shared-player paint blocker for the mounted Watch-Party route; cold dev-client entry remains a separate harness concern and is not reopened by this checkpoint.

A narrow Watch-Party Live participant-dock fix pass on April 13, 2026 now corrects the remaining participant-presentation mismatch on the refined shared-player owner `app/player/[id].tsx` without reopening the broader Watch-Party shell or unrelated surfaces. Exact owner proof showed that the reconciled refined player was still rendering the dock through a dock-only vertical override branch: each participant sat inside a rounded row shell with visible card chrome, which conflicted with the intended floating side-by-side participant presentation. The owner-local fix removes that dock-only row override and lets the dock reuse the floating horizontal participant strip, while preserving halo-free avatars, clear host identity, the refined `Comments` / `Menu` dock, and the branded Watch-Party background/header. Two-phone device proof on the canonical shared-player route `/player/f0d03df8-ced8-433f-a5c0-e2b930813eb0?partyId=5RUUH4` now confirms the intended participant treatment on the refined surface: `PROTECTED WATCH SESSION` is still gone, the old top host/sync pill is still gone, participants now sit side-by-side directly on the Chi'llywood background with no per-person card shell and no white border/halo, `Comments` / `Menu` still render correctly, `Party Room` return still works, `My List` still works, playback-rate selection still shows a visible active `2x` state, the real comments composer/send/render path still works, and standalone player without `partyId` still stays on its separate solo surface. `npm run typecheck` passed after the owner-local change. High-count participant scale is now structurally supported by the horizontal participant scroller, but hundreds-participant overflow is not yet physically proved.

A narrow Watch-Party Live reconciliation pass on April 13, 2026 now lands the intended refined shared-player surface on clean `main` without mixing unrelated local dirt. The active owner stayed single-file at `app/player/[id].tsx` because clean main already exposed the needed `sendPartyMessage(...)` helper from `_lib/watchParty.ts`, so the reconcile lane could extract only the owner-local Watch-Party delta in a clean worktree. This pass preserves the already-landed host halo clip fix while bringing the previously local refined Watch-Party surface onto main: `PROTECTED WATCH SESSION` is no longer shown on the shared party player, the old top host/sync pill is suppressed for `Watch-Party Live`, the Chi'llywood-native branded background/header treatment is restored, the old top-right chip cluster is replaced by the intended `Comments` / `Menu` dock, the real party comment composer/send/render path is present on the refined surface, and the participant area is moved onto the refined dock instead of the old horizontal strip. `npm run typecheck` passed in the clean worktree before push. This checkpoint is a mainline reconciliation, not a fresh runtime closure: the exact next lane is a new two-phone Watch-Party Live proof pass on the now-correct refined layout.

A clean Live Stage layout-alignment landing on April 12, 2026 now closes the active stage-presentation refinement lane on `main` without reopening media routing, room truth, role logic, or broader room doctrine. The active owner remains `app/watch-party/live-stage/[partyId].tsx`. `Live Watch-Party` now keeps the landed hybrid cleanup: no extra hybrid info box or side rails, a cleaner longer scrollable `Chi'lly Party Members` card with separated room count, and the bottom room-comments lane instead of the old hybrid pills. `Live First` now shares that cleaner stage rhythm while staying live-only: the old caption/reveal box is gone, the cleaner top row stays, the shared bottom comments lane is in place, and physical Android proof confirmed tap-to-hide and tap-to-show still work on the live-stage route. Same-branch device proof also re-confirmed that `Live Watch-Party` stayed intact, and the remaining low-confidence adb comment-entry noise did not prove a real owner regression. `npm run typecheck` passed on the landed branch before merge, and this combined Live Stage layout alignment is now saved on `main`.

A clean Live Stage `Live Watch-Party` hybrid refinement landing on April 12, 2026 now closes the active hybrid-only layout lane on `main` without reopening Live First, media routing, room truth, or role logic. The active owner remained `app/watch-party/live-stage/[partyId].tsx`, and the landed hybrid-only work now removes the extra hybrid info box and the pre-tap side rails, keeps a single cleaner `Chi'lly Party Members` community card with separated room count, extends that card into a longer scrollable surface for larger audiences, and replaces the old bottom hybrid pills with the real room-comments lane at the bottom of the stage. Runtime/device proof on the clean hybrid branch confirmed the requested layout rhythm and preserved hybrid functions, while `Live First` stayed on its separate preserved presentation path with no intentional changes in this landing. `npm run typecheck` passed on the landed hybrid branch before merge.

A narrow Live Stage `Live Watch-Party` clutter-cleanup pass on April 12, 2026 now closes the specific default-bottom-overlay blocker on the active live-stage owner without reopening transport, route doctrine, or broader room work. Owner proof stayed local to `app/watch-party/live-stage/[partyId].tsx`: the repeated `LIVE WATCH-PARTY` pills, duplicated title/body stack, and the default hybrid-mode bottom info card all came from JSX in that owner (`stageHeroOverlay`, the hidden-state `stageTapRevealSurface`, and the hybrid branch under `stageBottomInfoCard` / community header rendering). The fix now suppresses those default informational text stacks in `Live Watch-Party` mode while keeping the stage reveal surface and participant/video logic intact, and leaving `Live First` copy untouched. Runtime proof on the physical Android dev build confirmed that `Live Watch-Party` no longer shows the bottom informational clutter by default on `Live Stage`, while `Live First` still renders its preserved caption UI. `npm run typecheck` passed after the owner-local change.

A narrow Live Stage overlay/menu interaction proof pass on April 12, 2026 now saves a truthful partial checkpoint for the active live-room interaction lane without overclaiming the broader `Live Watch-Party` functionality. On two real Android phones, the owner-local interaction work in `app/watch-party/live-stage/[partyId].tsx` now re-proves that host and viewer can both open the top `Menu` reliably after invisible touch-bound improvements only; the visible control size stayed unchanged, `tap-to-show` still works, `5-second auto-hide` still works, `Comments` and `React` reset the overlay timer, and the menu sheet now dismisses as soon as a real panel opens so those panels no longer sit visually inside the menu box. `npm run typecheck` passed after the owner-local interaction changes. This is only a partial Live Stage checkpoint: the broader `Live Watch-Party` toggle and its role-aware functionality are still open and must be proved or fixed next before the full Live / Live Stage lane can be called honestly closed.

A narrow Chi'lly Chat two-phone video-call closure pass on April 11, 2026 now closes the active Expo dev-client social runtime lane without reopening broader chat/call architecture, Watch-Party, or Live Stage. After the earlier call-lane cleanup in commit `c2394cc` and the repo-truth sync in commit `b395e68`, the remaining real blocker moved from noisy dev-client logs to transport and then to post-ICE media proof. The durable owner-small path now includes relay-capable ICE runtime support in `app.config.ts` and `_lib/communication.ts`, plus owner-local RTC diagnostics and stream handling hardening in `hooks/use-communication-room-session.ts` that proved the earlier zero-at-connect RTP conclusion was only an early-sample false negative during settle rather than a persistent media-path failure. Real Twilio ICE servers were minted locally, loaded into `.env.local`, Metro/dev-client was restarted, and a real two-phone rerun then proved that ICE reached connected/completed, remote video became visibly live on both physical Android phones, host end still worked, both phones returned cleanly to the direct thread, and reopen stale-state cleanup still left the thread on `No Active Call` instead of reviving an ended room. `npm run typecheck` finished cleanly after the current proof-lane code changes. The active Chi'lly Chat call presentation truth remains unchanged: active thread video calls present as a full-page call surface from chat, exactly 2 participants use a true 50/50 split, and 3+ participants use a balanced full-page grid. This social runtime proof lane is now honestly closed and the repo can return to the broader testing/store-readiness queue already recorded.

A narrow Chi'lly Chat call-lane runtime-proof pass on April 11, 2026 is now pushed on `origin/main` as commit `c2394cc` and closes the misleading dev-client "many errors" noise that surfaced during the real two-phone Expo proof. The exact issue was not a fresh thread-call failure, signaling regression, or provider regression; it was render-only Chi'lly Chat call instrumentation in the active chat/call lane still using `console.error(...)`, which React Native dev-client surfaced as `E ReactNativeJS` error noise on-device. The owner-small fix keeps the already-landed chat/call behavior intact while changing only those render-only call logs to `console.log(...)` inside the current call lane owners. Two-device proof then confirmed those render-only call logs no longer surface as `E ReactNativeJS` noise, and `npm run typecheck` passed. The active Chi'lly Chat video-call presentation truth now remains: active thread video calls present as a full-page call surface from chat, exactly 2 participants use a true 50/50 split, and 3+ participants use a balanced full-page grid. Previously landed call-lane fixes remain preserved on `main`, including the PostHog provider/runtime fix, the thread-lane PostHog hook removal, live-room invite recovery, communication signaling/channel hardening, and the full-page in-thread call presentation/workflow now proved in the chat lane.

A narrow Chi'lly Chat invite-identity hardening pass on April 10, 2026 now removes the next most likely repo-side blocker before final two-real-phone Expo dev-client proof of internal invite search and direct-thread video calling. After the earlier `user_profiles` sync fix, the remaining risk was not realtime subscription ownership, Party/Live routing, or communication-room architecture; it was default identity quality. If a real signed-in member reached the app without an existing saved profile row, `_lib/userData.ts` could still generate a random `Studio Guest ####` username, which kept the row technically searchable but made two-phone people lookup brittle because the fallback identity was unpredictable and hard to query from the invite sheet. The owner-small fix now derives the initial fallback username from the signed-in auth email local-part when available and also seeds the fallback display name from auth metadata before syncing into `public.user_profiles`. This keeps the canonical profile system intact, keeps Chi'lly Chat thread-based sharing intact, and avoids reopening auth/session, Party/Live doctrine, or communication-room architecture. `npm run typecheck` passed after the fix. The exact next proof target remains the same final two-real-phone Expo dev-client verification: find the other user in the internal invite sheet, send the invite into the correct direct thread, start and join a fresh thread video call, end and restart, leave and rejoin, and kill/reopen to confirm stale-call state stays cleared.

A narrow Chi'lly Chat social-identity hardening pass on April 10, 2026 now closes the most likely remaining repo-side blocker before final two-real-phone Expo dev-client proof of the new internal invite path and the stabilized direct-thread video-call path. The exact risk was not Party/Live routing, direct-thread ownership, or communication-room architecture; it was social identity persistence. `_lib/chat.ts` already used authenticated `user_profiles` search for the new Chi'llywood-native invite sheet, but `_lib/userData.ts` still persisted the active member profile only in local AsyncStorage, which meant a real signed-in device could be fully usable locally while still remaining invisible to `user_profiles`-backed people search on another phone. The owner-small fix now keeps canonical local profile behavior intact while best-effort syncing the signed-in member profile into `public.user_profiles` on read/save, and it also hydrates local profile state from the remote row when available. This does not change Chi'lly Chat doctrine, Party/Live doctrine, profile doctrine, Rachi doctrine, or communication-room architecture; it only hardens the canonical social identity foundation so the new invite search is less likely to fail during the final two-phone Expo dev-client run. `npm run typecheck` passed after the fix. The exact next proof target is now the two-real-phone Expo dev-client verification of: find the other user in the internal invite sheet, send the invite into the correct direct thread, start and join a fresh thread video call, end and restart, leave and rejoin, and kill/reopen to confirm stale-call state stays cleared.

A narrow Chi'llywood-native people search / internal share-invite pass on April 10, 2026 now closes a real repo-owned social-sharing gap on the active Party and Live surfaces without reopening monetization, admin, iOS/store-identity work, Party/Live route doctrine, profile doctrine, Rachi doctrine, or broader Chi'lly Chat architecture. The exact blocker was not room creation or thread ownership; it was missing internal share wiring. `app/watch-party/index.tsx`, `app/watch-party/[partyId].tsx`, and `app/watch-party/live-stage/[partyId].tsx` all still surfaced room-code display or system share, while the repo had no reusable people-search owner and no small shared path for "find a Chi'llywood member -> open/create direct thread -> send invite inside Chi'llywood." The owner-small fix now adds that missing shared path without inventing a second product: `_lib/chat.ts` now exposes authenticated `user_profiles` search plus a direct-thread invite send helper, `components/chat/internal-invite-sheet.tsx` now provides the single reusable in-app people-picker/search shell, and the active Party Waiting Room, Party Room, and Live Room owners now route invite/share into that Chi'lly Chat-native path first while retaining generic system share only as fallback. This keeps sharing canonical to Chi'lly Chat, keeps Party and Live separate, and keeps `/communication` out of the user-facing invite model. `npm run typecheck` passed after the fix. The exact next lane should return to the broader repo sequence already recorded unless a fresh runtime proof exposes a direct regression in this new internal invite path.

A narrow Chi'lly Chat video-call stabilization pass on April 10, 2026 now closes a real repo-owned direct-thread calling blocker without reopening monetization, admin, Party flow, Live flow, profile doctrine, Rachi doctrine, or broader communication architecture. The active owner for this lane was `app/chat/[threadId].tsx`, which remains the canonical Chi'lly Chat thread owner on `/chat/[threadId]` and correctly reuses `useCommunicationRoomSession(...)` for thread-based voice/video instead of drifting into `/communication` as a user-facing product. The exact blocker was stale active-call state, not the base call architecture: if a thread still held an `activeCommunicationRoomId` after that communication room had already ended, the thread screen treated the call as live, disabled fresh call-start actions, and could route the user into a dead join path before any new video call could begin. The owner-small fix now reconciles ended call state in two places inside `app/chat/[threadId].tsx`: on thread load/focus it checks the active communication room snapshot and clears stale call linkage before rendering the thread as call-live, and on join/open-call requests it rechecks the room snapshot and clears dead call state before opening the in-thread panel. `npm run typecheck` passed after the fix. This materially stabilizes the Chi'lly Chat video-call path by removing the highest-priority stale-session wedge: a dead call no longer blocks the next real thread-based video call from starting cleanly. The exact next lane should still follow the broader repo sequence already recorded unless a fresh runtime proof exposes another direct Chi'lly Chat calling regression.

A narrow Public v1 store-surface hardening pass on April 10, 2026 now closes the remaining repo-owned misleading monetization-surface blocker while monetization setup itself stays intentionally deferred until after testing and store readiness. The active surface owners for this lane were `components/monetization/access-sheet.tsx`, `app/title/[id].tsx`, `app/player/[id].tsx`, `app/watch-party/index.tsx`, `app/watch-party/[partyId].tsx`, and `app/settings.tsx`. This pass did not reopen `_lib/revenuecat.ts`, `_lib/monetization.ts`, analytics, auth/session, Party/Live doctrine, Chi'lly Chat doctrine, Rachi doctrine, admin work, iOS implementation, or unrelated UI. The gating truth remains honest: premium-locked titles and rooms still stay locked, no fake unlock path was added, and no entitlement logic changed. What changed is the tester/store-facing presentation layer only. Settings now presents Premium as a deferred informational state instead of an active billing console, title/player/party surfaces now use `Coming Soon`-style lock copy instead of live purchase expectation, and the shared `AccessSheet` now keeps the same shell while hiding offer cards plus purchase/restore/manage actions and explaining that Premium and Party Pass purchasing are not enabled in this build yet. `npm run typecheck` passed after the surface-only hardening pass. This closes the repo-owned contradiction between deferred monetization sequencing and live purchase-facing Public v1 UI. The exact next lane should now move away from RevenueCat/store-offer closure and into testing/store-readiness closure, starting with the remaining release/store identity blockers rather than reopening monetization owners.

A narrow Premium/store runtime-proof pass on April 10, 2026 now closes the remaining repo-owned uncertainty around Public v1 monetization without reopening Party/Live room doctrine, player ownership, Chi'lly Chat doctrine, Rachi doctrine, analytics, auth/session, admin work, iOS work, or unrelated UI. The active monetization owners remain `_lib/revenuecat.ts`, `_lib/monetization.ts`, `components/monetization/access-sheet.tsx`, `app/title/[id].tsx`, and `app/settings.tsx`, and this pass proved the remaining blocker is external offering/configuration truth rather than a new repo-owned code bug in those owners. Fresh Android dev-client proof on physical device re-confirmed that a real premium title (`Chicago Streets`) still resolves on the canonical title-detail owner, still shows an honest Premium gate (`Unlock to Play`), and still opens the shared `AccessSheet` instead of faking an unlock path. The active sheet now proves exact current truth on-device: `Unlock This Title`, helper state `OFFER STATUS`, primary action `Retry Offer Lookup`, utility actions `Restore purchases` and `Manage subscription`, and the exact warning `This purchase path is not available in the current RevenueCat offering configuration yet.` Runtime logs also prove the same state from the active owners: `purchaseTargetId: 'none'`, `offeringAvailable: false`, and `monetization_gate_shown` / `monetization_manage_subscription_opened` events are emitted without any fake purchase success. `Manage subscription` opens Google Play subscriptions successfully, proving the manage path is live. `Restore purchases` runs honestly, reports `Purchases restored.`, returns `activeEntitlementCount: 0`, and leaves the title locked with `Access is still locked for this title after the monetization check.` because there is still no active entitlement to unlock. No runtime code changed in this pass because the active owners already behave honestly. The exact remaining blocker is now clearly external to repo code on the current dev build: the configured RevenueCat test-store environment has offerings/products available, but the Public v1 premium purchase path for the active gate is not mapped into the current offering configuration in a way that yields a real `purchaseTargetId` / purchasable offer on this surface. The exact next lane should therefore stay out of repo code and move to human-owned RevenueCat/store configuration closure plus a focused runtime re-proof after that external setup is corrected.

A narrow auth/session runtime re-proof pass on April 9, 2026 now closes the next repo-owned Public v1 blocker after analytics without reopening room-flow debugging, monetization/store owners, Chi'lly Chat doctrine, Rachi doctrine, or unrelated UI. The active auth/session owners remain `app/_layout.tsx`, `app/(auth)/login.tsx`, `_lib/session.tsx`, and `app/settings.tsx`, and this pass proved the blocker was a proof gap rather than a new code regression in those owners. Starting from a clean Expo dev-client launch on physical Android, the current worktree still cold-opens to `/login`, valid sign-in still reaches the signed-in app shell with `Settings` visible, the account settings surface still exposes real `Log Out`, and a signed-out cold relaunch still lands back on `/login`. The earlier uncertainty came from unstable proof harness behavior after the settings/logout step, not from a broken auth gate, broken login handler, broken session bootstrap, or broken logout owner. No runtime code changed in this pass. The exact next lane should now move past auth/session and into a narrow Premium/store purchase proof closure pass, keeping the remaining store-offer truth explicit as external configuration unless a fresh runtime proof exposes a direct repo-owned monetization bug.

A narrow Public v1 analytics / error-monitoring closure pass on April 9, 2026 now closes the repo-owned dead/no-op telemetry gap without reopening room-flow debugging, auth/session logic, monetization/store owners, Chi'lly Chat, profile/channel doctrine, Rachi doctrine, or unrelated UI. The active shared analytics owner remains `_lib/analytics.ts`, and `app/_layout.tsx` remains the single app-shell owner that mounts the live PostHog bridge. The exact blocker was real code, not proof noise: the app already emitted `trackScreen(...)`, `trackEvent(...)`, `runtime_error`, and `fatal_boundary_hit` through the shared analytics API, but the shared sink stayed a no-op because nothing in the active shell ever called `setAnalyticsSink(...)`. The fix stays owner-small inside `app/_layout.tsx`: the existing PostHog bridge now installs the live analytics sink, forwards shared screen/event calls into PostHog, reuses the shared `identifyUser` / `clearUser` path from `SessionProvider`, and resets the sink cleanly on teardown instead of bypassing the shared analytics owner. `npm run typecheck` passed. Fresh Expo dev-client proof on physical Android re-confirmed the current route/event emission path on the active build (`[analytics] screen:/`, `[analytics] identify_user`, `[analytics] event:monetization_entitlement_resolved`), the served Metro bundle again contained the real `.env.local` PostHog host and API key, and on-device `files/.posthog-rn.json` now proves the live PostHog client is active with the signed-in distinct id, person properties, remote config, fetched feature-flag details, and `flags_endpoint_was_hit: true`. That closes the repo-owned analytics / error-monitoring sink blocker: route analytics and runtime-error events no longer terminate at the local no-op layer. The exact next lane should now move away from analytics and into a narrow auth/session runtime re-proof pass on the current worktree so login-first, settings, and real logout remain carried forward honestly while the remaining external Premium/store configuration truth stays out of repo-code scope.

A narrow live-stage runtime-proof closeout pass on April 9, 2026 now closes the previously recorded Live Room -> Live Stage blocker on pushed `main` without changing runtime product code. Starting from a clean Expo dev-client Home launch on physical Android, the canonical `Home -> Live Watch-Party -> Live Waiting Room -> Create Live Room -> Live Room` path still holds, and the active `app/watch-party/live-stage/[partyId].tsx` owner now re-proves the next step into actual Live Stage as well. The earlier failure was not a broken `onEnterLiveStage` handler or a broken `liveSurface` state branch. Proof on the current dev-client build showed the stage route remained healthy, but the Expo dev warning card at the bottom of the screen could cover the live-room footer CTA and Maestro's text-target tap could miss the actual center of the visible `Continue to Live Stage` button even though a direct footer tap immediately switched `liveSurface` to `stage`. Once the footer CTA itself was tapped cleanly, Live Stage rendered the stage-side dock, exposed `Comments`, and opened the honest `Comments are not available in this live room yet.` drawer with no fake/demo/join chatter and no Party-vs-Live drift. `npm run typecheck` passed. No runtime code changed in this pass. The exact next lane is no longer a live-stage handoff repair lane; the next clean repo-owned Public v1 blocker should now be chosen from the remaining gap audit, with analytics/error-monitoring closure the clearest next implementation target while the carried-forward Party / Live / Profile / Chi'lly Chat / Rachi truth stays unchanged.

A narrow Public v1 social-honesty pass on April 9, 2026 now closes the active fake-comment blocker without reopening auth/session, monetization/store logic, analytics, room doctrine, or unrelated polish. This supersedes the earlier planning-only checkpoint language as the newest runtime checkpoint. The only active owners touched were `app/watch-party/live-stage/[partyId].tsx` and `app/player/[id].tsx` after proving that both currently rendered seeded/local fake comment surfaces in the active Public v1 path. Live Stage no longer auto-seeds demo room chatter or fake join comments, and the active player comment drawers no longer seed local demo comments or fake-send local-only social proof. Where no real comment source exists on those surfaces, the app now shows an honest unavailable state instead of pretending comment activity is real; the existing real Party Room message path and title-adjacent live metadata path remain untouched. `npm run typecheck` passed. The exact next proof target is now focused runtime proof on the active Live Stage and player comment surfaces to confirm that no fake seeded comments render, the honest empty/unavailable state holds, and no unrelated Party / Live / Profile / Chi'lly Chat / Rachi behavior changed.

A doctrine-only product truth sync pass on April 8, 2026 now updates the active control-file system to match the latest locked strategy truth without reopening runtime feature work, app code, Maestro flows, migrations, packages, or release tooling. This supersedes the earlier live-stage proof lane language as the newest local checkpoint. `PRODUCT_DOCTRINE.md` now serves as the single governing home for cross-cutting monetization, compliance, product-phase, and profile/channel-platform truth; `ROADMAP.md` now expresses the Public v1 / Post-v1 / Later phase planning split; and `ROOM_BLUEPRINT.md` remains limited to room-specific doctrine instead of becoming a second home for general profile/channel platform truth. Locked Chi'lly Chat doctrine and locked Rachi official-account doctrine remain carried forward unchanged in this pass. No runtime code changed, no new runtime proof was attempted, and the previously proved Party / Live / Profile / Chi'lly Chat / Rachi baseline remains carried forward exactly as already recorded. The exact next lane is now a phased blueprint/planning pass derived from the synced doctrine, covering Public v1, Post-v1, Later phase, and dependencies, blockers, and compliance-sensitive areas before any new coding lane begins.

A narrow Live Stage studio-surface tightening pass on April 6, 2026 now restores Live Stage as a presentation-first live/studio surface without reopening Live Room, Party Room, Watch-Party Live shared playback, monetization, auth, profile, or chat lanes. This supersedes the earlier handoff-repair checkpoint language as the newest local checkpoint. The active live-lane owner remains `app/watch-party/live-stage/[partyId].tsx`, and `app/player/[id].tsx` is the only additional live-camera owner touched for face-filter consistency. The exact stage drift was local to those proven owners: the stage branch still rendered a large `Protected Live Session` block plus a large `Host-led live focus` / stage-mode card directly on the main surface, so stage-only controls occupied premium stage space instead of living behind a dismissible control layer. The fix now keeps Live Room separate, removes those large permanent stage cards from the main surface, moves stage-only controls and capture/protection status into a dismissible `Studio` sheet on Live Stage, keeps the locked `Live-First` and `Live Watch-Party` toggle visible on the actual stage surface, and adds a consistent `Filters` entry on the active live-camera surfaces owned by the canonical live-stage route and the live-mode player route. `npm run typecheck` passed. Fresh Android runtime proof is still pending because `adb devices -l` returned no attached Android target, so the verified post-change state is owner-code plus typecheck proof with focused live-stage studio/filter runtime proof next.

A narrow live-lane handoff repair pass on April 6, 2026 now fixes the broken Live Room -> Live Stage transition without reopening Party Room, Watch-Party Live shared player, monetization, auth, profile, or chat lanes. This supersedes the earlier room-purpose-restoration checkpoint language where the corrected Live Room layout existed but the handoff could still appear broken in practice. The active live owner remains `app/watch-party/live-stage/[partyId].tsx`, and the exact cause was local to that canonical live route: Live Room and Live Stage are separate surfaces inside the same route, `onEnterLiveStage` already correctly called `setLiveSurface("stage")`, but the `Continue to Live Stage` / `Join Live Stage` CTA lived inside a non-scrollable Live Room layout under multiple large control cards while the route still showed stale `Opening Live Stage…` loading copy before landing in Live Room. The fix now keeps Live Room as the pre-stage control surface, moves the stage-entry CTA into a persistent live-room footer, makes the Live Room controls scrollable so the handoff cannot disappear below the fold, and updates the loading text to `Opening Live Room…` so the route no longer promises stage-entry before the room shell appears. `npm run typecheck` passed. Fresh Android runtime proof is still pending because `adb devices -l` returned no attached physical device and no local Android emulator command was available, so the verified post-change state is owner-code plus typecheck proof with focused live-lane runtime proof next.

A narrow room-purpose restoration pass on April 6, 2026 now corrects Live Waiting Room, Live Room, Live Stage, Party Waiting Room, Party Room, and the shared Watch-Party Live player without reopening monetization, auth, profile, chat, or broader route lanes. This supersedes the earlier post-closeout checkpoint language that still pointed at pushing local closeout commits and still described the older room-purpose composition. `app/watch-party/index.tsx` remains the waiting-room owner, `app/watch-party/live-stage/[partyId].tsx` remains the live-lane owner, `app/watch-party/[partyId].tsx` remains the Party Room owner, and `app/player/[id].tsx` remains the standalone/shared-player owner. The corrected current truth is now: waiting rooms are concise pre-entry control surfaces in their own lanes, Live Room is a pre-stage control/customization room instead of a stacked stage shell, Live Stage is the actual presentation surface, Party Room is the pre-entry watch-together control room instead of a playback-control shell, and the shared Watch-Party Live player is playback-first with the host/viewer strip in the lower band instead of top helper blocks. `npm run typecheck` passed. No fresh Android runtime proof was run in this pass because `adb devices -l` returned no attached target, so the corrected room-purpose baseline is currently owner-code plus typecheck proved and the next exact lane is a focused Android dev-client proof of the corrected Live / Party surfaces before broader work resumes.

A deferred final combined room-campaign proof / push-readiness pass on April 6, 2026 now closes the combined Chapters 1 through 4 verification lane on the physical Android dev client without reopening earlier chapters. This supersedes the previously deferred-final-proof next-lane checkpoint. The exact next operational step is now to push the current local task-pure closeout commits on `main`, starting with `5f1da73` `Close Chapter 4 monetization proof` and the closeout checkpoint from this pass, then choose the next chapter only after push. `npm run typecheck` passed, `adb devices -l` again proved the physical Android dev client on `R3CXA0DS5JV` / `SM-S928U1`, and the current Metro bundle re-proved the carried-forward combined surface set: login-first plus valid sign-in, Home, canonical title detail, canonical standalone player, `/settings` plus real `Log Out`, Live Waiting Room handoff into the canonical live route on `/watch-party/live-stage/[partyId]`, Party Waiting Room into Party Room plus shared `Watch-Party Live` player return, canonical `/profile/[userId]` viewer and official Rachi paths, canonical Chi'lly Chat inbox/direct/official threads, and the Chapter 4 monetization status / premium gate / paywall / restore / manage behaviors on the real backend title row. No new route drift or room-boundary regression was observed across Live, Party, Profile / Channel, Chi'lly Chat, Rachi, or monetization surfaces. Current external monetization truth remains explicit and unchanged: real purchase completion still depends on RevenueCat / Google Play offer configuration, but that is no longer a repo-code blocker or a push-readiness blocker for the implemented in-app foundation.

A Chapter 4 monetization / entitlements Phase 5 analytics-admin-visibility-closeout pass on April 6, 2026 now closes the monetization wave without reopening earlier Chapter 4 phases or earlier room chapters. The exact next implementation lane is now the deferred final combined room-campaign proof / push-readiness pass. `_lib/analytics.ts` remains the single analytics owner, `_lib/monetization.ts` remains the central monetization / entitlement snapshot owner, `components/monetization/access-sheet.tsx` remains the shared paywall / offer surface owner, and `app/settings.tsx` now adds the smallest owner-safe monetization visibility surface instead of inventing an admin dashboard. `npm run typecheck` passed, and the physical Android dev client on `R3CXA0DS5JV` / `SM-S928U1` proved the new Phase 5 hooks and closeout behavior on the current Metro bundle: `/settings` now shows `MONETIZATION STATUS` with `Free account`, `Ready`, and `None active`; its `Manage subscription` action logged `monetization_manage_subscription_opened` and handed off into the real Google Play `com.android.vending` subscriptions/account surface; the canonical premium player route for `Chicago Streets` still resolved `PLAYER MATCH SOURCE: matched from db:advanced:id`, still rendered the honest `Unlock This Title` gate, and still opened the shared `TITLE ACCESS` / `OFFER STATUS` sheet without local fallback bypass. The same on-device proof now also logs `monetization_entitlement_resolved`, `monetization_restore_started`, `monetization_restore_result`, and `monetization_paywall_dismissed`; `Restore purchases` stays a real utility path that rechecks access honestly and, on the current account, returns to `Playback is still locked for this title after the monetization check` instead of faking an unlock. Current external monetization truth stays explicit: the in-app Chapter 4 foundation is now complete, but the active RevenueCat / Play offering configuration still does not expose a live purchasable offer on this Android dev-client path, so true purchase completion remains an external store/dashboard configuration step rather than a repo-code blocker.

A Chapter 4 monetization / entitlements Phase 4 surface-integration pass on April 6, 2026 now closes the blocked premium-player runtime lane without reopening earlier monetization phases. The exact next implementation lane is now Chapter 4, Phase 5: analytics / admin visibility / closeout proof. `app/player/[id].tsx` remains the standalone-player owner, and the narrow Phase 4 fix removed the stale `thumbnail_url` request from the player's remote `titles` query so the route now resolves the real backend row instead of falling back to bundled local data. `npm run typecheck` passed, the connected Android dev client on `R3CXA0DS5JV` / `SM-S928U1` again ran the debuggable `com.chillywood.mobile` build against Metro, and the premium test title `Chicago Streets` (`f0d03df8-ced8-433f-a5c0-e2b930813eb0`) now proves on the canonical standalone player route with `PLAYER MATCH SOURCE: matched from db:advanced:id`. The solo-first player surface stays intact, `Watch-Party Live` still remains owned by the standalone player, and the real missing-entitlement behavior now renders honestly on-device: the player shows `Unlock This Title`, the `PLAYER HELPER` premium-lock copy, and the shared `TITLE ACCESS` sheet with `OFFER STATUS`, `Restore purchases`, `Manage subscription`, `Retry Offer Lookup`, and `Not now` instead of bypassing the gate with free local fallback data.

A Chapter 4 monetization / entitlements Phase 3 paywalls-offers-purchase-UX pass on April 6, 2026 now closes the previously blocked runtime lane without reopening earlier monetization phases. The exact next implementation lane is now Chapter 4, Phase 4: surface integration. `_lib/monetization.ts` remains the central owner for paywall state, restore/manage entry, retry/purchase intent, and honest fallback messaging; `components/monetization/access-sheet.tsx` now owns the reusable visible paywall surface; and `app/title/[id].tsx`, `app/watch-party/index.tsx`, `app/watch-party/[partyId].tsx`, and `app/communication/index.tsx` now stay thin route owners that recheck access after sheet actions instead of scattering raw purchase flow logic. `npm run typecheck` passed, and the connected Android dev client on `R3CXA0DS5JV` / `SM-S928U1` proved three real Phase 3 paywall surfaces on the current worktree bundle: live room code `LTCUZX` opened `LIVE ACCESS`, party-pass room code `HXJUAS` opened `WATCH-PARTY ACCESS`, and premium title-room code `5Y4YPM` opened `TITLE ACCESS`. All three showed the upgraded `OFFER STATUS` fallback with `Restore purchases`, `Manage subscription`, `Retry Offer Lookup`, and `Not now`; `Manage subscription` opened the real Google Play `Subscriptions` surface; `Restore purchases` rechecked access honestly instead of faking an unlock; and analytics again logged real `monetization_gate_shown` events from the waiting-room owner. Current external truth is also now explicit: these targets still do not have a live purchase path in the current RevenueCat offering configuration, so Phase 3 closes on honest fallback UX and real utility-entry behavior, not on fake pricing or fake purchase success.

A narrow background restoration / background unification pass on April 6, 2026 now updates the shared Chi'llywood branded-background truth without reopening the active Chapter 4 monetization lane. The exact next implementation lane remains Chapter 4, Phase 3: paywalls / offers / purchase UX. `assets/images/chillywood-branded-background.png` now preserves the exact uploaded nighttime city image from this Codex thread as the repo's source-of-truth branded background asset, while `assets/images/chicago-skyline.jpg` has been restored from that same image direction and remains the shared runtime background owner for the active login, support, admin, channel-settings, and skyline-fallback poster surfaces. This pass intentionally leaves room-owned backgrounds alone: `app/watch-party/index.tsx`, `app/watch-party/[partyId].tsx`, `app/watch-party/live-stage/[partyId].tsx`, `app/player/[id].tsx`, `app/profile/[userId].tsx`, and the canonical Chi'lly Chat routes still own their room/content-specific background treatments, so the restored branded skyline does not flatten Live, Party, Player, Profile, or Chat into one generic shell.

A new Chapter 4 monetization / entitlements Phase 2 entitlement-plumbing pass on April 6, 2026 now intentionally supersedes the previously scheduled final combined room-campaign proof as the active implementation lane. The room campaign through Chapter 3 remains carried-forward and pushed on `main`, and the new exact checkpoint is now Chapter 4, Phases 1 and 2 complete with Chapter 4, Phase 3 next. `app/_layout.tsx` still owns the single app-start monetization bootstrap, `_lib/revenuecat.ts` still owns raw RevenueCat SDK configuration / customer identity / offerings / purchase / restore / manage-subscription wiring, and `_lib/monetization.ts` now additionally owns the identity-safe snapshot refresh, centralized entitlement target resolution, target-aware access-sheet presentation, and purchase-block handling used by the active route owners. `_lib/roomRules.ts`, `_lib/watchParty.ts`, `app/watch-party/index.tsx`, `app/watch-party/[partyId].tsx`, `app/communication/index.tsx`, `app/title/[id].tsx`, and `components/monetization/access-sheet.tsx` now all consume the same centralized monetization gate model instead of fake local unlock helpers. `npm run typecheck` passed, and the physical Android dev-client runtime proof on device `R3CXA0DS5JV` / `SM-S928U1` proved three real entitlement-gated viewer paths: live room code `LTCUZX` opened `LIVE ACCESS` / `Unlock Live Access`, party-pass room code `HXJUAS` opened `WATCH-PARTY ACCESS` / `Unlock This Room`, and premium title-room code `5Y4YPM` opened `TITLE ACCESS` / `Unlock This Title`, while analytics logged real `monetization_gate_shown` events from the waiting-room owner. This pass also fixed the RevenueCat `subscribers/null` identity bug and replaced the SDK's default dev log handling so the Android Test Store `canMakePayments requires the Google Play Store` warning no longer escalates into a full-screen React Native LogBox during proof. Direct title-detail runtime proof still lacks a real premium title row on the current backend, but the canonical title-detail owner now routes through the same central title-access target and Phase 3 visible paywall / offer UX is the next exact lane.

Stage 4 remains completed/proved on the current build. Current JS via Expo Go / Metro proved Home, Player, Profile, Chi'lly Chat, Live Waiting Room, Party Waiting Room, Live Room, Party Room, and Live Stage behavior as described below.

The latest proof pass also closed two reported current-build regressions on April 5, 2026: logged-out first open now lands on login again, and Home `Top Rated` / `Browse` content cards now route to the real title detail surface instead of `Not found`. The current fix set is intentionally small and owner-based: `app/_layout.tsx` now lets the root auth gate own post-auth redirect resolution, `app/(auth)/login.tsx` no longer dispatches its own tabs-group replace, `app/(tabs)/index.tsx` now pushes the canonical structured `/title/[id]` target, and `app/title/[id].tsx` no longer requests the nonexistent `titles.thumbnail_url` column during title hydration. Typecheck passed afterward. PostHog env wiring is present locally and the current Expo runtime proof below now confirms both remote default-off stability and remote on-state delivery for the gated chat flags.

An additional owner-surface proof pass on April 5, 2026 now also closes the visible Settings / Logout requirement without collapsing profile doctrine: Home exposes a visible `Settings` entry beside the existing self-profile avatar, own profile exposes a self-only `Settings` chip alongside the owner controls, `app/settings.tsx` owns the new settings route plus real `Log Out`, signing out returns to `/login`, cold reopen while logged out lands on `/login`, and other-user profile behavior remains the separate public/profile + channel + Chi'lly Chat surface. Typecheck remained green after the additive route and action changes.

A narrow login visual restoration pass on April 5, 2026 now also restores the intended Chi'llywood presentation on `app/(auth)/login.tsx` without reopening auth/session/route behavior: the login route again owns the skyline-backed branded card surface, `app/(auth)/_layout.tsx` remains wrapper-only, `assets/images/chicago-skyline.jpg` remains the background asset truth, and the stale-token cleanup plus login-first behavior stay exactly as already implemented. Android emulator Maestro proof re-confirmed logged-out first open lands on login, the branded `CHI'LLYWOOD` login surface renders, a valid login still returns to Home with `Top Rated`, `Browse`, and `Settings` visible, and typecheck stayed green.

A follow-up login spacing adjustment pass on April 5, 2026 now keeps that same owner surface but lowers the login card further so more skyline branding remains visible above it: `app/(auth)/login.tsx` still owns the background, overlay, branding, inputs, and button styling; the only visual movement comes from the route-local container spacing that positions the card. This micro-pass did not run a fresh emulator proof by explicit request, and no auth/session/route logic changed while making the visual-only spacing adjustment.

An execution-plan reset on April 5, 2026 now carries forward the still-valid Stage 4, PostHog, auth/home, Settings/Logout, and login-visual truth without reopening those already-proved lanes: build phases now prioritize ownership proof, route/render-path proof, static validation, deterministic file-level checks, and `npm run typecheck`, while emulator/device-heavy proof is intentionally deferred to the final integration phase unless a true blocker requires one targeted runtime check.

A new Phase 0 Rachi foundation pass on April 5, 2026 now adds Chi'llywood's official platform-owned seeded account without rewriting profile/chat architecture: `_lib/officialAccounts.ts` owns the official identity and protection metadata, `_lib/userData.ts` decorates canonical channel identities with protected official-account fields, `app/profile/[userId].tsx` renders the official/platform-badged Rachi profile and channel surface while blocking normal-user self-claim semantics, and `app/chat/index.tsx` plus `app/chat/[threadId].tsx` expose the official starter presence, canonical direct-thread open path, and Rachi starter-message prompts. This pass used owner/path proof, static validation, and typecheck only; runtime integration proof for Rachi is deferred to the final integration phase by the new execution plan.

A new Phase 2 moderation/admin minimums pass on April 5, 2026 now extends that protected Rachi foundation without reopening broader profile/chat completion: `_lib/moderation.ts` now owns shared moderation actor access, report context, platform-role lookup, and safety-review query helpers; `supabase/migrations/202603270007_create_safety_reports.sql` plus `supabase/migrations/202604050003_create_platform_role_memberships.sql` now establish the repo-owned safety-report intake table and the active platform-role review foundation; `app/title/[id].tsx`, `app/profile/[userId].tsx`, `app/chat/[threadId].tsx`, `app/watch-party/[partyId].tsx`, `app/watch-party/live-stage/[partyId].tsx`, and `app/communication/[roomId].tsx` now open or submit safety reports with consistent source-route and audit-minded context; and `app/admin.tsx` now adds the smallest role-aware moderation review card instead of a broad dashboard rewrite. This pass used owner/path proof and `npm run typecheck` only; runtime integration proof for the new moderation/admin behavior remains deferred to the final integration phase by the active execution plan.

A new Phase 3 Home/content polish pass on April 5, 2026 now tightens the active content-owner surfaces without reopening player/live/watch-party doctrine: `app/(tabs)/index.tsx` now reads the repo-owned Home config for continue-watching/favorites visibility, browse label/query, and per-rail limits while keeping Continue Watching at the top hero when progress exists; Home favorites cards now preserve the same metadata, added-date, and live social signals as the discovery rails; and `app/title/[id].tsx` now restores the documented title-resolution order of exact `id`, then exact `title`, then local fallback while adding title metadata, added date, and live room comment/reaction indicators where active title-linked rooms exist. This pass used owner/path proof and `npm run typecheck` only; runtime integration proof for the new Home/content behavior remains deferred to the final integration phase by the active execution plan.

A new Phase 4 release-hardening pass on April 5, 2026 now closes the concrete release/config issues that current tool output proved instead of inventing extra platform work: `app.config.ts` now uses Expo's resolved config object as the dynamic owner so static `app.json` values remain the canonical base without tripping Expo doctor, `package.json` plus `package-lock.json` now intentionally own the missing `react-native-svg` peer required by `posthog-react-native`, and `package.json` now records the explicit Expo doctor exclusion for `react-native-webrtc` so the known new-architecture warning stops blocking release validation for the current WebRTC stack. This pass re-ran `npm run validate:runtime`, `npx expo-doctor`, and `npm run typecheck`; all three now pass on the current repo state. No new Sentry integration was introduced in this phase because no repo-owned Sentry surface or prior implementation existed to harden without guessing.

A first Phase 5 final integration proof attempt on April 5, 2026 then confirmed the release-hardened app still starts but did not complete device-backed runtime proof: `npm run typecheck` passed again, `npx expo start --port 8084` served Metro successfully, and the first Maestro smoke flow stopped immediately with `0 devices connected`. `adb devices -l` showed no attached Android target, local Android emulator tooling was unavailable, and `xcrun simctl` was not usable from the current Command Line Tools-only Xcode selection. Final integration/runtime proof therefore remains pending as an environment blocker, not a newly proved product regression.

A second Phase 5 proof sequence on April 5, 2026 then moved final integration proof onto the real physical Android dev client only and closed it on current build truth. The connected device `R3CXA0DS5JV` / `SM-S928U1` ran the installed debuggable `com.chillywood.mobile` development build against Metro, `npm run typecheck` stayed green, login-first behavior proved, `Settings` / `Log Out` still returned to `/login`, Home / title / player valid paths still resolved, and the canonical Rachi profile/chat flow still rendered on device. The only runtime blocker found inside that proof was the admin moderation card's backend miss for `public.platform_role_memberships` in the schema cache.

A targeted backend/admin unblock pass on April 5, 2026 then resolved that last Phase 5 blocker without reopening earlier lanes or changing repo app code: the linked remote Supabase project was missing `202604050003_create_platform_role_memberships.sql`, `supabase db push` applied that single pending migration, and the same physical Android dev client re-ran only the admin moderation proof. The prior schema-cache error disappeared, the admin studio now loads the real role-aware moderation state, and the current signed-in operator account resolves honestly to `Review Queue Locked` with `No active review role on this account yet` instead of a backend failure. Phase 5 final integration proof is therefore now closed on the current build: moderation/admin minimum surfaces no longer break runtime, and the remaining review-queue unlock is a data-provisioning choice for granting an active `operator` or `moderator` platform role membership, not a product or schema blocker.

A doctrine-only room-blueprint pass on April 5, 2026 now adds `ROOM_BLUEPRINT.md` as the durable room-by-room product reference without reopening the active stability lane. The new blueprint preserves the locked Chi'llywood naming, canonical routes, Party / Live split, standalone Chi'lly Chat doctrine, Profile / Channel identity, and protected Rachi foundation while defining purpose, entry/exit, controls, permissions, room-specific AI posture, MVP scope, later expansion, anti-patterns, and product success tests for the major room surfaces. This pass intentionally changed doctrine only: no app logic changed, no emulator/device work resumed, and Phase 5 final integration proof remains the active next lane.

A follow-up doctrine/product-architecture expansion pass on April 5, 2026 now promotes `ROOM_BLUEPRINT.md` from a baseline room map into the master room-feature and AI doctrine for the app without reopening runtime proof. The expanded blueprint now records current-vs-future fit, room-specific AI boundaries, core features now, later premium expansion, data/system dependencies, monetization and growth relevance, accessibility/usability expectations, and safety/abuse considerations for Live Waiting Room, Live Room, Live Stage, Party Waiting Room, Party Room, Watch-Party Live/shared party player, Standalone Player, Profile / Channel, Chi'lly Chat, and Rachi. This pass remains doctrine-only: no routes changed, no room features were implemented, and the exact next active lane is still Phase 5 final integration proof.

A first post-stability room implementation pass on April 6, 2026 now starts the `ROOM_BLUEPRINT.md` campaign with Chapter 1, Room 1: Live Waiting Room. `app/watch-party/index.tsx` remains the waiting-room owner, but live entry is now separated cleanly from party waiting behavior: Live Waiting Room owns pre-entry live identity, room preview, host/viewer join modes, readiness, visibility/control guidance, and a bounded smart-entry helper without absorbing Live Room or Live Stage responsibilities. Live create/join handoff now routes only to `/watch-party/live-stage/[partyId]`, live waiting rejects watch-party room codes that belong to the title/player social path, `npm run typecheck` passed, and Android dev-client runtime proof showed the live-only waiting cards on device before `Create Live Room` opened the live-stage owner on `/watch-party/live-stage/2H7ZWE` with `Live-First` and `Live Watch-Party` visible.

A second post-stability room implementation pass on April 6, 2026 now completes Chapter 1, Room 2: Live Room on the same canonical `/watch-party/live-stage/[partyId]` owner without inventing a second live route. `app/watch-party/live-stage/[partyId].tsx` now renders an explicit `LIVE ROOM` shell above the stage with room-role clarity, mode/join/capture state, permission guidance, quick room actions, and a bounded live-room helper while keeping `LIVE STAGE` labeled separately below as the presentation layer. `npm run typecheck` passed again, Android dev-client runtime proof on `/watch-party/live-stage/2H7ZWE` showed the new `LIVE ROOM` shell and separate `LIVE STAGE` label together on device, and the new `Leave room` control returned cleanly to Live Waiting Room instead of drifting into Party Room.

A third post-stability room implementation pass on April 6, 2026 now completes Chapter 1, Room 3: Live Stage inside that same canonical `/watch-party/live-stage/[partyId]` owner. The stage layer now has a dedicated `STAGE MODE` card, mode-aware stage actions, and integrated stage-helper guidance while preserving the route-level split: Live Room remains the room shell above, and Live Stage remains the presentation layer below. `npm run typecheck` passed again, Android dev-client runtime proof on `/watch-party/live-stage/2H7ZWE` showed `Host-led live focus` in `Live-First`, then switched on the same route to `Shared watch moment` with `WATCH MOMENT HELPER` in `Live Watch-Party`, so Chapter 1 Live now closes with waiting-room handoff, room-shell clarity, and stage-mode identity all proved together.

A fourth post-stability room implementation pass on April 6, 2026 now starts Chapter 2 with Room 4: Party Waiting Room on `app/watch-party/index.tsx`. The title-first waiting-room owner now keeps Party Waiting Room distinct from Live Waiting Room: player-origin `Watch-Party Live` entry resolves the selected title cleanly, party-only context/readiness/access/helper cards now render before entry, the create path now carries the locked `titleId` forward instead of silently dropping it, and party entry now rejects live-room codes instead of drifting across room doctrine. `npm run typecheck` passed again, Android dev-client runtime proof on the quoted player-origin deep link showed `Party Waiting Room`, `WATCH-PARTY PREVIEW`, `Chicago Streets`, and `ENTRY CHECK`, and `Create Party Room` then handed off into `/watch-party/EP6C9T` with Party Room visible instead of Live Stage.

A fifth post-stability room implementation pass on April 6, 2026 now closes Chapter 2, Room 5: Party Room without widening file scope, because the current `/watch-party/[partyId]` owner already matches the blueprint closely enough after proof. `app/watch-party/[partyId].tsx` already owns the social watch shell around playback: host/viewer role clarity, `Share invite`, visible participant presence, tailored local room arrangement controls, bounded moderation/report hooks, and separate host playback/room controls while keeping `Watch-Party Live` as a downstream shared-player CTA instead of a route drift into Live Stage. `npm run typecheck` remained green, Android dev-client runtime proof on `/watch-party/EP6C9T` showed `Host Controls Active`, `Party Room`, the participant/social shell, and the separate `Watch-Party Live` CTA, so Party Room now closes as an owner-proof checkpoint with no additional app-code change required.

A sixth post-stability room implementation pass on April 6, 2026 now closes Chapter 2, Room 6: Watch-Party Live / shared party player with the smallest deterministic owner fix set. `app/watch-party/[partyId].tsx` now keeps Party Room's `Watch-Party Live` CTA on the shared party-player path instead of forcing `liveMode`, `app/watch-party/index.tsx` now infers the title-first waiting-room type from locked party context instead of the fragile source flag alone, and `app/player/[id].tsx` now presents explicit shared-player context plus a `Party Room` return that uses `router.dismissTo(...)` so the return path pops back to the existing Party Room when present instead of stacking a fresh room instance. `npm run typecheck` passed again, Android dev-client runtime proof on fresh party room `TGC2EA` showed `WATCH-PARTY LIVE`, `Party Room`, `1 viewer synced`, `Host Controls · Paused`, and the `SYNC HELPER` copy on `/player/f0d03df8-ced8-433f-a5c0-e2b930813eb0`, then returned to `/watch-party/TGC2EA` with `Party Room`, `Host Controls Active`, and `Watching together: Chicago Streets` still visible and no `Room not found` fallback.

A seventh post-stability room implementation pass on April 6, 2026 now closes Chapter 2, Room 7: Standalone Player alignment without reopening the Party or Live owners. `app/player/[id].tsx` remains the sole owner for `/player/[id]`, but it now treats live-mode as explicit route state instead of inferring it from media-source readiness, loads resume state into visible UI context, and renders a standalone-only `SOLO PLAYBACK` context card so solo viewing stays visually distinct from the shared party player. `npm run typecheck` passed again, Android dev-client runtime proof on `/player/f0d03df8-ced8-433f-a5c0-e2b930813eb0` showed `SOLO PLAYBACK`, `You are in solo playback.`, `Watch-Party Live from here`, and the new `PLAYER HELPER` copy with no Party Room return chrome, and tapping `Watch-Party Live` from that standalone route still handed off into `Party Waiting Room` instead of turning the player itself into a room shell.

An eighth post-stability room implementation pass on April 6, 2026 now closes Chapter 3, Room 8: Profile / Channel on the canonical `/profile/[userId]` owner without reopening Chi'lly Chat or Rachi implementation lanes. `app/profile/[userId].tsx` remains the sole route owner, but it now defaults non-self viewers into the channel-home side of the surface, adds honest self-channel signals from the existing saved-title and watch-progress foundations, promotes bounded `CHANNEL HELPER` guidance so profile reads like a mini network/channel instead of a settings page, and replaces the fakest owner placeholders with real owner-facing actions that stay anchored to `Manage Channel`, `Chi'lly Chat`, and `Settings`. `npm run typecheck` passed again, Android dev-client runtime proof on the real self route `/profile/4b5e7761-5bf1-4e18-9eb7-d6037a0eb32f?self=1` showed `OWNER CHANNEL`, `Your Channel`, the owner helper copy, and real saved/resume signals, while the real non-official viewer route `/profile/11111111-1111-4111-8111-111111111111` showed `PUBLIC CHANNEL`, viewer-facing channel-home guidance, and a working `Chi'lly Chat` handoff that opened the direct thread for `Proof User` instead of turning profile into in-surface messaging.

A ninth post-stability room implementation pass on April 6, 2026 now closes Chapter 3, Room 9: Chi'lly Chat on the canonical `/chat` and `/chat/[threadId]` owners without reopening Profile / Channel or broad messaging architecture. `app/chat/index.tsx` now strengthens the inbox as Chi'llywood's native communication layer with an explicit `INBOX HELPER`, honest direct-vs-official thread counts, clearer row-level thread kind/status/context, and stronger official/live/unread visual distinction without inventing fake thread data. `app/chat/[threadId].tsx` now adds a bounded `THREAD GUIDE` that distinguishes official vs direct conversations, keeps profile identity separate from thread communication, keeps voice/video entry native to the thread, and surfaces deterministic thread recap/help copy that future AI can extend later without faking heavy AI wiring. `npm run typecheck` passed again, and physical Android dev-client proof on `R3CXA0DS5JV` / `SM-S928U1` showed `/chat` rendering `Inbox`, `INBOX HELPER`, `1 official`, `2 direct`, and the Rachi official entry; the viewer profile route `/profile/11111111-1111-4111-8111-111111111111` still handed off through `Chi'lly Chat` into the canonical direct thread for `Proof User`; and both the direct thread plus the canonical Rachi official thread rendered `THREAD GUIDE` with the correct direct-vs-official distinction, voice/video entry, composer, and official identity markers on the same canonical thread route.

A tenth post-stability room implementation pass on April 6, 2026 now closes Chapter 3, Room 10: Rachi polish/alignment without reopening broad profile or chat lanes. `_lib/officialAccounts.ts` now carries the stronger official concierge metadata and trusted guidance topics for the protected platform account, `app/profile/[userId].tsx` now makes the canonical Rachi profile/channel route read immediately as an official concierge surface with `OFFICIAL PLATFORM PRESENCE`, `Official Concierge`, `CONCIERGE READY`, `PROTECTED`, bounded guidance topics, and canonical Chi'lly Chat handoff copy that explicitly rejects the idea of a separate support app, `app/chat/index.tsx` now upgrades the canonical inbox starter card so Rachi reads as a trusted official platform entry instead of a generic account row, and `app/chat/[threadId].tsx` now adds an official-presence card plus official-aware thread status/call framing so the canonical Rachi thread reads as platform-owned Chi'llywood concierge communication instead of a normal direct thread. `npm run typecheck` passed again, and physical Android dev-client proof on `R3CXA0DS5JV` / `SM-S928U1` showed `/profile/platform_rachi_official` rendering `OFFICIAL PLATFORM PRESENCE`, `Official Concierge`, `CONCIERGE READY`, `PROTECTED`, `OFFICIAL STATUS`, the trusted guidance topics, and the `Chi'lly Chat` handoff; the profile handoff opened the canonical Rachi thread on `/chat/3f17f8b8-cf20-45ca-8c9c-da351c46e280`; `/chat/[threadId]` rendered `OFFICIAL`, `Official thread`, `THREAD GUIDE`, `Official platform conversation`, `PLATFORM OWNED`, and the official concierge helper card; and `/chat` still rendered Rachi as the official starter presence inside Chi'lly Chat instead of a generic support center.

## Current-Build Proved Items
- Home: `Continue Watching` proved at the top by temporarily seeding `watch_history` for `Chicago Streets` and then clearing it; `Browse` / `Top Rated` cards show title, info line, and `Added` date; the lower Home area is reserved for `Chi'llywood Originals`; on April 5, 2026 a clean `adb shell pm clear host.exp.exponent` first-open proof landed on `/login`, then the authenticated Home proof landed on `/` with `Top Rated`, `Browse`, the `Chicago Streets` card metadata visible in the UI tree, and the new visible `Settings` entry still sitting beside the self-profile avatar instead of replacing it
- Player: the bundled Android asset-path playback bug was fixed in `app/player/[id].tsx` by resolving the bundled asset via `expo-asset` `localUri` before `expo-av` `Video`, and the current JS player route showed a real video frame while retaining the `Watch-Party Live` CTA
- Own profile: canonical `/profile/[userId]` now proves as an owner channel surface with `OWNER CHANNEL`, `Your Channel`, real saved/resume channel signals, and helper copy that keeps Chi'lly Chat plus Settings separate from the public-facing channel home
- Other-user profile: canonical viewer route now proves as a public channel surface with `PUBLIC CHANNEL`, channel-home-first guidance, `Profile` / `Channel Home` view switching, `Chi'lly Chat` visible, and no owner-only Settings control
- Chi'lly Chat: canonical `/chat` now proves as a stronger native inbox with `INBOX HELPER`, direct-vs-official thread context, Rachi's official starter entry, and clearer thread row identity without fake data; canonical `/chat/[threadId]` now proves as a stronger direct/official thread surface with `THREAD GUIDE`, voice/video entry, composer, optional PostHog-gated smart replies, and working profile-to-thread handoff
- Rachi: canonical `/profile/platform_rachi_official` now proves as an official platform-owned concierge/channel surface with `OFFICIAL PLATFORM PRESENCE`, `Official Concierge`, `CONCIERGE READY`, `PROTECTED`, `OFFICIAL STATUS`, and bounded guidance topics; the in-profile `Chi'lly Chat` action still hands off into the canonical official thread, and canonical `/chat/[threadId]` now proves `OFFICIAL`, `Official thread`, `Official platform conversation`, `PLATFORM OWNED`, and the trusted concierge helper card without turning Rachi into a separate support center
- Settings / Logout: `/settings` is now reachable from both Home and own profile on the current build; `Log Out` clears the Supabase session, analytics recorded `auth_sign_out_requested` plus `auth_sign_out_success`, the UI returned to `/login`, and a fresh `adb shell pm clear host.exp.exponent` reopen while logged out again landed on `/login`
- Admin / moderation: physical Android dev-client proof now shows `Content Studio`, `Actor Operator`, `Admin Access Enabled`, and the real role-aware `Review Queue Locked` state with `No active review role on this account yet`; the old `public.platform_role_memberships` schema-cache failure is gone, so the admin surface now resolves as intended instead of failing at backend lookup time
- Live Waiting Room and Party Waiting Room: the active owners now restore both surfaces as concise lane-specific pre-entry control rooms instead of generic helper/instruction panels; the latest live-lane handoff pass did not rerun Android proof because no device was attached
- Live Room: the active live owner now restores a distinct pre-stage control/customization room with invite/share, viewing defaults, room defaults, and a persistent visible stage-entry CTA, and it no longer renders the stage as a stacked section on the same room surface
- Party Room: the active party owner now restores a distinct pre-entry watch-together control/customization room with room-default controls and a clear `Watch-Party Live` CTA, and it no longer behaves like the shared player or a host playback-control shell
- Live Stage: the active live owner now keeps the stage as the actual presentation surface with `Live-First` / `Live Watch-Party` stage modes separated from the pre-stage Live Room controls; `Live Watch-Party` now keeps the landed hybrid cleanup with the cleaner `Chi'lly Party Members` card plus bottom room-comments lane, `Live First` now shares the cleaner stage rhythm without the old caption/reveal box, physical Android proof confirmed the cleaner top row plus tap-to-hide/tap-to-show on Live First, and same-branch proof also confirmed that `Live Watch-Party` stayed intact

## PostHog And Flags
- The current Expo session on `8084` loaded `.env.local` and exported `EXPO_PUBLIC_POSTHOG_API_KEY` plus `EXPO_PUBLIC_POSTHOG_HOST`; the served Android `node_modules/expo-router/entry.bundle` also contained those injected values and the two active chat flag keys
- Default-off stability is now proved on the PostHog-enabled runtime for `chilly_chat_expanded_v1` and `ai_chat_suggestions_v1`: `/chat` still opened on the current build, `/chat/[threadId]` still opened on the current build, and the thread surface kept `Proof User`, `Direct thread`, voice/video actions, `Attach`, `React`, `Write a message`, and `Send` while `AI SMART REPLIES` and `PostHog gated` remained absent
- `app/_layout.tsx` now mounts a signed-in PostHog bridge inside `SessionProvider`, identifies the authenticated Supabase user, and immediately reloads feature flags so current-runtime flag evaluation is no longer limited to the anonymous client
- Remote on-state delivery was re-run on April 5, 2026 after rollout targeting was updated in PostHog. A fresh live `POST https://us.i.posthog.com/flags/?v=2` check for distinct id `4b5e7761-5bf1-4e18-9eb7-d6037a0eb32f` returned both `chilly_chat_expanded_v1` and `ai_chat_suggestions_v1` as `enabled: true` with reason `condition_match`
- Current-runtime on-state delivery is now proved: after a fresh Expo Go reload, Home reopened cleanly, own profile reopened cleanly, and `/chat/[threadId]` reopened cleanly with the gated card visible in-app
- The proved on-state thread markers are `AI SMART REPLIES`, `PostHog gated`, and the visible smart-reply chips `Hey Proof, I'm here`, `Let's do a voice call`, and `Send me the details`
- Remote PostHog on-state delivery for `chilly_chat_expanded_v1` and `ai_chat_suggestions_v1` is therefore now closed on the current runtime
- The active chat-thread flag consumers are only `chilly_chat_expanded_v1` and `ai_chat_suggestions_v1`
- Waiting-room and live flags are probe-only in `app/_layout.tsx` and are not wired to active UI owners

## Repo And Harness Reconciliation
- `SESSION_START_PROTOCOL.md` now exists and locks the control-file-first preflight plus checkpoint-bookkeeping order
- `ROADMAP.md`, `CURRENT_STATE.md`, and `NEXT_TASK.md` now agree on the preserved checkpoint truth: Stage 4 closed, PostHog default-off closed, and PostHog on-state closed
- `ROOM_BLUEPRINT.md` now exists as the durable room-by-room product reference and records the locked four-question rule every major room must answer: why am I here, what can I do here that I cannot do elsewhere, who can see/control what here, and what AI help belongs here without getting in the way
- `ROOM_BLUEPRINT.md` now also serves as the master room-feature and AI doctrine, so future room implementation passes can place features by room purpose, maturity, permissions, safety, and system fit instead of dumping them into the nearest active screen
- `maestro/` now records the active baseline smoke lane explicitly, uses shared `CHILLYWOOD_APP_ID` / `EXPO_URL` contracts instead of the stale `8081` hardcode, and replaces the missing auth-subflow / dead-id flow 09 path with an optional authenticated self-profile owner-rails flow
- No app logic changed in this reconciliation pass; the unrelated Supabase/media/screenshot worktree noise remains outside this checkpoint
- Expo Go surfaced intermittent Android ANR dialogs plus `Unable to activate keep awake` during the PostHog-enabled proof sessions, but the underlying Home/chat/profile routes still rendered and both default-off plus on-state proof completed
- with the updated execution plan now corrected after the completed Phase 0 Rachi foundation pass, completed Phase 2 moderation/admin minimums pass, completed Phase 3 Home/content polish pass, completed Phase 4 release-hardening pass, the now-closed Phase 5 final integration proof, the closed combined Chapters 1 through 4 verification lane, the room-purpose restoration pass, and the new live-lane handoff repair pass, the repo baseline now carries forward closed Chapters 1 through 4 plus the corrected Live / Party room-purpose boundaries; the next exact lane is a focused Android dev-client proof of the repaired Live Waiting Room -> Live Room -> Live Stage path unless a later blocker proves the need for a tiny supporting edit elsewhere

## EAS Update Readiness
- `eas.json` already maps the active build profiles to `development`, `preview`, and `production` channels, and EAS remote state already has preview builds on channel `preview` with runtime `1.0.0`
- shared app config now owns `runtimeVersion` with the Expo `appVersion` policy plus `updates.url=https://u.expo.dev/c384ed57-5454-4e80-81ad-dcc218b8a3c8`, so repo config and Android native update metadata now agree on the OTA transport
- `package.json` now exposes `npm run validate:runtime`, and `scripts/validate-runtime.mjs` now loads `.env.local` before verifying the required runtime env vars plus the effective Expo config for `extra.eas.projectId`, `updates.url`, `runtimeVersion`, and `betaEnvironment`
- On April 5, 2026, `npm run validate:runtime` passed locally from the current repo state after the real runtime allowlist value was added to `.env.local`; the gate now resolves `projectId=c384ed57-5454-4e80-81ad-dcc218b8a3c8`, the repo-owned `updates.url`, `runtimeVersion.policy=appVersion`, and `betaEnvironment=public-v1`
- On April 5, 2026, `npx expo install expo-updates` completed cleanly and confirmed the SDK 54-compatible `expo-updates@~29.0.16` dependency is now repo-owned in `package.json` and `package-lock.json`, so `eas update` no longer needs to mutate package files just to begin the preview OTA lane
- `README.md` and `docs/public-v1-release-checklist.md` now record the manual EAS Update flow: validate -> preview publish -> verify delivery -> production rollout -> rollback/revert if needed
- Expo's EAS Update setup guidance requires a build that already contains `expo-updates`; that requirement is now satisfied on Android by preview build `3daa1615-d003-46d5-af7a-af8500497155`, which finished on April 5, 2026 for channel `preview`, build profile `preview`, and runtime `1.0.0`
- On April 5, 2026, `npx eas-cli@latest update --channel preview --message "Chi'llywood preview OTA - settings logout auth/home fixes"` published successfully from commit `c8cc828186fabefed316ca9ff4ebc977200fdd25` to branch `preview` / channel `preview` for both Android and iOS on runtime `1.0.0`
- The published preview OTA checkpoint is update group `7519a9ee-c320-4d24-939d-4deeec57048b`, with Android update id `019d5f29-586e-7329-8f64-53be32301836` and iOS update id `019d5f29-586e-7aaf-8b87-8bc1cde40fbb`
- No production builds exist yet on EAS for the `production` profile, and the next controlled rollout step is now: verify the published preview OTA on the installed preview build before any production OTA rollout
- The current runtime version policy in effect is `appVersion`, which resolves to `1.0.0` on the current checkpoint

## What Was Fixed In This Pass
- `app/_layout.tsx`, `app/(auth)/login.tsx`, `app/(tabs)/index.tsx`, and `app/title/[id].tsx` now close the reported auth/home regressions: the root auth gate owns post-auth redirect resolution from auth routes, the login screen no longer dispatches the broken tabs-group replace, Home discovery cards push the canonical structured `/title/[id]` target, and title hydration no longer requests the missing `thumbnail_url` column that forced valid DB titles into the `Not found` fallback
- `app/player/[id].tsx` now resolves bundled assets through `expo-asset` `localUri` before `expo-av` `Video`, which fixed the real Android preview/current-JS playback bug
- `app/watch-party/live-stage/[partyId].tsx` now removes any existing room channel with the same topic before the stage subscribes, which fixed the `cannot add presence callbacks after joining a channel` runtime error
- `app/_layout.tsx` now identifies the signed-in Supabase user to PostHog and reloads feature flags inside the current provider tree, which fixed the missing identified-user flag-request path during the remote on-state proof lane
- `SESSION_START_PROTOCOL.md`, `ROADMAP.md`, and the active `maestro/` docs/flows were reconciled so repo truth, checkpoint truth, and proof-harness truth now describe the same closed Stage 4 + PostHog baseline without changing app behavior
- `app.json`, `package.json`, `scripts/validate-runtime.mjs`, `README.md`, and `docs/public-v1-release-checklist.md` now make the EAS Update readiness path explicit without changing app features or business logic
- `scripts/validate-runtime.mjs` now loads `.env.local` directly, which removed the plain `npm run validate:runtime` blocker once the real local beta operator allowlist value was present
- `package.json` and `package-lock.json` now intentionally own `expo-updates@~29.0.16` through the Expo-managed install path, which closes the preview OTA auto-install blocker without changing app logic
- `app/_layout.tsx`, `app/(tabs)/index.tsx`, `app/profile/[userId].tsx`, and `app/settings.tsx` now add the smallest visible Settings / Logout surface that product required: Home gained a visible `Settings` entry, own profile gained a self-only `Settings` chip, the new `/settings` route owns session sign-out, and post-logout routing returns to `/login` without changing other-user profile behavior
- `app/(auth)/login.tsx` now restores the intended Chi'llywood login presentation with the skyline background, dark overlay, and branded card treatment while leaving auth/session/error/redirect logic untouched; `app/(auth)/_layout.tsx` remains the headerless wrapper only
- `app/(auth)/login.tsx` now also lowers the login card further via container spacing only, which keeps more skyline branding visible above the card without changing the background asset, overlay, field styling, button styling, or any auth/session/route behavior
- `_lib/officialAccounts.ts`, `_lib/userData.ts`, `app/profile/[userId].tsx`, `app/chat/index.tsx`, and `app/chat/[threadId].tsx` now add the Phase 0 Rachi foundation through the canonical profile and Chi'lly Chat surfaces: Rachi is a protected official platform account, the profile/channel route renders official platform identity instead of a normal user shell, the Chi'lly Chat inbox exposes a discoverable official starter presence, and the canonical Rachi direct thread now opens with starter-message prompts instead of an empty dead-end thread
- `_lib/moderation.ts`, `supabase/migrations/202603270007_create_safety_reports.sql`, `supabase/migrations/202604050003_create_platform_role_memberships.sql`, `app/admin.tsx`, `app/title/[id].tsx`, `app/profile/[userId].tsx`, `app/chat/[threadId].tsx`, `app/watch-party/[partyId].tsx`, `app/watch-party/live-stage/[partyId].tsx`, and `app/communication/[roomId].tsx` now close the minimum moderation/admin lane after Rachi without a broad dashboard or profile/chat rewrite: safety reports have a repo-owned intake foundation, active platform roles can unlock review access, the admin studio exposes a bounded moderation review card, and title/profile/chat/room report flows all preserve consistent source-route and audit-minded context
- `app/(tabs)/index.tsx` and `app/title/[id].tsx` now close the Home/content polish lane without reopening player/live/watch-party semantics: Home respects the repo-owned continue-watching/favorites visibility and browse label/query inputs, favorites cards now keep metadata plus added-date/live-social cues, and title detail again resolves by exact `id`, then exact `title`, then local fallback while exposing title metadata and active room comment/reaction signals
- `app.config.ts`, `package.json`, and `package-lock.json` now close the release-hardening lane without product refactors: Expo's dynamic config owner uses the resolved static config as its base, the missing `react-native-svg` peer for `posthog-react-native` is repo-owned, Expo doctor now excludes the known `react-native-webrtc` new-architecture warning explicitly, and the current release validators all pass from the checked-in repo state

## What Is Proved In Repo
- A clean Expo Go first-open reset now lands on `/login` when the session is absent, and a valid login now returns to Home on `/` without the prior unhandled `"(tabs)"` replace error
- Party Room is canonical on `/watch-party/[partyId]`
- Live Room is canonical on `/watch-party/live-stage/[partyId]`
- Party Waiting Room routes into Party Room, while Live Waiting Room routes into Live Room
- Live Waiting Room on `/watch-party?mode=live` now owns live-only pre-entry control lanes, room-default preview, audience/visibility defaults, and next-room handoff cues instead of reusing Party Waiting Room behavior or generic helper copy
- Creating a live room from Live Waiting Room now hands off into `/watch-party/live-stage/[partyId]`; the current Android dev-client proof created live room `2H7ZWE` and landed on the live-stage owner with `Live-First` and `Live Watch-Party` visible instead of Party Room
- Live Room on `/watch-party/live-stage/[partyId]` now restores a distinct pre-stage control/customization surface with invite/share, audience ordering defaults, room-default policies, and a persistent footer `Continue to Live Stage` / `Join Live Stage` handoff instead of visually containing the stage itself
- Leaving Live Room now still returns to Live Waiting Room on the live flow instead of drifting into Party Room, while `LIVE STAGE` remains the separate presentation surface on the same canonical route
- Live Stage now keeps `Live-First` and `Live Watch-Party` as visibly distinct in-room modes on the same canonical live route while leaving pre-stage room controls back on Live Room instead of collapsing the two surfaces together; the latest handoff repair also corrects the route-loading copy to `Opening Live Room…` so the live lane no longer implies that stage entry happens before the Live Room control surface appears
- Party Waiting Room now stays title-first on `app/watch-party/index.tsx`, showing lane-specific pre-entry controls, room-access defaults, and next-room context before `/watch-party/[partyId]` instead of reusing Live Waiting Room semantics
- Creating from Party Waiting Room now carries the selected title into Party Room and lands on `/watch-party/[partyId]`; current Android dev-client proof created party room `EP6C9T` and opened Party Room instead of Live Stage
- Party Room on `/watch-party/[partyId]` now behaves as the pre-entry watch-together control/customization room rather than as Live Room, raw player UI, or a playback-control shell: invite/share continuity, room defaults, visibility/capture shaping, and the clear shared-player CTA all stay visible on the canonical party route
- Watch-Party Live / shared party player now keeps synchronized playback on `/player/[id]?partyId=...` with the content/player as the main focus, the host/viewer presence moved into a lower participant band, and a bounded return path back to the existing Party Room instead of reopening a broken room instance, drifting into Live Stage, or leading with a top instruction block
- Standalone Player on `/player/[id]` now stays solo-first with explicit standalone context, visible resume/playback guidance, and `Watch-Party Live` still owned as a deliberate handoff into Party Waiting Room instead of a room shell takeover
- Party Room must not hand off to Live Stage
- `/communication` and `/communication/[roomId]` remain compatibility-only room redirects
- Home `Top Rated` and `Browse` title cards route through `app/(tabs)/index.tsx` into `app/title/[id].tsx`, and the current build now re-proves that both visible Home rails open the `Chicago Streets` title detail surface instead of `Not found`
- Home now keeps both top-right owner entries visible and separate: `Settings` opens `/settings`, while the existing avatar still opens the canonical self-profile `/profile/[userId]` owner/control surface
- Title Detail resolves by exact `id`, then exact `title`, then local fallback, so valid titles such as `Chicago Streets` do not fall through from the Home rails without exhausting real resolution paths; the removed `thumbnail_url` select is part of that proved fix
- `Live Watch-Party`, `Watch-Party Live`, `Live First`, and `Chi'lly Chat` are locked in the repo control files alongside the corrected Party / Live route split
- runtime/admin config cannot rename locked product labels such as `watchPartyLabel`, `liveRoomTitle`, or `partyRoomTitle`
- standalone Chi'lly Chat exists on `/chat` and `/chat/[threadId]`
- self-profile entry opens the canonical `/profile/[userId]` owner channel surface, while the in-profile Chi'lly Chat action still opens `/chat` for the authenticated user
- self-profile keeps owner-only controls (`Manage Channel`, `Chi'lly Chat`, `Settings`, `Support`) while other-user profiles remain on the separate `Profile` + `Channel Home` + `Chi'lly Chat` behavior with no Settings owner control
- another profile stays viewer-facing on the same canonical `/profile/[userId]` route, then resolves or creates a direct Chi'lly Chat thread and opens `/chat/[threadId]`
- `/settings` is now a repo-owned additive route for account/session actions only, and `Log Out` returns the app to `/login` while a signed-out cold reopen still lands on `/login`
- room-native communication copy in Party Room and Live Room uses Chi'lly Chat-native wording instead of `In-Room Call`
- repo control files explicitly lock Chi'lly Chat as a messenger-first system, profiles as social identity hubs, and like/share/download/cast as reusable rights-aware content primitives
- repo control files explicitly lock Chi'lly Chat as Chi'llywood's built-in messenger layer, with `/chat` inbox, `/chat/[threadId]` direct threads, the `chat_threads` / `chat_thread_members` / `chat_messages` MVP data model, and shared communication-room primitives for thread calls
- repo control files explicitly lock profiles as native social identity hubs that grow toward each user's mini streaming platform / creator channel
- Rachi now exists in repo-owned Phase 0 truth as Chi'llywood's official platform-owned seeded account, using the canonical `/profile/[userId]` and `/chat/[threadId]` paths with explicit official identity markers, protected non-claimable behavior, and a starter-message path that future moderation/admin/audit features can safely extend
- safety reports now exist in repo-owned Phase 2 truth as a first-class moderation intake path with consistent route/source/target context across title, profile, Chi'lly Chat, watch-party room, live-stage, and communication-room surfaces
- active platform moderation roles now exist in repo-owned Phase 2 truth as the additive review gate that future moderator/operator behavior can extend without inventing a second admin identity system
- the admin studio now keeps its existing content controls while adding only a minimal role-aware moderation review surface, preserving the no-broad-dashboard-rewrite rule for this phase
- the admin studio now resolves its moderation card on the physical Android dev client without the old `public.platform_role_memberships` schema-cache failure; operator access, review-role gating, and the `Review Queue Locked` fallback all render as real role-aware states instead of a backend miss
- Home now preserves Continue Watching at the top hero when progress exists while also respecting repo-owned rail visibility and browse-query inputs on the active Home owner surface
- title detail now preserves repo-owned resolution truth by exact `id`, then exact `title`, then local fallback, so valid title paths do not fall through prematurely when a canonical title string is supplied
- title detail now exposes the same title metadata/add-date baseline and active title-linked live comment/reaction signals that the Home rails already surface where those signals actually exist
- Expo runtime validation, Expo doctor, and TypeScript now all pass together on the release-minded config surface, so the current repo state no longer carries the proved missing-peer/config-ownership/doctor-warning blockers that were present before Phase 4
- `ROOM_BLUEPRINT.md` now serves as the durable room-by-room product reference for Live Waiting Room, Live Room, Live Stage, Party Waiting Room, Party Room, Watch-Party Live/shared party player, Standalone Player, Profile / Channel, Chi'lly Chat, and Rachi without changing the current implementation lane
- `ROOM_BLUEPRINT.md` now additionally defines the current-vs-future feature fit, AI placement rules, systems dependencies, monetization relevance, accessibility expectations, and safety posture for those same major rooms without changing the active runtime-proof lane
