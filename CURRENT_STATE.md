# CURRENT STATE

## Hot-Path Control Rule
This file is intentionally compact. Long two-phone proof lanes were repeatedly forcing Codex into remote session compaction, and the remote compact stream can disconnect before completion. Keep current truth here, keep detailed older checkpoint history archived, and do not load the archive during normal preflight.

Full checkpoint history through April 24, 2026 is preserved at `docs/archive/current-state-history-through-2026-04-24.md`. Read that archive only when investigating an older checkpoint or reconciling historical branch truth.

## Current Checkpoint
The current `main` checkpoint now includes the Public v1 Creator Media System foundation for creator-uploaded videos. The active owners are `_lib/creatorVideos.ts`, `app/channel-settings.tsx`, `app/profile/[userId].tsx`, `app/player/[id].tsx`, `supabase/migrations/202604250001_creator_video_upload_foundation.sql`, and `supabase/database.types.ts`.

Durable Public v1 and later-phase planning for Premium/access gates, Moderation/Safety, creator-upload Watch-Party linking, Billing/Subscriptions, Monetization, and Native Game Streaming now lives in `docs/PUBLIC_V1_AND_LATER_SYSTEMS_PLAN.md`; the next implementation lane remains Creator Media public/draft and owner/non-owner runtime proof.

The full-app route/owner audit now lives in `docs/FULL_APP_ROUTE_OWNER_BEHAVIOR_AUDIT.md`, and the highest-priority audit hardening items are fixed: the template modal redirects to tabs, `/communication` redirects to canonical Chi'lly Chat while `/communication/[roomId]` remains guarded compatibility, duplicate Supabase client creation was collapsed to the canonical `_lib/supabase.ts` owner, and missing platform Player ids now show an honest unavailable state instead of falling back to the first local/sample title.

The 2026-04-28 room behavior / route / button pass hardened the remaining proved room-owner risks without changing the approved layouts: Party waiting room create/code paths now refuse no-source Party Room creation instead of silently creating live rooms, invalid platform title ids no longer fall back to the first title, Live Stage rejects non-live Party Room ids, Player Watch-Party Live rejects live room ids and preserves platform source handoff on waiting-room fallback, and the `livekit-token` edge function rejects Live Stage / Watch-Party Live surface mismatches. This was a code/static pass only; Android route smoke, two-device proof, and edge-function deployment/runtime proof remain pending.

The focused 2026-04-28 Supabase Edge Function pass deployed `livekit-token` version 8 to linked project `bmkkhihfbmsnnmcqkoly` and proved the stricter surface guardrails remotely without printing tokens or secrets: valid `live-stage` with a live room returned token-shaped success, `live-stage` with a title Party Room returned `409 room_surface_mismatch`, valid `watch-party-live` with a title Party Room returned token-shaped success, and `watch-party-live` with a live room returned `409 room_surface_mismatch`. This was function-level proof only; Android runtime route smoke, release proof, and two-device proof remain pending.

The 2026-04-28 non-room app behavior audit now lives in `docs/FULL_APP_NON_ROOM_BEHAVIOR_AUDIT.md`. The pass hardened only proved non-room issues: bare `/player/[id]` no longer opens creator videos by fallback, creator-video Player routes require explicit `source=creator-video`, signup has a Sign In handoff for existing users, signed-out Chat inbox/thread states have Sign In handoffs with redirect context, and JS string literals no longer render `&apos;` in Chat/Support/invite fallback copy. This was a static/code-validation pass only; Android non-room route smoke, Creator Media public/draft proof, owner/non-owner proof, moderation proof, Premium/access proof, external store/legal setup, and release proof remain pending.

The 2026-04-28 Public v1 smoothness hardening audit now lives in `docs/PUBLIC_V1_SMOOTHNESS_HARDENING_AUDIT.md`. This pass polished only launch-readiness states and copy: Home/Explore no longer show raw backend/developer-facing copy, My List has a branded empty/error retry state, Channel Settings creator-video loading/picker/upload/manage states are clearer, Subscribe/Settings Premium copy stays honest about store/setup gating, and runtime error/Crashlytics reporting redacts common token, signed URL, API key, bearer, and JWT patterns before logging/analytics handoff. This was static validation only; Android route smoke, release build proof, release log audit, and two-device/live proof remain pending.

The 2026-04-28 resumed one-phone Android proof on device `R5CR120QCBF` is saved under `/tmp/chillywood-one-phone-v1-proof-20260428-093842/resume-20260428-current`. It proves the explicit Chat routes, public legal routes, Settings, Support, Subscribe missing-entitlement/store-unavailable copy, non-admin Admin denial, platform Player, creator-video Player route ownership, Channel Settings owner Content visibility, and creator-video Report sheet opening in the dev-client runtime. The proof also found that the current public creator video `677650c2-4790-4298-8ddb-03ba7df5b424` / `20260326_113131` resolves as a remote creator-video source but its signed Storage object is zero bytes (`HEAD` content length `0`, range probe `416`) despite metadata claiming a 30 MB `video/mp4`; current playback therefore fails source sanity until the media object is repaired or re-uploaded. `_lib/creatorVideos.ts` now verifies non-empty uploaded objects after each upload method and removes/retries an empty object so future creator-video uploads cannot silently accept the same failure.

Non-proof Public v1 launch-readiness now has a dedicated tracker at `docs/PUBLIC_V1_READINESS_CHECKLIST.md`; this pass adds an honest `/subscribe` Premium account surface plus bundled Community Guidelines and Copyright/DMCA pages without claiming runtime, store, or two-device proof. A follow-up non-proof closeout normalized the tracker to exact launch statuses, removed developer-facing discovery empty-state copy, and moved Player source/Watch-Party diagnostics behind the dev-only logger. Public v1 external setup now has a separate checklist at `docs/EXTERNAL_SETUP_PUBLIC_V1_CHECKLIST.md`; it records Google Play, RevenueCat, Firebase, Supabase, LiveKit, Android release, legal/support, and production-env setup as pending until dashboard/manual proof exists. Dashboard-by-dashboard launch operations now live in `docs/DASHBOARD_SETUP_COMMAND_CENTER.md`, with Google Play app record/package confirmation as the highest-priority manual dashboard action.

Production env/secrets readiness now lives in `docs/PRODUCTION_ENV_SECRETS_RUNBOOK.md`; the Supabase client honors runtime Supabase config when present, RevenueCat SDK log mirroring is dev-only, and `scripts/validate-runtime.mjs` can run strict production presence checks without printing values. Play Store listing assets/content rating readiness now lives in `docs/PLAY_STORE_LISTING_CONTENT_RATING_RUNBOOK.md`; it documents draft listing copy, missing feature graphic/screenshots, content-rating prep, and UGC/Data Safety consistency without claiming Play Console proof. Legal/safety hardening added visible signup acceptance links and expanded Privacy, Terms, Community Guidelines, Copyright/DMCA, Account Deletion, and Support into structured launch-readiness draft pages covering UGC, creator uploads, rooms/live/chat, data categories, moderation/takedown, DMCA/repeat infringement, request-based deletion, retention, support categories, and emergency/support caveats; final wording remains pending attorney/legal approval.

The Live Layout and Chi’llyfects System is now defined in `docs/LIVE_LAYOUT_AND_EFFECTS_SYSTEM.md`, and the user-approved Live Stage / Watch-Party Live major layout structure is locked by `docs/LIVE_WATCH_PARTY_LAYOUT_LOCK.md`. The code foundation makes Home Live Watch-Party / Live Stage people-first with the Chi'lly Party Members grid shared by Live First and Live Watch-Party, excludes the current user's own preview from that remote-feed grid, shows remote live feeds three across with two visible rows before scroll, keeps Player Watch-Party Live content-first in the normal Party Room, and adds a reusable honest Chi’llyfects catalog/foundation. Watch-Party Live Party Room now puts the shared content/source surface at the top and the host/viewer feed bubbles below it in a five-across, two-row-visible scroll grid without routing into Live Stage. Current visible comments placement is part of the lock: do not move comments or replace them with menu-only, tap-menu, drawer, modal, bottom-sheet, overlay-only, or hidden secondary behavior unless the user explicitly asks for a comments redesign. The user-approved Live Stage overlay behavior is also locked: entering Live Stage from the Live Room and switching Live First / Live Watch-Party both arm the same 10-second overlay/comments auto-hide timer, while Lock Controls keeps it visible. Controls may be moved, deleted, renamed, or refined later only by explicit user request and only inside the approved structure. `Off` is the only real active Chi’llyfect/camera state; beauty, makeup, funny, aging, glam, mirror, and novelty face-card Chi’llyfects are post-v1 native/video-processing work. The Snap Camera Kit audit now lives in `docs/CHILLYFECTS_SNAP_CAMERA_KIT_AUDIT.md` as post-v1 planning input only. Public v1 must not add Snap Camera Kit, DeepAR, MediaPipe processing, custom WebRTC frame processors, or any native AR SDK; real Chi’llyfects AR processing remains unimplemented unless a future post-v1 proof shows processed output reaching remote LiveKit users. Android/two-device proof is pending and was not run in this pass.

Chicago Streets is intended to be open-access platform content for Public v1. Focused migration `supabase/migrations/202604270001_open_chicago_streets_title.sql` has been applied to the linked remote Supabase project, and an anon read verified the `Chicago Streets` title now has `content_access_rule = 'open'`.

Public v1 hardening now adds code-backed Premium/Billing and Creator Media safety foundations on top of the creator upload lane. `_lib/premiumEntitlements.ts`, `_lib/monetization.ts`, and `supabase/migrations/202604260001_billing_entitlement_foundation.sql` add backend-trusted `user_entitlements` / `billing_events` truth and stop local/user-writable premium or party-pass rows from granting protected access. `_lib/moderation.ts`, `_lib/creatorVideos.ts`, `app/player/[id].tsx`, `app/admin.tsx`, and `supabase/migrations/202604260002_creator_video_moderation_foundation.sql` add creator-video reports, `videos.moderation_status`, public playback/list filters for clean/reported videos only, and owner/operator hide/remove/restore writes. Focused local RLS proof now passes for creator video metadata, safety reports/moderation fields, premium/billing entitlement tables, creator events, notifications, reminders, and Watch-Party room access after `supabase/migrations/202604260004_tighten_watch_party_room_rls.sql` removed anonymous room insert/read and made premium-room membership writes require backend entitlements. The remote Supabase migration chain through `202604260004_tighten_watch_party_room_rls.sql` is now applied and rollback-proved for Watch-Party source persistence, creator-video eligibility blocking, open membership, premium membership blocking without entitlement, and anon room-create denial. Direct SQL cannot prove Storage API delete because Supabase blocks direct `storage.objects` deletes with `storage.protect_delete()`, so delete/remove proof stays in the Android/Storage API lane.

The creator media foundation adds a signed-in Channel Settings upload/manage lane, `expo-document-picker` file selection, `videos` metadata with `draft` / `public` visibility, private `creator-videos` Supabase Storage bucket policy intent, owner CRUD, public-read-only-public video policy intent, Profile/Channel uploaded-video display, owner-only empty state and draft badges, and Player support for `/player/[id]?source=creator-video`. Creator-upload Watch-Party linking is now implemented in code and remote schema with a `watch_party_rooms.source_type/source_id` source model, normal `/watch-party` waiting room flow, Party Room handoff to `/player/[id]?source=creator-video`, and no Live Stage route fallback; static/local Supabase proof and focused remote rollback SQL proof passed. Android proof on device `R5CR120QCBF` then proved creator upload `20260326_113131` / `677650c2-4790-4298-8ddb-03ba7df5b424` can publish, open `/player/[id]?source=creator-video`, start Watch-Party Live into the normal waiting room, enter `/watch-party/[partyId]` with room code `87AXTR`, open the shared Watch-Party Live player using creator-video source, and direct-rejoin the same party room without Live Stage, platform-title, or bundled-sample fallback. This pass did not add comment media upload, native game streaming, paid/subscriber videos, payouts, or automatic transcoding.

Static validation passed for the creator media foundation with `npm run typecheck`, `npm run lint`, and `git diff --check`. Android runtime proof on device `R5CT30GM6NY` now proves `/channel-settings` opens, the owner Content panel renders, the native Android file picker opens, selected video file name is visible, Upload visibly enters an uploading state, and creator-video metadata/storage save succeeds. The latest uploaded creator video was `f6a5c96a-812a-4132-8a2b-e3ecab51c983`, title `Screen_Recording_20260326_152944_Expo`, visibility `draft`, stored under the private `creator-videos` bucket path for owner `35d7a18a-c847-4b54-b452-a23b01284712`. Human device proof confirmed tapping an uploaded Profile/Channel discovery card opens the standalone Player for the creator video, not the platform/admin title surface or bundled sample fallback; Metro logged `PLAYER MATCH SOURCE: matched from creator-video:id` and `PLAYER VIDEO SOURCE: remote` for creator video `5cf469eb-d375-436a-861a-e82ece5cc47d`.

The creator-video Player follow-up is also proved on Android device `R5CT30GM6NY`. Platform/admin title `/player/t1` still uses the existing premium Chi'llywood Player shell. Creator route `/player/5cf469eb-d375-436a-861a-e82ece5cc47d?source=creator-video` now feeds the uploaded video into the same standalone Player shell/layout/treatment while keeping source resolution honest: creator-video routes do not borrow platform fallback artwork, do not use the bundled sample/platform fallback, and log only `PLAYER VIDEO SOURCE: remote` rather than signed media URLs. Missing creator sources stay inside the premium Player shell with "Creator video unavailable" copy. Creator-upload Watch-Party Linking now has Android one-device runtime proof saved at `/tmp/chillywood-android-creator-video-watch-party-proof-20260426-115259`; platform title Watch-Party still reaches normal waiting room/Party Room, and creator-video room `87AXTR` persisted remotely as `source_type=creator_video` with source id `677650c2-4790-4298-8ddb-03ba7df5b424`. A scoped follow-up fix in `app/watch-party/[partyId].tsx` prevents post-room bootstrap errors from being mislabeled as "Room not found" after a room row has already resolved. Two-device creator-video Watch-Party join proof and draft/private blocked-state proof are still pending. Remote schema/RLS proof remains saved at `/tmp/chillywood-remote-creator-video-watch-party-proof-20260426-114433.txt`; proof folder for the earlier blocked linking pass remains `/tmp/chillywood-creator-video-watch-party-proof-20260426-105433`.

The Android rebuild required for `expo-document-picker` exposed a real native build blocker: both `@livekit/react-native-webrtc` and the older direct `react-native-webrtc` package were present, causing duplicate `org.webrtc.*` classes in `:app:checkDebugDuplicateClasses`. The current fix keeps `@livekit/react-native-webrtc` as the single WebRTC native owner, removes the legacy direct WebRTC dependency/config plugin, and points communication compatibility imports at the LiveKit WebRTC package.

The current `main` checkpoint preserves the April 24, 2026 two-real-device LiveKit live-stage camera-visibility fix. The active owner remains `app/watch-party/live-stage/[partyId].tsx`, with route-local hybrid LiveKit ownership using explicit `Room` instances and room-specific teardown tracking.

The proved bug was Live First stage entry for a signed-in non-host who could enter while still holding a `viewer` LiveKit participant role. The fix resolves the entry role before minting the LiveKit token: the route claims the participant seat as `speaker`, persists `canSpeak`, updates local participant state, clears any pending seat request, broadcasts the seat state, refreshes the snapshot, and only then prepares the LiveKit join contract.

The prior stale-runtime containment pass is also preserved. The shared LiveKit stage surface suppresses stale teardown-era signal-loop read errors before they surface through LiveKit websocket handling, and the route-local hybrid LiveKit host uses the same patched signal-loop lifecycle rather than an unpatched internal room path. The relevant files are `app/watch-party/live-stage/[partyId].tsx` and `components/watch-party-live/livekit-stage-media-surface.tsx`.

## Proof Truth
- One-device runtime containment smoke reached fresh room `K4W7ZT` cleanly after stale room `AR5VPC`: pre-stage Live Room and actual Live Stage mounted, logs showed `prepared live-stage join contract` then `room connected`, and there was no `Unexpected first message`, no `NegotiationError`, no stale websocket toast, and no fallback to the legacy media path.
- Two-phone Live First proof used room `E4U5FP`: host and guest were in the same LiveKit room, both had local camera true, each saw one remote track, and `visibleTrackCount` was `2` on both devices.
- Two-phone Live Watch-Party proof used room `NN9RLU`: both devices again had local camera true, one remote track, and `visibleTrackCount: 2`; leave-to-Live-Room and rejoin also passed on `NN9RLU`.
- Validation after the proof passed with `npm run typecheck`; `npm run lint` exited 0 with the existing repo warning set only.

Saved proof artifacts from the latest two-phone pass include `/tmp/chillywood-R3-livefirst-stage-after-fix.png`, `/tmp/chillywood-R5-livefirst-stage-after-fix.png`, `/tmp/chillywood-R3-livefirst-after-fix-logcat.txt`, `/tmp/chillywood-R5-livefirst-after-fix-logcat.txt`, `/tmp/chillywood-R3-hybrid-stage-direct-after-fix.png`, `/tmp/chillywood-R5-hybrid-stage-direct-after-fix.png`, `/tmp/chillywood-R3-hybrid-direct-after-fix-logcat.txt`, `/tmp/chillywood-R5-hybrid-direct-after-fix-logcat.txt`, `/tmp/chillywood-R3-hybrid-rejoin-after-fix.png`, `/tmp/chillywood-R5-hybrid-rejoin-after-fix.png`, `/tmp/chillywood-R3-hybrid-rejoin-after-fix-logcat.txt`, and `/tmp/chillywood-R5-hybrid-rejoin-after-fix-logcat.txt`.

## Known Remaining Seams
- Later two-phone live/watch proof remains the next product lane for sustained interaction retention, shared-media persistence, participant/member visibility, and cross-device audio truth.
- Repeated Android deep links can leave older proof-room route instances logging harmless render-branch lines even while the current LiveKit room works. Treat this as an optional route-stack cleanup audit only if a later proof lane makes it a real blocker.
- The `Error running remote compact task: stream disconnected before completion` message is Codex session/tooling failure, not a Live Stage app-runtime failure. The durable repo mitigation is to keep control-file preflight compact and store raw proof logs in files rather than pouring them into chat.

## Current Next Action
Continue Public v1 runtime proof from the saved one-phone folder, not from scratch. The next safe lane is to repair or re-upload the zero-byte creator-video Storage object, rerun creator-video Player playback/source sanity, then continue Creator Media System proof with the remaining public-vs-draft and owner-vs-non-owner checks. One-device Android creator-upload Watch-Party linking now passes for public creator video room `87AXTR`; next prove two-device creator-video Watch-Party join/rejoin only when a second phone is available, then verify draft/private creator videos are blocked from public Watch-Party. Include the non-room hardening in final route smoke: signup existing-account handoff, signed-out Chat Sign In handoffs, valid/invalid platform Player routes, valid/invalid creator-video Player routes requiring `source=creator-video`, and no creator-video fallback from bare `/player/[id]`. Include the new room-owner hardening in final route smoke: no-source Party waiting room create/code guards, invalid platform title unavailable state, direct cross-surface Live Stage/Party Room route guards, and Player Watch-Party Live source preservation. The remote `livekit-token` surface mismatch guard has function-level proof, but Android route smoke and two-device proof still need to verify app behavior against the deployed function. Also include the locked Live Layout and Chi’llyfects foundation in the later Android/two-device route proof: Home Live Watch-Party should show the people-first Chi'lly Party Members grid with only other remote live feeds, three across/two visible rows before scroll, Watch-Party Live should remain content-first in `/watch-party/[partyId]`, current visible comments placement must remain unchanged, and Chi’llyfects should show honest foundation-only/no-camera-processing status. Do not start Snap Camera Kit, DeepAR, MediaPipe, custom WebRTC frame processors, or other real AR processing before Public v1; keep those as post-v1 only. Then publish/unpublish the uploaded creator video, verify public Profile/Channel visibility from a non-owner view, verify draft privacy, verify edit/delete management, verify creator-video reporting writes a real `safety_reports` row, verify hidden/removed creator videos do not appear publicly or play publicly, and prove creator-video Storage API delete/remove behavior.

During proof, save raw logcat, screenshots, and long command output to `/tmp` or a task-specific proof artifact file, then summarize only the facts needed for `CURRENT_STATE.md` and `NEXT_TASK.md`.

Use `npm run proof:session -- live-stage-two-phone` before long proof runs. It creates a `/tmp/chillywood-proof-*` artifact directory plus `/tmp/chillywood-proof-latest.txt` so the next session can resume from files if remote Codex compaction disconnects.

## Staging Discipline
- Work on current `main` only.
- Keep unrelated local dirt out of the checkpoint.
- Never stage `supabase/.temp/`.
- Stage only task-pure files for the active lane.
